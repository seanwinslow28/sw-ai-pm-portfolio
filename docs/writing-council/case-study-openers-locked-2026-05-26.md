# Case-Study Openers — Locked Content

**Date:** 2026-05-26
**Status:** LOCKED — all 5 case-study openers (A-1, A-2, A-3, A-4, A-5) ready to apply on Mac Mini. **Note:** A-4 opener locks, but the rest of `the-block.mdx` (frontmatter dates, anchor metric, tagline, methods, four_q, investigation board) is anchored to a wrong premise (2024–2026 tenure, "two products," fabricated subscriber chart) and needs a separate cleanup pass — see §A-4 Spec implications. A-5 Methods strip has a related stack drift (Seedance listed for sprite gen instead of NB2) — see §A-5 Spec implications.
**Scope:** The 3-paragraph opener prose for each of the 5 case-study pages, per `case-study-spec-v1.md` §4 + §13. Sedaris-coded, specific nouns, no setup, no tagline echo.
**Authors:** Sean + Claude (Sonnet 4.6), brainstorm session 2026-05-26 (continued from About B-1/B-2/B-4 lock on 2026-05-25)
**Build environment:** Mac Mini (this doc was authored on MBP — no MDX edits committed here to avoid Github conflicts; Sean applies on Mac Mini)

---

## TL;DR — what's locked so far

1. **A-1 · animation-pipeline opener LOCKED** — Sean Mode hybrid: pencil-sketch-on-iPad → teaches Gemini Nano Banana 2 → why pencil-test not Midjourney → motion authored in Procreate Dreams, agents fill frames, Seedance interpolates. Closes "*anima*. 220 frames in, Act 1 plays clean."
2. **A-2 · code-brain opener LOCKED** — Kerouac dash-rhythm hybrid: 02:30/03:30/08:30 overnight fleet cadence → Obsidian vault that built itself a fleet → three-machine orchestration + parallel-CLI critic. Closes "Comprehension is the artifact. The fleet is how I keep comprehending while I do other work." Third and final appearance of "comprehension is the artifact" across the portfolio (ceiling reached).
3. **A-3 · intent-engineering-mcp opener LOCKED** — Sean Mode tightening of existing prose: opens "It shipped on May 12, 13 days early." → cut decision = the point → screenwriter→PM bridge ("the way you cut a scene that's slowing the second act") → closes "The ship date was just the receipt." Replaces council-flavored existing opener; "since 2013" carryover cut (no concrete biographical anchor).
4. **A-4 · the-block opener LOCKED** — merged best-of: opens "The Block is an institutional crypto data and research firm whose customers ran money for a living" (present-tense company identity, past-tense work-context — the ARCHIVED-register move). Preserves four strongest moves from existing prose ("Snowflake share / PDF / phone call," "build it, ship it, write it down," "restructured around me / products didn't," "no UX to lean on, only the agreement"). Scope corrected to multi-surface (Campus + ETF pages + Data API + Simon + RevOps + AdOps + bi-weekly P&E). Tenure corrected from spec's "two years" to factual Nov 2025 – May 2026 (six months); cold-scan opener doesn't lead with the duration.
5. **A-5 · 16bitfit opener LOCKED** — existing prose preserved verbatim with one micro-tweak ("feeding" → "supposed to feed" for factual accuracy). Strongest opener of the five; no council-rush tells found. Sedaris-coded throughout: "a name as pleased with itself as '16BitFit,'" "same hairline, same belt buckle, same stupid grin," "an evening I would have spent angrily exporting frames I knew weren't right yet," "no hard feelings" closer. Pipeline-is-the-artifact framing nailed.
4. **Stack truth restored on A-1** — the original council pass had "Seedream 2.0" for style anchor; the real stack uses **Gemini Nano Banana 2** for stills + **Seedance 2.0** for video interpolation. Opener prose corrected; case-study spec §13.1 needs a downstream amendment (see Spec implications below).
6. **Voice calibration confirmed across all five** — A-1 uses Hard Cut signature move + compound-noun parallel ("animator's idiom" / "content-farm idiom"). A-2 uses Kerouac dash rhythm + "the fleet has been working since I went to bed" as ¶1 closer. A-3 uses screenwriter→PM bridge ("the way you cut a scene that's slowing the second act") as load-bearing metaphor. A-4 uses present/past tense split + reader-dismissal signature move ("The firm restructured around me; the products didn't. This page is about the products."). A-5 uses rule-of-three-with-fourth-item-pivot ("same hairline, same belt buckle, same stupid grin") + Vonnegut-flat pause cascade. Zero desperation across all five; reads identically hired vs hunting.

---

## Carry-forward constraints (apply to all 5 openers)

These are calibration rules established as we lock each opener. They apply to every remaining opener (A-3, A-4, A-5) and to any future case-study writing.

1. **Code Brain — no fixed counts of skills, agents, or hooks anywhere.** The fleet is constantly evolving; numbers are stale within a week. Use language that leans into the fluctuation ("a fleet that grows and shrinks with the work" / "a roster that never sits still") or omit the count entirely and let "fleet" carry the meaning. Applies to all surfaces — case-study openers, Methods strips, anchor metrics, About prose, footer copy.
2. **iPad callback ceiling reached.** Used in About B-1 beats 3 + 6 + A-1 ¶1. Three is the cap; do not let the iPad surface in A-3, A-4, or A-5 openers.
3. **"Comprehension is the artifact" ceiling reached.** Used in About B-2 ¶2 + B-4 ¶1 (as "Comprehension didn't") + A-2 ¶3. Three is the cap; do not let it surface in A-3, A-4, or A-5 openers.
4. **"Building got cheap" ceiling reached.** Used in About B-2 ¶2 + B-4 ¶1. Two is the cap for this phrase (it's a tighter rhyme than the thesis-family ones); do not let it surface in any case-study opener.
5. **HybridRouter STOP-DOING.** Per spec §4 / PMP §3.4: HybridRouter lives in Methods strip rows only, never in opener prose, never in 4Q. Applies to A-1 (animation-pipeline orchestration) and A-2 (code-brain critic routing) explicitly; flag any future case study that might be tempted.
6. **No traces of desperation.** Page reads identically hired vs hunting. Zero "looking for work / please hire me / in the room" cadence. The opener is a piece of work showing itself, not an ask.
7. **No tenure-foregrounding on public surfaces** *(added 2026-05-27)*. Date data lives in frontmatter fields (`date_started`, `date_active_through`) for anyone who needs the timeline; user-facing prose stays focused on the work. Applies to all surfaces — case-study openers, 4Q blocks, About prose, taglines, anchor metrics. Drove three amendments on 2026-05-27: A-4 opener ¶3 ("six months, " deleted), A-4 4Q `what` ("Six months as" prefix deleted before Mac Mini apply), About B-2 ¶1 ("Six months into the PM job" → "Deep enough into the PM job"). Companion artifact: [`project-four-q-locked-2026-05-27.md`](project-four-q-locked-2026-05-27.md).

---

## 2026-05-27 amendment — A-4 opener ¶1 + ¶3 reopened

Two changes to the A-4 opener prose locked above, driven by Sean's 2026-05-27 instruction to (a) anchor The Block content on resume-confirmed surfaces and (b) remove tenure-foregrounding from public prose:

1. **¶1 surface inventory swap.** "the Data API, the Simon AI feature, the RevOps and AdOps systems that ran pricing and contracts" → "the Polymarket × Campus sponsored-microcourse vertical, the .co homepage redesign competitive audit, the RevOps automation pipeline behind Salesforce closes." Three resume-unsupported surfaces swapped for three resume-confirmed surfaces. Same item count, same rhythm.
2. **¶3 "six months, " deletion.** Four-item list ("six months, multiple surfaces, sanitized artifacts, and a public reference link at the end") becomes a cleaner rule-of-three.

**¶1 flag — opener ¶2 still references "the Simon feature ships faster..."** as an example. Sean did not reopen ¶2 in the 2026-05-27 pass; the Simon-feature reference there carries an inconsistency with the new ¶1 surface inventory. Worth surfacing in a future pass if Sean wants the page fully resume-evidence-aligned end-to-end. Surgical fix would swap "the Simon feature ships faster when you've already drafted the line that justifies it" for an example anchored on a ¶1-listed surface (e.g., the Polymarket microcourse PRD, the .co homepage redesign brief).

---

## 2026-05-27 — second amendment (Tier 1 mechanical + Tier 2 anchor-metric + opener fixes)

Second pass on 2026-05-27, driven by a hallucination audit run against on-disk MDX + source-of-truth files (anima CLAUDE.md + production-checklist, code-brain CLAUDE.md + CHANGELOG, intent-engineering README + CHANGELOG, personal context v2.0). Companion to the first 2026-05-27 amendment above (A-4 ¶1 + ¶3 surface inventory swap + tenure deletion). MBP session continuation; Mac Mini still owns the apply.

### Two new findings from the audit

1. **Code Brain real start date confirmed: 2026-02-06.** Currently on disk as `date_started: 2025-09-01` — wrong by five months. Sean confirmed: the v1.0.0 CHANGELOG entry dated "2024-01-XX" is a mistake; the v2.0.0 entry at the bottom of the CHANGELOG (currently dated "2025-02-06") is the real first entry and the actual year is 2026. Affects A-2 anchor metric candidates (the previously-suggested "LIVE SINCE 2024" is wrong; not picked) and A-2 frontmatter.
2. **16BitFit pause date confirmed: 2026-04-20.** Currently on disk as `date_active_through: 2026-04-01` (frontmatter) + investigation board `date="APR 2"` (BoardArtifact "Pause decision board"). Both wrong. Personal context v2.0 §"Active Projects": "16BitFit paused 2026-04-20." Affects A-5 frontmatter + investigation board.

### Tier 1 — mechanical fixes

| # | Surface | Current | Amended |
|---|---|---|---|
| T1.1 | A-3 opener ¶3 (in A-3 LOCKED block below) | "...reads the install counts at **08:45** every morning." | "...reads the install counts at **08:30** every morning." |
| T1.2 | A-5 Methods strip (frontmatter only — original §A-5 spec implications below confirmed) | `Seedance 2.0 + pixel-art Gemini skill` / `~$1/run` | `Gemini Nano Banana 2 (pixel-art Gemini skill)` / cost TBD pending real sprite-gen telemetry — Sean to verify before commit |
| T1.3 | A-2 frontmatter (on-disk only) | `date_started: 2025-09-01` | `date_started: 2026-02-06` |
| T1.4 | A-5 frontmatter (on-disk only) | `date_active_through: 2026-04-01` | `date_active_through: 2026-04-20` |
| T1.5 | A-5 investigation board (on-disk only) | BoardArtifact `date="APR 2"` "Pause decision board" | `date="APR 20"` |

**Why T1.1 matters:** code-brain CLAUDE.md confirms Daily Driver fires 08:30; meta-agent fires 08:45. The shipped-stats endpoint is written by Daily Driver. The 08:45 in A-3's locked opener carried over from the wrong agent's time slot. Audit flag: A-3's locked opener was the only public-facing instance of "08:45 as Daily Driver time" found across the portfolio.

### Tier 2 — anchor metrics + opener fixes

#### T2.1 — A-1 opener ¶3 closer

**Currently locked in A-1 LOCKED block below:** "...The pipeline is called *anima*. 220 frames in, Act 1 plays clean."

**Amended:** "...The pipeline is called *anima*. Act 1 plays clean. Act 2 is on the board."

**Why:** Production-checklist confirms Act 1 = 42 frames (Phases 1–4 complete; exports rebuilt at 12fps as GIF/MP4/WebM); Act 2 = ~600 frames target across 10 Seedance clips + 4 holds + 3 hard cuts (Phase 6 ready to execute, M1 rough-cut + M2 full-fidelity tasks queued). "220 frames" was invented in the prior council pass and matches no real count. Status-pair replacement reads as current-state without naming a count that ages weekly until June 11.

#### T2.2 — A-1 anchor metric

**Currently on disk:** `anchor_metric: 220 FRAMES`

**Locked:** `anchor_metric: "ACT 1 SHIPPED · ACT 2 IN FLIGHT"`

**Why:** Status-pair, no count, ages past June 11 cleanly. Mirrors the opener ¶3 closer's status-pair rhythm without verbatim repeat ("plays clean / on the board" in prose, "SHIPPED / IN FLIGHT" in metric). Carry-forward constraint #1 honored. The "SHIPPED" status here describes Act 1's internal completion — distinct from the case-study page's `status: ACTIVE` frontmatter, which describes the whole project.

#### T2.3 — A-2 anchor metric

**Currently on disk:** `anchor_metric: "118 SKILLS · 14 HOOKS · 8 AGENTS"`

**Locked:** `anchor_metric: "OVERNIGHT FLEET · LAUNCHD-NATIVE"`

**Why:** Rhythm-based, no count, no date. Matches A-2 opener ¶1's overnight-fleet thesis (02:30 → 03:30 → 08:30 timing cascade). Carry-forward constraint #1 honored. "Launchd-native" is the credibility move — names the scheduling primitive that makes the autonomy real, rather than just labeling the result. "Fleet" used in anchor metric is allowed per tagline-lock §"Carry-forward constraints" #1 (the ceiling is tagline-level only).

#### T2.4 — A-4 anchor metric

**Currently on disk:** `anchor_metric: "2024 – 2026 · TWO PRODUCTS · ONE FIRM"` (fully wrong — wrong tenure, wrong scope, foregrounds dates)

**Locked:** `anchor_metric: "B2B PM · INSTITUTIONAL CRYPTO RESEARCH"`

**Why:** Sean's pick per the original 2026-05-26 lock §A-4 spec implications. Recruiter-readable, no tenure foregrounding (carry-forward constraint #7), no false-scope claim. Relates to the locked tagline ("B2B PM work where the agreement is the UX") without doubling — the metric anchors firm-identity, the tagline anchors craft-insight.

#### T2.5 — A-4 opener ¶2 Simon-feature swap

**Currently locked in A-4 LOCKED block below (after the first 2026-05-27 amendment):** "...the ETF launch page scopes easier when you've already drafted the version that fits in 90 words; **the Simon feature ships faster when you've already drafted the line that justifies it**."

**Amended:** "...the ETF launch page scopes easier when you've already drafted the version that fits in 90 words; **the Polymarket microcourse builds faster when you've already drafted the five components out loud**."

**Why:** Simon AI feature is not in the ¶1 resume-confirmed surface inventory (per the first 2026-05-27 amendment). Polymarket × Campus sponsored-microcourse vertical IS in ¶1 and is the marquee 0-to-1 build Sean owned end-to-end (per A-4 4Q `what` in [`project-four-q-locked-2026-05-27.md`](project-four-q-locked-2026-05-27.md): "PRD v1 to v3, 5-component 0-to-1 build"). "Five components" is the resume-grounded specificity that mirrors "90 words" in the previous clause — both examples now name a unit-of-measurement the write-it-down discipline produced. Companion update lands in the 4Q lock doc's A-4 "Companion opener amendments" section (per that doc's 2026-05-27 second amendment).

### Updated apply-on-Mac-Mini delta

Supersedes step 6 of the original 12-step apply checklist (below) and adds steps 6a–6d covering the new amendments. Steps 1–5 + 7–12 unchanged unless explicitly noted.

6. Open `src/content/work/the-block.mdx`:
   a. Replace `<Opener>` body (lines 52–54) with the A-4 LOCKED 3 paragraphs as further amended by the first 2026-05-27 amendment (¶1 + ¶3) + T2.5 (¶2) — full amended text mirrored in [`project-four-q-locked-2026-05-27.md`](project-four-q-locked-2026-05-27.md) §A-4 "Companion opener amendments" as updated by that doc's 2026-05-27 second amendment.
   b. Replace `anchor_metric:` (line 19) with `"B2B PM · INSTITUTIONAL CRYPTO RESEARCH"` per T2.4
6a. Open `src/content/work/animation-pipeline.mdx`:
   - In `<Opener>` ¶3, replace final sentence "anima. 220 frames in, Act 1 plays clean." with "anima. Act 1 plays clean. Act 2 is on the board." per T2.1
   - Replace `anchor_metric:` (currently `220 FRAMES`) with `"ACT 1 SHIPPED · ACT 2 IN FLIGHT"` per T2.2
6b. Open `src/content/work/code-brain.mdx`:
   - Replace `date_started:` with `2026-02-06` per T1.3
   - Replace `anchor_metric:` with `"OVERNIGHT FLEET · LAUNCHD-NATIVE"` per T2.3
6c. Open `src/content/work/intent-engineering-mcp.mdx`:
   - In `<Opener>` ¶3, replace "08:45 every morning" with "08:30 every morning" per T1.1
6d. Open `src/content/work/16bitfit.mdx`:
   - Replace `date_active_through:` with `2026-04-20` per T1.4
   - In `methods:` array, swap `Seedance 2.0 + pixel-art Gemini skill` for `Gemini Nano Banana 2 (pixel-art Gemini skill)` per T1.2 (cost field: Sean to set based on real sprite-gen run telemetry; original `~$1/run` is also flagged as needs-verification)
   - In the investigation board, change BoardArtifact `date="APR 2"` ("Pause decision board") to `date="APR 20"` per T1.5

Updated commit message recommendation: `5 opener/4Q/tagline/metric locks + 5 Tier-1 mechanical fixes + 4 Tier-2 anchor-metric/opener locks (A-1 + A-4 + A-5 stack-truth and date corrections)`

### Updated spec implications (case-study-spec-v1.md)

Adds to the original list in this lock doc's §"Spec implications (case-study-spec-v1.md §13.1 + §13.2)" below + the original 4Q lock's spec implications:

| Spec section | Currently says | Amend to |
|---|---|---|
| §13.1 Anchor metric (A-1) | `220 FRAMES` | `"ACT 1 SHIPPED · ACT 2 IN FLIGHT"` |
| §13.2 Anchor metric (A-2) | `16 SKILLS · 14 HOOKS · 8 AGENTS` | `"OVERNIGHT FLEET · LAUNCHD-NATIVE"` |
| §13.4 Anchor metric (A-4) | `2024 – 2026 · TWO PRODUCTS · ONE FIRM` | `"B2B PM · INSTITUTIONAL CRYPTO RESEARCH"` |
| §13.5 Methods strip (A-5) | `Phaser / Seedance 2.0 / animation pipeline (cross-link to /work/animation-pipeline) / pixel-art Gemini skill` | `Phaser / Gemini Nano Banana 2 (pixel-art Gemini skill) / animation pipeline (cross-link to /work/animation-pipeline)` |
| §13 — general | (silent on anchor-metric shape) | Add one line: "Anchor metrics are status-pair, rhythm-based, or role-identity strings — never fixed counts of skills/agents/hooks/frames/etc. that fluctuate week to week. Carry-forward constraint #1 in [`case-study-openers-locked-2026-05-26.md`](../writing-council/case-study-openers-locked-2026-05-26.md) is the portfolio-wide rule." |

Apply as a single spec-amendment commit on Mac Mini after the lock-doc apply lands. Closes out the Seedream/NB2/Seedance corrections + count-based anchor-metric guidance across the spec.

---

## A-1 · animation-pipeline — LOCKED

**File:** `src/content/work/animation-pipeline.mdx` — opener prose body (renders inside `<Opener />` per case-study-spec §2 + §13.1).

Replace any current opener placeholder with the three paragraphs below.

```mdx
  I drew a pencil sketch of myself on the iPad — me at 33, slightly stooped, stylus in the right hand. Then I taught Gemini Nano Banana 2 to redraw the same guy 220 times in a row, in 16:9, on cream paper, with the same facial scruff in every frame. The model has a list of ways it can fail me now. They have numbers. SF02 is "wrong jaw."

  It could've been Midjourney. Midjourney would've been faster. Midjourney would've also been gauzy, vertical, photo-real, and someone else's. The pipeline is a pencil test on purpose — construction lines visible, paper grain, hole-punch marks down the left edge. It's an animator's idiom, not a content-farm idiom. The discipline is the proof that a human still owns the timing.

  I block the motion in Procreate Dreams. The agents fill the frames. Seedance 2.0 interpolates between the approved keys. A critic agent flags identity drift before the render burns budget. The pipeline is called *anima*. 220 frames in, Act 1 plays clean.
```

**Arc:** hook → choice → method. ¶1 is the recruiter-cold-scan paragraph: pencil sketch, Nano Banana 2, 220 frames, the fail-code system. ¶2 is the *why pencil test* — the discipline argument. ¶3 is the *how* — the agent-fleet description, closing on the project name + current state.

**Page-level callbacks honored:**
- **iPad** — third appearance on the portfolio (About B-1 beats 3 + 6, now here). Three is the ceiling; do not let it appear in A-2, A-3, A-4, or A-5 openers.
- **"Drew"** verb echoes Ash Ketchum opener in About B-1 beat 1 — same activity at age 8 and age 33, no signpost needed.

**STOP-DOING honored:** HybridRouter not mentioned. Methods strip cell (`Code Brain` cross-link) will carry the orchestration story. The opener stays on the medium and the discipline.

---

## A-2 · code-brain — LOCKED

**File:** `src/content/work/code-brain.mdx` — opener prose body (renders inside `<Opener />` per case-study-spec §2 + §13.2).

Replace any current opener placeholder with the three paragraphs below.

```mdx
  At 02:30 every morning while I am asleep, an agent on my Mac Mini reads everything I wrote yesterday and turns one item of it into a concept — and at 03:30 two more agents read what the first one wrote and argue about whether it's any good — and by 08:30 a fourth agent has drafted my morning note, and the day starts with a fleet that has been working since I went to bed.

  This is Code Brain — an Obsidian vault that built itself a fleet, SDK agents that grow and shrink with the work, running on launchd, a knowledge graph that lints itself on Sundays, a critic that splits to Codex CLI and Anti-Gravity CLI in parallel so the variance is real, and a daily-driver agent that writes the dateline at the top of this portfolio.

  The orchestration runs across three machines — Mac Mini for the long fleet, MacBook Pro for heavy local inference, an Alienware RTX 5080 for the CUDA jobs. Comprehension is the artifact. The fleet is how I keep comprehending while I do other work.
```

**Arc:** scene → system → orchestration. ¶1 is a single Kerouac-engine sentence showing the overnight fleet firing in three timed beats (02:30 → 03:30 → 08:30); closer "a fleet that has been working since I went to bed" is the Sedaris sentence-end punchline. ¶2 names the system and inventories its parts without ever giving a fixed count — "built itself a fleet" is the Sedaris minor-weirdness that reinforces the autonomy thesis; "grow and shrink with the work" embeds the fluctuation as a feature. ¶3 closes on the three-machine truth, then lands the thesis callback + forward-tilt.

**Page-level callbacks honored:**
- **"Comprehension is the artifact"** — third and final appearance (About B-2 ¶2 → B-4 ¶1 → here). Thesis-family rhyme; ceiling reached.
- **"The dateline at the top of this portfolio"** — meta-rhyme with the actual dateline strip the case-study page renders. The opener describes the agent that writes the chrome of the page the reader is currently on. Self-referential without being cute.

**STOP-DOING honored:** HybridRouter not mentioned. The critic-routing detail ("Codex CLI and Anti-Gravity CLI in parallel") is about variance, not model routing — different system layer, no STOP-DOING violation. HybridRouter lives in Methods strip cell only.

**Code Brain numerics rule applied:** No fixed counts. "SDK agents that grow and shrink with the work" replaces the original "twelve SDK agents" line. Carry-forward constraint #1 — applies across the whole portfolio.

---

## A-3 · intent-engineering-mcp — LOCKED

**File:** `src/content/work/intent-engineering-mcp.mdx` — opener prose body (lines 57–61, inside `<Opener>` slot).

Replace the existing 3-paragraph opener with the three paragraphs below. **Frontmatter unchanged** — no edits to the methods, anchor metric, four_q, or any other field. This is an opener-prose-only swap.

```mdx
  <p>It shipped on May 12, 13 days early. <code>@swins/intent-engineering-mcp</code> — a single-purpose Model Context Protocol server that lives on npm, runs locally, and gives any MCP client a small set of intent-engineering tools: prompt scaffolds, intent templates, an audit step. The kind of thing I had been hand-rolling into terminals for months before it occurred to me that this might be a package.</p>

  <p>The reason it shipped early is that I cut the most exciting thing in the spec. The v1 had an embedded tool-call demo right here on this page — click a button, fire a real MCP call, render the response inline. I loved it. I cut it on April 30, the way you cut a scene that's slowing the second act, and slid it to a separate future spec where it is currently gathering dust instead of breaking production.</p>

  <p>What stayed is the load-bearing part — the DNS-verified registry listing, the npm publish, the install verification block, the handful of tools that actually do the job. The live block above this paragraph reads the install counts at 08:45 every morning. The investigation board below is the build calendar with the cut sitting visibly in the middle. The point of this page is the cut decision, not the ship date. The ship date was just the receipt.</p>
```

**Arc:** ship → cut → what stayed. ¶1 is the recruiter cold-scan paragraph: the date, the days-early number, the npm package name, what it actually is. ¶2 is the load-bearing argument — the PM principle that *cutting the exciting thing made the ship date* — anchored by the screenwriter→PM bridge ("the way you cut a scene that's slowing the second act"). ¶3 inventories what stayed and lands the closer.

**What changed from the existing prose:**
- ¶1 ran ~90 words after the comma; tightened to a 2-sentence rhythm + Sedaris sentence-end punchline ("...this might be a package").
- "since roughly 2013" cut — no concrete biographical anchor (beat 1 is 2000, beat 2 is 2015; 2013 was a council carryover).
- "five in the morning for months" sentimentality cut — replaced with the durable "for months."
- "on purpose, on the calendar" triple-on rhythm trimmed.
- ¶3 unchanged from existing — closer was already locked.

**Page-level callbacks honored:**
- **The screenwriter→PM bridge** — first appearance on the portfolio. About B-1 beat 4 references the WGA strike at age 30 (the moment the screenwriting career path closed); A-3 ¶2 references the screenwriting craft (cutting scenes) as the PM skill that shipped the work. Biographical extension, not repetition. Worth holding for the connection-noticing reader.
- **"08:45 every morning"** — meta-rhyme with the actual `<ShippedNow />` live block above the opener on the rendered page. Self-referential without being cute.
- **"the load-bearing part"** — first portfolio appearance. Available for future use.

**STOP-DOING honored:** No tagline echo ("Drawing up agents to act with intent." not echoed verbatim or paraphrased).

**No spec amendments needed** — the frontmatter `methods`, `anchor_metric`, `four_q`, and `shipped_stats_endpoint` already match spec §13.3 and project reality. Opener prose is the only change.

---

## A-4 · the-block — LOCKED

**File:** `src/content/work/the-block.mdx` — opener prose body (lines 52–54, inside `<Opener>` slot).

Replace the three existing `<p>` tags inside the `<Opener>` with the three paragraphs below. **Opener prose only.** The rest of the file (frontmatter dates, anchor metric, tagline, methods, four_q, investigation board) needs a separate cleanup pass — see Spec implications below.

```mdx
  <p>The Block is an institutional crypto data and research firm whose customers ran money for a living and whose product was, depending on the day, a Snowflake share, a PDF, or a phone call returned within the hour. Multiple surfaces — the Campus education product, the ETF launch page workflow on WordPress, the Polymarket × Campus sponsored-microcourse vertical, the .co homepage redesign competitive audit, the RevOps automation pipeline behind Salesforce closes. The bi-weekly Product and Engineering status update sat above all of them.</p>

  <p>The job was build it, ship it, and write it down carefully enough that leadership could read the bi-weekly update in under five minutes. That discipline transferred everywhere — the ETF launch page scopes easier when you've already drafted the version that fits in 90 words; the Simon feature ships faster when you've already drafted the line that justifies it. Institutional B2B doesn't have UX to lean on — only the agreement.</p>

  <p>The firm restructured around me; the products didn't. This page is about the products. The investigation board below is the build calendar in forward order — multiple surfaces, sanitized artifacts, and a public reference link at the end. The Methods strip is lean by design: most of the receipts from institutional B2B work have to be redacted before they ship.</p>
```

**Arc:** identity → discipline → page structure. ¶1 names the firm in present tense (ARCHIVED-register move — the company persists), inventories the surfaces in past tense (what Sean worked on while there), and lands the bi-weekly P&E update as the marquee artifact sitting above all of it. ¶2 names the load-bearing discipline (write-it-down compression for leadership) and shows how it transferred across surfaces. ¶3 uses the reader-dismissal signature move ("The firm restructured around me; the products didn't. This page is about the products.") to clear the layoff topic in one sentence and pivot to the work, then describes the page structure.

**What changed from the existing prose:**
- "Two years at a research firm..." → "The Block is an institutional crypto data and research firm..." (factual tenure correction; data + research framing per Sean's call).
- "Campus + RevOps" framing → multi-surface inventory (Campus + ETF launch pages + Data API + Simon + RevOps + AdOps + bi-weekly P&E).
- "2024 was Campus v1... 2025 was the tier structure... 2026 was the handoff doc" sentence cut (timeline was fabricated; Sean's tenure was 6 months, not 3 years).
- "the products didn't run on" extended Snowflake-share/PDF/phone-call list trimmed back to the strongest 3-item version.
- ¶2 added "Institutional B2B doesn't have UX to lean on — only the agreement" as the sharper PM-insight closer.

**Page-level callbacks honored:**
- **Reader-dismissal signature move** — preserved from existing prose. Same family as the parenthetical (Yes, 6. There are no rules, nerd) reader-dismissals catalogued in the voice-modes skill. One sentence to address the elephant, immediate pivot back to the work.
- **"Build it, ship it, write it down"** — Vonnegut affirmative constraint, first portfolio appearance. Available for future use (e.g., A-5 16bitfit if the discipline-of-shipping-the-pipeline angle calls for it).

**STOP-DOING honored:** The existing tagline ("Two products, one institutional research firm, two years.") is factually wrong and conflicts with the new scope; needs amendment in the broader cleanup. Opener prose does not echo any tagline.

---

## A-4 spec implications — bigger than the opener

A-4 is **opener-locked**, but `src/content/work/the-block.mdx` has additional fields anchored to the wrong premise that need a follow-up cleanup pass. Listed here for the Mac Mini work session that wants to ship the page complete:

| Field / section | Current state (wrong) | Needs to become |
|---|---|---|
| `tagline:` (frontmatter line 5) | `Two products, one institutional research firm, two years.` | Per Sean's anchor pick: `B2B PM · institutional crypto data and research` or similar; spec is silent on the tagline shape, so Sean's editorial call. |
| `date_started:` (line 15) | `2024-02-01` | `2025-11-01` (or actual start date in November 2025) |
| `date_active_through:` (line 16) | `2026-02-01` | `2026-05-04` (or actual layoff date) |
| `anchor_metric:` (line 19) | `"2024 – 2026 · TWO PRODUCTS · ONE FIRM"` | `"B2B PM · INSTITUTIONAL CRYPTO RESEARCH"` (per Sean's pick) |
| `methods:` array (lines 24–36) | Campus + RevOps + data partnerships only | Broaden to match multi-surface scope: Campus / ETF launch pages (Track Insight schema) / Data API / Simon AI feature / RevOps & AdOps / bi-weekly P&E stakeholder update. Sean to curate which 4–6 land on the Methods strip per spec §8.1 "4-7 rows typical." |
| `four_q:` (lines 38–43) | Tells a 2024–2026 three-tier-structure narrative; "B2B research is a relationship product" learn | Needs a fresh 4Q anchored to the actual 6-month multi-surface story. Suggest: Q1 = what the role was (APM Technical across multiple surfaces); Q2 = why the bi-weekly P&E update was load-bearing across all of them; Q3 = what would have broken (named failure modes); Q4 = what the role taught about institutional B2B PM craft. |
| Investigation board artifacts (lines 57–95) | Fabricated dates + metrics (Campus v1 launch "2024," tier structure "2025 Q1," subscriber chart 18→134 across "2024 Q2"–"2025 Q4") | Needs full rework. Forward-chronological calendar of the actual 6-month tenure with sanitized artifacts: a PRD decision Sean actually made, a sanitized board excerpt, a metric chart from real (sanitizable) data, the bi-weekly P&E update as a structural reference. No fabricated subscriber growth charts. |
| Spec §13.4 sample preamble | `"Two years of B2B and infrastructure work for an institutional crypto research firm. Campus (a B2B research platform) and the internal RevOps system that supported the broader Block business."` | If a separate `<FrameTheWorkPreamble />` component is still wanted per spec §12.4, sample text needs amendment to reflect 6-month multi-surface scope. Or — since the new opener ¶1 already does the framing work, consider whether the separate preamble is still required. **Spec edit needed either way.** |

**Recommended:** treat the broader the-block.mdx cleanup as a separate work session after the opener lands. It's not just text — the investigation board artifacts need real (sanitizable) source material Sean has access to. Larry Cermak as a named reference is a personal-network fact, not a page fact (per the no-desperation rule, no contactable references are named in the public prose).

---

## A-5 · 16bitfit — LOCKED

**File:** `src/content/work/16bitfit.mdx` — opener prose body (lines 54–56, inside `<Opener>` slot).

The existing prose is preserved verbatim with **one word change**: ¶2's "the game it was feeding" → "the game it was **supposed to feed**." All other prose carries forward as currently committed. Replace the three existing `<p>` tags with the three paragraphs below.

```mdx
  <p>I had every intention of making a 16-bit fitness game where you punch your way through a roster of cartoon brutes between sets of actual pushups. I had the fighter sketches, the punch-frame storyboards, a Phaser project with a working hit-stun timer, and the kind of unearned optimism that comes from picking a side project with a name as pleased with itself as "16BitFit." What I did not have, three months in, was a sprite pipeline I could trust to produce the same fighter twice in a row.</p>

  <p>So I looked at the two unfinished bets on the desk and picked one. The pipeline went one way. The game went on the shelf with a date on it. The pipeline became the case study. A system that spits out the same fighter through idle, punch, hurt, and KO — same hairline, same belt buckle, same stupid grin — turns out to be more interesting than the game it was supposed to feed. The game is going to inherit a tested production system. It is not going to inherit a half-built one I duct-taped to itself at 11pm.</p>

  <p>What got cut when the pause landed: a v1 gameplay loop that wasn't carrying its own weight without the polish layer, a Phaser atlas-streaming experiment I'd talked myself into mid-scope-creep, and an evening I would have spent angrily exporting frames I knew weren't right yet. What's below is the pipeline's progress through May, the pause decision, and the gameplay-loop work that got marked KILLED with no hard feelings.</p>
```

**Arc:** setup → pause decision → cut list. ¶1 builds the tension (everything was in place except the production system); ¶2 lands the pause as a calm PM call ("looked at the two unfinished bets on the desk and picked one"); ¶3 inventories what got cut with the closer "no hard feelings" doing the Vonnegut-flat work.

**What changed:** one word — "feeding" → "supposed to feed." The pipeline never actually fed sprites into the game (pause happened before anima shipped its first full short), so the past continuous was slightly inaccurate. "Supposed to feed" reads true and preserves the rhythm.

**Page-level callbacks honored:**
- **Anima cross-link** — "the game is going to inherit a tested production system" is the load-bearing PM logic that makes A-5 the natural sibling page to A-1. The Methods strip cross-link to `/work/animation-pipeline` makes this explicit.
- **"Same X, same X, same X" rule-of-three** — A-5's "same hairline, same belt buckle, same stupid grin" echoes the family-shape of About B-1 beat 6's "Same desk, different purpose" without verbatim repetition. Same signature, different specifics.
- **Rule of three with fourth-item pivot** — ¶1's "fighter sketches, the punch-frame storyboards, a Phaser project with a working hit-stun timer, and the kind of unearned optimism..." lands the unearned-optimism Sedaris closer. Signature move from voice-modes skill.

**STOP-DOING honored:** No iPad, no "Comprehension is the artifact," no "Building got cheap," no HybridRouter, no tagline echo. The tagline "The pipeline is the artifact, not the game." is implicit in the prose's logic but never echoed verbatim.

---

## A-5 spec implications — Methods strip stack drift

A-5's opener locks cleanly, but the existing `16bitfit.mdx` Methods strip has the same Seedance/NB2 confusion as the A-1 case-study spec:

| Field | Current state | Should be |
|---|---|---|
| `methods:` line 31–34 — sprite generation | `Seedance 2.0 + pixel-art Gemini skill` | `Gemini Nano Banana 2 (pixel-art Gemini skill)` — Seedance is video interpolation, not sprite stills. Pixel-art generation runs through the Nano Banana skill per Code-Brain inventory. |
| `methods:` cost on same row | `~$1/run` | Likely closer to `~$0.04/frame` per the A-1 NB2 cost; Sean to confirm actual sprite-gen run economics. |

This is the same stack-truth correction as A-1. Apply consistently across A-1 + A-5 in the same commit.

---

## Spec implications (case-study-spec-v1.md §13.1 + §13.2)

### A-1 · animation-pipeline stack errors (from earlier council pass)

The original spec §13.1 carries two stack errors. Both need amendment so Methods + opener prose stay aligned:

| Spec line | Currently says | Should say |
|---|---|---|
| §13.1 Opener angle hint | `"I taught Seedream to draw me."` | `"I taught Gemini Nano Banana 2 to draw me."` |
| §13.1 Methods strip line | `rembg / ffmpeg+libvpx-vp9 / Seedream 2.0 / Code Brain orchestration` | `rembg / ffmpeg+libvpx-vp9 / Gemini Nano Banana 2 / Seedance 2.0 / Code Brain orchestration` |
| §8.1 sample Methods table | `style anchor — Seedream 2.0 — ~$0.40/run` | `keyframe stills — Gemini Nano Banana 2 — ~$0.04/frame` + `motion interpolation — Seedance 2.0 — ~$0.40/clip (Fast tier)` |
| Appendix B sample frontmatter (`methods:`) | `Seedream 2.0` row | Two rows — NB2 for stills, Seedance 2.0 for motion |

### A-2 · code-brain anchor metric (count-based; fluctuates)

The original spec §13.2 hardcodes counts that will be stale within a week:

| Spec line | Currently says | Recommended |
|---|---|---|
| §13.2 Anchor metric | `16 SKILLS · 14 HOOKS · 8 AGENTS` | Drop counts; lean into the rhythm of the fleet. Three candidates: (a) `OVERNIGHT FLEET · LAUNCHD-NATIVE`; (b) `3 MACHINES · 1 VAULT · A LIVE FLEET`; (c) `LIVE SINCE 2024` *(if Sean has the actual start date)*. Sean to pick. |
| §13.2 Methods strip | `Claude Agent SDK / launchd / Ollama (Mac Mini + MBP) / nomic-embed-text / Qwen3-14B` | Unchanged — these are durable system names, not counts. Add `Codex CLI + Anti-Gravity CLI` row for the parallel critic per A-2 opener prose. |

**Recommended:** add a §1.2-style deferral/correction entry to case-study-spec-v1.md noting both corrections, dated 2026-05-26, rationale: "A-1: original §13.1 listed Seedream 2.0 as the style anchor; the production stack uses Gemini Nano Banana 2 for keyframe stills and Seedance 2.0 for motion interpolation per `anima/CLAUDE.md` + `PHILOSOPHY.md`. A-2: original §13.2 hardcoded skill/hook/agent counts that fluctuate weekly; anchor metric should be rhythm-based or date-based, not count-based. Carry-forward constraint #1 in `case-study-openers-locked-2026-05-26.md` makes this a portfolio-wide rule."

---

## Apply-on-Mac-Mini checklist — all 5 openers

When Sean opens this on the Mac Mini:

1. `cd /Users/seanwinslow/Code-Brain/sw-ai-pm-portfolio`
2. `git pull` (confirm clean working tree)
3. Open `src/content/work/animation-pipeline.mdx` → replace opener prose body with the locked 3 paragraphs per §A-1 above
4. Open `src/content/work/code-brain.mdx` → replace opener prose body with the locked 3 paragraphs per §A-2 above
5. Open `src/content/work/intent-engineering-mcp.mdx` → replace lines 57–61 (existing `<Opener>` body, explicit `<p>` tags) with the locked 3 paragraphs per §A-3 above. Frontmatter untouched.
6. Open `src/content/work/the-block.mdx` → replace lines 52–54 (existing `<Opener>` body, explicit `<p>` tags) with the locked 3 paragraphs per §A-4 above. **Opener only this session** — frontmatter, methods, four_q, and investigation board need a separate cleanup pass per §A-4 spec implications.
7. Open `src/content/work/16bitfit.mdx` → replace lines 54–56 (existing `<Opener>` body, explicit `<p>` tags) with the locked 3 paragraphs per §A-5 above. One-word change ("feeding" → "supposed to feed") in ¶2.
8. (Optional, recommended in same commit) Update `case-study-spec-v1.md` §13.1 + §13.2 + §13.4 + §8.1 + Appendix B per "Spec implications" above
9. (Optional, recommended in same commit) Fix Methods strip stack drift in `16bitfit.mdx` per §A-5 spec implications — `Seedance 2.0 + pixel-art Gemini skill` → `Gemini Nano Banana 2 (pixel-art Gemini skill)`, same correction as A-1.
10. Run `npm run dev` and confirm all five pages render the openers correctly
11. Commit: `git add . && git commit -m "5 case-study openers locked; stack corrections (NB2/Seedance) applied to A-1 + A-5"`
12. **Follow-up (separate session):** the-block.mdx broader cleanup — frontmatter dates, anchor metric, tagline, methods array, four_q, fabricated investigation board artifacts. Needs Sean's sanitizable source material.

---

*Authored 2026-05-26 in the brainstorm-ideas-existing skill session on MBP. Companion artifact to the About lock at [`about-b1-b2-b4-locked-2026-05-25.md`](about-b1-b2-b4-locked-2026-05-25.md). Sean retains voice authority on every locked string above.*

---
