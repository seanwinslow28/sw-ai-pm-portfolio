---
title: Ship Plan — seanwinslow.com v1 launch
status: RATIFIED 2026-05-24 (Q1/Q2/Q3/Q4/Q5 all locked; ready for writing-plans handoff)
created: 2026-05-23
authored_by: Claude Opus 4.7 (drafting), Sean Winslow (ratifying)
launch_target: 2026-05-25 (Mon EOD) or 2026-05-26 (Tue AM) per Option A
supersedes: nothing (first ship plan)
references:
  - docs/bugs-changes/phase-3f-ship-ready-plan.md                         # tactical authority for Pass A/B/C (per-pass detail)
  - docs/superpowers/specs/2026-05-24-project-tile-media-design.md        # B3 tile-media design spec — per-tile direction LOCKED
  - docs/superpowers/plans/2026-05-24-project-tile-media-implementation.md # B3 tile-media tactical implementation plan (7 tasks)
  - docs/specs/BLUEPRINT-COMPLETE.md                                      # §6 phase sequence
  - docs/specs/PORTFOLIO-MASTER-PLAN.md                                   # §3.3 voice register, §10 decisions
  - docs/prompts-and-references/2026-05-06-unified-roadmap copy.md        # Task 1 Steps 3.5–3.9
---

# Ship Plan — seanwinslow.com v1 launch

## 0. TL;DR

The build is much further along than CLAUDE.md indicates. Phase 2 + most of Phase 3 are shipped. The launch-encompassing remaining work is:

1. **Phase 3F** (per [`phase-3f-ship-ready-plan.md`](../bugs-changes/phase-3f-ship-ready-plan.md)) — Pass A (Cursor fix + strip 72 placeholders + author 5 case studies + refresh daily JSON), Pass B (A-3 tile overflow + teaser cascade + author 4 tile media), Pass C (bundle split + reduced-motion + essay consistency + trailing slashes + repo hygiene). **10–14 hours of focused work**, distributed Sat 5/24 → Mon 5/26.
2. **Phase 4** (this doc) — production config sanity, Vercel project link + deploy, `seanwinslow.com` apex via Cloudflare DNS-only, LinkedIn refresh same-day, supersede the deferred-deployment doc, update CLAUDE.md. **~2.5 hours of focused work**, Mon 5/26 midday.
3. **v1.1 stretch** — the 2 missing `EXPLANATION.md` files to hit ledger ≥5, Daily Driver morning cron wiring, agent-feed footer module, post-launch audit cleanup. Tue–Fri.

**Launch target: Mon 2026-05-25 EOD or Tue 2026-05-26 AM.** No-template-feeling gut-check is the final gate.

## 1. Context

### 1.1 Reality vs CLAUDE.md

CLAUDE.md says "Not started — all specs locked, all Phase 0 assets authored; Astro 5 scaffold is the next session." Git says branch `phase-2-foundations` is mid Phase 3e bug-fix, with 32 phase-3* commits since 5/19. **The actual state is post-Phase-3, pre-launch.**

| What CLAUDE.md says | What git says |
|---|---|
| Phase 2 not started | Phase 2 + Phase 3 + Phase 3a–3e all shipped |
| Scaffold next | All 17 pages exist + render |
| Daily Driver endpoints "to write" | All 4 JSON endpoints exist at `public/api/` |
| 5 surface specs to lock | All 9 locked; in-flight 3F bug-fix from today's audit |

CLAUDE.md status banner gets updated in Phase 4B.

### 1.2 Sean's Phase 3F decisions (load-bearing — captured in the IDE-opened doc)

From [`docs/bugs-changes/phase-3f-ship-ready-plan.md`](../bugs-changes/phase-3f-ship-ready-plan.md):

| Q | Decision |
|---|---|
| Sequencing | Pass A first, then Pass B, then Pass C. Stop the bleeding, then fix the funnel, then polish. |
| Case studies | **Author all 5.** Not 1 finished + 4 `COMING`. |
| Daily Driver JSON | **Refresh today's data manually** with real content from `vault/10_timeline/daily/2026-05-23.md`. |
| Project tile media | **Author all 4 missing tiles** (A-1, A-2, A-4, A-5). Style direction = open (Q1 below). |

This plan honors all four decisions. The 3-polish + 2-stamp alternative I'd previously sketched is **dropped**.

### 1.3 Open questions — status after the two folded-in plans

| # | Question | Status |
|---|---|---|
| Q1 | Tile-style direction | **LOCKED at per-tile direction** (Option 3 from the Phase 3F brainstorm) per [`docs/superpowers/specs/2026-05-24-project-tile-media-design.md`](../superpowers/specs/2026-05-24-project-tile-media-design.md). Each tile carries its own register: A-1 pencil-test walk cycle (Claude mascot solo) · A-2 pencil-and-ink concept graph (Sean inside) · A-3 editorial terminal on cream (Sean as marginalia) · A-4 hand-drawn 3-tier matrix (Sean annotating from margin) · A-5 photographic Game Boy crop (existing `assets/projects-tiles/16bitfit-photo-1.png`). Character placement varies per tile; rest beat on A-1. Fallback rule: any tile failing 2 attempts pivots to Option 2 (pencil-test register matching the hero) without forcing the others to follow. |
| Q2 | Daily-driver tone | **LOCKED — paper over the off-day, use today's morning Daily Driver fire as the signal.** Today (Sun 5/24) the Daily Driver fired this morning per Sean's confirmation — extract today's fleet signal from `vault/10_timeline/daily/2026-05-24.md` (or equivalent morning artifact) during A3, write a wire-service body that names what shipped this morning. Do NOT mention yesterday's power outage. |
| Q3 | next-piece.json target | **LOCKED — Vault Scorecard, date_target `2026-06-03`.** The Daily Driver surfaces the next artifact in the production pipeline, not the portfolio launch itself. |
| Q4 | Case-study authoring order | **LOCKED — A-3 → A-2 → A-1 → A-4 → A-5** (recruiter click-path). Independent from the **tile-image generation order** (A-5 → A-3 → A-1 → A-4 → A-2, risk-managed per the tile-media implementation plan) — the two orderings run on different work streams. |
| Q5 | Launch date | **LOCKED — Tue 5/26 AM target, Mon 5/25 EOD = pull-forward stretch if Sun closes Pass A ahead of schedule.** Sean plans to lock into today (Sun); Mon EOD launch is a possibility but not the commitment. The stretch-unlock rule: ahead of schedule = pull v1.1 forward / pull Phase 4 forward, never debug past 22:00. |

## 2. Named phases — full launch sequence

Maps to BLUEPRINT §6 + the Phase 3F doc + roadmap Task 1 Steps 3.5–3.9.

| Phase | When | What | Authoritative doc | Estimated effort |
|---|---|---|---|---|
| **3F-A** | Sat 5/24 (whole day) | Cursor.tsx fix · strip 72 `[PLACEHOLDER]` markers · author 5 case studies · refresh daily JSON · verify + commit | [`phase-3f-ship-ready-plan.md`](../bugs-changes/phase-3f-ship-ready-plan.md) §"Pass A" | **5–8h** (heaviest day; A2 case-study writing is the long pole) |
| **3F-B** | Mon 5/25 (per Q5 rec: Tue launch); B3 tile-media work parallelized across day | **B1** A-3 tile content overflow CSS fix (~30m) · **B2** teaser cascade JS/CSS fix (~45m) · **B3** author 5 project tile media — uses the LOCKED per-tile spec + implementation plan in [`docs/superpowers/plans/2026-05-24-project-tile-media-implementation.md`](../superpowers/plans/2026-05-24-project-tile-media-implementation.md) (Task 0 pre-flight → Task 1 A-5 crop → Task 2 A-3 terminal → Task 3 A-1 walk cycle → Task 4 A-4 matrix → Task 5 A-2 graph → Task 6 full-set QA → Task 7 CHANGELOG). Run via `superpowers:subagent-driven-development` for parallel tile generation where steps are independent. · verify + commit | `phase-3f-ship-ready-plan.md` §"Pass B" + tile-media implementation plan | **3–5h** (B1+B2: ~75m; B3 5-tile authoring: ~2–4h with subagent parallelization + 2-attempt-per-tile budget + visual QA per tile) |
| **3F-C** | Sun 5/25 late PM | Code-split diagram libs · reduced-motion contract fix · essay status/title/date consistency · trailing-slash audit · architecture slug spot-check · repo hygiene · verify + commit | `phase-3f-ship-ready-plan.md` §"Pass C" | **~2h** |
| **MVP-CONTENT-GATE** | Sun 5/25 EOD | All Phase 3F acceptance criteria green: zero `[PLACEHOLDER]` in `src/content/`, zero console errors on home, all 11 surfaces render at 390/1440, all 5 tiles carry authored media, swiper cascade visible, reduced-motion contract honored, Lighthouse Performance ≥95 on home | (this doc) | — |
| **4A — Production config sanity** | Mon 5/26 AM (~30m) | `npm run build` locally · verify `dist/sitemap-index.xml` + `dist/transactions/rss.xml` carry `seanwinslow.com` URLs (NOT `localhost:4321`) · diff dev vs `dist/` rendering · spot-check no broken View Transitions in built output | (this doc) | 30m |
| **4B — Vercel deploy** | Mon 5/26 mid-AM (~30m) | Push `phase-2-foundations` → `main` · Vercel dashboard → New Project → import `seanwinslow28/sw-ai-pm-portfolio` · framework auto-detects Astro · build command `npm run build` · output `dist/` · no env vars · deploy · verify `*.vercel.app` preview URL: home shows 5 tiles + 1 next-in-prod card, `/transactions/` shows 3 ledger rows, all 5 `/work/<slug>/` deep-dives resolve, `/architecture/vault-knowledge-mcp/` resolves, `/essays/meaning-over-access/` resolves | Roadmap Task 1 Step 3.6 verbatim | 30m |
| **4C — Apex DNS** | Mon 5/26 midday (~30m + 5m SSL wait) | Vercel → Project Settings → Domains → add `seanwinslow.com` + `www.seanwinslow.com` (www→apex redirect) · Vercel returns target CNAME/A records · Cloudflare DNS dashboard: add records with proxy = **DNS only** (orange-cloud OFF) · wait ~5min SSL provisioning · verify `https://seanwinslow.com` resolves with clean cert | Roadmap Task 1 Step 3.7 verbatim | 30m |
| **4D — Comms** | Mon 5/26 mid-PM (~30m) | LinkedIn refresh per Step 3.8: banner = `/transactions/` screenshot · headline = `AI PM \| Agent fleet operator \| seanwinslow.com/transactions` · "Open to Work" toggle ON, **recruiter-only** visibility · About paragraph rewrite ending with `seanwinslow.com` URL (LLM-council pass — see §5) · supersede [`2026-05-13-personal-site-deployment-deferred.md`](../prompts-and-references/2026-05-13-personal-site-deployment-deferred.md) (frontmatter `status: superseded-by-gap-fill-3` + 1-paragraph top note) · update CLAUDE.md status banner to "LIVE at seanwinslow.com 2026-05-25" + bump current-status date · save LinkedIn copy decision trail to `docs/comms/2026-05-26-linkedin-launch.md` | Roadmap Steps 3.8 + 3.9 | 30m |
| **MVP-STABLE-GATE** | Mon 5/26 EOD | All 5 Task 1 verification criteria green (see §3) + Sean's no-template-feeling gut-check | (this doc) | — |
| **4E — v1.1 stretch** (only if MVP-stable gate green) | Tue 5/26 → Fri 5/29 | (1) Lift 2 EXPLANATION.md from code-brain → ledger ≥5 rows. (2) Wire Daily Driver morning cron (`RunAtLoad=true` + idempotency guards + the 4 JSON writers). (3) Author agent-feed footer module (sub-daily ledger surface — currently deferred per Phase 3F "Out of scope"). (4) Process any post-launch recruiter feedback. | BLUEPRINT §6 Phase 4 + §6 Phase 5 head | Distributed |

### 2.1 Why Tue AM, not Mon EOD

The 5/23 Sat planning day elapsed without code work. Today is Sun 5/24. Realistic remaining scope:

- **Sun 5/24** (today): Pass A heavy day. 5–8h of focused work to land Cursor fix + 72 placeholder strips + 5 case studies + JSON refresh.
- **Mon 5/25**: Pass B + Pass C. 5–7h. B1+B2 visual bugs (~75m) + B3 5 tile images (~2–4h via subagent parallelization) + Pass C 6 polish items (~2h).
- **Tue 5/26 AM**: Phase 4. 2.5h. Production sanity → Vercel deploy → apex DNS → LinkedIn refresh → comms.

**Mon EOD launch is possible** if today (Sun) closes Pass A green AND Mon closes both Pass B + Pass C green AND Sean accepts running Phase 4 sequence into the evening. **Tue AM launch is the unhurried path** — Mon EOD = MVP-content-gate green; Tue AM = Phase 4 with a rested Sean.

**If today runs ahead of schedule** (via aggressive subagent-driven-development on case-study writing AND tile-media generation in parallel), Phase 4 sequence can pull forward to Mon midday — launch Mon afternoon instead of Tue AM. The stretch-unlock rule from Option A applies: ahead of schedule = pull v1.1 forward, never debug past 22:00.

### 2.3 Execution model — subagent-driven-development for the long poles

Two phases have natural parallelism that benefits from `superpowers:subagent-driven-development`:

| Long pole | Parallelization shape | Subagent count |
|---|---|---|
| **3F-A2.a case-study writing** (5 case studies × ~14 placeholders each) | One subagent per case study. Each subagent: invoke `.claude/skills/writing-voice-modes` (Sedaris opener · declarative-thesis 4Q · wire-service captions) → draft v0 → invoke `/llm-council` for opener (only) → write the placeholder fills → return MDX diff. Sean reviews 5 diffs in parallel (~15-20m each). | 5 (one per case study) |
| **3F-B3 tile-media authoring** (5 tiles, each ~30m gen + 2-attempt budget + QA + commit) | Per tile-media implementation plan: Task 1 (A-5 crop) runs first (validates pipeline + dirs); then Tasks 2–5 run in parallel as 4 subagents. Each subagent: write prompt → generate → visual-QA → if fail regenerate (attempt 2) → if fail again pivot to Option 2 fallback → resize/convert/commit. Task 6 (full-set QA) runs after all 4 return. | 4 (Tasks 2–5 in parallel after Task 1) |

The two pools run on **separate work streams** — case-study writing pool can run on Sun while tile-media pool runs on Mon. Or both pools run concurrently if Sun pulls ahead of schedule. The Sean-review bottleneck is the natural rate-limit (he reviews each subagent's output before commit).

LLM-council scope stays at 6 surfaces per §5 (5 case-study openers + LinkedIn About paragraph). About B-2 ¶3 is the 7th if the §5 inventory says yes (it does).

### 2.2 Files-touched manifest

**Phase 3F (per Sean's doc, summarized):**
- `src/components/Cursor.tsx` (A1) + `astro.config.mjs` Vite `optimizeDeps.dedupe` (A1)
- 9 MDX files in `src/content/work/` + `src/content/transactions/` + `src/content/about/` (A2 — strip 72 markers + author 5 case studies + write 2 about paragraphs)
- 3 JSON files in `public/api/` (A3)
- `src/components/projects/ProjectsSection.astro` (B1, C4) + `src/components/teaser/TeaserSwiper.astro` + `src/scripts/teaser-swiper.js` (B2)
- 5 new tile-media files at `public/assets/projects/` per the tile-media implementation plan: `animation-pipeline.webp` · `code-brain.webp` · `intent-engineering-mcp.webp` (REPLACES the existing misdirected OG-card PNG — that PNG gets `git rm`'d) · `the-block.webp` · `16bitfit.webp`
- New working dirs from the tile-media plan: `assets/tile-generation/raw/` (gitignored) + `assets/tile-generation/prompts/` (committed for traceability — 4 prompt files: animation-pipeline.txt · code-brain.txt · intent-engineering-mcp.txt · the-block.txt; A-5 doesn't need a prompt because it's a crop)
- `.gitignore` update to exclude `assets/tile-generation/raw/`
- 5 MDX files in `src/content/work/` get `hero_media_alt` text updated (and A-3 gets `hero_media` extension updated `.png` → `.webp`)
- `astro.config.mjs` Vite `rollupOptions.manualChunks` (C1) + `src/components/hero/CharacterLane.astro` reduced-motion gate (C2) + `src/content/essays/meaning-over-access.mdx` frontmatter (C3) + `src/pages/architecture/[slug].astro` spot-check (C5)
- Repo root cleanup: delete `hotfix-tear-boundary-current.png`, `audit-*.png` × 17, `.audit-screens/` (C6)

**Phase 4 (this doc):**
- No code changes in 4A/4B/4C (dashboard work + DNS only)
- 4D: `docs/prompts-and-references/2026-05-13-personal-site-deployment-deferred.md` frontmatter + top note · `CLAUDE.md` status banner + current-status date · `docs/comms/2026-05-26-linkedin-launch.md` (new file)

**Phase 4E (v1.1):**
- 2 new MDX files in `src/content/transactions/` (vault-synthesizer-eval-suite + substack-drafter, lifted from code-brain with slug-map + crosslink validation)
- New launchd plist + 1 shell script in `~/Code-Brain/code-brain/agents-sdk/` (Daily Driver cron — lives outside this repo)
- New agent-feed footer module — spec scope unauthored; defer to its own brainstorm in v1.1

## 3. MVP launch criteria — the gate

All 5 must be green by Mon 5/26 EOD. Drawn from roadmap Task 1 verification gate + Phase 3F acceptance criteria.

| # | Criterion | How to verify |
|---|---|---|
| 1 | `https://seanwinslow.com` resolves with clean SSL | Browser visit · `curl -I https://seanwinslow.com` returns 200 · Vercel domain status = "Valid Configuration" |
| 2 | Home shows 5 project cards + 1 next-in-production card per `projects-section-spec-v1` | Manual visit; all 5 tile media authored (not flat colors); next-in-prod card renders Vault Scorecard from `/api/next-piece.json` |
| 3 | `/transactions/` shows ≥3 ledger rows with working 4Q + repo links | Visit; click each of 3 deep-dives; verify 4Q renders; verify GitHub repo link resolves *(downgraded from ≥5 per Option A — ≥5 is a v1.1 stretch, not launch blocker)* |
| 4 | `/sitemap-index.xml` and `/transactions/rss.xml` both resolve with `seanwinslow.com` URLs (not `localhost:4321`) | Visit both URLs at apex; grep for `localhost` should return zero matches |
| 5 | Sean's no-template-feeling gut-check | Sean reviews live site against PMP §1.3 diagnosis ("does this feel like the design-system-viewer trap OR the luxury-minimal-PM-portfolio trap?") |

**Plus the Phase 3F acceptance criteria** (from `phase-3f-ship-ready-plan.md` §"Acceptance criteria"):
- `grep -r "PLACEHOLDER" src/content/ src/pages/` returns zero matches
- `npm run build` succeeds; only post-C1 expected warnings remain
- Zero console errors on `/` desktop + mobile (Cursor.tsx fix verified)
- All 11 surfaces render at 390/1440 viewports
- Reduced-motion: no `<video>` in DOM
- Lighthouse Performance ≥95 on home (after C1 bundle split)
- Today's date appears in home dateline (not 5/21 staleness)

## 4. v1.1 deferral list — explicit

| Item | Why deferred from MVP | Trigger to ship |
|---|---|---|
| 2 missing `EXPLANATION.md` → ledger ≥5 rows (vault-synthesizer-eval-suite, substack-drafter) | Source-file lift + slug-map + crosslink validation = ~2h focused; risks rushed deploy if jammed into Mon midday | Tue 5/27 AM if MVP-stable |
| Daily Driver morning cron (auto-write 4 JSON endpoints + `RunAtLoad=true` + idempotency guards) | Touches code-brain repo + scheduling work; today's power outage proves the manual fallback works; cron-wiring is risk-adjacent on launch day | Wed 5/28 |
| Agent-feed footer module (sub-daily ledger surface per CLAUDE.md "three load-bearing things" §3) | Currently absent (footer is link-columns only). Needs its own spec; not a launch blocker because the dateline + about-pulse carry the daily-dated layer | Whenever the sub-daily surface earns its own spec |
| §B-1 timeline ladder restructure | Closest the site comes to violating parallel-lineage by form. Content is parallel, visual form reads as ladder. Out of scope for ship per Phase 3F | Post-launch design pass |
| `RunAtLoad=true` plist hardening | ~30m work tracked in today's daily note as 5/23 medium | Bundle with Daily Driver cron work above |
| Plausible analytics single inline script | BLUEPRINT §5 lists as v2 enhancement | Whenever |
| MCP embed page (`/work/intent-engineering-mcp` with live read-only MCP tool call) | Embed is retrospective post-ship surface per PMP §7; not coupled to launch | Whenever the embed page earns its own spec |
| `the-block` + `16bitfit` content review against `case-study-spec §13.4 / §13.5` | Sean writes prose Sat 5/24; the "careful first draft" framing review per spec is a v1.1 polish pass | Tue or Wed |

**Stretch unlock rule:** if MVP-stable gate is green AND Sun ends ahead of schedule, pull v1.1 items in priority order (ledger rows → Daily Driver → agent-feed footer). **Drop any item that pushes the user past 22:00.** Never debug at midnight before a job-hunt week.

## 5. Writing-surface inventory — voice mode + llm-council per surface

Total writing surfaces touched in 3F-A2 + 4D = **11 surfaces** plus **72 placeholder slot fills**. The writing-voice-modes + llm-council loop fires per Sean's STEP 3 instructions:

1. Invoke `.claude/skills/writing-voice-modes` per surface (lock voice mode first; draft v0 in-mode)
2. Invoke `/llm-council` (premium profile default) with: surface · voice mode · the locked design philosophy · the three load-bearing things · the template-trap warning · ask for voice fidelity score + template-drift sniff + line-level edits + cliché kill-list
3. Apply council edits selectively — voice authority sits with Sean
4. Save council transcript next to the surface file for the decision trail

| Surface | State at 5/23 | Voice mode | LLM-council pass? |
|---|---|---|---|
| Hero tagline | LOCKED in code (PMP §4 Sean-voiced) | n/a | n/a |
| About lead + 7 stacked beats | LOCKED in code (Sean-voiced) | n/a | n/a |
| About B-2 ¶1-2 (comprehension thesis) | Landed prose in code | n/a | n/a |
| **About B-2 ¶3 (Sedaris pivot)** | `[PLACEHOLDER]` | **Sedaris (Domestic Observer)** | **Yes** — high-visibility About surface |
| **About B-4 ¶2 (building-toward bet)** | `[PLACEHOLDER]` | Sedaris (Domestic Observer) | No — too short for council overhead; single-shot through voice-modes |
| 6 cartoon-canon lesson captions | LOCKED in code (wire-service register) | n/a | n/a |
| Essay `meaning-over-access` (1600 words) | LOCKED in code (sober middle + Sedaris bookends) | n/a | n/a |
| Architecture `vault-knowledge-mcp` Q2-Q4 | Landed prose with vestige `[PLACEHOLDER —]` markers | strip markers only | No |
| Transaction `intent-engineering-mcp` Q2-Q4 | Landed prose with vestige markers | strip markers only | No |
| Transaction `vault-knowledge-mcp` Q2-Q4 | Landed prose with vestige markers | strip markers only | No |
| Transaction `phase-d-typed-edges` Q2-Q4 | Landed prose with vestige markers | strip markers only | No |
| **Case study `intent-engineering-mcp` (A-3)** | `[PLACEHOLDER]` × ~9 (opener + 4Q + 4 captions + Slack DMs) | **Sedaris (opener); declarative-thesis (4Q); wire-service (captions)** | **Yes** — opener is the highest-leverage prose on the most-visible tile |
| **Case study `code-brain` (A-2)** | `[PLACEHOLDER]` × ~14 | Same register split | **Yes** — opener |
| **Case study `animation-pipeline` (A-1)** | `[PLACEHOLDER]` × ~13 | Same register split | **Yes** — opener (unfakeable craft signal) |
| **Case study `the-block` (A-4)** | `[PLACEHOLDER]` × ~10 | **Same register split + sensitive framing per spec §13.4** | **Yes** — opener + framing review (frame-the-work-not-the-exit per locked decision) |
| **Case study `16bitfit` (A-5)** | `[PLACEHOLDER]` × 15 | Same register split + return-condition framing per spec §13.5 | **Yes** — opener |
| /contact body | LOCKED in code (sober wire-service) | n/a | n/a |
| /404 body | LOCKED in code (sober wire-service) | n/a | n/a |
| **3 daily-driver JSON refresh bodies** (dateline · about-pulse · next-piece) | Stale (last updated 5/21) | **Wire-service (dateline body); machine-data (about-pulse + next-piece)** | No — single-line strings; council overhead unjustified |
| **LinkedIn About paragraph** (Phase 4D) | NEW; needs authoring | **Sedaris (Domestic Observer)** | **Yes** — recruiter-facing single most-leveraged paragraph |
| (v1.1) `vault-synthesizer-eval-suite.mdx` 4Q | NEW | wire-service + sober/declarative | No — wire-service single-shot |
| (v1.1) `substack-drafter.mdx` 4Q | NEW | wire-service + sober/declarative | No — wire-service single-shot |

**LLM-council pass count: 6 surfaces** — About B-2 ¶3 · 5 case-study openers · LinkedIn About paragraph. Council transcripts saved per surface to `docs/writing-council/<surface-name>-2026-05-{date}.md`.

**Voice register cheat sheet for case-study writing (Pass A2.a):**
- **Opener (3 paragraphs):** Sedaris-coded (Domestic Observer). Specific nouns. Comedic juxtaposition without punchline. First-person warm. The parallel-lineage rule (PMP §3.1).
- **4Q block (4 short paragraphs):** Declarative-thesis register (essays middle-section voice). Sober. Past + present tense interchangeably. **No comedic register.** One claim per paragraph.
- **Investigation board artifact captions:** Wire-service short. One sentence. Mono register if anything.
- **Slack DM placeholders:** Real or anonymized DMs Sean actually had — otherwise **cut the artifact** rather than fake one. Spec §11.4 favors authenticity over completeness.

## 6. Risk forks + rollback per phase

| Phase | Risk | Likelihood | Mitigation | Rollback |
|---|---|---|---|---|
| 3F-A | Cursor.tsx fix doesn't dedupe React; console errors persist after rebuild | Medium | Fallback path in Phase 3F doc §A1.5: replace with plain `<canvas>` + vanilla TS. The cursor effect doesn't need React. | If fallback also fails, ship without the custom cursor (degraded but functional) |
| 3F-A | Case-study writing runs past Sat EOD (estimated 4–7h, possible 8–10h) | High | Use writing-voice-modes + Claude drafts in-mode → llm-council pass → Sean reviews 15-20m each (vs writing from scratch). If still over, pause; pick up Sun AM; postpone B-pass to Sun PM. | If case-study writing absolutely cannot close by Sun EOD: revert to 3-polish + 2-stamp split (this plan's prior shape); ship Mon with DRAFT-stamped the-block + 16bitfit; finish prose in v1.1 |
| 3F-B | Tile-media authoring runs over budget (per-tile direction = 5 unique registers, 2-attempt budget per tile + visual QA + retry pivots) | Medium-high | The tile-media implementation plan §"Task 1-5" has a 2-attempt-per-tile budget with Option 2 fallback (pencil-test register matching the hero) explicit. A-3 terminal text legibility is the highest technical risk → tackled second (Task 2) so failure surfaces early. Run Tasks 2–5 as 4 parallel subagents per §2.3. | Any single tile failing 2 attempts falls to Option 2 fallback (pencil-test of Sean + the project's core verb) per design spec §2 "Locked fallback." Doesn't force the others to follow. If multiple tiles fall to fallback, the projects splash reverts to a pencil-test set — closer to my originally-recommended Option 1 — which is still on-brand, just less varied. |
| 3F-B | A-3 terminal text legibility fails both attempts (the load-bearing technical risk per implementation plan Task 2) | Medium | Implementation plan §"Step 2.4" has the two-pass HTML/SVG composite pivot specced: generate background+character via image-gen, render terminal text as HTML + Playwright screenshot, composite via PIL. ~30m additional work. | If two-pass composite also fails, ship A-3 with a tile-media-style placeholder (npm logo + terminal-block-rendered-in-HTML at 800×1000) for MVP; regenerate properly in v1.1 |
| 3F-C | Bundle-split manualChunks change breaks an existing import | Low | Verify in `astro build` output before commit; smoke-test architecture page rendering | `git revert` the manualChunks commit; ship without C1 (Lighthouse score drops below 95 but page still works) |
| 4A | `dist/` output carries `localhost:4321` URLs in sitemap/RSS | Low | `astro.config.mjs` already has `site: 'https://seanwinslow.com'` — verified. Build script `prebuild` runs validators; build will fail if URLs are wrong | Fix `site:` constant; rerun build before push |
| 4B | Vercel build fails because `prebuild` script can't find a referenced file (e.g. `validate_about.mjs` byte-match against PMP §4 string) | Medium | Run `npm run prebuild && npm run build` LOCALLY first in 4A. Don't skip. | Fix the validation drift; commit; re-push (Vercel auto-deploys on push) |
| 4C | Vercel SSL handshake fails because Cloudflare proxy is orange-cloud ON | Medium-high | Roadmap Step 3.7 verbatim: set DNS-only / orange-cloud OFF BEFORE adding records. Verify in Cloudflare BEFORE adding Vercel domain. | If SSL fails: verify Cloudflare proxy status, retry. Vercel domain attachment can lag launch by a day without harm — fall back to `*.vercel.app` URL in LinkedIn copy; fix apex Tuesday |
| 4D | LinkedIn UI edit fails (banner upload, headline edit) | Low | LinkedIn UI is well-behaved; retry on failure | Revert LinkedIn to pre-launch state; ship LinkedIn refresh Tuesday; site is live regardless |
| 4E (v1.1) | Anything | n/a | Don't ship. Sleep. Revisit next day. v1.1 is stretch; failing it costs nothing | n/a |

## 7. Owners per task

- **Claude owns:** all code triage + commits + Vercel dashboard navigation guidance + DNS step-by-step + writing-voice-modes draft work + llm-council orchestration + spec doc writing + LinkedIn copy draft + CLAUDE.md status banner update
- **Sean owns:** Vercel dashboard clicks (login required) · Cloudflare DNS dashboard clicks (login required) · LinkedIn UI clicks + send · final voice authority on every council-revised draft (council recommends; Sean ratifies) · the gut-check at MVP-stable gate · the "is v1.1 stretch worth pulling tonight" call · the Q1-Q4 confirmations at ratification

## 8. Sequencing summary (calendar view) — assumes Q5 = Tue 5/26 launch

```
SUN 2026-05-24  (today — Pass A heavy day)
  AM    Phase 3F-A1   Cursor.tsx fix (Claude → verify clean console)     ~1h
  AM    Phase 3F-A3   Daily JSON refresh — needs Q2 confirmation         ~15m
  AM    Phase 3F-A2.b Strip vestige markers + 2 About ¶s
                      (About B-2 ¶3 gets council pass per §5)            ~1h
  PM    Phase 3F-A2.a Author 5 case studies via subagent-driven-dev:
                      5 parallel subagents (one per case study) draft
                      in writing-voice-modes → /llm-council on each
                      opener → return MDX diffs; Sean reviews            ~4-6h focused
  EOD   Phase 3F-A4   Verify + commit                                    ~15m

MON 2026-05-25  (Pass B + Pass C)
  AM    Phase 3F-B1   A-3 tile content overflow CSS fix                  ~30m
  AM    Phase 3F-B2   Teaser cascade JS/CSS fix                          ~45m
  AM    Phase 3F-B3   Task 0 pre-flight + Task 1 A-5 crop (sequential)   ~30m
  Mid   Phase 3F-B3   Tasks 2-5 parallel subagents (A-3 terminal + A-1
                      walk cycle + A-4 matrix + A-2 graph); per-tile
                      2-attempt budget + visual QA + commit              ~2-3h
  PM    Phase 3F-B3   Task 6 full-set QA + Task 7 CHANGELOG              ~30m
  PM    Phase 3F-C1-C6 Code-split · reduced-motion · essay · trailing
                      slash · arch slug · repo hygiene · verify          ~2h
  EOD   Phase 3F      MVP-CONTENT-GATE                                   —
        OR if ahead of schedule → start Phase 4 tonight (pull Mon EOD
        launch forward) — never debug past 22:00

TUE 2026-05-26  (launch day)
  AM    Phase 4A      Production config sanity                           ~30m
  AM    Phase 4B      Vercel deploy + *.vercel.app preview smoke-test   ~30m
  Mid   Phase 4C      Apex DNS (Cloudflare DNS-only) + SSL wait         ~30m + 5m
  PM    Phase 4D      LinkedIn refresh (council pass on About ¶) +
                      supersede deferred-deploy doc + CLAUDE.md update  ~30m
  EOD   MVP-STABLE-GATE                                                  —

WED-FRI 2026-05-27 → 2026-05-29  (Phase 4E v1.1 stretch)
  In priority order, drop any item that pushes past 22:00:
    Wed AM   2 missing EXPLANATION.md → ledger ≥5 rows                   ~2h
    Wed PM   Daily Driver cron + RunAtLoad + idempotency                 ~1.5h
    Thu      Agent-feed footer module spec + scaffold                    ~2h
    Fri      Audit cleanup + recruiter-feedback patches                  ~1h
```

**If Q5 = Mon 5/25 launch instead:** Pass B + Pass C compress into Mon AM (~5h); Phase 4 runs Mon late-PM (~2.5h); MVP-stable gate hits ~21:00. Accept the compression only if Sun closes Pass A early enough that Sean is rested for Mon's launch day.

## 9. Acceptance — what "shipped" means

Phase 4 is complete when **all of** the following are true (combines Phase 3F + Phase 4 + recruiter-facing tests):

- [ ] All Phase 3F §"Acceptance criteria (entire phase)" items green
- [ ] All 5 MVP launch criteria green (§3 above)
- [ ] LinkedIn refresh visible to a logged-out viewer with the new headline
- [ ] CLAUDE.md status banner reflects live state
- [ ] `2026-05-13-personal-site-deployment-deferred.md` carries `status: superseded-by-gap-fill-3`
- [ ] LinkedIn copy decision trail saved to `docs/comms/2026-05-26-linkedin-launch.md`
- [ ] Recruiter test (re-run after live deploy): hero passes 5s · case-study clickthrough passes 30s · deep-read passes 2 min
- [ ] No template-feeling gut-check passes against PMP §1.3 diagnosis

## 10. References

- [`docs/bugs-changes/phase-3f-ship-ready-plan.md`](../bugs-changes/phase-3f-ship-ready-plan.md) — tactical authority for Phase 3F Pass A/B/C
- [`docs/superpowers/specs/2026-05-24-project-tile-media-design.md`](../superpowers/specs/2026-05-24-project-tile-media-design.md) — Pass B-B3 design spec (per-tile direction LOCKED, shared compositional constraints, fallback rules)
- [`docs/superpowers/plans/2026-05-24-project-tile-media-implementation.md`](../superpowers/plans/2026-05-24-project-tile-media-implementation.md) — Pass B-B3 tactical implementation plan (7 tasks · per-tile prompts character-for-character · 2-attempt budget · two-pass HTML composite fallback for A-3)
- [`docs/specs/BLUEPRINT-COMPLETE.md`](BLUEPRINT-COMPLETE.md) §6 — phase sequence inheritance
- [`docs/specs/PORTFOLIO-MASTER-PLAN.md`](PORTFOLIO-MASTER-PLAN.md) §3.3 — voice register per surface · §10 — open cross-cutting decisions
- [`docs/prompts-and-references/2026-05-06-unified-roadmap copy.md`](../prompts-and-references/2026-05-06-unified-roadmap%20copy.md) Task 1 Steps 3.5–3.9 — deploy sequence verbatim
- [`CLAUDE.md`](../../CLAUDE.md) — orientation + three-load-bearing-things + template-trap warning
- [`.claude/skills/writing-voice-modes/`](../../.claude/skills/writing-voice-modes/) — voice calibration source
- `superpowers:subagent-driven-development` — execution model for the two long poles (case-study writing pool + tile-media generation pool, see §2.3)
