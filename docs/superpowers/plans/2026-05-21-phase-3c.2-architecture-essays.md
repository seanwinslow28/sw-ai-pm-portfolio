# Phase 3c.2 Implementation Plan — `/architecture/` + `/essays/` Surfaces

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (direct controller execution) to implement this plan task-by-task. **Do NOT dispatch via superpowers:subagent-driven-development** — Phase 2 / 2b / 3a / 3b / 3c.1 all attempted subagent dispatch and all hit hallucinated context constraints in the implementer subagent. Direct controller execution worked. Re-attempt subagents only if the harness has demonstrably improved since Phase 3c.1. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship `/architecture/` and `/essays/` — the two remaining content collection surfaces — per [`architecture-spec-v1.md`](../../specs/architecture-spec-v1.md) and [`essays-spec-v1.md`](../../specs/essays-spec-v1.md). Two index pages + two per-slug deep-dive routes + two RSS feeds + `astro-mermaid` integration with a 5-var palette override + extended prebuild gates (fetch a 3rd + 4th time; validate a 2nd + 3rd time with JD URL HEAD + lastValidated staleness; derive_crosslinks the 4-way close with essays' `plottedArtifacts` reverse-derivation). Reuses Phase 3b's `<FourQBlock />` + `<MethodsStrip />` shared components verbatim by import (architecture deep-dives only — essays don't render Methods). Forward-declared cross-link from `src/content/transactions/vault-knowledge-mcp.mdx` (`relatedArchitecture: vault-knowledge-mcp`, shipped in Phase 3c.1) lights up automatically when Task 7.1 lands `src/content/architecture/vault-knowledge-mcp.mdx`.

**Architecture:** Two Astro 5 content collections (`architecture` + `essays`) + 4 new routes + 2 RSS endpoints. Phase 3c.1's three prebuild scripts (`fetch_canonical_sources.mjs`, `validate_content.mjs`, `derive_crosslinks.mjs`) get extended in place at their declared extension points — no new prebuild scripts, no forks. The architecture deep-dive composes the 11-band layout per [`architecture-spec-v1.md`](../../specs/architecture-spec-v1.md) §2.2 (dateline → title block → thesis pullquote → essay body with embedded ER diagram + scoreboard + HONEST NOTE callouts → Try It Yourself → `<MethodsStrip />` → `<FourQBlock />` → 2 "Read the canonical →" links → Related → Next/Prev → registration mark). The essays deep-dive composes the 12-band layout per [`essays-spec-v1.md`](../../specs/essays-spec-v1.md) §2.2 (dateline → title block → published/revised strip → thesis pullquote → essay body with embedded quadrantChart + legend + role-map → plotted-artifacts → `<FourQBlock />` → 3 foot-of-page links → Related → Next/Prev → registration mark). Each index lists rows desc by `shipped` (architecture) or `published` (essays). View Transition seams `arch-title-<slug>` + `essay-title-<slug>` morph the row title into the deep-dive `<h1>` per the respective specs' §15. Cross-link graph closes 4-way: an essay's `plottedArtifacts: [intent-engineering-mcp]` auto-renders "← named in: Access vs Meaning" on the ledger row + (when present) the architecture writeup; an architecture writeup's `relatedLedgerRow: vault-knowledge-mcp` ⇄ the existing ledger row's `relatedArchitecture: vault-knowledge-mcp` close bidirectionally on first build after this plan ships. Mermaid `erDiagram` (architecture) + `quadrantChart` (essays) render as inline SVG at build time via `astro-mermaid` with a shared 5-var palette override; no client-side JS for diagrams.

**Tech Stack:** Astro 5 + Tailwind 4 (already installed Phase 2), `@astrojs/rss` (already installed Phase 3c.1), `astro-mermaid` (NEW), Node 20 for prebuild scripts, vanilla JS where unavoidable. No React. No charting library — the scoreboard is hand-rolled SVG; Mermaid handles ER + quadrantChart.

**Scope:** Matches [`architecture-spec-v1.md`](../../specs/architecture-spec-v1.md) §§2-18 + [`essays-spec-v1.md`](../../specs/essays-spec-v1.md) §§2-19.
- **IN:** `astro-mermaid` integration + 5-var palette override; architecture + essays content collection schemas; three prebuild-script extensions (the *same* three scripts from 3c.1, extended a 2nd time); 9 architecture-specific components (`Scoreboard`, `ScoreSymbol`, `HonestNote`, `TryItYourself`, `ErDiagram`, `ThesisPullQuote`, `IndexHeader`, `WriteupRow`, `RelatedBlock`); 9 essays-specific components (`QuadrantChart`, `QuadrantLegend`, `RoleMap`, `PublishedRevisedStrip`, `PlottedArtifacts`, `FootLinks`, `IndexHeader`, `EssayRow`, `RelatedBlock`); 6 routes (`/architecture/index.astro`, `/architecture/[slug].astro`, `/architecture/rss.xml.ts`, `/essays/index.astro`, `/essays/[slug].astro`, `/essays/rss.xml.ts`); 2 hand-written placeholder MDX entries (`vault-knowledge-mcp` for `/architecture/` + `meaning-over-access` for `/essays/`) each with a committed Mermaid source file; DoD walk-through against architecture-spec §18 items 1-18 + essays-spec §19 items 1-19.
- **OUT (Sean authors post-plan):** Real prose authoring beyond the 2 placeholder bodies (Sean owns post-plan; Q2-Q4 prose carries `[PLACEHOLDER —` for grep-ability the way 3c.1 did); upstream `VAULT_AS_AGENT_INFRASTRUCTURE_EXPLANATION.md` + `MEANING_OVER_ACCESS_EXPLANATION.md` + `MEANING_OVER_ACCESS.md` (the essay body) + `VAULT_AS_AGENT_INFRASTRUCTURE.md` (the architecture long-form essay body) — placeholders ship inline-body fallback per the OQ-C decision below; when Sean ships each upstream file, swap the placeholder's `explanationUrl: null` → real URL + drop the inline-body section; the next build's `fetch_canonical_sources.mjs` activates the canonical path.
- **OUT (Phase 3d):** `/about/` page body.
- **OUT (Phase 4):** Plausible analytics; Vercel deploy; custom-domain DNS; hard cutover from V3 bridge; the Daily Driver agent's `architecture_total` / `essay_total` virtual patterns (per architecture-spec §13 + essays-spec §13 — both surfaces synthesize their datelines at *build time* in Phase 3c.2 by reading their own collection; the daily-dated layer's home-hero dateline already fires `ledger_row` when an architecture or essay ships because each has a paired ledger row).
- **OUT entirely (v2):** Cross-surface View Transition morphs (architecture ↔ ledger, essay ↔ ledger ↔ architecture); aggregated root `/rss.xml` (gated per ledger §10.4 until `/essays/` >1 entry — v1 has 1); year/status/tag filters on either surface; sticky TOC; client-side Mermaid rendering; interactive Mermaid; OG image auto-generation via satori (v1 uses static PNGs); thesis-clustering on `/essays/` index; auto-compute `readingTime` from word count.

**Open Question resolutions (locked at planning time):**

| OQ | Decision | Why |
|---|---|---|
| **OQ-A — `astro-mermaid` palette override completeness?** | **Ship v1 with the 5-var override.** Observe the rendered Vault Scorecard ER diagram + Access-vs-Meaning quadrantChart during Section 7 smoke. Add more overrides only if the rendered diagram looks visually inconsistent with the surrounding page. | Architecture-spec OPEN-3's recommended default. The 5 vars (`background`, `primaryColor`, `primaryTextColor`, `primaryBorderColor`, `lineColor` + the `quadrant1Fill`-`quadrant4Fill` set for essays' quadrantChart per essays-spec §8.2) cover entity-relationship + quadrantChart rendering at the small scale v1 will hit. Mermaid has ~25 additional vars but they govern flowchart / sequence / gantt diagrams the portfolio doesn't render. Premature expansion would gold-plate without a concrete visual defect to fix. |
| **OQ-B — Mermaid SSR vs. mmdc PNG fallback if SVG rendering is inconsistent?** | **Ship SSR via `astro-mermaid`.** Document `mermaid-cli` (mmdc) build-time PNG export as the v1.1 escape hatch only if the rendered SVG looks broken on either of the 2 placeholders. | Architecture-spec §9.5 already documents the SVG fallback contract: when `astro-mermaid` fails to render (syntax error, missing source), the build succeeds but the page renders a `<pre>` showing the raw Mermaid source. The validator's MDX-front-matter `mermaidSource:` existence check (Task 3.2 Step 3 extension) catches the missing-file case before deploy. SSR-first matches the broader anti-stack discipline (no client-side JS for diagrams); mmdc would add a build-time dep + complicate the build matrix for a hypothetical defect that v1's 2 simple diagrams almost certainly won't hit. |
| **OQ-C — Ship Vault Scorecard + Access-vs-Meaning with inline-body 4Q fallback?** | **Yes — same pattern as Phase 3c.1's OQ3 (a).** Both placeholders ship with `explanationUrl: null` AND inline-body 4Q h2 sections (`## What is this?` / `## Why this approach?` / `## What would break?` / `## What did I learn?`) in the MDX body. The validator's `requiredOneOf: [explanationUrl, __inline_4Q__]` rule from 3c.1 is extended to both collections in Task 3.2. When Sean ships the upstream `VAULT_AS_AGENT_INFRASTRUCTURE_EXPLANATION.md` + `MEANING_OVER_ACCESS_EXPLANATION.md`, swap the placeholder's `explanationUrl: null` → real URL + drop the inline-body sections from the MDX body (or leave them as a Q1-only opener and let the canonical EXPLANATION render below — both work). | This is the **strict deviation from architecture-spec §3.1**, which explicitly forbids inline-body fallback for architecture entries ("no legacy V3 entries to migrate, so the strict canonical-only rule applies from day one"). The kickoff prompt explicitly signs off on the deviation: *"Recommended default: same as Phase 3c.1's OQ3 (a) — ship the infrastructure now, hand-author placeholders with inline-body 4Q, defer upstream canonical wiring until Sean publishes the upstream `_EXPLANATION.md` files."* Phase 3c.1 OQ3 (a) shipped 3 ledger rows with inline-body 4Q end-to-end with no validator pain; replicate the pattern here. Document the deviation in the validator comment AND in each placeholder's frontmatter comment so a future audit can find it. When Sean ships canonical sources, the deviation closes itself — no schema migration needed. |
| **OQ-D — Split 3c.2 (architecture) from 3c.3 (essays)?** | **SHIP TOGETHER as 3c.2** at ~26 tasks. | Counted: 1 preflight + 1 mermaid integration + 1 schema task + 3 prebuild script extensions + 5 architecture component tasks (9 components bundled where they share scope) + 4 essays component tasks (9 components bundled) + 4 route tasks + 2 placeholder MDX tasks + 5 verification + DoD tasks = **26 tasks total**. Below the 30-35 upper bound the kickoff prompt set as the subagent-fatigue ceiling. Splitting would force the second plan to re-establish the `astro-mermaid` integration + the unified-validator merge + the `derive_crosslinks.mjs` 4-way close — all shared infrastructure built once in 3c.2. The 26-task footprint is comparable to 3c.1's ~30 tasks; controller execution comfortably handled that. |

**Branch state:** `phase-2-foundations` at HEAD `07a8cf1` (Phase 3c.1 completion mark — 21 commits past the planning commit `ff21c1c`). 2 untracked screenshots (`phase-2b-full.png`, `teaser-closeup.png`) + 3 untracked PNG backups under `reference-images/about-cartoons/removed/` are pre-existing; ignore. **Do NOT switch branches.** Sean's standing rule is "Sean works on `main`" but Phase 2 / 2b / 3a / 3b / 3c.1 accumulated on this branch — the spec'd cutover to main is Phase 4. Each task in this plan commits atomically; ~28-32 incremental commits total.

**Operating posture (direct controller execution):** Run each task in order. After each commit, verify the next task's preconditions are still true (e.g., dev server still serving 200 on existing routes, `npm run validate` still passes, prebuild gates still exit 0). Surface BLOCKED status only if genuinely stuck (real error in build log, route returning non-2xx that should be 200). Don't pause to check in between routine tasks — the user signed off on continuous execution.

**Lessons folded in from Phase 3c.1 execution:**
- **MDX gotcha:** placeholder prose mentioning component-shaped names (`<ShippedNow />`, `<Annotation />`, `<Scoreboard />`, etc.) MUST be wrapped in backticks. MDX parses bare `<UppercaseTag />` in the body as a JSX component reference and fails the build with `Expected component X to be defined`. Phase 3c.1's `intent-engineering-mcp` Q4 prose hit this and needed a follow-up commit (07a8cf1). When drafting the architecture + essays placeholders (Tasks 7.1 + 7.2), audit every `<Something />` in the body prose and wrap in backticks unless it's a real component you're invoking. Apply the same rule to Q2-Q4 placeholder text — anything that *looks* like a component name needs backticks.
- **Dev server caches content collections.** When a new MDX lands in `src/content/architecture/` or `src/content/essays/`, `astro sync` alone isn't enough — the dev server's vite cache holds the old `getCollection()` result. Between Task 7.1 / 7.2 (placeholder MDX authoring) and the Section 8 smoke tests, **kill the dev server and re-run `npm run dev`**, OR rely on the production build (`npm run build`) for end-to-end smoke as 3c.1's Task 9.2 did.
- **Astro routing collision pattern doesn't apply here.** `/architecture/` and `/essays/` each use a *single* `[slug].astro` dynamic route — no `[category]/index.astro` collision risk like 3c.1's `[surface]/index.astro` vs. `[slug].astro`. No `forbiddenSlugs` entries needed on either new collection. (If you find yourself wanting to add a second dynamic route under either surface — e.g., `/architecture/<topic>/index.astro` — stop and re-spec; the v1 design assumes one route per surface beyond the index.)
- **The Phase 3b shared components import cleanly.** `MethodsStrip.astro` takes `{ methods: MethodRow[] }`; `FourQBlock.astro` takes `{ frame, slug, fourQ?, canonicalMarkdown?, explanationUrl? }`. Both are used in 3c.1 at `src/pages/transactions/[slug].astro` and 3c.2's architecture deep-dive (Task 6.2) imports them with the same prop shape. Essays does NOT render `<MethodsStrip />` (essays-spec §3.1 explicitly excludes `methods[]` from the essays schema — essays cite artifacts via `plottedArtifacts`, not methods). Essays DOES render `<FourQBlock />` (essays-spec §19 DoD item 12).

---

## File Structure

Files this plan creates or modifies, organized by responsibility:

```
sw-ai-pm-portfolio/
├── package.json                                          ← Task 1.1: add astro-mermaid dependency
├── astro.config.mjs                                      ← Task 1.1: add mermaid integration + 5-var palette override
│
├── scripts/
│   ├── fetch_canonical_sources.mjs                       ← Task 3.1: extend WALK_COLLECTIONS — add architecture (2 walks: explanationUrl + essaySourceUrl) + essays (2 walks: explanationUrl + sourceUrl)
│   ├── validate_content.mjs                              ← Task 3.2: extend COLLECTIONS — add architecture + essays; merge in JD URL HEAD checks + roleMap.lastValidated staleness (warnings only)
│   └── derive_crosslinks.mjs                             ← Task 3.3: swap archSlugs/essaySlugs empty Sets to readSlugSet() calls on real dirs; add architecture + essays passes; compute essays' plottedArtifacts reverse-derivation
│
├── src/
│   ├── content/
│   │   ├── config.ts                                     ← Task 2.1: add architecture + essays collections
│   │   ├── architecture/                                 ← NEW folder
│   │   │   ├── vault-knowledge-mcp.mdx                   ← Task 7.1: first occupant; status SHIPPED; inline-body 4Q (OQ-C fallback)
│   │   │   ├── diagrams/                                 ← NEW folder
│   │   │   │   └── concept-edges-erd.mmd                 ← Task 7.1: erDiagram source committed
│   │   │   └── essays/                                   ← NEW folder, gitignored — fetched at build by fetch_canonical_sources.mjs (architecture's long-form essay bodies)
│   │   └── essays/                                       ← NEW folder
│   │       ├── meaning-over-access.mdx                   ← Task 7.2: first occupant; status PUBLISHED; inline-body 4Q (OQ-C fallback)
│   │       ├── diagrams/                                 ← NEW folder
│   │       │   └── access-meaning-spectrum.mmd           ← Task 7.2: quadrantChart source committed
│   │       └── essay-bodies/                             ← NEW folder, gitignored — fetched at build by fetch_canonical_sources.mjs (essays' long-form bodies)
│   │
│   ├── components/
│   │   ├── architecture/                                 ← NEW folder
│   │   │   ├── ScoreSymbol.astro                         ← Task 4.1: inline SVG ✓ ✓✓ ⚠ ✗ at 14×14 with palette tokens
│   │   │   ├── Scoreboard.astro                          ← Task 4.1: flat comparison table; reads frontmatter scoreboard:
│   │   │   ├── HonestNote.astro                          ← Task 4.2: mono callout with stamp-amber prefix + 1px secondary-ink left border
│   │   │   ├── TryItYourself.astro                       ← Task 4.3: teal-wash code-block callout with Shiki bash highlight
│   │   │   ├── ErDiagram.astro                           ← Task 4.4: thin wrapper around Mermaid SVG output + caption
│   │   │   ├── ThesisPullQuote.astro                     ← Task 4.4: Newsreader 24px italic with 0.5px teal rules (REUSED by essays via import)
│   │   │   ├── IndexHeader.astro                         ← Task 4.5: h1 + italic hook + sober subhead + metadata strip for /architecture/
│   │   │   ├── WriteupRow.astro                          ← Task 4.5: single row on the index (~140px desktop)
│   │   │   └── RelatedBlock.astro                        ← Task 4.5: reads .crosslinks.json for reverse-rendering
│   │   └── essays/                                       ← NEW folder
│   │       ├── QuadrantChart.astro                       ← Task 5.1: wraps Mermaid quadrantChart SVG + caption
│   │       ├── QuadrantLegend.astro                      ← Task 5.1: wire-service mono table reading quadrantLegend[]
│   │       ├── RoleMap.astro                             ← Task 5.2: 4-column structured table + last validated mono badge
│   │       ├── PublishedRevisedStrip.astro               ← Task 5.2: conditional render; only when lastRevised ≠ published
│   │       ├── PlottedArtifacts.astro                    ← Task 5.3: auto-rendered list from plottedArtifacts[] + crosslinks.json
│   │       ├── FootLinks.astro                           ← Task 5.3: Substack + canonical source + canonical EXPLANATION links
│   │       ├── IndexHeader.astro                         ← Task 5.4: h1 + italic hook + sober subhead + metadata strip for /essays/
│   │       ├── EssayRow.astro                            ← Task 5.4: single row on the index (~150px desktop)
│   │       └── RelatedBlock.astro                        ← Task 5.4: reads .crosslinks.json for reverse-rendering
│   │
│   ├── pages/
│   │   ├── architecture/                                 ← NEW folder
│   │   │   ├── index.astro                               ← Task 6.1: /architecture/ index
│   │   │   ├── [slug].astro                              ← Task 6.2: /architecture/<slug>/ deep-dive (11 bands)
│   │   │   └── rss.xml.ts                                ← Task 6.1: RSS endpoint
│   │   └── essays/                                       ← NEW folder
│   │       ├── index.astro                               ← Task 6.3: /essays/ index
│   │       ├── [slug].astro                              ← Task 6.4: /essays/<slug>/ deep-dive (12 bands)
│   │       └── rss.xml.ts                                ← Task 6.3: RSS endpoint
│   │
│   └── content/
│       ├── architecture/.crosslinks.json                 ← Task 3.3: derived at build (gitignored)
│       └── essays/.crosslinks.json                       ← Task 3.3: derived at build (gitignored)
│
├── .gitignore                                            ← Task 3.1 + 3.3: add architecture/essays/{.crosslinks.json,essays/,essay-bodies/}
│
└── docs/superpowers/plans/
    └── 2026-05-21-phase-3c.2-architecture-essays.md      ← this file
```

The `architecture/` + `essays/` folders are new under `src/content/`, `src/components/`, and `src/pages/`. Mirrors the Phase 3c.1 `transactions/` split exactly.

**No routing collision in 3c.2.** Both `/architecture/[slug].astro` and `/essays/[slug].astro` are sole dynamic routes under their respective folders (no `[category]/index.astro` to collide with). No `forbiddenSlugs` entries needed on either schema.

**No `[OPEN-N]` items remain in either spec after the OQ-A through OQ-D resolutions above.** Architecture-spec §16 OPEN-1 (Control Architecture surface placement) defers to a post-3c.2 ship — Task 14 in the roadmap, not in scope. OPEN-2 (index page hook) is locked to the Vault Scorecard identity claim per the spec default; OPEN-4 (manual reading time) is the schema default; OPEN-5 (honest-note anchor mismatch = warning) is the validator default in Task 3.2. Essays-spec §17 OPEN-1 through OPEN-5 are all locked to their recommended defaults per BLUEPRINT §4.7.

---

## Section 0 — Pre-flight

### Task 0.1: Verify Phase 3c.1 baseline + clean working tree

**Files:**
- Read-only verification

- [ ] **Step 1: Verify branch + HEAD + working tree state**

Run: `git branch --show-current && git rev-parse HEAD && git status --porcelain | grep -vE "phase-2b-full\.png|teaser-closeup\.png|reference-images/about-cartoons/removed/"`

Expected: branch `phase-2-foundations`, HEAD `07a8cf1` (or later if any v1.1 follow-ups landed since planning), `git status --porcelain` returns no output once the pre-existing untracked files are filtered out.

- [ ] **Step 2: Verify Phase 3c.1 artifacts are in place**

Run in parallel (4 reads):
- `ls scripts/fetch_canonical_sources.mjs scripts/validate_content.mjs scripts/derive_crosslinks.mjs`
- `ls src/components/case-study/FourQBlock.astro src/components/case-study/MethodsStrip.astro`
- `ls src/content/transactions/*.mdx | wc -l` → expected `3`
- `ls src/pages/transactions/index.astro src/pages/transactions/\[slug\].astro src/pages/transactions/rss.xml.ts`

Expected: all paths exist; transactions has 3 MDX entries (`phase-d-typed-edges`, `intent-engineering-mcp`, `vault-knowledge-mcp`).

- [ ] **Step 3: Verify prebuild gates pass on existing state**

Run: `npm run validate && node scripts/derive_crosslinks.mjs && node scripts/fetch_canonical_sources.mjs`

Expected: all three exit 0. The validator prints `done.` with `✓ transactions/...` lines for 3 entries; derive_crosslinks prints `done. validated N cross-links` including the forward-declared `vault-knowledge-mcp → /architecture/vault-knowledge-mcp/` line; fetch_canonical_sources prints `done.` with no actual fetches (no transaction declares `explanationUrl` in v1). If any gate fails, fix the regression before continuing — Phase 3c.2 builds on a green Phase 3c.1 baseline.

- [ ] **Step 4: Confirm Phase 3c.1's forward-declared relatedArchitecture is still in place**

Run: `grep -A1 "^relatedArchitecture:" src/content/transactions/vault-knowledge-mcp.mdx`

Expected: line reads `relatedArchitecture: vault-knowledge-mcp` (the slug Task 7.1 of this plan creates). This is the forward declaration that will light up bidirectionally once Task 7.1 ships `src/content/architecture/vault-knowledge-mcp.mdx`.

- [ ] **Step 5: Start dev server in background**

Run: `npm run dev` in a background terminal (or confirm one is already running via `curl -sI http://localhost:4321/transactions/ | head -1` → `HTTP/1.1 200 OK`).

- [ ] **Step 6: No commit**

Verification only.

---

## Section 1 — `astro-mermaid` integration + 5-var palette override

Both surfaces depend on Mermaid SSR. Land the integration once, configured at build time, with a shared 5-var palette override that covers `erDiagram` (architecture) + `quadrantChart` (essays) per architecture-spec §9.2 + essays-spec §8.2. No client-side JS; the integration emits inline SVG on the rendered page.

### Task 1.1: Install `astro-mermaid` + configure 5-var palette override

**Files:**
- Modify: `package.json`
- Modify: `astro.config.mjs`

- [ ] **Step 1: Install the dependency**

Run: `npm install astro-mermaid --save`

Expected: `package.json` gains `"astro-mermaid": "^1.x"` under `dependencies`. (The latest minor at the time of this plan is sufficient; pin to the installed version after install resolves.)

- [ ] **Step 2: Verify the install**

Run: `node -e "console.log(require('astro-mermaid/package.json').version)"`

Expected: prints the installed version. If this fails, the package isn't resolvable — retry the install with `--legacy-peer-deps` only if a peer-conflict error is the actual cause.

- [ ] **Step 3: Read existing `astro.config.mjs`**

Run: `wc -l astro.config.mjs`

Expected: ~25 lines. Confirm before editing — the edit replaces the `integrations:` line to add `mermaid()` between `mdx()` and `sitemap()` (Mermaid must run after MDX so the MDX renderer can parse the resulting SVG-wrapped fenced code blocks).

- [ ] **Step 4: Replace `astro.config.mjs` with the extended version**

Replace the entire file with:

```js
import { defineConfig } from 'astro/config';
import mdx from '@astrojs/mdx';
import sitemap from '@astrojs/sitemap';
import react from '@astrojs/react';
import mermaid from 'astro-mermaid';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  site: 'https://seanwinslow.com',
  // ============================================================
  // Phase 3c.2 — astro-mermaid integration
  // Source: architecture-spec-v1.md §9.2 + essays-spec-v1.md §8.2.
  // 5-var palette override (OQ-A: ship v1 with 5-var, observe,
  // expand only if rendered SVG looks visually inconsistent).
  // Mermaid must come AFTER mdx() so MDX renders the SVG-wrapped
  // fenced code blocks correctly.
  // ============================================================
  integrations: [
    mdx(),
    mermaid({
      theme: 'base',
      autoTheme: false,
      mermaidConfig: {
        themeVariables: {
          background: '#FFF9F0',
          primaryColor: '#FFF9F0',
          primaryTextColor: '#0A3E42',
          primaryBorderColor: '#0A3E42',
          lineColor: '#546E71',
          fontFamily: '"JetBrains Mono", monospace',
          fontSize: '13px',
          // Essays quadrantChart fills — all four quadrants share the paper
          // bg (no quadrant tinting). Per essays-spec §8.2.
          quadrant1Fill: '#FFF9F0',
          quadrant2Fill: '#FFF9F0',
          quadrant3Fill: '#FFF9F0',
          quadrant4Fill: '#FFF9F0',
        },
      },
    }),
    sitemap(),
    react(),
  ],
  vite: {
    plugins: [tailwindcss()],
  },
  experimental: {
    // Astro 5 has ClientRouter View Transitions stable; no flag needed.
  },
  build: {
    inlineStylesheets: 'auto',
  },
  prefetch: {
    prefetchAll: false,
    defaultStrategy: 'hover',
  },
});
```

- [ ] **Step 5: Verify dev server reloads cleanly**

Run: `curl -sI http://localhost:4321/ | head -1`

Expected: `HTTP/1.1 200 OK`. If the dev server failed to restart cleanly (the integration loaded with a syntax error), Astro will print the error in the terminal where `npm run dev` runs. Restart the dev server: kill the existing process, then `npm run dev`.

- [ ] **Step 6: Commit**

```bash
git add package.json package-lock.json astro.config.mjs
git commit -m "$(cat <<'EOF'
feat(phase-3c.2): astro-mermaid integration + 5-var palette override

Installs astro-mermaid and configures the build-time SSR integration
for both /architecture/ erDiagram + /essays/ quadrantChart per
architecture-spec §9.2 + essays-spec §8.2.

The 5-var palette override (OQ-A: ship v1 with 5-var, observe,
expand only if rendered SVG looks visually inconsistent) covers:
  - background, primaryColor, primaryTextColor, primaryBorderColor,
    lineColor (the architecture-spec named primary 5)
  - quadrant1Fill–quadrant4Fill (the essays-spec quadrantChart
    parallel; all four quadrants share paper bg, no tinting)
  - fontFamily JetBrains Mono + fontSize 13px

Mermaid integration ordered AFTER mdx() so MDX renders the SVG-wrapped
fenced code blocks correctly. autoTheme: false locks the theme to
'base' + our overrides (no system-prefers-color-scheme switching).

Per architecture-spec §9.2 + essays-spec §8.2 + BLUEPRINT §3.3.
EOF
)"
```

---

## Section 2 — Content collection schemas

The architecture + essays collections land in `src/content/config.ts` alongside the existing `work` + `teaserDeck` + `transactions` collections. Both schemas mirror their respective specs' Appendix B field names exactly.

### Task 2.1: Extend `src/content/config.ts` with `architecture` + `essays` collections

**Files:**
- Modify: `src/content/config.ts`

Per architecture-spec §3 + essays-spec §3 + their respective Appendix Bs.

- [ ] **Step 1: Read existing config**

Run: `wc -l src/content/config.ts`

Expected: ~118 lines (3 collections × Phase 3c.1 state). Confirm before editing — the edit appends two new collection definitions without touching the existing three blocks.

- [ ] **Step 2: Replace `src/content/config.ts` with the extended version**

Replace the entire file with:

```ts
import { defineCollection, z } from "astro:content";

const methodRow = z.object({
  task: z.string(),
  tool: z.string(),
  cost: z.string().optional(),  // architecture-spec §3 makes cost optional
  link: z.string().nullable().optional(),
});

const fourQ = z.object({
  what: z.string(),
  why: z.string(),
  break: z.string(),
  learn: z.string(),
});

const work = defineCollection({
  type: "content",
  schema: z.object({
    // `slug` is reserved by Astro — comes from frontmatter override or filename
    frame: z.enum(["A-1", "A-2", "A-3", "A-4", "A-5"]),
    title: z.string(),
    tagline: z.string().optional(),
    status: z.enum(["ACTIVE", "COMING", "PAUSED", "ARCHIVED", "SHIPPED"]),
    tags: z.array(z.string()),
    hero_media: z.string(),
    hero_media_type: z.enum(["video", "image"]),
    hero_media_alt: z.string(),
    order: z.number().int().min(1).max(5),
    date_started: z.coerce.string().optional(),
    date_active_through: z.coerce.string().optional(),
    case_study_dateline_pattern: z
      .enum(["fleet_pulse", "ship_log", "reading_log", "now_line", "ledger_row"])
      .optional(),

    // --- Phase 3b additions (case-study-spec Appendix B) ---
    role: z.string(),
    anchor_metric: z.string(),
    investigation_order: z.enum(["reverse", "forward"]).default("reverse"),

    methods: z.array(methodRow).min(1),

    // One of (four_q, explanation_url) is required — enforced by
    // scripts/validate_content.mjs since Zod can't express "one-of" cleanly.
    four_q: fourQ.optional(),
    explanation_url: z.string().url().optional(),

    // --- Status-specific frontmatter (validator enforces presence per status) ---
    shipped_at: z.coerce.string().optional(),
    shipped_stats_endpoint: z.string().optional(),
    return_condition: z.string().optional(),
    archived_reference_url: z.string().url().optional(),
  }),
});

// ============================================================
// Phase 3c.1 — transactions collection
// Source: transactions-spec-v1.md §3.2
//        + architecture-spec-v1.md §14.1 (relatedArchitecture additive)
//        + BLUEPRINT-COMPLETE.md §5 punch-list #1
// ============================================================

const SURFACE_ENUM = ["fleet", "pipeline", "product", "writing", "infra"] as const;

const transactions = defineCollection({
  type: "content",
  schema: z.object({
    // --- Identity ---
    title: z.string(),
    dateline: z.string(),                                    // "BOSTON, MAY 13, 2026"
    shipped: z.string().regex(/^\d{4}-\d{2}-\d{2}$/),        // ISO YYYY-MM-DD

    // --- IA + status ---
    surface: z.enum(SURFACE_ENUM),
    status: z.enum(["ACTIVE", "SHIPPED", "PAUSED", "ARCHIVED"]),

    // --- Comprehension layer ---
    valueProp: z.string().max(280),
    methods: z.array(methodRow).min(1),
    limitations: z.array(z.string()).min(1),

    // --- 4Q source (one of two required — enforced by validate_content.mjs) ---
    explanationUrl: z.string().url().optional(),

    // --- Cross-links (bidirectional graph; derive_crosslinks.mjs enforces resolution) ---
    relatedCaseStudy: z.string().optional(),                 // /work/<slug>
    relatedTransactions: z.array(z.string()).optional(),
    relatedEssay: z.string().optional(),                     // /essays/<slug>
    previousVersion: z.string().optional(),
    relatedArchitecture: z.union([z.string(), z.array(z.string())]).optional(),

    // --- Optional editorial ---
    repoUrl: z.string().url().optional(),
    loomUrl: z.string().url().optional(),
    loomPosterUrl: z.string().url().optional(),
  }),
});

// ============================================================
// Phase 3c.2 — architecture collection
// Source: architecture-spec-v1.md §3 + Appendix B.
// Strict deviation from §3.1: validator (Task 3.2) accepts inline-body
// 4Q as a fallback to explanationUrl, mirroring the transactions
// pattern, per Phase 3c.2 OQ-C. When upstream EXPLANATION.md ships,
// swap explanationUrl: null → real URL + drop the inline-body section.
// ============================================================

const SCORE_SYMBOL = z.enum(["✓", "✓✓", "⚠", "✗"]);

const architecture = defineCollection({
  type: "content",
  schema: z.object({
    // --- Identity ---
    title: z.string(),
    dateline: z.string(),                                    // "BOSTON, JUNE 3, 2026"
    shipped: z.string().regex(/^\d{4}-\d{2}-\d{2}$/),
    readingTime: z.number().int().positive(),                // minutes, manually authored (OPEN-4 default)

    // --- IA + status ---
    status: z.enum(["DRAFT", "SHIPPED"]),                    // only 2 statuses
    tags: z.array(z.string()).max(6),                        // 3-6 wire-service tags

    // --- Comprehension layer ---
    lead: z.string().max(280),
    methods: z.array(methodRow).min(1),

    // --- Canonical sources (fetched at build) ---
    // OQ-C: both fields nullable for v1; placeholders ship inline-body
    // 4Q + body. When Sean ships upstream EXPLANATION.md + the long-form
    // essay source, populate both.
    essaySourceUrl: z.string().url().nullable().optional(),
    explanationUrl: z.string().url().nullable().optional(),

    // --- Diagram (optional but expected on architecture surface) ---
    mermaidSource: z.string().optional(),                    // path under src/content/architecture/diagrams/<slug>.mmd OR inline source
    mermaidCaption: z.string().optional(),

    // --- Scoreboard (optional structured data) ---
    scoreboard: z.object({
      columns: z.array(z.string()).min(2).max(6),
      rows: z.array(z.object({
        label: z.string(),
        scores: z.array(SCORE_SYMBOL).min(2).max(6),
      })).min(2).max(8),
    }).optional(),

    // --- Honesty callouts (load-bearing credibility signal; ≤3 per page per §8.4) ---
    honestNotes: z.array(z.object({
      anchor: z.string(),                                    // section heading where callout anchors
      body: z.string(),
    })).max(3).optional(),

    // --- Cross-links (bidirectional via derive_crosslinks.mjs) ---
    relatedLedgerRow: z.string().optional(),                 // /transactions/<slug>
    relatedCaseStudy: z.string().optional(),                 // /work/<slug>
    relatedEssay: z.string().optional(),                     // /essays/<slug>
    relatedArchitecture: z.array(z.string()).optional(),     // siblings

    // --- Optional editorial ---
    sourceRepoUrl: z.string().url().optional(),
    tryItYourselfUrl: z.string().url().optional(),
    tryItYourselfCommand: z.string().optional(),             // multi-line bash; rendered via Shiki
  }),
});

// ============================================================
// Phase 3c.2 — essays collection
// Source: essays-spec-v1.md §3 + Appendix B.
// Same OQ-C inline-body-fallback deviation as architecture.
// ============================================================

const essays = defineCollection({
  type: "content",
  schema: z.object({
    // --- Identity ---
    title: z.string(),
    subtitle: z.string().optional(),
    dateline: z.string(),                                    // "BOSTON, JUNE 19, 2026"
    published: z.string().regex(/^\d{4}-\d{2}-\d{2}$/),
    lastRevised: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).nullable().optional(),
    readingTime: z.number().int().positive(),

    // --- IA + status ---
    status: z.enum(["DRAFT", "PUBLISHED"]),
    tags: z.array(z.string()).min(2).max(6),

    // --- Comprehension layer ---
    excerpt: z.string().max(280),

    // --- Canonical sources (fetched at build) ---
    // OQ-C: both nullable for v1.
    sourceUrl: z.string().url().nullable().optional(),
    explanationUrl: z.string().url().nullable().optional(),

    // --- Visual centerpiece (optional but expected for thesis-shaped essays) ---
    mermaidSource: z.string().optional(),                    // path or inline
    mermaidCaption: z.string().optional(),

    // --- Structured chart legend (parallel to architecture scoreboard) ---
    quadrantLegend: z.array(z.object({
      key: z.string(),
      label: z.string(),
      artifact: z.string().nullable().optional(),            // slug; null for negative-space callouts
    })).optional(),

    // --- Role map (structured table, validated at build per §9.2) ---
    roleMap: z.object({
      lastValidated: z.string().regex(/^\d{4}-\d{2}-\d{2}$/),
      rows: z.array(z.object({
        buyer: z.string(),
        position: z.string(),
        vocabularyTell: z.string(),
        jdUrl: z.string().url(),
      })).min(2).max(8),
    }).optional(),

    // --- Cross-links ---
    plottedArtifacts: z.array(z.string()).optional(),        // slugs; auto-reverse-rendered on each cited surface
    relatedLedgerRow: z.string().optional(),
    relatedCaseStudy: z.string().optional(),
    relatedArchitecture: z.array(z.string()).optional(),
    relatedEssays: z.array(z.string()).optional(),

    // --- Syndication + footer affordances ---
    crossPostedTo: z.array(z.object({
      name: z.string(),
      url: z.string().url(),
    })).optional(),
    sourceRepoUrl: z.string().url().optional(),

    // --- OG ---
    ogImage: z.string().optional(),
  }),
});

const teaserDeck = defineCollection({
  type: "data",
  schema: z.object({
    cards: z.array(
      z.object({
        cardIndex: z.number().int().min(0).max(9),
        src: z.string(),
        alt: z.string(),
        style: z.string(),
      })
    ).length(10),
  }),
});

export const collections = { work, teaserDeck, transactions, architecture, essays };
```

- [ ] **Step 3: Verify Astro content sync**

Run: `npx astro sync 2>&1 | tail -25`

Expected: sync completes cleanly. No errors on existing collections. Astro reports zero entries for `architecture` + `essays` (the directories don't exist yet — Section 7 creates them). If Astro errors with `Collection "architecture" not found at src/content/architecture`, that's acceptable for this task — the directory is created in Task 7.1. If Astro errors with a Zod parse issue on either new schema, fix the issue inline (most likely a typo in the field names or enum values).

- [ ] **Step 4: Commit**

```bash
git add src/content/config.ts
git commit -m "$(cat <<'EOF'
feat(phase-3c.2): extend content schema — architecture + essays collections

Adds the architecture collection per architecture-spec §3:
  - title, dateline, shipped (ISO regex), readingTime (manual per OPEN-4)
  - status (2-enum: DRAFT/SHIPPED), tags[] max 6
  - lead (max 280), methods[] (≥1, shared methodRow)
  - essaySourceUrl + explanationUrl (both nullable for v1 per OQ-C
    — OPEN-3 fallback to inline-body 4Q; tighten when upstream
    canonical sources ship)
  - mermaidSource + mermaidCaption (optional)
  - scoreboard{ columns: 2-6, rows: 2-8 with ✓/✓✓/⚠/✗ symbol enum }
  - honestNotes[]: max 3 per page per §8.4
  - relatedLedgerRow, relatedCaseStudy, relatedEssay,
    relatedArchitecture[]
  - sourceRepoUrl, tryItYourselfUrl, tryItYourselfCommand (multi-line
    bash; rendered via Shiki at build)

Adds the essays collection per essays-spec §3:
  - title, subtitle (optional), dateline, published (ISO regex),
    lastRevised (nullable), readingTime
  - status (2-enum: DRAFT/PUBLISHED), tags[] min 2 max 6
  - excerpt (max 280; serves as 1-line thesis pullquote + page meta)
  - sourceUrl + explanationUrl (both nullable for v1 per OQ-C)
  - mermaidSource + mermaidCaption (optional)
  - quadrantLegend[]{ key, label, artifact (nullable for negative-
    space callouts) } — auto-links artifact slugs via crosslinks.json
  - roleMap{ lastValidated, rows[]: 2-8 with buyer + position +
    vocabularyTell + jdUrl (URL-validated via HEAD at build) }
  - plottedArtifacts[] — auto-reverse-renders "← named in: <essay>"
    on cited ledger rows + architecture writeups
  - relatedLedgerRow, relatedCaseStudy, relatedArchitecture[],
    relatedEssays[]
  - crossPostedTo[]{ name, url } — Substack + future destinations
  - sourceRepoUrl, ogImage (optional)

methodRow's `cost` is now optional (architecture-spec §3 doesn't
require it; transactions + work still receive a string but the field
accepts undefined gracefully — purely additive permissiveness).

OQ-C is the OQ3-pattern-extended deviation from architecture-spec §3.1
(strict canonical-only). Validator (Task 3.2) accepts inline-body 4Q
on both architecture + essays as the v1 fallback. When Sean ships
upstream _EXPLANATION.md + body sources, swap nullable URLs to real
URLs + drop the inline-body sections from the placeholder MDXs.

Per architecture-spec §3 + Appendix B + essays-spec §3 + Appendix B.
EOF
)"
```

---

## Section 3 — Prebuild script extensions

Phase 3c.1 shipped three prebuild scripts with `work`-collection + `transactions`-collection scope, each with a Phase 3c.2 TODO comment naming the extension point verbatim. Section 3 follows those extension points — no new scripts, no forks. This is the same 2-extension pattern Phase 3c.1 ran against Phase 3b's scripts.

### Task 3.1: Extend `fetch_canonical_sources.mjs` — walk architecture (2 URL fields) + essays (2 URL fields)

**Files:**
- Modify: `scripts/fetch_canonical_sources.mjs`
- Modify: `.gitignore`

Per architecture-spec §11.1 + essays-spec §11.1. Architecture introduces a *second* URL field per row (`essaySourceUrl` for the long-form body, separate from `explanationUrl` for the 4Q canonical). Essays introduces the same shape (`sourceUrl` for the body, `explanationUrl` for the 4Q). Each collection therefore needs TWO walks in `WALK_COLLECTIONS` with different `outDir`s.

- [ ] **Step 1: Read existing script**

Run: `wc -l scripts/fetch_canonical_sources.mjs`

Expected: ~154 lines (Phase 3c.1 state with the named TODO comment block).

- [ ] **Step 2: Replace `scripts/fetch_canonical_sources.mjs` with the extended version**

Replace the entire file with:

```js
#!/usr/bin/env node
/**
 * scripts/fetch_canonical_sources.mjs — canonical source fetcher.
 *
 * Phase 3b scope: work collection (`explanation_url` field).
 * Phase 3c.1 scope: transactions collection (`explanationUrl` field).
 * Phase 3c.2 scope (added): architecture (TWO walks per row:
 *   `explanationUrl` for 4Q + `essaySourceUrl` for the long-form
 *   essay body) AND essays (TWO walks: `explanationUrl` for 4Q +
 *   `sourceUrl` for the body).
 *
 * Walks each collection's MDX files looking for entries that declare
 * the named URL field, fetches the markdown raw via HTTPS, validates
 * against the 4 canonical EXPLANATION headings (4Q walks only — body
 * walks skip validation since long-form essays don't carry the 4Q
 * heading structure), writes to the per-collection outDir. ETag-cached
 * via .cache/canonical-sources.lockfile so subsequent builds skip
 * unchanged sources.
 *
 * Graceful no-dir: missing content directories return empty.
 * Graceful no-URL: rows with `<urlField>: null` (OQ-C inline-body
 *   fallback path) are skipped silently.
 *
 * Source: case-study-spec-v1.md §9 + §15; transactions-spec-v1.md §11.1;
 *         architecture-spec-v1.md §11.1; essays-spec-v1.md §11.1.
 */
import fs from "node:fs/promises";
import path from "node:path";
import process from "node:process";

const ROOT = process.cwd();
const CACHE_PATH = path.join(ROOT, ".cache/canonical-sources.lockfile");
const EXPLANATIONS_DIR = path.join(ROOT, "src/content/explanations");
const ARCH_ESSAYS_DIR = path.join(ROOT, "src/content/architecture/essays");
const ESSAY_BODIES_DIR = path.join(ROOT, "src/content/essays/essay-bodies");

const WALK_COLLECTIONS = [
  // --- 4Q canonical fetches (validated against 4 EXPLANATION headings) ---
  {
    name: "work",
    contentDir: path.join(ROOT, "src/content/work"),
    urlField: "explanation_url",
    outDir: EXPLANATIONS_DIR,
    validateHeadings: true,
  },
  {
    name: "transactions",
    contentDir: path.join(ROOT, "src/content/transactions"),
    urlField: "explanationUrl",
    outDir: EXPLANATIONS_DIR,
    validateHeadings: true,
  },
  {
    name: "architecture",
    contentDir: path.join(ROOT, "src/content/architecture"),
    urlField: "explanationUrl",
    outDir: EXPLANATIONS_DIR,
    validateHeadings: true,
  },
  {
    name: "essays",
    contentDir: path.join(ROOT, "src/content/essays"),
    urlField: "explanationUrl",
    outDir: EXPLANATIONS_DIR,
    validateHeadings: true,
  },
  // --- Body fetches (no heading validation — long-form essays) ---
  {
    name: "architecture-body",
    contentDir: path.join(ROOT, "src/content/architecture"),
    urlField: "essaySourceUrl",
    outDir: ARCH_ESSAYS_DIR,
    validateHeadings: false,
  },
  {
    name: "essays-body",
    contentDir: path.join(ROOT, "src/content/essays"),
    urlField: "sourceUrl",
    outDir: ESSAY_BODIES_DIR,
    validateHeadings: false,
  },
];

const REQUIRED_HEADINGS = [
  /^##\s+[\w\-.]+\s*Q1\s+What is this\?/m,
  /^##\s+[\w\-.]+\s*Q2\s+Why this approach\?/m,
  /^##\s+[\w\-.]+\s*Q3\s+What would break\?/m,
  /^##\s+[\w\-.]+\s*Q4\s+What did I learn\?/m,
];

async function loadCache() {
  try {
    const raw = await fs.readFile(CACHE_PATH, "utf8");
    return JSON.parse(raw);
  } catch {
    return {};
  }
}

async function saveCache(cache) {
  await fs.mkdir(path.dirname(CACHE_PATH), { recursive: true });
  await fs.writeFile(CACHE_PATH, JSON.stringify(cache, null, 2), "utf8");
}

function parseFrontmatter(src) {
  const m = src.match(/^---\n([\s\S]+?)\n---/);
  if (!m) return {};
  const fm = {};
  for (const line of m[1].split("\n")) {
    const kv = line.match(/^([\w_]+):\s*(.+?)\s*$/);
    if (!kv) continue;
    let value = kv[2];
    if (value.startsWith('"') && value.endsWith('"')) value = value.slice(1, -1);
    fm[kv[1]] = value;
  }
  return fm;
}

async function fetchWithETag(url, etag) {
  const headers = etag ? { "If-None-Match": etag } : {};
  const res = await fetch(url, { headers });
  if (res.status === 304) return { changed: false };
  if (!res.ok) throw new Error(`fetch ${url} → ${res.status}`);
  const text = await res.text();
  return { changed: true, text, etag: res.headers.get("etag") ?? null };
}

function validateHeadings(slug, text) {
  const missing = [];
  for (const re of REQUIRED_HEADINGS) {
    if (!re.test(text)) missing.push(re.source);
  }
  if (missing.length > 0) {
    throw new Error(
      `${slug} EXPLANATION.md missing canonical headings:\n  - ${missing.join("\n  - ")}`
    );
  }
}

async function walkCollection(coll, cache) {
  const entries = await fs.readdir(coll.contentDir).catch(() => []);
  for (const fname of entries) {
    if (!fname.endsWith(".mdx") && !fname.endsWith(".md")) continue;
    const filePath = path.join(coll.contentDir, fname);
    const src = await fs.readFile(filePath, "utf8");
    const fm = parseFrontmatter(src);
    const url = fm[coll.urlField];
    // OQ-C inline-body fallback: skip rows with null/empty URL silently.
    if (!url || url === "null" || url === "") continue;
    const slug = fm.slug ?? fname.replace(/\.mdx?$/, "");
    const cacheKey = `${coll.name}:${slug}`;
    const cached = cache[cacheKey];
    try {
      const result = await fetchWithETag(url, cached?.etag);
      if (!result.changed) {
        process.stdout.write(`  ✓ ${cacheKey} (cached)\n`);
        continue;
      }
      if (coll.validateHeadings) validateHeadings(slug, result.text);
      await fs.mkdir(coll.outDir, { recursive: true });
      const outFile = path.join(coll.outDir, `${slug}.md`);
      await fs.writeFile(outFile, result.text, "utf8");
      cache[cacheKey] = { etag: result.etag, fetchedAt: new Date().toISOString() };
      process.stdout.write(`  → ${cacheKey} fetched (${(result.text.length / 1024).toFixed(1)}KB)\n`);
    } catch (e) {
      // Network failures + 404s log warning + leave committed version in place
      // (architecture-spec §11.1 fallback contract). The architecture body and
      // essay body walks write to gitignored dirs, but the 4Q walks write to
      // src/content/explanations/ which IS committed — so a failed fetch leaves
      // the last-known-good file in place.
      process.stderr.write(`  ⚠ ${cacheKey}: ${e.message} — leaving committed version in place\n`);
    }
  }
}

async function main() {
  process.stdout.write("\nfetch_canonical_sources.mjs — Phase 3c.2 scope (work + transactions + architecture × 2 + essays × 2)\n");
  const cache = await loadCache();
  for (const coll of WALK_COLLECTIONS) {
    process.stdout.write(`\n[${coll.name}]\n`);
    await walkCollection(coll, cache);
  }
  await saveCache(cache);
  process.stdout.write("\ndone.\n");
}

main().catch((e) => {
  process.stderr.write(`fetch_canonical_sources.mjs: ${e.message}\n`);
  process.exit(1);
});
```

- [ ] **Step 3: Update `.gitignore` to exclude the architecture/essays body output dirs**

Run: `grep -E "src/content/(architecture|essays)" .gitignore || echo "missing"`

If output is `missing`, append to `.gitignore`:

```
# Phase 3c.2 — fetched at build by scripts/fetch_canonical_sources.mjs
src/content/architecture/essays/
src/content/architecture/.crosslinks.json
src/content/essays/essay-bodies/
src/content/essays/.crosslinks.json
```

Use Edit tool to append to the file. (The 4Q output at `src/content/explanations/<slug>.md` IS committed — it's the small canonical comprehension layer; the long-form bodies are gitignored because they regenerate cleanly + are larger.)

- [ ] **Step 4: Smoke the script**

Run: `node scripts/fetch_canonical_sources.mjs 2>&1 | tail -10`

Expected: prints `[work]`, `[transactions]`, `[architecture]`, `[essays]`, `[architecture-body]`, `[essays-body]` headers. Each section reports 0 actual fetches (no architecture or essays MDXs exist yet; transactions placeholders declare `explanationUrl: null`; work hasn't gained any `explanation_url`s since 3b). Exits 0.

If the script errors, check that the `WALK_COLLECTIONS` array has all 6 entries and that each entry's `urlField` matches the spec.

- [ ] **Step 5: Commit**

```bash
git add scripts/fetch_canonical_sources.mjs .gitignore
git commit -m "$(cat <<'EOF'
feat(phase-3c.2): fetch_canonical_sources — extend to architecture + essays

Extends WALK_COLLECTIONS from 2 entries to 6:
  - work: explanation_url → src/content/explanations/ (unchanged)
  - transactions: explanationUrl → src/content/explanations/ (unchanged)
  - architecture: explanationUrl → src/content/explanations/ (NEW)
  - essays: explanationUrl → src/content/explanations/ (NEW)
  - architecture-body: essaySourceUrl → src/content/architecture/essays/ (NEW)
  - essays-body: sourceUrl → src/content/essays/essay-bodies/ (NEW)

Architecture + essays each get TWO walks because each row has TWO
URL fields: a 4Q-canonical URL (validated against 4 EXPLANATION
headings, written to the shared explanations/ dir) and a body URL
(no heading validation, written to a per-collection bodies/ dir).

OQ-C inline-body fallback path: rows with `<urlField>: null` are
skipped silently. The validator (Task 3.2) is the gate that ensures
each row has EITHER a URL OR inline-body 4Q in the MDX body.

Network failures + 404s log warnings (not errors) and leave the
committed version in place — per architecture-spec §11.1's fallback
contract. The body output dirs are gitignored; the 4Q output dir is
committed (small + canonical).

Per architecture-spec §11.1 + essays-spec §11.1.
EOF
)"
```

### Task 3.2: Extend `validate_content.mjs` — architecture + essays + JD URL HEAD checks + roleMap staleness

**Files:**
- Modify: `scripts/validate_content.mjs`

Per architecture-spec §11.1 + essays-spec §11.2 (the unified-validator merge). The merge consolidates per-collection logic; the existing dispatch-per-collection pattern from 3c.1 absorbs the new collections without restructuring. JD URL HEAD checks + `roleMap.lastValidated` staleness checks are warnings only — never fail the build on a routine HR event.

- [ ] **Step 1: Read existing script**

Run: `wc -l scripts/validate_content.mjs`

Expected: ~268 lines (Phase 3c.1 state).

- [ ] **Step 2: Replace `scripts/validate_content.mjs` with the extended version**

Replace the entire file with:

```js
#!/usr/bin/env node
/**
 * scripts/validate_content.mjs — schema validator (unified).
 *
 * Phase 3b scope: work collection.
 * Phase 3c.1 scope: transactions collection.
 * Phase 3c.2 scope (added): architecture + essays collections.
 *
 * Astro's content collection schema (src/content/config.ts) catches
 * most structural issues at build time. This validator catches
 * semantic rules Astro can't express:
 *   - exactly one of {four_q, explanation_url} must be present (work)
 *   - exactly one of {explanationUrl, inline-body 4Q headings}
 *     for transactions + architecture + essays (OQ-C v1 fallback path)
 *   - status-specific frontmatter (work)
 *   - methods[] / limitations[] are non-empty
 *   - slug must not collide with a routing-level enum (transactions only)
 *   - architecture: honestNotes[].anchor matches a section heading in
 *     the fetched essay body (warning only — upstream essay edits may
 *     have renamed the heading)
 *   - architecture: mermaidSource file exists when set to a path
 *   - architecture: scoreboard row/column counts consistent
 *   - essays: jdUrl HEAD checks (warning on 4xx/5xx)
 *   - essays: roleMap.lastValidated staleness (>30 days = warning)
 *
 * Source: case-study-spec-v1.md §15 + transactions-spec-v1.md §11.1 +
 *         architecture-spec-v1.md §11.1 + essays-spec-v1.md §11.1 +
 *         §11.2 (the unified-validator merge).
 */
import fs from "node:fs/promises";
import path from "node:path";
import process from "node:process";

const ROOT = process.cwd();

const COLLECTIONS = [
  {
    name: "work",
    contentDir: path.join(ROOT, "src/content/work"),
    requiredFields: [
      "frame", "title", "status", "hero_media", "hero_media_type",
      "hero_media_alt", "order", "role", "anchor_metric",
    ],
    requiredOneOf: [["four_q", "explanation_url"]],
    statusFields: {
      SHIPPED: ["shipped_at", "shipped_stats_endpoint"],
      PAUSED: ["return_condition"],
      ARCHIVED: ["archived_reference_url"],
    },
    requiredArrays: ["tags", "methods"],
  },
  {
    name: "transactions",
    contentDir: path.join(ROOT, "src/content/transactions"),
    requiredFields: [
      "title", "dateline", "shipped", "surface", "status", "valueProp",
    ],
    requiredOneOf: [["explanationUrl", "__inline_4Q__"]],
    statusFields: {},
    requiredArrays: ["methods", "limitations"],
    forbiddenSlugs: ["fleet", "pipeline", "product", "writing", "infra"],
    crossLinkFields: [
      "relatedCaseStudy", "relatedTransactions", "relatedEssay",
      "previousVersion", "relatedArchitecture",
    ],
  },
  // ============================================================
  // Phase 3c.2 — architecture collection
  // Source: architecture-spec-v1.md §11.1.
  // OQ-C deviation from §3.1: requiredOneOf accepts inline-body 4Q
  // as fallback (mirrors transactions); tighten to canonical-only
  // when Sean ships upstream EXPLANATION.md sources.
  // ============================================================
  {
    name: "architecture",
    contentDir: path.join(ROOT, "src/content/architecture"),
    requiredFields: [
      "title", "dateline", "shipped", "readingTime", "status", "lead",
    ],
    requiredOneOf: [["explanationUrl", "__inline_4Q__"]],
    statusFields: {},
    requiredArrays: ["tags", "methods"],
    crossLinkFields: [
      "relatedLedgerRow", "relatedCaseStudy", "relatedEssay",
      "relatedArchitecture",
    ],
    mermaidSourceField: "mermaidSource",      // existence check if value is a path
    honestNotesField: "honestNotes",           // anchor warning check
    scoreboardField: "scoreboard",             // row/column count consistency
  },
  // ============================================================
  // Phase 3c.2 — essays collection
  // Source: essays-spec-v1.md §11.1 + §11.2.
  // ============================================================
  {
    name: "essays",
    contentDir: path.join(ROOT, "src/content/essays"),
    requiredFields: [
      "title", "dateline", "published", "readingTime", "status", "excerpt",
    ],
    requiredOneOf: [["explanationUrl", "__inline_4Q__"]],
    statusFields: {},
    requiredArrays: ["tags"],
    crossLinkFields: [
      "relatedLedgerRow", "relatedCaseStudy", "relatedArchitecture",
      "relatedEssays", "plottedArtifacts",
    ],
    mermaidSourceField: "mermaidSource",
    roleMapField: "roleMap",                   // jdUrl HEAD checks + lastValidated staleness
  },
];

const ROLEMAP_STALE_DAYS = 30;

/**
 * Parses YAML frontmatter robustly enough for our schema. Detects:
 *   - inline scalars: key: value
 *   - lists of scalars: key: \n  - item \n  - item
 *   - objects: key: \n  child: val
 *   - lists of objects: key: \n  - child: val \n    other: val
 *   - block scalars: key: | \n  multi \n  line
 */
function parseFrontmatter(src) {
  const m = src.match(/^---\n([\s\S]+?)\n---/);
  if (!m) return null;
  const lines = m[1].split("\n");
  const fm = {};
  let i = 0;
  while (i < lines.length) {
    const line = lines[i];
    if (line.trim() === "" || line.trim().startsWith("#")) { i++; continue; }
    const kv = line.match(/^([\w_]+):\s*(.*)$/);
    if (!kv) { i++; continue; }
    const key = kv[1];
    const inline = kv[2];
    if (inline === "" || inline === "|" || inline === ">") {
      let j = i + 1;
      while (j < lines.length && lines[j].trim() === "") j++;
      if (j < lines.length && /^\s+-\s+(?!\w+:)/.test(lines[j])) {
        const list = [];
        while (j < lines.length && /^\s+-\s/.test(lines[j])) {
          list.push(lines[j].replace(/^\s+-\s+/, "").replace(/^["']|["']$/g, ""));
          j++;
        }
        fm[key] = list;
        i = j;
        continue;
      }
      if (j < lines.length && /^\s+-\s+\w+:/.test(lines[j])) {
        const list = [];
        let current = null;
        while (j < lines.length && (/^\s+-\s/.test(lines[j]) || /^\s{4,}\w+:/.test(lines[j]))) {
          if (/^\s+-\s/.test(lines[j])) {
            if (current) list.push(current);
            current = {};
            const inlineKv = lines[j].match(/^\s+-\s+(\w+):\s*(.*)$/);
            if (inlineKv) {
              const v = inlineKv[2].replace(/^["']|["']$/g, "");
              current[inlineKv[1]] = v === "null" ? null : v;
            }
          } else {
            const itemKv = lines[j].match(/^\s+(\w+):\s*(.*)$/);
            if (itemKv && current) {
              const v = itemKv[2].replace(/^["']|["']$/g, "");
              current[itemKv[1]] = v === "null" ? null : v;
            }
          }
          j++;
        }
        if (current) list.push(current);
        fm[key] = list;
        i = j;
        continue;
      }
      if (j < lines.length && /^\s+\w+:/.test(lines[j])) {
        const obj = {};
        while (j < lines.length && /^\s+\w+:/.test(lines[j])) {
          const objKv = lines[j].match(/^\s+(\w+):\s*(.*)$/);
          if (objKv) {
            const v = objKv[2].replace(/^["']|["']$/g, "");
            obj[objKv[1]] = v;
          }
          j++;
        }
        fm[key] = obj;
        i = j;
        continue;
      }
      if (inline === "|") {
        let acc = "";
        let j2 = i + 1;
        while (j2 < lines.length && (lines[j2].startsWith("  ") || lines[j2] === "")) {
          acc += (acc ? "\n" : "") + lines[j2].replace(/^  /, "");
          j2++;
        }
        fm[key] = acc;
        i = j2;
        continue;
      }
      fm[key] = "";
      i++;
      continue;
    }
    if (inline === "[]") { fm[key] = []; i++; continue; }
    fm[key] = inline.replace(/^["']|["']$/g, "");
    i++;
  }
  return fm;
}

/**
 * Scans an MDX file body for the 4 canonical EXPLANATION h2 headings.
 * Used by transactions/architecture/essays' requiredOneOf check.
 */
function detectInline4Q(src) {
  const bodyMatch = src.match(/^---\n[\s\S]+?\n---\n([\s\S]*)$/);
  if (!bodyMatch) return false;
  const body = bodyMatch[1];
  const required = [
    /^##\s+What is this\?/m,
    /^##\s+Why this approach\?/m,
    /^##\s+What would break\?/m,
    /^##\s+What did I learn\?/m,
  ];
  return required.every((re) => re.test(body));
}

async function headCheck(url) {
  try {
    const res = await fetch(url, { method: "HEAD", redirect: "follow" });
    return res.status;
  } catch (e) {
    return -1;
  }
}

async function validateCollection(coll, warnings) {
  const entries = await fs.readdir(coll.contentDir).catch(() => []);
  const errors = [];
  for (const fname of entries) {
    if (!fname.endsWith(".mdx") && !fname.endsWith(".md")) continue;
    const slug = fname.replace(/\.mdx?$/, "");
    const filePath = path.join(coll.contentDir, fname);
    const src = await fs.readFile(filePath, "utf8");
    const fm = parseFrontmatter(src);
    if (!fm) {
      errors.push(`${coll.name}/${slug}: missing or malformed frontmatter`);
      continue;
    }
    // Required scalar fields
    for (const f of coll.requiredFields) {
      if (fm[f] === undefined || fm[f] === "") {
        errors.push(`${coll.name}/${slug}: missing required field "${f}"`);
      }
    }
    // Required arrays
    for (const arrField of coll.requiredArrays ?? []) {
      if (!Array.isArray(fm[arrField]) || fm[arrField].length === 0) {
        errors.push(`${coll.name}/${slug}: required array "${arrField}" is missing or empty`);
      }
    }
    // Forbidden slugs
    for (const forbidden of coll.forbiddenSlugs ?? []) {
      if (slug === forbidden) {
        errors.push(`${coll.name}/${slug}: slug "${slug}" collides with the surface enum — pick a different slug`);
      }
    }
    // requiredOneOf (with __inline_4Q__ detection for transactions + architecture + essays)
    const hasInline4Q = detectInline4Q(src);
    for (const oneOf of coll.requiredOneOf ?? []) {
      const present = oneOf.filter((f) => {
        if (f === "__inline_4Q__") return hasInline4Q;
        // null/empty/literal-"null" all count as absent for OneOf semantics
        return fm[f] !== undefined && fm[f] !== "" && fm[f] !== "null" && fm[f] !== null;
      });
      if (present.length === 0) {
        const label = oneOf.map((f) => (f === "__inline_4Q__" ? "inline-body 4Q headings" : f)).join(", ");
        errors.push(`${coll.name}/${slug}: must declare one of [${label}]`);
      }
    }
    // status-specific fields
    const statusReq = coll.statusFields?.[fm.status] ?? [];
    for (const f of statusReq) {
      if (fm[f] === undefined || fm[f] === "") {
        errors.push(`${coll.name}/${slug}: status=${fm.status} requires field "${f}"`);
      }
    }
    // architecture: mermaidSource path existence
    if (coll.mermaidSourceField && fm[coll.mermaidSourceField]) {
      const val = fm[coll.mermaidSourceField];
      // Only check when value looks like a path (not inline mermaid source).
      // Heuristic: value contains `/` or ends in `.mmd` AND has no newlines.
      if (!val.includes("\n") && (val.endsWith(".mmd") || val.includes("/"))) {
        const mmdPath = path.join(coll.contentDir, val);
        try {
          await fs.access(mmdPath);
        } catch {
          errors.push(`${coll.name}/${slug}: mermaidSource path "${val}" does not exist at ${mmdPath}`);
        }
      }
    }
    // architecture: scoreboard row/column count consistency
    if (coll.scoreboardField && fm[coll.scoreboardField]) {
      const sb = fm[coll.scoreboardField];
      if (sb && typeof sb === "object" && Array.isArray(sb.columns) && Array.isArray(sb.rows)) {
        const colCount = sb.columns.length;
        for (const [idx, row] of sb.rows.entries()) {
          if (Array.isArray(row.scores) && row.scores.length !== colCount) {
            errors.push(`${coll.name}/${slug}: scoreboard row ${idx} ("${row.label}") has ${row.scores.length} scores; expected ${colCount} (matching columns count)`);
          }
        }
      }
    }
    // architecture: honestNotes[].anchor matches a section heading
    // (warning — upstream essay edits may have renamed the heading)
    if (coll.honestNotesField && Array.isArray(fm[coll.honestNotesField])) {
      const essayBodyPath = path.join(ROOT, "src/content/architecture/essays", `${slug}.md`);
      let essayBody = "";
      try { essayBody = await fs.readFile(essayBodyPath, "utf8"); } catch { /* not fetched yet */ }
      // Only warn when the essay body exists — otherwise we have nothing to check against.
      if (essayBody) {
        for (const note of fm[coll.honestNotesField]) {
          const anchor = note.anchor;
          if (!anchor) continue;
          // Match either `## <anchor>` or `## <Anchor>` (case-insensitive heading match)
          const re = new RegExp(`^##\\s+.*${anchor.replace(/[-/\\^$*+?.()|[\]{}]/g, "\\$&")}`, "im");
          if (!re.test(essayBody)) {
            warnings.push(`${coll.name}/${slug}: honestNote anchor "${anchor}" does not match any heading in the fetched essay body. Did Sean rename the heading upstream?`);
          }
        }
      }
    }
    // essays: roleMap jdUrl HEAD checks + lastValidated staleness
    if (coll.roleMapField && fm[coll.roleMapField]) {
      const rm = fm[coll.roleMapField];
      if (rm.lastValidated) {
        const lvDate = new Date(`${rm.lastValidated}T12:00:00Z`);
        const ageDays = Math.floor((Date.now() - lvDate.getTime()) / 86400000);
        if (ageDays > ROLEMAP_STALE_DAYS) {
          warnings.push(`${coll.name}/${slug}: roleMap.lastValidated is ${ageDays} days old; refresh recommended (>${ROLEMAP_STALE_DAYS} days)`);
        }
      }
      if (Array.isArray(rm.rows)) {
        for (const row of rm.rows) {
          if (!row.jdUrl) continue;
          const status = await headCheck(row.jdUrl);
          if (status >= 400 && status < 600) {
            warnings.push(`${coll.name}/${slug}: roleMap row "${row.buyer}" jdUrl ${row.jdUrl} returned ${status} — JD may have been removed. Build keeps shipping; update on next review.`);
          } else if (status === -1) {
            warnings.push(`${coll.name}/${slug}: roleMap row "${row.buyer}" jdUrl ${row.jdUrl} fetch failed (network/DNS). Treating as transient.`);
          }
        }
      }
    }

    process.stdout.write(`  ✓ ${coll.name}/${slug}\n`);
  }
  return errors;
}

async function main() {
  process.stdout.write("\nvalidate_content.mjs — Phase 3c.2 scope (work + transactions + architecture + essays)\n");
  const allErrors = [];
  const warnings = [];
  for (const coll of COLLECTIONS) {
    process.stdout.write(`\n[${coll.name}]\n`);
    const errs = await validateCollection(coll, warnings);
    allErrors.push(...errs);
  }
  if (warnings.length > 0) {
    process.stdout.write(`\n⚠ ${warnings.length} warnings (build proceeds):\n`);
    for (const w of warnings) process.stdout.write(`  - ${w}\n`);
  }
  if (allErrors.length > 0) {
    process.stderr.write(`\n✗ ${allErrors.length} validation errors:\n`);
    for (const e of allErrors) process.stderr.write(`  - ${e}\n`);
    process.exit(1);
  }
  process.stdout.write("\ndone.\n");
}

main().catch((e) => {
  process.stderr.write(`validate_content.mjs: ${e.stack || e.message}\n`);
  process.exit(1);
});
```

- [ ] **Step 3: Smoke the script**

Run: `npm run validate 2>&1 | tail -15`

Expected: prints `[work]`, `[transactions]`, `[architecture]`, `[essays]` headers. Each existing collection's entries are listed with `✓` lines. Architecture + essays sections list 0 entries (no MDXs yet). 0 warnings. Exits 0.

If the script errors, the most likely culprit is a typo in the COLLECTIONS array — verify each entry's `name` + `contentDir` + `requiredFields`.

- [ ] **Step 4: Commit**

```bash
git add scripts/validate_content.mjs
git commit -m "$(cat <<'EOF'
feat(phase-3c.2): validate_content — extend to architecture + essays + unified merge

Extends COLLECTIONS from 2 entries to 4 (the unified-validator merge
per essays-spec §11.2):
  - architecture: requiredFields, requiredOneOf [explanationUrl,
    __inline_4Q__] per OQ-C, mermaidSource path existence, scoreboard
    row/column count consistency, honestNotes anchor matching against
    fetched essay body (warning only)
  - essays: requiredFields, requiredOneOf [explanationUrl, __inline_4Q__],
    mermaidSource path existence, roleMap.lastValidated staleness
    (>30 days = warning), jdUrl HEAD checks (warning on 4xx/5xx)

JD URL HEAD checks + lastValidated staleness are WARNINGS not errors.
JDs go down for benign reasons (role filled, company restructure); hard-
failing the build on a 404 means a routine HR event breaks Sean's deploy.

The honestNotes anchor matching is also a warning (architecture-spec
OPEN-5 default): upstream essay heading renames shouldn't break the
portfolio build; Sean fixes the anchor on next review.

requiredOneOf now treats `null` / `"null"` / `""` as absent for OneOf
semantics. The OQ-C inline-body fallback path needs this so the
placeholder MDXs with `explanationUrl: null` satisfy the rule via the
inline-body 4Q headings.

Per architecture-spec §11.1 + essays-spec §11.1 + §11.2.
EOF
)"
```

### Task 3.3: Extend `derive_crosslinks.mjs` — 4-way close + essays' plottedArtifacts reverse-derivation

**Files:**
- Modify: `scripts/derive_crosslinks.mjs`

Per essays-spec §10.3 + §14. The 4-way close: an essay's `plottedArtifacts: [<slug>]` reverse-renders "← named in: <essay title>" on every cited ledger row + (when present) architecture writeup. The forward declarations from Phase 3c.1 (`archSlugs` + `essaySlugs` empty Sets at lines ~187-188) get swapped to `readSlugSet()` calls on real dirs. The architecture pass adds its own `relatedLedgerRow`, `relatedCaseStudy`, `relatedEssay`, `relatedArchitecture` walks. The essays pass adds `plottedArtifacts`, `relatedLedgerRow`, `relatedCaseStudy`, `relatedArchitecture`, `relatedEssays` walks. Each pass writes a per-collection `.crosslinks.json` file (`src/content/architecture/.crosslinks.json` + `src/content/essays/.crosslinks.json`); the existing `src/content/transactions/.crosslinks.json` gets extended with new `namedInEssays[]` entries per ledger row.

- [ ] **Step 1: Read existing script**

Run: `wc -l scripts/derive_crosslinks.mjs`

Expected: ~347 lines (Phase 3c.1 state with forward-declared empty Sets).

- [ ] **Step 2: Replace `scripts/derive_crosslinks.mjs` with the extended version**

Replace the entire file with:

```js
#!/usr/bin/env node
/**
 * scripts/derive_crosslinks.mjs — 4-way cross-link graph derivator.
 *
 * Phase 3b scope: work collection's methods[].link cross-references.
 * Phase 3c.1 scope: transactions bidirectional graph (work ⇄ transactions
 *   + transaction siblings + previousVersion + forward-declared
 *   relatedArchitecture / relatedEssay slots).
 * Phase 3c.2 scope (added): architecture + essays passes; closes the
 *   4-way graph. Essays' plottedArtifacts reverse-renders onto every
 *   cited ledger row + architecture writeup as `namedInEssays[]`.
 *
 *   Writes:
 *     - src/content/transactions/.crosslinks.json  (extended: namedInEssays[] per row)
 *     - src/content/architecture/.crosslinks.json  (NEW)
 *     - src/content/essays/.crosslinks.json        (NEW; mostly empty in v1 — no reverse links flow INTO essays)
 *
 * Source: case-study-spec-v1.md §8.2; transactions-spec-v1.md §11.1 + §14;
 *         architecture-spec-v1.md §14 + §11.1;
 *         essays-spec-v1.md §10 + §14.
 */
import fs from "node:fs/promises";
import path from "node:path";
import process from "node:process";

const ROOT = process.cwd();
const WORK_DIR = path.join(ROOT, "src/content/work");
const TXN_DIR = path.join(ROOT, "src/content/transactions");
const ARCH_DIR = path.join(ROOT, "src/content/architecture");
const ESSAY_DIR = path.join(ROOT, "src/content/essays");
const TXN_CROSSLINKS_OUT = path.join(TXN_DIR, ".crosslinks.json");
const ARCH_CROSSLINKS_OUT = path.join(ARCH_DIR, ".crosslinks.json");
const ESSAY_CROSSLINKS_OUT = path.join(ESSAY_DIR, ".crosslinks.json");

function parseFrontmatterBlock(src) {
  const m = src.match(/^---\n([\s\S]+?)\n---/);
  return m ? m[1] : null;
}

function extractSlug(fname) {
  return fname.replace(/\.mdx?$/, "");
}

function extractMethodsLinks(fm) {
  const lines = fm.split("\n");
  const links = [];
  let inMethods = false;
  for (const line of lines) {
    if (/^methods:/.test(line)) { inMethods = true; continue; }
    if (inMethods && /^\w+:/.test(line)) { inMethods = false; }
    if (!inMethods) continue;
    const linkMatch = line.match(/^\s+link:\s*(.+)$/);
    if (linkMatch) {
      const val = linkMatch[1].trim().replace(/^["']|["']$/g, "");
      if (val !== "null" && val !== "") links.push(val);
    }
  }
  return links;
}

function extractScalar(fm, field) {
  const re = new RegExp(`^${field}:\\s*(.+?)\\s*$`, "m");
  const match = fm.match(re);
  if (!match) return null;
  const val = match[1].replace(/^["']|["']$/g, "");
  if (val === "null" || val === "") return null;
  return val;
}

function extractStringList(fm, field) {
  const lines = fm.split("\n");
  const list = [];
  let inList = false;
  for (let i = 0; i < lines.length; i++) {
    const line = lines[i];
    if (new RegExp(`^${field}:\\s*\\[`).test(line)) {
      const inlineMatch = line.match(new RegExp(`^${field}:\\s*\\[(.+)\\]`));
      if (inlineMatch) {
        return inlineMatch[1]
          .split(",")
          .map((s) => s.trim().replace(/^["']|["']$/g, ""))
          .filter((s) => s && s !== "null");
      }
    }
    if (new RegExp(`^${field}:\\s*$`).test(line)) { inList = true; continue; }
    if (inList && /^\w+:/.test(line)) { inList = false; }
    if (!inList) continue;
    const itemMatch = line.match(/^\s+-\s+(.+)$/);
    if (itemMatch) {
      const val = itemMatch[1].trim().replace(/^["']|["']$/g, "");
      if (val && val !== "null") list.push(val);
    }
  }
  return list;
}

function extractStringOrList(fm, field) {
  const scalar = extractScalar(fm, field);
  if (scalar !== null) return [scalar];
  return extractStringList(fm, field);
}

/**
 * Extracts the `quadrantLegend[].artifact` slugs from an essays MDX
 * frontmatter. Returns the list of artifact slugs (skipping null entries).
 */
function extractQuadrantLegendArtifacts(fm) {
  const lines = fm.split("\n");
  const artifacts = [];
  let inLegend = false;
  for (const line of lines) {
    if (/^quadrantLegend:/.test(line)) { inLegend = true; continue; }
    if (inLegend && /^\w+:/.test(line)) { inLegend = false; }
    if (!inLegend) continue;
    const m = line.match(/^\s+artifact:\s*(.+)$/);
    if (m) {
      const val = m[1].trim().replace(/^["']|["']$/g, "");
      if (val && val !== "null") artifacts.push(val);
    }
  }
  return artifacts;
}

function levenshtein(a, b) {
  const m = a.length, n = b.length;
  if (m === 0) return n; if (n === 0) return m;
  const d = Array.from({ length: m + 1 }, () => new Array(n + 1).fill(0));
  for (let i = 0; i <= m; i++) d[i][0] = i;
  for (let j = 0; j <= n; j++) d[0][j] = j;
  for (let i = 1; i <= m; i++) {
    for (let j = 1; j <= n; j++) {
      const cost = a[i - 1] === b[j - 1] ? 0 : 1;
      d[i][j] = Math.min(d[i - 1][j] + 1, d[i][j - 1] + 1, d[i - 1][j - 1] + cost);
    }
  }
  return d[m][n];
}

function suggestion(needle, haystack) {
  let best = null;
  let bestDist = Infinity;
  for (const candidate of haystack) {
    const d = levenshtein(needle, candidate);
    if (d < bestDist) { bestDist = d; best = candidate; }
  }
  if (best && bestDist <= 2) return ` Did you mean "${best}"?`;
  return "";
}

async function readSlugSet(dir) {
  const entries = await fs.readdir(dir).catch(() => []);
  return new Set(
    entries
      .filter((f) => f.endsWith(".mdx") || f.endsWith(".md"))
      .map(extractSlug)
  );
}

async function main() {
  process.stdout.write("\nderive_crosslinks.mjs — Phase 3c.2 scope (4-way close: work + transactions + architecture + essays)\n");

  // ----- Phase 3c.2: forward-declared sets are now REAL -----
  const workSlugs = await readSlugSet(WORK_DIR);
  const txnSlugs = await readSlugSet(TXN_DIR);
  const archSlugs = await readSlugSet(ARCH_DIR);
  const essaySlugs = await readSlugSet(ESSAY_DIR);

  const errors = [];

  // ----- Transactions reverse tables (extended with namedInEssays) -----
  const reverseTxns = new Map();   // txnSlug → { supersededBy, siblingShips: [], namedInEssays: [] }
  const byCaseStudy = new Map();
  const byArchitecture = new Map();
  const byEssay = new Map();
  // Helper to safely append to an array property on the reverse map.
  function appendReverse(map, key, prop, value) {
    if (!map.has(key)) map.set(key, {});
    const obj = map.get(key);
    obj[prop] = obj[prop] ?? [];
    if (!obj[prop].includes(value)) obj[prop].push(value);
  }

  // ----- Architecture reverse tables -----
  const reverseArch = new Map();   // archSlug → { siblingArchitecture: [], namedInEssays: [] }
  const archByLedger = new Map();  // txnSlug → [archSlug]
  const archByCaseStudy = new Map();
  const archByEssay = new Map();

  // ----- Essays reverse tables -----
  // Essays in v1 receive cross-links mostly OUTBOUND (plottedArtifacts +
  // relatedLedgerRow + relatedArchitecture + relatedCaseStudy). The few
  // inbound paths are essay-to-essay via relatedEssays[].
  const reverseEssays = new Map(); // essaySlug → { siblingEssays: [] }

  // ============================================================
  // Pass 1: work collection (Phase 3b methods cross-links — unchanged)
  // ============================================================
  process.stdout.write("\n[work] methods cross-links\n");
  const workEntries = await fs.readdir(WORK_DIR).catch(() => []);
  let totalWork = 0;
  for (const fname of workEntries) {
    if (!fname.endsWith(".mdx") && !fname.endsWith(".md")) continue;
    const slug = extractSlug(fname);
    const src = await fs.readFile(path.join(WORK_DIR, fname), "utf8");
    const fm = parseFrontmatterBlock(src);
    if (!fm) continue;
    const links = extractMethodsLinks(fm);
    for (const link of links) {
      totalWork++;
      const t = link.match(/^\/work\/([\w-]+)\/?$/);
      if (!t) continue;
      const targetSlug = t[1];
      if (!workSlugs.has(targetSlug)) {
        errors.push(`work/${slug}: methods cross-link to /work/${targetSlug}/ — no such slug.${suggestion(targetSlug, [...workSlugs])}`);
      } else {
        process.stdout.write(`  ✓ ${slug} → /work/${targetSlug}/\n`);
      }
    }
  }

  // ============================================================
  // Pass 2: transactions cross-links + reverse graph (Phase 3c.1 unchanged)
  // ============================================================
  process.stdout.write("\n[transactions] cross-links + bidirectional graph\n");
  const txnEntries = await fs.readdir(TXN_DIR).catch(() => []);
  let totalTxn = 0;
  for (const fname of txnEntries) {
    if (!fname.endsWith(".mdx") && !fname.endsWith(".md")) continue;
    const slug = extractSlug(fname);
    const src = await fs.readFile(path.join(TXN_DIR, fname), "utf8");
    const fm = parseFrontmatterBlock(src);
    if (!fm) continue;

    for (const link of extractMethodsLinks(fm)) {
      totalTxn++;
      const t = link.match(/^\/work\/([\w-]+)\/?$/);
      if (!t) continue;
      const target = t[1];
      if (!workSlugs.has(target)) {
        errors.push(`transactions/${slug}: methods cross-link to /work/${target}/ — no such slug.${suggestion(target, [...workSlugs])}`);
      } else {
        process.stdout.write(`  ✓ ${slug} → /work/${target}/ (methods)\n`);
      }
    }

    const relCs = extractScalar(fm, "relatedCaseStudy");
    if (relCs) {
      totalTxn++;
      if (!workSlugs.has(relCs)) {
        errors.push(`transactions/${slug}: relatedCaseStudy "${relCs}" → /work/${relCs}/ — no such slug.${suggestion(relCs, [...workSlugs])}`);
      } else {
        process.stdout.write(`  ✓ ${slug} → /work/${relCs}/ (relatedCaseStudy)\n`);
        if (!byCaseStudy.has(relCs)) byCaseStudy.set(relCs, []);
        byCaseStudy.get(relCs).push(slug);
      }
    }

    for (const sib of extractStringList(fm, "relatedTransactions")) {
      totalTxn++;
      if (!txnSlugs.has(sib)) {
        errors.push(`transactions/${slug}: relatedTransactions "${sib}" → /transactions/${sib}/ — no such slug.${suggestion(sib, [...txnSlugs])}`);
      } else {
        process.stdout.write(`  ✓ ${slug} ↔ /transactions/${sib}/ (sibling)\n`);
        appendReverse(reverseTxns, sib, "siblingShips", slug);
        appendReverse(reverseTxns, slug, "siblingShips", sib);
      }
    }

    const prev = extractScalar(fm, "previousVersion");
    if (prev) {
      totalTxn++;
      if (!txnSlugs.has(prev)) {
        errors.push(`transactions/${slug}: previousVersion "${prev}" → /transactions/${prev}/ — no such slug.${suggestion(prev, [...txnSlugs])}`);
      } else {
        process.stdout.write(`  ✓ ${slug} → /transactions/${prev}/ (supersedes)\n`);
        if (!reverseTxns.has(prev)) reverseTxns.set(prev, {});
        reverseTxns.get(prev).supersededBy = slug;
      }
    }

    // relatedArchitecture — NOW REAL (archSlugs populated from disk)
    for (const archSlug of extractStringOrList(fm, "relatedArchitecture")) {
      totalTxn++;
      if (!archSlugs.has(archSlug)) {
        // Phase 3c.2 still permits forward declaration when archSlugs is
        // empty (zero architecture MDXs in v1.x), but warns when archSlugs
        // is populated and the slug doesn't resolve.
        if (archSlugs.size > 0) {
          errors.push(`transactions/${slug}: relatedArchitecture "${archSlug}" → /architecture/${archSlug}/ — no such slug.${suggestion(archSlug, [...archSlugs])}`);
        } else {
          process.stdout.write(`  ✓ ${slug} → /architecture/${archSlug}/ (forward-declared; archSlugs empty)\n`);
        }
      } else {
        process.stdout.write(`  ✓ ${slug} → /architecture/${archSlug}/\n`);
      }
      if (!byArchitecture.has(archSlug)) byArchitecture.set(archSlug, []);
      byArchitecture.get(archSlug).push(slug);
    }

    const relEssay = extractScalar(fm, "relatedEssay");
    if (relEssay) {
      totalTxn++;
      if (!essaySlugs.has(relEssay)) {
        if (essaySlugs.size > 0) {
          errors.push(`transactions/${slug}: relatedEssay "${relEssay}" → /essays/${relEssay}/ — no such slug.${suggestion(relEssay, [...essaySlugs])}`);
        } else {
          process.stdout.write(`  ✓ ${slug} → /essays/${relEssay}/ (forward-declared; essaySlugs empty)\n`);
        }
      } else {
        process.stdout.write(`  ✓ ${slug} → /essays/${relEssay}/\n`);
      }
      if (!byEssay.has(relEssay)) byEssay.set(relEssay, []);
      byEssay.get(relEssay).push(slug);
    }
  }

  // ============================================================
  // Pass 3: architecture cross-links + reverse graph (NEW)
  // ============================================================
  process.stdout.write("\n[architecture] cross-links + bidirectional graph\n");
  const archEntries = await fs.readdir(ARCH_DIR).catch(() => []);
  let totalArch = 0;
  for (const fname of archEntries) {
    if (!fname.endsWith(".mdx") && !fname.endsWith(".md")) continue;
    const slug = extractSlug(fname);
    const src = await fs.readFile(path.join(ARCH_DIR, fname), "utf8");
    const fm = parseFrontmatterBlock(src);
    if (!fm) continue;

    // methods[].link → /work/<slug>
    for (const link of extractMethodsLinks(fm)) {
      totalArch++;
      const t = link.match(/^\/work\/([\w-]+)\/?$/);
      if (!t) continue;
      const target = t[1];
      if (!workSlugs.has(target)) {
        errors.push(`architecture/${slug}: methods cross-link to /work/${target}/ — no such slug.${suggestion(target, [...workSlugs])}`);
      } else {
        process.stdout.write(`  ✓ ${slug} → /work/${target}/ (methods)\n`);
      }
    }

    // relatedLedgerRow → /transactions/<slug> (closes the arch ⇄ txn loop)
    const relLed = extractScalar(fm, "relatedLedgerRow");
    if (relLed) {
      totalArch++;
      if (!txnSlugs.has(relLed)) {
        errors.push(`architecture/${slug}: relatedLedgerRow "${relLed}" → /transactions/${relLed}/ — no such slug.${suggestion(relLed, [...txnSlugs])}`);
      } else {
        process.stdout.write(`  ✓ ${slug} → /transactions/${relLed}/\n`);
        if (!archByLedger.has(relLed)) archByLedger.set(relLed, []);
        archByLedger.get(relLed).push(slug);
      }
    }

    // relatedCaseStudy → /work/<slug>
    const relCs = extractScalar(fm, "relatedCaseStudy");
    if (relCs) {
      totalArch++;
      if (!workSlugs.has(relCs)) {
        errors.push(`architecture/${slug}: relatedCaseStudy "${relCs}" → /work/${relCs}/ — no such slug.${suggestion(relCs, [...workSlugs])}`);
      } else {
        process.stdout.write(`  ✓ ${slug} → /work/${relCs}/\n`);
        if (!archByCaseStudy.has(relCs)) archByCaseStudy.set(relCs, []);
        archByCaseStudy.get(relCs).push(slug);
      }
    }

    // relatedEssay → /essays/<slug>
    const relEss = extractScalar(fm, "relatedEssay");
    if (relEss) {
      totalArch++;
      if (!essaySlugs.has(relEss)) {
        if (essaySlugs.size > 0) {
          errors.push(`architecture/${slug}: relatedEssay "${relEss}" → /essays/${relEss}/ — no such slug.${suggestion(relEss, [...essaySlugs])}`);
        } else {
          process.stdout.write(`  ✓ ${slug} → /essays/${relEss}/ (forward-declared; essaySlugs empty)\n`);
        }
      } else {
        process.stdout.write(`  ✓ ${slug} → /essays/${relEss}/\n`);
      }
      if (!archByEssay.has(relEss)) archByEssay.set(relEss, []);
      archByEssay.get(relEss).push(slug);
    }

    // relatedArchitecture[] (sibling architecture writeups)
    for (const sib of extractStringList(fm, "relatedArchitecture")) {
      totalArch++;
      if (!archSlugs.has(sib)) {
        errors.push(`architecture/${slug}: relatedArchitecture "${sib}" → /architecture/${sib}/ — no such slug.${suggestion(sib, [...archSlugs])}`);
      } else {
        process.stdout.write(`  ✓ ${slug} ↔ /architecture/${sib}/ (sibling)\n`);
        appendReverse(reverseArch, sib, "siblingArchitecture", slug);
        appendReverse(reverseArch, slug, "siblingArchitecture", sib);
      }
    }
  }

  // ============================================================
  // Pass 4: essays cross-links + reverse graph (NEW)
  // Essays are the source of the most reverse links: every plottedArtifact
  // slug gets "← named in: <essay title>" reverse-rendered on its txn +
  // architecture surfaces.
  // ============================================================
  process.stdout.write("\n[essays] cross-links + 4-way close\n");
  const essayEntries = await fs.readdir(ESSAY_DIR).catch(() => []);
  let totalEssay = 0;
  for (const fname of essayEntries) {
    if (!fname.endsWith(".mdx") && !fname.endsWith(".md")) continue;
    const slug = extractSlug(fname);
    const src = await fs.readFile(path.join(ESSAY_DIR, fname), "utf8");
    const fm = parseFrontmatterBlock(src);
    if (!fm) continue;

    // plottedArtifacts[] → reverse "← named in: <slug>" onto txn + arch surfaces
    const plotted = extractStringList(fm, "plottedArtifacts");
    // ALSO pick up the quadrantLegend[].artifact slugs (they're a superset
    // in v1 but explicit plottedArtifacts wins when both exist).
    const legendArtifacts = extractQuadrantLegendArtifacts(fm);
    const allArtifactSlugs = Array.from(new Set([...plotted, ...legendArtifacts]));
    for (const artifactSlug of allArtifactSlugs) {
      totalEssay++;
      const onTxn = txnSlugs.has(artifactSlug);
      const onArch = archSlugs.has(artifactSlug);
      if (!onTxn && !onArch) {
        errors.push(`essays/${slug}: plottedArtifacts entry "${artifactSlug}" does not resolve to /transactions/${artifactSlug}/ or /architecture/${artifactSlug}/ (no MDX at either).${suggestion(artifactSlug, [...txnSlugs, ...archSlugs])}`);
        continue;
      }
      if (onTxn) {
        process.stdout.write(`  ✓ ${slug} → /transactions/${artifactSlug}/ (plotted artifact)\n`);
        appendReverse(reverseTxns, artifactSlug, "namedInEssays", slug);
      }
      if (onArch) {
        process.stdout.write(`  ✓ ${slug} → /architecture/${artifactSlug}/ (plotted artifact)\n`);
        appendReverse(reverseArch, artifactSlug, "namedInEssays", slug);
      }
    }

    // relatedLedgerRow → /transactions/<slug>
    const relLed = extractScalar(fm, "relatedLedgerRow");
    if (relLed) {
      totalEssay++;
      if (!txnSlugs.has(relLed)) {
        errors.push(`essays/${slug}: relatedLedgerRow "${relLed}" → /transactions/${relLed}/ — no such slug.${suggestion(relLed, [...txnSlugs])}`);
      } else {
        process.stdout.write(`  ✓ ${slug} → /transactions/${relLed}/\n`);
        appendReverse(reverseTxns, relLed, "namedInEssays", slug);
      }
    }

    // relatedCaseStudy → /work/<slug>
    const relCs = extractScalar(fm, "relatedCaseStudy");
    if (relCs) {
      totalEssay++;
      if (!workSlugs.has(relCs)) {
        errors.push(`essays/${slug}: relatedCaseStudy "${relCs}" → /work/${relCs}/ — no such slug.${suggestion(relCs, [...workSlugs])}`);
      } else {
        process.stdout.write(`  ✓ ${slug} → /work/${relCs}/\n`);
      }
    }

    // relatedArchitecture[] → /architecture/<slug>
    for (const archSlug of extractStringList(fm, "relatedArchitecture")) {
      totalEssay++;
      if (!archSlugs.has(archSlug)) {
        errors.push(`essays/${slug}: relatedArchitecture "${archSlug}" → /architecture/${archSlug}/ — no such slug.${suggestion(archSlug, [...archSlugs])}`);
      } else {
        process.stdout.write(`  ✓ ${slug} → /architecture/${archSlug}/\n`);
        appendReverse(reverseArch, archSlug, "namedInEssays", slug);
      }
    }

    // relatedEssays[] (sibling essays)
    for (const sib of extractStringList(fm, "relatedEssays")) {
      totalEssay++;
      if (!essaySlugs.has(sib)) {
        errors.push(`essays/${slug}: relatedEssays "${sib}" → /essays/${sib}/ — no such slug.${suggestion(sib, [...essaySlugs])}`);
      } else {
        process.stdout.write(`  ✓ ${slug} ↔ /essays/${sib}/ (sibling)\n`);
        appendReverse(reverseEssays, sib, "siblingEssays", slug);
        appendReverse(reverseEssays, slug, "siblingEssays", sib);
      }
    }
  }

  // ============================================================
  // Halt on errors
  // ============================================================
  if (errors.length > 0) {
    process.stderr.write(`\n✗ ${errors.length} cross-link errors:\n`);
    for (const e of errors) process.stderr.write(`  - ${e}\n`);
    process.exit(1);
  }

  // ============================================================
  // Write the three per-collection crosslinks files
  // ============================================================
  const txnOut = {
    schemaVersion: "phase-3c.2",
    generatedAt: new Date().toISOString(),
    transactions: Object.fromEntries(reverseTxns),
    byCaseStudy: Object.fromEntries(byCaseStudy),
    byArchitecture: Object.fromEntries(byArchitecture),
    byEssay: Object.fromEntries(byEssay),
  };
  await fs.mkdir(TXN_DIR, { recursive: true });
  await fs.writeFile(TXN_CROSSLINKS_OUT, JSON.stringify(txnOut, null, 2), "utf8");
  process.stdout.write(`\n  wrote ${path.relative(ROOT, TXN_CROSSLINKS_OUT)}\n`);

  const archOut = {
    schemaVersion: "phase-3c.2",
    generatedAt: new Date().toISOString(),
    architecture: Object.fromEntries(reverseArch),
    byLedger: Object.fromEntries(archByLedger),
    byCaseStudy: Object.fromEntries(archByCaseStudy),
    byEssay: Object.fromEntries(archByEssay),
  };
  await fs.mkdir(ARCH_DIR, { recursive: true });
  await fs.writeFile(ARCH_CROSSLINKS_OUT, JSON.stringify(archOut, null, 2), "utf8");
  process.stdout.write(`  wrote ${path.relative(ROOT, ARCH_CROSSLINKS_OUT)}\n`);

  const essayOut = {
    schemaVersion: "phase-3c.2",
    generatedAt: new Date().toISOString(),
    essays: Object.fromEntries(reverseEssays),
  };
  await fs.mkdir(ESSAY_DIR, { recursive: true });
  await fs.writeFile(ESSAY_CROSSLINKS_OUT, JSON.stringify(essayOut, null, 2), "utf8");
  process.stdout.write(`  wrote ${path.relative(ROOT, ESSAY_CROSSLINKS_OUT)}\n`);

  process.stdout.write(`\ndone. validated ${totalWork + totalTxn + totalArch + totalEssay} cross-links (${totalWork} work + ${totalTxn} transactions + ${totalArch} architecture + ${totalEssay} essays).\n`);
}

main().catch((e) => {
  process.stderr.write(`derive_crosslinks.mjs: ${e.stack || e.message}\n`);
  process.exit(1);
});
```

- [ ] **Step 3: Smoke the script (still no architecture/essays MDXs)**

Run: `node scripts/derive_crosslinks.mjs 2>&1 | tail -15`

Expected: prints `[work]`, `[transactions]`, `[architecture]`, `[essays]` headers. work + transactions pass with existing entries; architecture + essays sections list 0 entries (no MDXs yet). The forward-declared `vault-knowledge-mcp → /architecture/vault-knowledge-mcp/` from `src/content/transactions/vault-knowledge-mcp.mdx` STILL passes because `archSlugs` is still empty (no architecture MDX yet — the script's empty-set forward-declaration fallback continues to permit it). Writes 3 `.crosslinks.json` files. Exits 0.

If the script errors, the most likely culprit is a typo in one of the new pass functions — check that `extractStringList` is called with the right field name.

- [ ] **Step 4: Verify the 3 crosslinks files exist + look sane**

Run in parallel:
- `cat src/content/transactions/.crosslinks.json | head -20`
- `cat src/content/architecture/.crosslinks.json`
- `cat src/content/essays/.crosslinks.json`

Expected: txn file has the same content as Phase 3c.1 PLUS a `byEssay: {}` field (still empty). Architecture file has `schemaVersion: "phase-3c.2"`, empty `architecture`, `byLedger`, `byCaseStudy`, `byEssay` objects. Essays file has `schemaVersion: "phase-3c.2"`, empty `essays` object. All three valid JSON.

- [ ] **Step 5: Commit**

```bash
git add scripts/derive_crosslinks.mjs
git commit -m "$(cat <<'EOF'
feat(phase-3c.2): derive_crosslinks — 4-way close + essays plottedArtifacts reverse

Closes the 4-collection cross-link graph (work + transactions +
architecture + essays). Adds Pass 3 (architecture) and Pass 4 (essays);
extends the transactions reverse table with namedInEssays[].

Architecture pass:
  - methods[].link → /work/<slug> (same primitive as transactions)
  - relatedLedgerRow → /transactions/<slug> (the arch ⇄ ledger close)
  - relatedCaseStudy → /work/<slug>
  - relatedEssay → /essays/<slug>
  - relatedArchitecture[] (sibling architecture writeups; bidirectional)

Essays pass — the surface that drives the most reverse links:
  - plottedArtifacts[] → reverse-renders "← named in: <essay slug>" on
    every cited /transactions/<slug>/ + /architecture/<slug>/ (the
    4-way close per essays-spec §10.3 + §14)
  - quadrantLegend[].artifact slugs also get pulled into the same
    reverse-derivation (the spec calls them a superset of plotted
    artifacts; explicit plottedArtifacts wins when both exist; v1
    treats the union)
  - relatedLedgerRow → /transactions/<slug>; also adds namedInEssays[]
    on the ledger row (essay is the canonical source-of-truth for
    "what cites this artifact")
  - relatedCaseStudy → /work/<slug>
  - relatedArchitecture[] → /architecture/<slug>; reverse-adds
    namedInEssays[] on the architecture writeup
  - relatedEssays[] (sibling essays; bidirectional)

Writes 3 per-collection .crosslinks.json files (transactions/, architecture/,
essays/). The forward-declared archSlugs/essaySlugs empty Sets from Phase
3c.1 (lines ~187-188) are swapped to readSlugSet() calls on the real
directories; when both are empty (zero MDXs), the forward-declaration
fallback continues to permit slugs into either surface.

Phase 3c.1's vault-knowledge-mcp ledger row's forward-declared
relatedArchitecture: vault-knowledge-mcp now lights up bidirectionally
the first build after Task 7.1 ships src/content/architecture/vault-
knowledge-mcp.mdx — zero code changes needed downstream.

Per architecture-spec §14 + essays-spec §10 + §14.
EOF
)"
```

---

## Section 4 — Architecture-specific components

Nine Astro components for the `/architecture/` surface. Bundled into 5 tasks where components share idiom or scope. Each component is single-file Astro (no React). All inherit the BaseLayout's design tokens (`--paper`, `--ink`, `--teal`, `--stamp-amber`, `--ink-secondary`, `--border-paper`, `--font-serif`, `--font-mono`, `--ease-out`). The `<ThesisPullQuote />` originates here and is imported by the essays deep-dive (Task 6.4) — same primitive, no fork per BLUEPRINT §3.2.

### Task 4.1: `<ScoreSymbol />` + `<Scoreboard />`

**Files:**
- Create: `src/components/architecture/ScoreSymbol.astro`
- Create: `src/components/architecture/Scoreboard.astro`

Per architecture-spec §7. The scoreboard renders as a flat HTML `<table>` from structured frontmatter; each score cell renders an inline SVG symbol via `<ScoreSymbol />`. All rows visually identical — no row highlight, no Sean's-vault accent, no zebra striping. Equality of presentation IS the credibility signal.

- [ ] **Step 1: Write `<ScoreSymbol />`**

Create `src/components/architecture/ScoreSymbol.astro`:

```astro
---
/**
 * <ScoreSymbol /> — inline SVG for ✓ / ✓✓ / ⚠ / ✗ (14×14px).
 * Palette tokens per architecture-spec §5 + §6:
 *   ✓ + ✓✓ → success teal (#0F6E56), ✓✓ is heavier stroke
 *   ⚠     → stamp amber (#7C2D12)
 *   ✗     → secondary ink (#546E71)
 *
 * Per architecture-spec §7.1 + §7.3.
 */
interface Props { symbol: "✓" | "✓✓" | "⚠" | "✗" }
const { symbol } = Astro.props;
const label =
  symbol === "✓" ? "passes" :
  symbol === "✓✓" ? "exceeds" :
  symbol === "⚠" ? "partial" :
  "fails";
---
<span class={`score-symbol score-${label}`} aria-label={label} role="img">
  {symbol === "✓" && (
    <svg width="14" height="14" viewBox="0 0 14 14" aria-hidden="true">
      <path d="M2.5 7.5L5.5 10.5L11.5 3.5" stroke="#0F6E56" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
    </svg>
  )}
  {symbol === "✓✓" && (
    <svg width="14" height="14" viewBox="0 0 14 14" aria-hidden="true">
      <path d="M1.5 7.5L4 10L8.5 4.5" stroke="#0F6E56" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
      <path d="M5.5 7.5L8 10L12.5 4.5" stroke="#0F6E56" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
    </svg>
  )}
  {symbol === "⚠" && (
    <svg width="14" height="14" viewBox="0 0 14 14" aria-hidden="true">
      <path d="M7 1.5L13 12H1L7 1.5Z" stroke="#7C2D12" stroke-width="1.4" stroke-linejoin="round" fill="none"/>
      <line x1="7" y1="6" x2="7" y2="9" stroke="#7C2D12" stroke-width="1.4" stroke-linecap="round"/>
      <circle cx="7" cy="10.5" r="0.7" fill="#7C2D12"/>
    </svg>
  )}
  {symbol === "✗" && (
    <svg width="14" height="14" viewBox="0 0 14 14" aria-hidden="true">
      <line x1="3" y1="3" x2="11" y2="11" stroke="#546E71" stroke-width="1.6" stroke-linecap="round"/>
      <line x1="11" y1="3" x2="3" y2="11" stroke="#546E71" stroke-width="1.6" stroke-linecap="round"/>
    </svg>
  )}
</span>
<style>
  .score-symbol { display: inline-flex; align-items: center; justify-content: center; line-height: 0; }
</style>
```

- [ ] **Step 2: Write `<Scoreboard />`**

Create `src/components/architecture/Scoreboard.astro`:

```astro
---
/**
 * <Scoreboard /> — flat comparison table.
 *
 * Reads frontmatter scoreboard:{ columns[], rows[]{ label, scores[] } }.
 * All rows visually identical (no Sean's-vault accent). Mobile keeps
 * the table — equality of presentation extends to mobile per §7.4.
 *
 * Per architecture-spec §7 + §16 (DoD #7).
 */
import ScoreSymbol from "./ScoreSymbol.astro";

interface ScoreboardData {
  columns: string[];
  rows: Array<{ label: string; scores: Array<"✓" | "✓✓" | "⚠" | "✗"> }>;
}
interface Props { data: ScoreboardData }
const { data } = Astro.props;
---
<section class="scoreboard-wrapper" aria-labelledby="scoreboard-heading">
  <h2 id="scoreboard-heading" class="visually-hidden">Scoreboard</h2>
  <div class="scoreboard-scroll">
    <table class="scoreboard">
      <caption class="visually-hidden">Comparison of systems across {data.columns.length} tests.</caption>
      <thead>
        <tr>
          <th scope="col" class="th-label">&nbsp;</th>
          {data.columns.map((col) => (
            <th scope="col" class="th-col">{col}</th>
          ))}
        </tr>
      </thead>
      <tbody>
        {data.rows.map((row) => (
          <tr>
            <th scope="row" class="td-label">{row.label}</th>
            {row.scores.map((s) => (
              <td class="td-score"><ScoreSymbol symbol={s} /></td>
            ))}
          </tr>
        ))}
      </tbody>
    </table>
  </div>
  <p class="scoreboard-legend" aria-label="Symbol legend">
    <ScoreSymbol symbol="✓✓" /> exceeds &middot;
    <ScoreSymbol symbol="✓" /> passes &middot;
    <ScoreSymbol symbol="⚠" /> partial &middot;
    <ScoreSymbol symbol="✗" /> fails
  </p>
</section>
<style>
  .scoreboard-wrapper {
    max-width: 1120px;
    margin: 48px auto;
    padding: 0 24px;
  }
  .scoreboard-scroll { overflow-x: auto; }
  .scoreboard {
    width: 100%;
    border-collapse: collapse;
    border: 0.5px solid rgba(10, 62, 66, 0.15);
    background: var(--paper);
  }
  .scoreboard th, .scoreboard td {
    border: 0.5px solid rgba(10, 62, 66, 0.10);
    padding: 10px 16px;
    font-family: var(--font-mono);
    text-align: center;
  }
  .scoreboard .th-col {
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 1.8px;
    text-transform: uppercase;
    color: var(--teal);
  }
  .scoreboard .td-label, .scoreboard .th-label {
    font-size: 13px;
    font-weight: 400;
    letter-spacing: 0.4px;
    text-align: left;
    color: var(--ink);
  }
  .scoreboard .td-score { vertical-align: middle; }
  .scoreboard-legend {
    margin: 16px 0 0;
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--ink-secondary);
    letter-spacing: 0.4px;
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    align-items: center;
  }
  .scoreboard-legend .score-symbol { margin-right: 2px; }
  .visually-hidden {
    position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px;
    overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border: 0;
  }
  @media (max-width: 767px) {
    .scoreboard-wrapper { padding: 0 16px; }
    .scoreboard th, .scoreboard td { padding: 8px 12px; }
  }
</style>
```

- [ ] **Step 3: Commit**

```bash
git add src/components/architecture/ScoreSymbol.astro src/components/architecture/Scoreboard.astro
git commit -m "$(cat <<'EOF'
feat(phase-3c.2): ScoreSymbol + Scoreboard — flat comparison table

ScoreSymbol: 14×14 inline SVG for ✓ / ✓✓ / ⚠ / ✗. Palette per arch
§5: success teal (#0F6E56) for ✓ and ✓✓ (✓✓ heavier stroke), stamp
amber (#7C2D12) for ⚠, secondary ink (#546E71) for ✗. ARIA labels
("passes" / "exceeds" / "partial" / "fails") per §7.2 a11y.

Scoreboard: reads frontmatter scoreboard{ columns[], rows[] } and
renders a flat semantic <table> with <caption> + <thead> + <tbody>.
All rows visually identical — equality of presentation IS the
credibility signal per §6 KILL rule. Mobile keeps the table via
overflow-x: auto with right-fade gradient (§7.4).

Symbol legend renders below the table in mono 11px secondary ink.

Per architecture-spec §7 + DoD §18 #7.
EOF
)"
```

### Task 4.2: `<HonestNote />`

**Files:**
- Create: `src/components/architecture/HonestNote.astro`

Per architecture-spec §8. The load-bearing credibility signal. Wire-service mono inside Newsreader essay prose — the register switch IS the visual emphasis.

- [ ] **Step 1: Write the component**

Create `src/components/architecture/HonestNote.astro`:

```astro
---
/**
 * <HonestNote /> — mono callout with stamp-amber prefix.
 *
 * Anchored at a section heading by `anchor:` matching honestNotes[].
 * Per architecture-spec §8 (≤3 per page, load-bearing credibility
 * signal). Authored in frontmatter; renders inline in essay body
 * at the matching section heading.
 */
interface Props {
  anchor: string;
  body: string;
}
const { anchor, body } = Astro.props;
---
<aside class="honest-note" data-anchor={anchor} aria-label="Honest note">
  <header class="honest-note__prefix">HONEST NOTE &mdash;</header>
  <p class="honest-note__body">{body}</p>
</aside>
<style>
  .honest-note {
    margin: 32px auto;
    max-width: 680px;
    padding: 0 0 0 24px;
    border-left: 1px solid var(--ink-secondary);
    background: var(--paper);
  }
  .honest-note__prefix {
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 1.8px;
    text-transform: uppercase;
    color: var(--stamp-amber);
    margin: 0 0 8px;
  }
  .honest-note__body {
    font-family: var(--font-mono);
    font-size: 14px;
    font-weight: 400;
    letter-spacing: 0.4px;
    line-height: 1.5;
    color: var(--ink-secondary);
    margin: 0;
  }
  @media (max-width: 767px) {
    .honest-note { padding-left: 16px; font-size: 13px; }
    .honest-note__body { font-size: 13px; }
  }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add src/components/architecture/HonestNote.astro
git commit -m "$(cat <<'EOF'
feat(phase-3c.2): HonestNote — load-bearing credibility callout

Wire-service mono callout with stamp-amber "HONEST NOTE —" prefix
and 1px secondary-ink left border. Sits inside Newsreader essay
prose; the mono register switch IS the visual emphasis per §8.3.

Authored in frontmatter honestNotes[]{ anchor, body } (≤3 per page
per §8.4); renders inline at the matching section heading via the
deep-dive page's render pass (Task 6.2).

data-anchor attribute preserved on the rendered <aside> for the
validator's anchor-matching warning (Task 3.2) to cross-reference
against the fetched essay body's section headings.

Per architecture-spec §8.
EOF
)"
```

### Task 4.3: `<TryItYourself />`

**Files:**
- Create: `src/components/architecture/TryItYourself.astro`

Per architecture-spec §10. Optional callout — renders only when `tryItYourselfUrl` is set. Teal-wash background distinguishes it from the HONEST NOTE (paper bg). Shiki bash-highlighted code snippet inside.

- [ ] **Step 1: Write the component**

Create `src/components/architecture/TryItYourself.astro`:

```astro
---
/**
 * <TryItYourself /> — teal-wash callout with bash code snippet.
 *
 * Per architecture-spec §10. Renders only when tryItYourselfUrl set.
 * The teal wash distinguishes it from HonestNote (paper bg, no fill).
 *
 * Code rendered via Astro's built-in <Code /> component (Shiki),
 * bash lexer. No line numbers — brevity is the signal (§10.4).
 */
import { Code } from "astro:components";

interface Props {
  url: string;
  command: string;
}
const { url, command } = Astro.props;
---
<aside class="try-it" aria-label="Try it yourself">
  <header class="try-it__prefix">TRY IT YOURSELF &mdash;</header>
  <Code code={command} lang="bash" theme="github-light" />
  <p class="try-it__link">
    <a href={url} rel="external">&rarr; view the fixture on github</a>
  </p>
</aside>
<style>
  .try-it {
    margin: 48px auto;
    max-width: 680px;
    padding: 20px 24px;
    border-left: 1px solid var(--teal);
    background: rgba(10, 62, 66, 0.04);
  }
  .try-it__prefix {
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 1.8px;
    text-transform: uppercase;
    color: var(--stamp-amber);
    margin: 0 0 12px;
  }
  .try-it :global(pre) {
    margin: 0;
    padding: 12px;
    background: var(--paper);
    border: 0.5px solid rgba(10, 62, 66, 0.15);
    border-radius: 2px;
    font-family: var(--font-mono);
    font-size: 13px;
    line-height: 1.5;
    overflow-x: auto;
  }
  .try-it__link {
    margin: 12px 0 0;
    font-family: var(--font-mono);
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 1.0px;
    color: var(--teal);
  }
  .try-it__link a {
    color: var(--teal);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 160ms var(--ease-out);
  }
  .try-it__link a:hover,
  .try-it__link a:focus-visible {
    border-bottom-color: var(--teal);
    outline: none;
  }
  @media (max-width: 767px) {
    .try-it { padding: 16px; }
  }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add src/components/architecture/TryItYourself.astro
git commit -m "$(cat <<'EOF'
feat(phase-3c.2): TryItYourself — teal-wash reproducibility callout

Optional mid-essay callout with bash code snippet and a single link
to the fixture/demo repo. Rendered only when tryItYourselfUrl is set
in frontmatter. The teal-wash bg (rgba(10, 62, 66, 0.04)) + 1px teal
left border distinguish it from HonestNote (paper bg, no fill).

Code rendered via Astro's built-in <Code /> Shiki integration with
the github-light theme — matches the surrounding paper palette.
No line numbers (§10.4: brevity is the signal).

Placement (mid-essay, after closing section, before Methods strip)
is composed by the deep-dive page (Task 6.2).

Per architecture-spec §10.
EOF
)"
```

### Task 4.4: `<ErDiagram />` + `<ThesisPullQuote />`

**Files:**
- Create: `src/components/architecture/ErDiagram.astro`
- Create: `src/components/architecture/ThesisPullQuote.astro`

Per architecture-spec §9 + §2.2. `<ErDiagram />` is a thin wrapper around the Mermaid SSR output + caption; the actual SVG is generated by `astro-mermaid` from a fenced ` ```mermaid ` block in MDX, OR from a `.mmd` source file read at build via `set:html`. `<ThesisPullQuote />` is the Newsreader-italic 1-line pullquote primitive — shared with `/essays/` via import (BLUEPRINT §3.2).

- [ ] **Step 1: Write `<ErDiagram />`**

Create `src/components/architecture/ErDiagram.astro`:

```astro
---
/**
 * <ErDiagram /> — wrapper for build-time Mermaid SVG output.
 *
 * Reads the .mmd source file declared in frontmatter mermaidSource:.
 * Wraps the raw mermaid code in a fenced block; astro-mermaid renders
 * to inline SVG at build time. The wrapper just provides the caption
 * + mobile overflow scroll + fallback <pre> when SVG render fails.
 *
 * Per architecture-spec §9 (esp. §9.5 fallback contract).
 */
import fs from "node:fs/promises";
import path from "node:path";

interface Props {
  source: string;        // inline mermaid OR relative .mmd path from src/content/architecture/
  caption?: string;
}
const { source, caption } = Astro.props;

// Resolve inline-vs-path. Inline iff source contains a newline OR doesn't end in .mmd.
const isPath = !source.includes("\n") && (source.endsWith(".mmd") || source.includes("/"));

let mermaidBody = source;
if (isPath) {
  const ROOT = process.cwd();
  const mmdPath = path.join(ROOT, "src/content/architecture", source);
  try {
    mermaidBody = await fs.readFile(mmdPath, "utf8");
  } catch {
    mermaidBody = `erDiagram\n  ERROR ||--|| FALLBACK : "missing .mmd at ${source}"`;
  }
}
---
<figure class="er-diagram">
  <div class="er-diagram__svg-wrap">
    <pre class="mermaid">{mermaidBody}</pre>
  </div>
  {caption && <figcaption class="er-diagram__caption">{caption}</figcaption>}
</figure>
<style>
  .er-diagram {
    margin: 48px auto;
    max-width: 1120px;
    padding: 0 24px;
  }
  .er-diagram__svg-wrap {
    background: var(--paper);
    padding: 24px;
    overflow-x: auto;
  }
  .er-diagram__svg-wrap :global(svg) {
    max-width: 100%;
    height: auto;
    display: block;
    margin: 0 auto;
  }
  .er-diagram__caption {
    margin: 16px 0 0;
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 400;
    letter-spacing: 0.8px;
    color: var(--ink-secondary);
    text-align: center;
  }
  @media (max-width: 767px) {
    .er-diagram { padding: 0 16px; }
    .er-diagram__svg-wrap { padding: 16px; }
  }
</style>
```

- [ ] **Step 2: Write `<ThesisPullQuote />`**

Create `src/components/architecture/ThesisPullQuote.astro`:

```astro
---
/**
 * <ThesisPullQuote /> — Newsreader-italic 1-line pullquote primitive.
 *
 * Shared by /architecture/ (architecture-spec §2.2) AND /essays/
 * (essays-spec §2.2). Same Newsreader 24px italic, 0.5px teal rules,
 * max-width 680px per BLUEPRINT §3.2. /essays/ imports from this
 * location (no fork).
 */
interface Props { text: string }
const { text } = Astro.props;
---
<blockquote class="thesis-pullquote">
  <p>{text}</p>
</blockquote>
<style>
  .thesis-pullquote {
    max-width: 680px;
    margin: 48px auto;
    padding: 32px 0;
    border-top: 0.5px solid rgba(10, 62, 66, 0.25);
    border-bottom: 0.5px solid rgba(10, 62, 66, 0.25);
  }
  .thesis-pullquote p {
    margin: 0;
    font-family: var(--font-serif);
    font-size: clamp(22px, 2.8vw, 32px);
    font-weight: 300;
    font-style: italic;
    letter-spacing: -0.3px;
    line-height: 1.35;
    color: var(--teal);
  }
  @media (max-width: 767px) {
    .thesis-pullquote { padding: 24px 16px; margin: 32px auto; }
  }
</style>
```

- [ ] **Step 3: Commit**

```bash
git add src/components/architecture/ErDiagram.astro src/components/architecture/ThesisPullQuote.astro
git commit -m "$(cat <<'EOF'
feat(phase-3c.2): ErDiagram + ThesisPullQuote

ErDiagram: thin wrapper around astro-mermaid SSR output. Reads the
.mmd source declared in frontmatter mermaidSource: (or inline source
string if no newline + doesn't end in .mmd). Wraps the mermaid code
in a <pre class="mermaid"> block; astro-mermaid replaces with inline
SVG at build time. Mobile: overflow-x auto on the wrapper.

Fallback (§9.5): missing .mmd file at the declared path renders
`erDiagram\n  ERROR ||--|| FALLBACK : "missing .mmd at ..."` instead
of crashing — the validator catches missing-file cases in CI before
deploy (Task 3.2).

ThesisPullQuote: Newsreader 24px italic, 0.5px teal rules above +
below, max-width 680px. Shared primitive — /essays/ deep-dive imports
this file directly (no fork) per BLUEPRINT §3.2.

Per architecture-spec §9 + §2.2 + BLUEPRINT §3.2.
EOF
)"
```

### Task 4.5: `<IndexHeader />` + `<WriteupRow />` + `<RelatedBlock />` (architecture)

**Files:**
- Create: `src/components/architecture/IndexHeader.astro`
- Create: `src/components/architecture/WriteupRow.astro`
- Create: `src/components/architecture/RelatedBlock.astro`

Per architecture-spec §2.1 + §4.1 + §5 (IndexHeader); §2.1 + §5 (WriteupRow); §14 + §2.2 (RelatedBlock).

- [ ] **Step 1: Write `<IndexHeader />`**

Create `src/components/architecture/IndexHeader.astro`:

```astro
---
/**
 * <IndexHeader /> — h1 + 1-line italic hook + sober subhead +
 *                   metadata strip for /architecture/.
 *
 * Dateline strip is build-time rendered (architecture-spec §13):
 * "BOSTON, JUNE 3, 2026 — vault scorecard shipped. 1 writeup."
 *
 * Per architecture-spec §2.1 + §4.1 + §5.
 */
interface Props {
  count: number;            // total writeups
  lastUpdatedISO: string;   // ISO YYYY-MM-DD of most recent shipped
  mostRecentTitle?: string; // for the dateline body (low-case)
}
const { count, lastUpdatedISO, mostRecentTitle } = Astro.props;

const today = new Date();
const monthName = today.toLocaleDateString("en-US", { month: "long" }).toUpperCase();
const day = today.getDate();
const year = today.getFullYear();
const datelineBody = mostRecentTitle
  ? `${mostRecentTitle.toLowerCase()} shipped. ${count} writeup${count === 1 ? "" : "s"}.`
  : `${count} writeup${count === 1 ? "" : "s"}.`;

const lastUpdatedLabel = lastUpdatedISO.replace(/-/g, "-");
---
<header class="index-header">
  <p class="dateline">
    <span class="dateline__city">BOSTON, {monthName} {day}, {year}</span>
    <span class="dateline__sep">&mdash;</span>
    <span class="dateline__body">{datelineBody}</span>
  </p>
  <hr class="dateline-rule" />
  <h1 class="page-title">Architecture</h1>
  <p class="hook">Most people see Obsidian as content. I treat my vault as agent infrastructure.</p>
  <p class="subhead">Architectural arguments where the proof is the code, linked.</p>
  <p class="metadata-strip">
    <span>{count} WRITEUP{count === 1 ? "" : "S"}</span>
    <span aria-hidden="true">&middot;</span>
    <span>UPDATED {lastUpdatedLabel}</span>
    <span aria-hidden="true">&middot;</span>
    <a href="/architecture/rss.xml" class="rss-link">RSS &rarr;</a>
  </p>
</header>
<style>
  .index-header { max-width: 1120px; margin: 0 auto; padding: 60px 24px 0; }
  .dateline {
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 1.6px;
    color: var(--ink-secondary);
    margin: 0 0 12px;
  }
  .dateline__city { color: var(--stamp-amber); }
  .dateline__sep { margin: 0 6px; }
  .dateline-rule {
    border: none;
    border-top: 0.5px solid rgba(10, 62, 66, 0.25);
    margin: 0 0 40px;
  }
  .page-title {
    font-family: var(--font-serif);
    font-size: clamp(56px, 6vw, 96px);
    font-weight: 400;
    letter-spacing: -0.6px;
    line-height: 1.05;
    color: var(--teal);
    margin: 0 0 16px;
  }
  .hook {
    font-family: var(--font-serif);
    font-size: clamp(20px, 2.4vw, 32px);
    font-weight: 300;
    font-style: italic;
    letter-spacing: -0.2px;
    line-height: 1.3;
    color: var(--ink);
    margin: 0 0 16px;
    max-width: 680px;
  }
  .subhead {
    font-family: var(--font-serif);
    font-size: 18px;
    font-weight: 300;
    letter-spacing: -0.1px;
    line-height: 1.45;
    color: var(--ink-secondary);
    margin: 0 0 24px;
    max-width: 680px;
  }
  .metadata-strip {
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 1.6px;
    color: var(--ink-secondary);
    margin: 0 0 56px;
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    align-items: baseline;
  }
  .metadata-strip .rss-link {
    color: var(--teal);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 160ms var(--ease-out);
  }
  .metadata-strip .rss-link:hover,
  .metadata-strip .rss-link:focus-visible {
    border-bottom-color: var(--teal);
    outline: none;
  }
  @media (max-width: 767px) {
    .index-header { padding: 40px 16px 0; }
    .metadata-strip { margin-bottom: 32px; }
  }
</style>
```

- [ ] **Step 2: Write `<WriteupRow />`**

Create `src/components/architecture/WriteupRow.astro`:

```astro
---
/**
 * <WriteupRow /> — single row on the /architecture/ index.
 *
 * Dateline + status pill + title + 1-line lead. View transition
 * seam `arch-title-<slug>` shared with deep-dive <h1>.
 *
 * Per architecture-spec §2.1 + §5 + §15.
 */
interface Props {
  slug: string;
  shipped: string;     // ISO YYYY-MM-DD
  status: string;      // "DRAFT" | "SHIPPED"
  title: string;
  lead: string;
}
const { slug, shipped, status, title, lead } = Astro.props;
const datelineLabel = new Date(`${shipped}T12:00:00Z`)
  .toLocaleDateString("en-US", { month: "short", day: "2-digit", year: "numeric" })
  .toUpperCase();
const statusClass = `status-pill status-${status.toLowerCase()}`;
const ariaLabel = `Read architecture writeup: ${title}, shipped ${shipped}`;
---
<a href={`/architecture/${slug}/`} class="writeup-row" aria-label={ariaLabel}>
  <span class="row-dateline" aria-hidden="true">{datelineLabel}</span>
  <span class={statusClass} aria-hidden="true">{status}</span>
  <span class="row-body">
    <span class="row-title" style={`view-transition-name: arch-title-${slug}`}>{title}</span>
    <span class="row-lead">{lead}</span>
  </span>
  <span class="row-glyph" aria-hidden="true">&rarr;</span>
</a>
<style>
  .writeup-row {
    display: grid;
    grid-template-columns: 120px 96px 1fr 24px;
    column-gap: 16px;
    align-items: start;
    min-height: 140px;
    padding: 24px;
    color: var(--ink);
    text-decoration: none;
    border-bottom: 0.5px solid var(--border-paper);
    transition: background-color 200ms var(--ease-out);
  }
  .writeup-row:hover { background-color: rgba(10, 62, 66, 0.04); }
  .writeup-row:hover .row-title { box-shadow: inset 0 -1px 0 0 var(--teal); }
  .writeup-row:hover .row-glyph { transform: translateX(4px); }
  .writeup-row:focus-visible { outline: 2px solid var(--teal); outline-offset: -2px; }
  .row-dateline {
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 1.6px;
    color: var(--stamp-amber);
    line-height: 1.4;
    padding-top: 4px;
  }
  .status-pill {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 1.8px;
    text-transform: uppercase;
    color: var(--ink);
    border: 1px solid var(--ink);
    padding: 3px 8px;
    align-self: start;
    justify-self: start;
    margin-top: 2px;
  }
  .status-pill.status-shipped { color: var(--stamp-amber); border-color: var(--stamp-amber); }
  .status-pill.status-draft { color: var(--ink-secondary); border-color: var(--ink-secondary); }
  .row-body { display: flex; flex-direction: column; gap: 8px; }
  .row-title {
    font-family: var(--font-serif);
    font-size: 22px;
    font-weight: 400;
    letter-spacing: -0.2px;
    line-height: 1.3;
    color: var(--ink);
    transition: box-shadow 160ms var(--ease-out);
  }
  .row-lead {
    font-family: var(--font-serif);
    font-size: 16px;
    font-weight: 300;
    letter-spacing: -0.1px;
    line-height: 1.4;
    color: var(--ink-secondary);
    max-width: 60ch;
  }
  .row-glyph {
    font-family: var(--font-mono);
    font-size: 14px;
    color: var(--ink-secondary);
    align-self: center;
    transition: transform 200ms var(--ease-out);
  }
  @media (max-width: 767px) {
    .writeup-row { grid-template-columns: 1fr; min-height: auto; padding: 20px 16px; gap: 8px; }
    .row-glyph { display: none; }
    .row-title { font-size: 20px; }
  }
</style>
```

- [ ] **Step 3: Write `<RelatedBlock />`**

Create `src/components/architecture/RelatedBlock.astro`:

```astro
---
/**
 * <RelatedBlock /> — architecture deep-dive Related block.
 *
 * Renders:
 *   - relatedLedgerRow → /transactions/<slug>/
 *   - relatedCaseStudy → /work/<slug>/
 *   - relatedEssay     → /essays/<slug>/
 *   - relatedArchitecture[] → siblings
 *   - REVERSE: namedInEssays[] auto-rendered from .crosslinks.json
 *     (essays' plottedArtifacts reverse-derivation)
 *
 * Per architecture-spec §14 + §2.2.
 */
interface ReverseShape {
  siblingArchitecture?: string[];
  namedInEssays?: string[];
}
interface Crosslinks {
  architecture?: Record<string, ReverseShape>;
}
interface Props {
  slug: string;
  relatedLedgerRow?: string;
  relatedCaseStudy?: string;
  relatedEssay?: string;
  relatedArchitecture?: string[];
  crosslinks: Crosslinks;
  titleByArchSlug: Record<string, string>;
  titleByTxnSlug: Record<string, string>;
  titleByWorkSlug: Record<string, string>;
  titleByEssaySlug: Record<string, string>;
}
const {
  slug,
  relatedLedgerRow,
  relatedCaseStudy,
  relatedEssay,
  relatedArchitecture = [],
  crosslinks,
  titleByArchSlug,
  titleByTxnSlug,
  titleByWorkSlug,
  titleByEssaySlug,
} = Astro.props;

const reverse = crosslinks.architecture?.[slug] ?? {};
const siblingArch = Array.from(new Set([
  ...relatedArchitecture,
  ...(reverse.siblingArchitecture ?? []),
]));
const namedInEssays = reverse.namedInEssays ?? [];

const hasAny = Boolean(
  relatedLedgerRow || relatedCaseStudy || relatedEssay ||
  siblingArch.length > 0 || namedInEssays.length > 0
);
---
{hasAny && (
  <section class="related-block" aria-labelledby={`related-${slug}`}>
    <h2 id={`related-${slug}`} class="section-heading">&mdash; RELATED &mdash;</h2>
    <ul class="related-list">
      {relatedLedgerRow && (
        <li>
          <a href={`/transactions/${relatedLedgerRow}/`}>&rarr; ledger row: {titleByTxnSlug[relatedLedgerRow] ?? relatedLedgerRow}</a>
        </li>
      )}
      {relatedCaseStudy && (
        <li>
          <a href={`/work/${relatedCaseStudy}/`}>&rarr; companion case study: {titleByWorkSlug[relatedCaseStudy] ?? relatedCaseStudy}</a>
        </li>
      )}
      {relatedEssay && (
        <li>
          <a href={`/essays/${relatedEssay}/`}>&rarr; companion essay: {titleByEssaySlug[relatedEssay] ?? relatedEssay}</a>
        </li>
      )}
      {siblingArch.map((s) => (
        <li>
          <a href={`/architecture/${s}/`}>&harr; sibling architecture: {titleByArchSlug[s] ?? s}</a>
        </li>
      ))}
      {namedInEssays.map((s) => (
        <li>
          <a href={`/essays/${s}/`}>&larr; named in: {titleByEssaySlug[s] ?? s} (essay)</a>
        </li>
      ))}
    </ul>
  </section>
)}
<style>
  .related-block { max-width: 1120px; margin: 64px auto 0; padding: 0 24px; }
  .section-heading {
    font-family: var(--font-mono);
    font-size: 14px;
    font-weight: 500;
    letter-spacing: 2.4px;
    color: var(--teal);
    opacity: 0.8;
    text-transform: uppercase;
    margin: 0 0 16px;
  }
  .related-list { list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 8px; }
  .related-list a {
    font-family: var(--font-mono);
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 0.4px;
    color: var(--teal);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 160ms var(--ease-out);
  }
  .related-list a:hover,
  .related-list a:focus-visible { border-bottom-color: var(--teal); outline: none; }
  @media (max-width: 767px) { .related-block { padding: 0 16px; } }
</style>
```

- [ ] **Step 4: Commit**

```bash
git add src/components/architecture/IndexHeader.astro src/components/architecture/WriteupRow.astro src/components/architecture/RelatedBlock.astro
git commit -m "$(cat <<'EOF'
feat(phase-3c.2): architecture IndexHeader + WriteupRow + RelatedBlock

IndexHeader: h1 "Architecture" + Vault-Scorecard-italic hook (OPEN-2
default) + sober subhead + mono metadata strip (count + UPDATED date
+ RSS link). Dateline strip is build-time rendered per §13 — page
reads its own collection to synthesize the body.

WriteupRow: 140px desktop row with dateline + status pill + title
(view-transition-name arch-title-<slug>) + 1-line lead + glyph.
Hover: bg fade, title underline, glyph translates 4px.

RelatedBlock: reads .crosslinks.json to compute the union of forward
relatedArchitecture[] declarations + reverse siblingArchitecture[]
entries. Renders relatedLedgerRow, relatedCaseStudy, relatedEssay,
siblings (with ↔ glyph), AND reverse "← named in: <essay>" lines
when essays' plottedArtifacts cited this slug (§10.3 / §14
4-way close).

Per architecture-spec §2.1 + §5 + §13 + §14 + §15.
EOF
)"
```

---

## Section 5 — Essays-specific components

Nine Astro components for the `/essays/` surface. Bundled into 4 tasks. The `<FootLinks />` carries the 3 foot-of-page affordances (Substack + canonical source + canonical EXPLANATION) which v1 partially renders (Substack only — the canonical-source + canonical-EXPLANATION lines render only when their URLs are non-null per OQ-C).

### Task 5.1: `<QuadrantChart />` + `<QuadrantLegend />`

**Files:**
- Create: `src/components/essays/QuadrantChart.astro`
- Create: `src/components/essays/QuadrantLegend.astro`

Per essays-spec §8.

- [ ] **Step 1: Write `<QuadrantChart />`**

Create `src/components/essays/QuadrantChart.astro`:

```astro
---
/**
 * <QuadrantChart /> — wraps Mermaid quadrantChart SSR output.
 *
 * Mirrors <ErDiagram /> shape but reads from src/content/essays/
 * (different content root). Per essays-spec §8.2 + §8.5.
 */
import fs from "node:fs/promises";
import path from "node:path";

interface Props { source: string; caption?: string }
const { source, caption } = Astro.props;

const isPath = !source.includes("\n") && (source.endsWith(".mmd") || source.includes("/"));
let mermaidBody = source;
if (isPath) {
  const ROOT = process.cwd();
  const mmdPath = path.join(ROOT, "src/content/essays", source);
  try {
    mermaidBody = await fs.readFile(mmdPath, "utf8");
  } catch {
    mermaidBody = `quadrantChart\n  title ERROR\n  x-axis Missing --> Source\n  y-axis Missing --> File`;
  }
}
---
<figure class="quadrant-chart">
  <div class="quadrant-chart__svg-wrap">
    <pre class="mermaid">{mermaidBody}</pre>
  </div>
  {caption && <figcaption class="quadrant-chart__caption">{caption}</figcaption>}
</figure>
<style>
  .quadrant-chart { margin: 48px auto; max-width: 1120px; padding: 0 24px; }
  .quadrant-chart__svg-wrap { background: var(--paper); padding: 24px; overflow-x: auto; }
  .quadrant-chart__svg-wrap :global(svg) { max-width: 100%; height: auto; display: block; margin: 0 auto; }
  .quadrant-chart__caption {
    margin: 16px 0 0;
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 400;
    letter-spacing: 0.8px;
    color: var(--ink-secondary);
    text-align: center;
  }
  @media (max-width: 767px) { .quadrant-chart { padding: 0 16px; } .quadrant-chart__svg-wrap { padding: 16px; } }
</style>
```

- [ ] **Step 2: Write `<QuadrantLegend />`**

Create `src/components/essays/QuadrantLegend.astro`:

```astro
---
/**
 * <QuadrantLegend /> — wire-service mono table below the chart.
 *
 * Each row: short code → human-readable label. When `artifact` is set,
 * the label links to /transactions/<slug>/ (and to /architecture/<slug>/
 * when that surface also has the slug, via crosslinks). Null artifact
 * is the negative-space callout case.
 *
 * Per essays-spec §8.4.
 */
interface LegendEntry { key: string; label: string; artifact?: string | null }
interface Props {
  entries: LegendEntry[];
  txnSlugs: Set<string>;
  archSlugs: Set<string>;
}
const { entries, txnSlugs, archSlugs } = Astro.props;

function resolveLinks(artifact: string | null | undefined): Array<{ href: string; label: string }> {
  if (!artifact) return [];
  const out: Array<{ href: string; label: string }> = [];
  if (txnSlugs.has(artifact)) out.push({ href: `/transactions/${artifact}/`, label: "ledger" });
  if (archSlugs.has(artifact)) out.push({ href: `/architecture/${artifact}/`, label: "architecture" });
  return out;
}
---
<aside class="quadrant-legend" aria-label="Chart legend">
  <ul>
    {entries.map((e) => {
      const links = resolveLinks(e.artifact);
      return (
        <li>
          <span class="key">{e.key}</span>
          <span class="arrow" aria-hidden="true">&rarr;</span>
          <span class="label">{e.label}</span>
          {links.length > 0 && (
            <span class="links">
              {links.map((l, i) => (
                <>
                  <a href={l.href}>{l.label}</a>
                  {i < links.length - 1 && <span class="sep" aria-hidden="true">&middot;</span>}
                </>
              ))}
            </span>
          )}
        </li>
      );
    })}
  </ul>
</aside>
<style>
  .quadrant-legend {
    max-width: 680px;
    margin: 16px auto 32px;
    padding: 16px 24px;
    background: rgba(10, 62, 66, 0.02);
    border: 0.5px solid rgba(10, 62, 66, 0.15);
  }
  .quadrant-legend ul { list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 6px; }
  .quadrant-legend li {
    font-family: var(--font-mono);
    font-size: 13px;
    font-weight: 400;
    letter-spacing: 0.4px;
    color: var(--ink);
    display: flex;
    align-items: baseline;
    gap: 8px;
    flex-wrap: wrap;
  }
  .quadrant-legend .key { color: var(--teal); font-weight: 500; }
  .quadrant-legend .arrow { color: var(--ink-secondary); }
  .quadrant-legend .label { flex: 1; }
  .quadrant-legend .links { display: inline-flex; gap: 6px; align-items: baseline; }
  .quadrant-legend .links a {
    color: var(--teal);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    font-size: 12px;
    letter-spacing: 0.6px;
    transition: border-color 160ms var(--ease-out);
  }
  .quadrant-legend .links a:hover { border-bottom-color: var(--teal); }
  .quadrant-legend .sep { color: var(--ink-secondary); font-size: 12px; }
  @media (max-width: 767px) { .quadrant-legend { padding: 12px 16px; } }
</style>
```

- [ ] **Step 3: Commit**

```bash
git add src/components/essays/QuadrantChart.astro src/components/essays/QuadrantLegend.astro
git commit -m "$(cat <<'EOF'
feat(phase-3c.2): QuadrantChart + QuadrantLegend

QuadrantChart: same shape as architecture/ErDiagram but reads from
src/content/essays/diagrams/<slug>.mmd. Mermaid quadrantChart renders
inline SVG at build time via astro-mermaid + the 5-var palette
override (paper bg, ink+teal text, all four quadrant fills paper).

QuadrantLegend: wire-service mono table below the chart. Each row
is { key → label } (+ optional artifact slug). Artifact slugs auto-
resolve via txnSlugs + archSlugs Sets passed from the page; when
both surfaces have the slug, both links render (ledger · architecture).
Negative-space callouts (artifact: null) render label only.

Per essays-spec §8.4 + §8.5.
EOF
)"
```

### Task 5.2: `<RoleMap />` + `<PublishedRevisedStrip />`

**Files:**
- Create: `src/components/essays/RoleMap.astro`
- Create: `src/components/essays/PublishedRevisedStrip.astro`

Per essays-spec §9 + §2.2 + §5.

- [ ] **Step 1: Write `<RoleMap />`**

Create `src/components/essays/RoleMap.astro`:

```astro
---
/**
 * <RoleMap /> — 4-column structured table.
 *
 * Columns: Buyer · Position · Vocabulary tell · JD ↗.
 * Reads frontmatter roleMap{ lastValidated, rows[] }. The validator
 * (Task 3.2) HEAD-checks every jdUrl + warns on >30-day-old
 * lastValidated — this component just renders.
 *
 * Per essays-spec §9.
 */
interface RoleRow { buyer: string; position: string; vocabularyTell: string; jdUrl: string }
interface Props {
  lastValidated: string;   // ISO YYYY-MM-DD
  rows: RoleRow[];
}
const { lastValidated, rows } = Astro.props;

const lvDate = new Date(`${lastValidated}T12:00:00Z`);
const ageDays = Math.floor((Date.now() - lvDate.getTime()) / 86400000);
const stale = ageDays > 30;
---
<section class="role-map" aria-labelledby="rolemap-heading">
  <h2 id="rolemap-heading" class="visually-hidden">Role map</h2>
  <div class="role-map__scroll">
    <table>
      <caption class="visually-hidden">Buyer roles, market positions, vocabulary tells, and JD links.</caption>
      <thead>
        <tr>
          <th scope="col">BUYER</th>
          <th scope="col">POSITION</th>
          <th scope="col">VOCABULARY TELL</th>
          <th scope="col">JD &uarr;</th>
        </tr>
      </thead>
      <tbody>
        {rows.map((r) => (
          <tr>
            <td class="cell-buyer">{r.buyer}</td>
            <td class="cell-position">{r.position}</td>
            <td class="cell-tell">{r.vocabularyTell}</td>
            <td class="cell-jd">
              <a href={r.jdUrl} rel="external">link <span aria-hidden="true">&uarr;</span></a>
            </td>
          </tr>
        ))}
      </tbody>
    </table>
  </div>
  <p class={stale ? "lv-badge lv-stale" : "lv-badge"}>last validated {lastValidated}{stale && ` (${ageDays} days ago)`}</p>
</section>
<style>
  .role-map { max-width: 1120px; margin: 48px auto; padding: 0 24px; }
  .role-map__scroll { overflow-x: auto; }
  .role-map table {
    width: 100%;
    border-collapse: collapse;
    border: 0.5px solid rgba(10, 62, 66, 0.15);
    background: var(--paper);
  }
  .role-map th, .role-map td {
    border: 0.5px solid rgba(10, 62, 66, 0.10);
    padding: 10px 16px;
    font-family: var(--font-mono);
    text-align: left;
    vertical-align: top;
  }
  .role-map th {
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 1.8px;
    text-transform: uppercase;
    color: var(--teal);
    background: rgba(10, 62, 66, 0.02);
  }
  .role-map td {
    font-size: 13px;
    font-weight: 400;
    letter-spacing: 0.4px;
    color: var(--ink);
  }
  .role-map .cell-jd a {
    color: var(--teal);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    font-weight: 500;
    transition: border-color 160ms var(--ease-out);
  }
  .role-map .cell-jd a:hover { border-bottom-color: var(--teal); }
  .lv-badge {
    margin: 12px 0 0;
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 400;
    letter-spacing: 1.2px;
    color: var(--ink-secondary);
  }
  .lv-badge.lv-stale { color: var(--stamp-amber); }
  .visually-hidden {
    position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px;
    overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border: 0;
  }
  @media (max-width: 767px) {
    .role-map { padding: 0 16px; }
    .role-map th, .role-map td { padding: 8px 12px; }
  }
</style>
```

- [ ] **Step 2: Write `<PublishedRevisedStrip />`**

Create `src/components/essays/PublishedRevisedStrip.astro`:

```astro
---
/**
 * <PublishedRevisedStrip /> — conditional dual-date strip.
 *
 * Renders "PUBLISHED <date> · LAST REVISED <date>" only when
 * lastRevised is set AND different from published. Per essays-spec
 * §2.2 + §5.
 */
interface Props { published: string; lastRevised?: string | null }
const { published, lastRevised } = Astro.props;
const show = Boolean(lastRevised && lastRevised !== published);
---
{show ? (
  <p class="pub-rev-strip">
    <span class="pub-prefix">PUBLISHED</span> {published}
    <span class="sep" aria-hidden="true">&middot;</span>
    <span class="rev-prefix">LAST REVISED</span> {lastRevised}
  </p>
) : (
  <p class="pub-rev-strip">
    <span class="pub-prefix">PUBLISHED</span> {published}
  </p>
)}
<style>
  .pub-rev-strip {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 1.6px;
    color: var(--ink-secondary);
    margin: 16px 0 24px;
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    align-items: baseline;
  }
  .pub-prefix { color: var(--stamp-amber); }
  .rev-prefix { color: var(--ink-secondary); }
  .sep { color: var(--ink-secondary); }
</style>
```

- [ ] **Step 3: Commit**

```bash
git add src/components/essays/RoleMap.astro src/components/essays/PublishedRevisedStrip.astro
git commit -m "$(cat <<'EOF'
feat(phase-3c.2): RoleMap + PublishedRevisedStrip

RoleMap: 4-column structured table (Buyer · Position · Vocabulary
tell · JD ↗) reading frontmatter roleMap{ lastValidated, rows[] }.
The "last validated <date>" mono badge below the table turns stamp
amber when lastValidated > 30 days old (matches the validator's
staleness warning in Task 3.2). Mobile: overflow-x auto, same
primitive as architecture Scoreboard.

PublishedRevisedStrip: conditional render — when lastRevised is set
AND different from published, shows "PUBLISHED <date> · LAST REVISED
<date>"; otherwise shows "PUBLISHED <date>" alone. PUBLISHED prefix
in stamp amber; LAST REVISED prefix in secondary ink (calmer signal
that an editorial update happened, not a new publish).

Per essays-spec §9 + §2.2 + §5.
EOF
)"
```

### Task 5.3: `<PlottedArtifacts />` + `<FootLinks />`

**Files:**
- Create: `src/components/essays/PlottedArtifacts.astro`
- Create: `src/components/essays/FootLinks.astro`

Per essays-spec §10 + §16.

- [ ] **Step 1: Write `<PlottedArtifacts />`**

Create `src/components/essays/PlottedArtifacts.astro`:

```astro
---
/**
 * <PlottedArtifacts /> — auto-rendered list of cited artifacts.
 *
 * Reads frontmatter plottedArtifacts[]: slug. Each slug resolves
 * via the txnSlugs + archSlugs Sets passed from the page. When a
 * slug exists on both surfaces, both links render comma-separated.
 *
 * Per essays-spec §10.
 */
interface Props {
  slugs: string[];
  txnSlugs: Set<string>;
  archSlugs: Set<string>;
  titleByTxnSlug: Record<string, string>;
  titleByArchSlug: Record<string, string>;
}
const { slugs, txnSlugs, archSlugs, titleByTxnSlug, titleByArchSlug } = Astro.props;
const rendered = slugs.filter((s) => txnSlugs.has(s) || archSlugs.has(s));
---
{rendered.length > 0 && (
  <section class="plotted-artifacts" aria-labelledby="plotted-heading">
    <h2 id="plotted-heading" class="section-heading">&mdash; ARTIFACTS PLOTTED ON THE CHART &mdash;</h2>
    <ul>
      {rendered.map((s) => {
        const label = titleByTxnSlug[s] ?? titleByArchSlug[s] ?? s;
        const links: Array<{ href: string; tag: string }> = [];
        if (txnSlugs.has(s)) links.push({ href: `/transactions/${s}/`, tag: "ledger" });
        if (archSlugs.has(s)) links.push({ href: `/architecture/${s}/`, tag: "architecture" });
        return (
          <li>
            <span class="arrow" aria-hidden="true">&rarr;</span>
            <span class="label">{label}</span>
            <span class="dash" aria-hidden="true">&mdash;</span>
            {links.map((l, i) => (
              <>
                <a href={l.href}>{l.href}</a>
                {i < links.length - 1 && <span class="sep" aria-hidden="true">,</span>}
              </>
            ))}
          </li>
        );
      })}
    </ul>
  </section>
)}
<style>
  .plotted-artifacts { max-width: 1120px; margin: 80px auto 0; padding: 0 24px; }
  .section-heading {
    font-family: var(--font-mono);
    font-size: 14px;
    font-weight: 500;
    letter-spacing: 2.4px;
    color: var(--teal);
    opacity: 0.8;
    text-transform: uppercase;
    margin: 0 0 24px;
  }
  .plotted-artifacts ul { list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 8px; }
  .plotted-artifacts li {
    font-family: var(--font-mono);
    font-size: 14px;
    font-weight: 500;
    letter-spacing: 0.6px;
    color: var(--teal);
    display: flex;
    align-items: baseline;
    gap: 6px;
    flex-wrap: wrap;
  }
  .plotted-artifacts .arrow, .plotted-artifacts .dash, .plotted-artifacts .sep { color: var(--ink-secondary); }
  .plotted-artifacts .label { color: var(--ink); font-weight: 500; }
  .plotted-artifacts a {
    color: var(--teal);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 160ms var(--ease-out);
  }
  .plotted-artifacts a:hover { border-bottom-color: var(--teal); }
  @media (max-width: 767px) { .plotted-artifacts { padding: 0 16px; } }
</style>
```

- [ ] **Step 2: Write `<FootLinks />`**

Create `src/components/essays/FootLinks.astro`:

```astro
---
/**
 * <FootLinks /> — 3 foot-of-page links: Substack + canonical source
 * + canonical EXPLANATION.
 *
 * Substack renders from crossPostedTo[].url where name === "Substack".
 * Other crossPostedTo[] entries render as additional "Read on <name> →"
 * lines (forward-compatible without schema changes).
 * Canonical source link renders only when sourceUrl is set + non-null.
 * Canonical EXPLANATION link renders only when explanationUrl is set + non-null.
 *
 * Per essays-spec §2.2 + §16.
 */
interface CrossPost { name: string; url: string }
interface Props {
  crossPostedTo?: CrossPost[];
  sourceUrl?: string | null;
  explanationUrl?: string | null;
}
const { crossPostedTo = [], sourceUrl, explanationUrl } = Astro.props;

const showSrc = Boolean(sourceUrl && sourceUrl !== "null");
const showExpl = Boolean(explanationUrl && explanationUrl !== "null");
---
<section class="foot-links" aria-label="External references">
  {crossPostedTo.map((cp) => (
    <p class="foot-link">
      <a href={cp.url} rel="external">&rarr; read on {cp.name} <span aria-hidden="true">&uarr;</span></a>
    </p>
  ))}
  {showSrc && (
    <p class="foot-link">
      <a href={sourceUrl!} rel="external">&rarr; read the canonical source on github <span class="info-glyph" aria-hidden="true">&#9432;</span></a>
    </p>
  )}
  {showExpl && (
    <p class="foot-link">
      <a href={explanationUrl!} rel="external">&rarr; read the canonical EXPLANATION.md on github <span class="info-glyph" aria-hidden="true">&#9432;</span></a>
    </p>
  )}
</section>
<style>
  .foot-links { max-width: 1120px; margin: 32px auto 0; padding: 0 24px; }
  .foot-link {
    margin: 0 0 8px;
    font-family: var(--font-mono);
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 1.0px;
    color: var(--teal);
  }
  .foot-link a {
    color: var(--teal);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 160ms var(--ease-out);
  }
  .foot-link a:hover,
  .foot-link a:focus-visible { border-bottom-color: var(--teal); outline: none; }
  .foot-link .info-glyph { margin-left: 6px; color: var(--ink-secondary); font-size: 12px; }
  @media (max-width: 767px) { .foot-links { padding: 0 16px; } }
</style>
```

- [ ] **Step 3: Commit**

```bash
git add src/components/essays/PlottedArtifacts.astro src/components/essays/FootLinks.astro
git commit -m "$(cat <<'EOF'
feat(phase-3c.2): PlottedArtifacts + FootLinks

PlottedArtifacts: auto-rendered list at the foot of the essay body
(before 4Q + foot links). Each slug resolves via txnSlugs + archSlugs
Sets; when a slug exists on both surfaces, both links render comma-
separated ("ledger" + "architecture"). Slugs that don't resolve are
silently filtered out — the validator (Task 3.2) catches dangling
slugs at prebuild time.

FootLinks: 3 conditional foot-of-page links — crossPostedTo[].url
entries (currently Substack only; array shape is forward-compatible)
+ canonical source URL + canonical EXPLANATION URL. OQ-C nullable
URLs gracefully omit the line when sourceUrl/explanationUrl is null
(the placeholder ships with both as null + inline-body 4Q in the MDX
body).

Per essays-spec §10 + §16 (Substack syndication contract).
EOF
)"
```

### Task 5.4: essays `<IndexHeader />` + `<EssayRow />` + `<RelatedBlock />`

**Files:**
- Create: `src/components/essays/IndexHeader.astro`
- Create: `src/components/essays/EssayRow.astro`
- Create: `src/components/essays/RelatedBlock.astro`

Per essays-spec §2.1 + §4.1 + §5 (IndexHeader); §2.1 + §5 (EssayRow); §14 (RelatedBlock).

- [ ] **Step 1: Write `<IndexHeader />`**

Create `src/components/essays/IndexHeader.astro`:

```astro
---
/**
 * <IndexHeader /> — h1 + italic hook + sober subhead + metadata strip.
 *
 * Per essays-spec §2.1 + §4.1 + §5. Hook line locks to OPEN-1 default:
 * "I bet on meaning, not access." (Vault-Scorecard-style identity claim).
 */
interface Props {
  count: number;
  lastPublishedISO: string;
}
const { count, lastPublishedISO } = Astro.props;

const today = new Date();
const monthName = today.toLocaleDateString("en-US", { month: "long" }).toUpperCase();
const day = today.getDate();
const year = today.getFullYear();
const datelineBody = `${count} essay${count === 1 ? "" : "s"}.`;
---
<header class="index-header">
  <p class="dateline">
    <span class="dateline__city">BOSTON, {monthName} {day}, {year}</span>
    <span class="dateline__sep">&mdash;</span>
    <span class="dateline__body">{datelineBody}</span>
  </p>
  <hr class="dateline-rule" />
  <h1 class="page-title">Essays</h1>
  <p class="hook">I bet on meaning, not access.</p>
  <p class="subhead">Thesis-shaped writing where the artifacts back the claim.</p>
  <p class="metadata-strip">
    <span>{count} ESSAY{count === 1 ? "" : "S"}</span>
    <span aria-hidden="true">&middot;</span>
    <span>LAST PUBLISHED {lastPublishedISO}</span>
    <span aria-hidden="true">&middot;</span>
    <a href="/essays/rss.xml" class="rss-link">RSS &rarr;</a>
  </p>
</header>
<style>
  .index-header { max-width: 1120px; margin: 0 auto; padding: 60px 24px 0; }
  .dateline {
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 1.6px;
    color: var(--ink-secondary);
    margin: 0 0 12px;
  }
  .dateline__city { color: var(--stamp-amber); }
  .dateline__sep { margin: 0 6px; }
  .dateline-rule { border: none; border-top: 0.5px solid rgba(10, 62, 66, 0.25); margin: 0 0 40px; }
  .page-title {
    font-family: var(--font-serif);
    font-size: clamp(56px, 6vw, 96px);
    font-weight: 400;
    letter-spacing: -0.6px;
    line-height: 1.05;
    color: var(--teal);
    margin: 0 0 16px;
  }
  .hook {
    font-family: var(--font-serif);
    font-size: clamp(20px, 2.4vw, 32px);
    font-weight: 300;
    font-style: italic;
    letter-spacing: -0.2px;
    line-height: 1.3;
    color: var(--ink);
    margin: 0 0 16px;
    max-width: 680px;
  }
  .subhead {
    font-family: var(--font-serif);
    font-size: 18px;
    font-weight: 300;
    letter-spacing: -0.1px;
    line-height: 1.45;
    color: var(--ink-secondary);
    margin: 0 0 24px;
    max-width: 680px;
  }
  .metadata-strip {
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 1.6px;
    color: var(--ink-secondary);
    margin: 0 0 56px;
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    align-items: baseline;
  }
  .metadata-strip .rss-link { color: var(--teal); text-decoration: none; border-bottom: 1px solid transparent; transition: border-color 160ms var(--ease-out); }
  .metadata-strip .rss-link:hover, .metadata-strip .rss-link:focus-visible { border-bottom-color: var(--teal); outline: none; }
  @media (max-width: 767px) { .index-header { padding: 40px 16px 0; } .metadata-strip { margin-bottom: 32px; } }
</style>
```

- [ ] **Step 2: Write `<EssayRow />`**

Create `src/components/essays/EssayRow.astro`:

```astro
---
/**
 * <EssayRow /> — single row on the /essays/ index.
 *
 * Per essays-spec §2.1 + §5 + §15.
 */
interface Props {
  slug: string;
  published: string;
  status: string;
  title: string;
  excerpt: string;
}
const { slug, published, status, title, excerpt } = Astro.props;
const datelineLabel = new Date(`${published}T12:00:00Z`)
  .toLocaleDateString("en-US", { month: "short", day: "2-digit", year: "numeric" })
  .toUpperCase();
const statusClass = `status-pill status-${status.toLowerCase()}`;
const ariaLabel = `Read essay: ${title}, published ${published}`;
---
<a href={`/essays/${slug}/`} class="essay-row" aria-label={ariaLabel}>
  <span class="row-dateline" aria-hidden="true">{datelineLabel}</span>
  <span class={statusClass} aria-hidden="true">{status}</span>
  <span class="row-body">
    <span class="row-title" style={`view-transition-name: essay-title-${slug}`}>{title}</span>
    <span class="row-excerpt">{excerpt}</span>
  </span>
  <span class="row-glyph" aria-hidden="true">&rarr;</span>
</a>
<style>
  .essay-row {
    display: grid;
    grid-template-columns: 120px 96px 1fr 24px;
    column-gap: 16px;
    align-items: start;
    min-height: 150px;
    padding: 24px;
    color: var(--ink);
    text-decoration: none;
    border-bottom: 0.5px solid var(--border-paper);
    transition: background-color 200ms var(--ease-out);
  }
  .essay-row:hover { background-color: rgba(10, 62, 66, 0.04); }
  .essay-row:hover .row-title { box-shadow: inset 0 -1px 0 0 var(--teal); }
  .essay-row:hover .row-glyph { transform: translateX(4px); }
  .essay-row:focus-visible { outline: 2px solid var(--teal); outline-offset: -2px; }
  .row-dateline {
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 1.6px;
    color: var(--stamp-amber);
    line-height: 1.4;
    padding-top: 4px;
  }
  .status-pill {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 1.8px;
    text-transform: uppercase;
    color: var(--ink);
    border: 1px solid var(--ink);
    padding: 3px 8px;
    align-self: start;
    justify-self: start;
    margin-top: 2px;
  }
  .status-pill.status-published { color: var(--stamp-amber); border-color: var(--stamp-amber); }
  .status-pill.status-draft { color: var(--ink-secondary); border-color: var(--ink-secondary); }
  .row-body { display: flex; flex-direction: column; gap: 8px; }
  .row-title {
    font-family: var(--font-serif);
    font-size: 22px;
    font-weight: 400;
    letter-spacing: -0.2px;
    line-height: 1.3;
    color: var(--ink);
    transition: box-shadow 160ms var(--ease-out);
  }
  .row-excerpt {
    font-family: var(--font-serif);
    font-size: 16px;
    font-weight: 300;
    letter-spacing: -0.1px;
    line-height: 1.4;
    color: var(--ink-secondary);
    max-width: 65ch;
  }
  .row-glyph {
    font-family: var(--font-mono);
    font-size: 14px;
    color: var(--ink-secondary);
    align-self: center;
    transition: transform 200ms var(--ease-out);
  }
  @media (max-width: 767px) {
    .essay-row { grid-template-columns: 1fr; min-height: auto; padding: 20px 16px; gap: 8px; }
    .row-glyph { display: none; }
    .row-title { font-size: 20px; }
  }
</style>
```

- [ ] **Step 3: Write `<RelatedBlock />`** (essays)

Create `src/components/essays/RelatedBlock.astro`:

```astro
---
/**
 * <RelatedBlock /> — essays deep-dive Related block.
 *
 * Renders:
 *   - relatedLedgerRow → /transactions/<slug>/
 *   - relatedCaseStudy → /work/<slug>/
 *   - relatedArchitecture[] → architecture writeups
 *   - relatedEssays[] (forward + reverse via .crosslinks.json)
 *
 * Per essays-spec §14.
 */
interface ReverseShape { siblingEssays?: string[] }
interface Crosslinks { essays?: Record<string, ReverseShape> }

interface Props {
  slug: string;
  relatedLedgerRow?: string;
  relatedCaseStudy?: string;
  relatedArchitecture?: string[];
  relatedEssays?: string[];
  crosslinks: Crosslinks;
  titleByEssaySlug: Record<string, string>;
  titleByTxnSlug: Record<string, string>;
  titleByWorkSlug: Record<string, string>;
  titleByArchSlug: Record<string, string>;
}
const {
  slug,
  relatedLedgerRow,
  relatedCaseStudy,
  relatedArchitecture = [],
  relatedEssays = [],
  crosslinks,
  titleByEssaySlug,
  titleByTxnSlug,
  titleByWorkSlug,
  titleByArchSlug,
} = Astro.props;

const reverse = crosslinks.essays?.[slug] ?? {};
const siblingEssays = Array.from(new Set([
  ...relatedEssays,
  ...(reverse.siblingEssays ?? []),
]));
const hasAny = Boolean(relatedLedgerRow || relatedCaseStudy || relatedArchitecture.length > 0 || siblingEssays.length > 0);
---
{hasAny && (
  <section class="related-block" aria-labelledby={`related-${slug}`}>
    <h2 id={`related-${slug}`} class="section-heading">&mdash; RELATED &mdash;</h2>
    <ul class="related-list">
      {relatedLedgerRow && (
        <li><a href={`/transactions/${relatedLedgerRow}/`}>&rarr; ledger row: {titleByTxnSlug[relatedLedgerRow] ?? relatedLedgerRow}</a></li>
      )}
      {relatedCaseStudy && (
        <li><a href={`/work/${relatedCaseStudy}/`}>&rarr; companion case study: {titleByWorkSlug[relatedCaseStudy] ?? relatedCaseStudy}</a></li>
      )}
      {relatedArchitecture.map((s) => (
        <li><a href={`/architecture/${s}/`}>&rarr; companion architecture writeup: {titleByArchSlug[s] ?? s}</a></li>
      ))}
      {siblingEssays.map((s) => (
        <li><a href={`/essays/${s}/`}>&harr; sibling essay: {titleByEssaySlug[s] ?? s}</a></li>
      ))}
    </ul>
  </section>
)}
<style>
  .related-block { max-width: 1120px; margin: 64px auto 0; padding: 0 24px; }
  .section-heading {
    font-family: var(--font-mono);
    font-size: 14px;
    font-weight: 500;
    letter-spacing: 2.4px;
    color: var(--teal);
    opacity: 0.8;
    text-transform: uppercase;
    margin: 0 0 16px;
  }
  .related-list { list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 8px; }
  .related-list a {
    font-family: var(--font-mono);
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 0.4px;
    color: var(--teal);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 160ms var(--ease-out);
  }
  .related-list a:hover, .related-list a:focus-visible { border-bottom-color: var(--teal); outline: none; }
  @media (max-width: 767px) { .related-block { padding: 0 16px; } }
</style>
```

- [ ] **Step 4: Commit**

```bash
git add src/components/essays/IndexHeader.astro src/components/essays/EssayRow.astro src/components/essays/RelatedBlock.astro
git commit -m "$(cat <<'EOF'
feat(phase-3c.2): essays IndexHeader + EssayRow + RelatedBlock

IndexHeader: h1 "Essays" + italic hook locked to OPEN-1 default
"I bet on meaning, not access." + sober subhead + mono metadata
strip (count + LAST PUBLISHED + RSS link).

EssayRow: 150px desktop row with dateline + status pill (PUBLISHED
in stamp amber, DRAFT in secondary ink) + title (view-transition-name
essay-title-<slug>) + 1-line excerpt + glyph.

RelatedBlock: reads .crosslinks.json. Renders relatedLedgerRow,
relatedCaseStudy, relatedArchitecture[], and sibling essays
(union of forward relatedEssays[] + reverse siblingEssays[]).

Per essays-spec §2.1 + §5 + §14 + §15.
EOF
)"
```

---

## Section 6 — Routes

Six new files (4 page routes + 2 RSS endpoints). The architecture deep-dive composes the 11-band layout from §2.2; the essays deep-dive composes the 12-band layout. Both indexes are simple sorted lists. RSS feeds mirror the `/transactions/rss.xml` primitive.

### Task 6.1: `/architecture/` index + RSS endpoint

**Files:**
- Create: `src/pages/architecture/index.astro`
- Create: `src/pages/architecture/rss.xml.ts`

Per architecture-spec §2.1 + §12.

- [ ] **Step 1: Write the index page**

Create `src/pages/architecture/index.astro`:

```astro
---
/**
 * /architecture/ — index page.
 *
 * Lists every architecture writeup sorted desc by shipped ISO.
 * Bands: dateline → page title block → writeup rows → footer fold.
 *
 * Per architecture-spec §2.1 + §4.1.
 */
import BaseLayout from "~/layouts/BaseLayout.astro";
import { getCollection } from "astro:content";
import IndexHeader from "~/components/architecture/IndexHeader.astro";
import WriteupRow from "~/components/architecture/WriteupRow.astro";

const writeups = (await getCollection("architecture"))
  .sort((a, b) => b.data.shipped.localeCompare(a.data.shipped));

const count = writeups.length;
const lastUpdated = writeups[0]?.data.shipped ?? new Date().toISOString().slice(0, 10);
const mostRecentTitle = writeups[0]?.data.title;
---
<BaseLayout
  title="Architecture — Sean Winslow"
  description="Architectural arguments where the proof is the code, linked."
>
  <IndexHeader count={count} lastUpdatedISO={lastUpdated} mostRecentTitle={mostRecentTitle} />

  <section class="writeups-list" aria-label="Architecture writeups">
    {count === 0 && (
      <p class="empty-state">No architecture writeups yet. Check back ~weekly.</p>
    )}
    {writeups.map((w) => (
      <WriteupRow
        slug={w.slug}
        shipped={w.data.shipped}
        status={w.data.status}
        title={w.data.title}
        lead={w.data.lead}
      />
    ))}
  </section>

  <footer class="footer-fold">
    <p>
      <a href="/architecture/rss.xml">&rarr; subscribe via RSS</a>
      <span aria-hidden="true" class="sep">&nbsp;&nbsp;&nbsp;</span>
      <a href="/">&rarr; view the fleet <span aria-hidden="true">&uarr;</span></a>
    </p>
  </footer>
</BaseLayout>
<style>
  .writeups-list { max-width: 1120px; margin: 0 auto; padding: 0 24px; }
  .empty-state {
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--ink-secondary);
    padding: 24px 0;
    text-align: center;
  }
  .footer-fold {
    max-width: 1120px;
    margin: 80px auto 60px;
    padding: 0 24px;
  }
  .footer-fold p {
    font-family: var(--font-mono);
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 1.0px;
    color: var(--teal);
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
  }
  .footer-fold a {
    color: var(--teal);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 160ms var(--ease-out);
  }
  .footer-fold a:hover, .footer-fold a:focus-visible { border-bottom-color: var(--teal); outline: none; }
  @media (max-width: 767px) {
    .writeups-list { padding: 0 16px; }
    .footer-fold { padding: 0 16px; margin: 56px auto 40px; }
  }
</style>
```

- [ ] **Step 2: Write the RSS endpoint**

Create `src/pages/architecture/rss.xml.ts`:

```ts
/**
 * /architecture/rss.xml — RSS 2.0 feed for architecture writeups.
 *
 * Items carry the long-form essay HTML (read from
 * src/content/architecture/essays/<slug>.md when fetched, otherwise
 * the MDX body) as content:encoded. Sort matches the index.
 *
 * Per architecture-spec §12.
 */
import rss from "@astrojs/rss";
import { getCollection } from "astro:content";
import fs from "node:fs/promises";
import path from "node:path";

const ROOT = process.cwd();

async function readBody(slug: string): Promise<string | null> {
  const p = path.join(ROOT, "src/content/architecture/essays", `${slug}.md`);
  try { return await fs.readFile(p, "utf8"); } catch { return null; }
}

export async function GET(context: { site?: URL }) {
  const writeups = (await getCollection("architecture"))
    .sort((a, b) => b.data.shipped.localeCompare(a.data.shipped));

  const items = await Promise.all(
    writeups.map(async (w) => {
      let content = "";
      if (w.data.essaySourceUrl) {
        const body = await readBody(w.slug);
        if (body) content = body;
      }
      if (!content) content = w.body ?? "";
      return {
        title: w.data.title,
        link: `/architecture/${w.slug}/`,
        pubDate: new Date(`${w.data.shipped}T12:00:00Z`),
        description: w.data.lead,
        content,
        customData: `<status>${w.data.status}</status>`,
      };
    })
  );

  return rss({
    title: "Sean Winslow — Architecture",
    description: "Architectural arguments where the proof is the code, linked.",
    site: context.site ?? new URL("https://seanwinslow.com"),
    items,
    customData: `<language>en-us</language>`,
  });
}
```

- [ ] **Step 3: Smoke**

Run in parallel:
- `curl -sI http://localhost:4321/architecture/ | head -1`
- `curl -sI http://localhost:4321/architecture/rss.xml | head -1`

Expected: both return `HTTP/1.1 200 OK`. The dev server may need a restart after the new routes appear. Index shows the empty state (`No architecture writeups yet.`) — placeholder MDX lands in Task 7.1.

- [ ] **Step 4: Commit**

```bash
git add src/pages/architecture/index.astro src/pages/architecture/rss.xml.ts
git commit -m "$(cat <<'EOF'
feat(phase-3c.2): /architecture/ index + RSS endpoint

Index: sorted desc by shipped ISO. Renders empty state when zero
writeups (the placeholder MDX lands in Task 7.1). Footer fold with
RSS link + "view the fleet ↗" link.

RSS endpoint mirrors /transactions/rss.xml shape: reads the fetched
long-form body from src/content/architecture/essays/<slug>.md when
present, falls back to the MDX body. Sort matches the index.

Per architecture-spec §2.1 + §4.1 + §12.
EOF
)"
```

### Task 6.2: `/architecture/[slug].astro` — 11-band deep-dive

**Files:**
- Create: `src/pages/architecture/[slug].astro`

Per architecture-spec §2.2 + §9 + §10 + §14 + §15. Composes the 11 bands from §2.2. Imports `<MethodsStrip />` + `<FourQBlock />` from `case-study/` (BLUEPRINT §3.2).

- [ ] **Step 1: Write the deep-dive route**

Create `src/pages/architecture/[slug].astro`:

```astro
---
/**
 * /architecture/<slug>/ — deep-dive page.
 *
 * Composes the 11-band layout per architecture-spec §2.2:
 *   1. Dateline + reading-time strip
 *   2. Surface + status pills row
 *   3. Title + tags
 *   4. Thesis pullquote
 *   5. Essay body (rendered from fetched essaySourceUrl when present;
 *      otherwise MDX body via <Content />). Embedded:
 *        - ER diagram (mermaidSource)
 *        - Scoreboard (scoreboard frontmatter)
 *        - HONEST NOTE callouts (honestNotes[])
 *   6. Try It Yourself callout (optional)
 *   7. Methods strip (shared component)
 *   8. 4Q block (shared component) OR inline-body fallback
 *   9. "Read the canonical →" links (essaySource + EXPLANATION)
 *  10. Related block
 *  11. Next/Prev nav + registration mark
 *
 * Per architecture-spec §2.2.
 */
import BaseLayout from "~/layouts/BaseLayout.astro";
import { getCollection, type CollectionEntry } from "astro:content";
import fs from "node:fs/promises";
import path from "node:path";

import ThesisPullQuote from "~/components/architecture/ThesisPullQuote.astro";
import ErDiagram from "~/components/architecture/ErDiagram.astro";
import Scoreboard from "~/components/architecture/Scoreboard.astro";
import HonestNote from "~/components/architecture/HonestNote.astro";
import TryItYourself from "~/components/architecture/TryItYourself.astro";
import RelatedBlock from "~/components/architecture/RelatedBlock.astro";
import MethodsStrip from "~/components/case-study/MethodsStrip.astro";
import FourQBlock from "~/components/case-study/FourQBlock.astro";

type Arch = CollectionEntry<"architecture">;

export async function getStaticPaths() {
  const all = await getCollection("architecture");
  return all.map((entry) => ({ params: { slug: entry.slug }, props: { entry } }));
}

interface Props { entry: Arch }
const { entry } = Astro.props;
const { Content } = await entry.render();
const data = entry.data;
const slug = entry.slug;

// Adjacent writeups (desc shipped)
const all = (await getCollection("architecture")).sort((a, b) => b.data.shipped.localeCompare(a.data.shipped));
const idx = all.findIndex((t) => t.slug === entry.slug);
const next = idx >= 0 && idx + 1 < all.length ? all[idx + 1] : null;
const prev = idx > 0 ? all[idx - 1] : null;

// Cross-link graph + title lookup maps
const ROOT = process.cwd();
let crosslinks: any = {};
try {
  const raw = await fs.readFile(path.join(ROOT, "src/content/architecture/.crosslinks.json"), "utf8");
  crosslinks = JSON.parse(raw);
} catch { /* fresh checkout — render without reverse links */ }

const allArch = await getCollection("architecture");
const titleByArchSlug = Object.fromEntries(allArch.map((a) => [a.slug, a.data.title]));

const allTxn = await getCollection("transactions");
const titleByTxnSlug = Object.fromEntries(allTxn.map((t) => [t.slug, t.data.title]));

const allWork = await getCollection("work");
const titleByWorkSlug = Object.fromEntries(allWork.map((w) => [w.slug, w.data.title]));

const allEssays = await getCollection("essays");
const titleByEssaySlug = Object.fromEntries(allEssays.map((e) => [e.slug, e.data.title]));

// 4Q rendering
let canonicalMarkdown: string | undefined;
if (data.explanationUrl) {
  try {
    canonicalMarkdown = await fs.readFile(path.join(ROOT, "src/content/explanations", `${slug}.md`), "utf8");
  } catch { canonicalMarkdown = undefined; }
}
const useFourQBlock = Boolean(canonicalMarkdown);

// Long-form essay body (rendered AS the main body when essaySourceUrl is set)
let essayBodyMarkdown: string | undefined;
if (data.essaySourceUrl) {
  try {
    essayBodyMarkdown = await fs.readFile(path.join(ROOT, "src/content/architecture/essays", `${slug}.md`), "utf8");
  } catch { essayBodyMarkdown = undefined; }
}

const pageTitle = `${data.title} — Sean Winslow`;
const pageDescription = data.lead;
---
<BaseLayout title={pageTitle} description={pageDescription}>
  <article class="arch-deep-dive">
    <header class="head-band">
      <p class="dateline">
        <span class="dateline__city">{data.dateline.split(" — ")[0] ?? data.dateline}</span>
        <span class="reading-time">{data.readingTime} MIN READ</span>
      </p>
      <hr class="dateline-rule" />
      <p class="pills-row">
        <span class="pill surface-pill">ARCHITECTURE</span>
        <span class={`pill status-pill status-${data.status.toLowerCase()}`}>{data.status}</span>
      </p>
      <h1 class="page-title" style={`view-transition-name: arch-title-${slug}`}>{data.title}</h1>
      <p class="tags">
        {data.tags.map((t, i) => (
          <>
            <span>{t}</span>{i < data.tags.length - 1 && <span class="sep" aria-hidden="true"> &middot; </span>}
          </>
        ))}
      </p>
    </header>

    <ThesisPullQuote text={data.lead} />

    <section class="essay-body">
      {essayBodyMarkdown ? (
        <article set:html={essayBodyMarkdown} />
      ) : (
        <Content />
      )}
    </section>

    {data.mermaidSource && (
      <ErDiagram source={data.mermaidSource} caption={data.mermaidCaption} />
    )}

    {data.scoreboard && (
      <Scoreboard data={data.scoreboard} />
    )}

    {Array.isArray(data.honestNotes) && data.honestNotes.map((note) => (
      <HonestNote anchor={note.anchor} body={note.body} />
    ))}

    {data.tryItYourselfUrl && data.tryItYourselfCommand && (
      <TryItYourself url={data.tryItYourselfUrl} command={data.tryItYourselfCommand} />
    )}

    <MethodsStrip methods={data.methods} />

    {useFourQBlock ? (
      <FourQBlock frame="ARCH" slug={slug} canonicalMarkdown={canonicalMarkdown} explanationUrl={data.explanationUrl ?? undefined} />
    ) : (
      <section class="inline-body-4q" aria-labelledby={`4q-${slug}`}>
        <h2 id={`4q-${slug}`} class="section-heading">&mdash; EXPLANATION &mdash;</h2>
        <div class="prose">
          {/* The MDX body's 4 canonical h2 headings render here when no
              essaySourceUrl was set — the OQ-C inline-body fallback path.
              When essaySourceUrl IS set, the body above already rendered
              the long-form essay, so the inline-body 4Q duplicates only
              when both are absent. */}
          {!essayBodyMarkdown && <Content />}
        </div>
      </section>
    )}

    <section class="canonical-links">
      {data.explanationUrl && (
        <p><a href={data.explanationUrl} rel="external">&rarr; read the canonical EXPLANATION.md on github <span class="info-glyph" aria-hidden="true">&#9432;</span></a></p>
      )}
      {data.essaySourceUrl && (
        <p><a href={data.essaySourceUrl} rel="external">&rarr; read the long-form source on github <span class="info-glyph" aria-hidden="true">&#9432;</span></a></p>
      )}
    </section>

    <RelatedBlock
      slug={slug}
      relatedLedgerRow={data.relatedLedgerRow}
      relatedCaseStudy={data.relatedCaseStudy}
      relatedEssay={data.relatedEssay}
      relatedArchitecture={data.relatedArchitecture}
      crosslinks={crosslinks}
      titleByArchSlug={titleByArchSlug}
      titleByTxnSlug={titleByTxnSlug}
      titleByWorkSlug={titleByWorkSlug}
      titleByEssaySlug={titleByEssaySlug}
    />

    <nav class="next-prev" aria-label="Adjacent architecture writeups">
      {next ? (
        <a href={`/architecture/${next.slug}/`} class="np-link np-prev">
          &larr; {next.data.shipped.replace(/-/g, ".")} {next.data.title}
        </a>
      ) : <span></span>}
      {prev ? (
        <a href={`/architecture/${prev.slug}/`} class="np-link np-next">
          {prev.data.shipped.replace(/-/g, ".")} {prev.data.title} &rarr;
        </a>
      ) : <span></span>}
    </nav>

    <span class="registration-mark" aria-hidden="true">&#9680;</span>
  </article>
</BaseLayout>
<style>
  .arch-deep-dive { position: relative; padding-bottom: 80px; }
  .head-band { max-width: 1120px; margin: 0 auto; padding: 60px 24px 0; }
  .dateline { font-family: var(--font-mono); font-size: 12px; font-weight: 500; letter-spacing: 1.6px; color: var(--ink-secondary); margin: 0 0 12px; display: flex; justify-content: space-between; }
  .dateline__city { color: var(--stamp-amber); }
  .reading-time { color: var(--ink-secondary); }
  .dateline-rule { border: none; border-top: 0.5px solid rgba(10, 62, 66, 0.25); margin: 0 0 24px; }
  .pills-row { display: flex; gap: 12px; margin: 0 0 24px; }
  .pill { font-family: var(--font-mono); font-size: 11px; font-weight: 500; letter-spacing: 1.8px; text-transform: uppercase; padding: 3px 8px; border: 1px solid currentColor; }
  .surface-pill { color: var(--ink); }
  .status-pill.status-shipped { color: var(--stamp-amber); }
  .status-pill.status-draft { color: var(--ink-secondary); }
  .page-title { font-family: var(--font-serif); font-size: clamp(40px, 5vw, 72px); font-weight: 400; letter-spacing: -0.4px; line-height: 1.05; color: var(--teal); margin: 0 0 16px; max-width: 880px; }
  .tags { font-family: var(--font-mono); font-size: 12px; font-weight: 500; letter-spacing: 1.6px; color: var(--ink-secondary); margin: 0 0 24px; }
  .tags .sep { color: var(--ink-secondary); }

  .essay-body { max-width: 680px; margin: 32px auto; padding: 0 24px; }
  .essay-body :global(article), .essay-body :global(p) {
    font-family: var(--font-serif);
    font-size: 20px;
    font-weight: 300;
    letter-spacing: -0.1px;
    line-height: 1.6;
    color: var(--ink);
  }
  .essay-body :global(h2) {
    font-family: var(--font-serif);
    font-size: clamp(28px, 3.2vw, 40px);
    font-weight: 500;
    letter-spacing: -0.4px;
    color: var(--teal);
    margin: 48px 0 16px;
  }
  .essay-body :global(p) { margin: 0 0 16px; }

  .inline-body-4q { max-width: 1120px; margin: 80px auto 0; padding: 0 24px; }
  .section-heading {
    font-family: var(--font-mono);
    font-size: 14px;
    font-weight: 500;
    letter-spacing: 2.4px;
    color: var(--teal);
    opacity: 0.8;
    text-transform: uppercase;
    margin: 0 0 24px;
  }
  .prose { max-width: 680px; }
  .prose :global(h2) {
    font-family: var(--font-mono);
    font-size: 14px;
    font-weight: 500;
    letter-spacing: 2.4px;
    color: var(--teal);
    text-transform: uppercase;
    margin: 48px 0 12px;
  }
  .prose :global(p) {
    font-family: var(--font-serif);
    font-size: 18px;
    font-weight: 300;
    line-height: 1.55;
    color: var(--ink);
    margin: 0 0 16px;
  }

  .canonical-links { max-width: 1120px; margin: 32px auto 0; padding: 0 24px; }
  .canonical-links p { margin: 0 0 8px; font-family: var(--font-mono); font-size: 13px; font-weight: 500; letter-spacing: 1.0px; color: var(--teal); }
  .canonical-links a { color: var(--teal); text-decoration: none; border-bottom: 1px solid transparent; transition: border-color 160ms var(--ease-out); }
  .canonical-links a:hover, .canonical-links a:focus-visible { border-bottom-color: var(--teal); outline: none; }
  .canonical-links .info-glyph { margin-left: 6px; color: var(--ink-secondary); font-size: 12px; }

  .next-prev { max-width: 1120px; margin: 64px auto 0; padding: 0 24px; display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
  .np-link { font-family: var(--font-mono); font-size: 13px; font-weight: 500; letter-spacing: 1.0px; color: var(--teal); text-decoration: none; border-bottom: 1px solid transparent; transition: border-color 160ms var(--ease-out); }
  .np-next { text-align: right; }
  .np-link:hover, .np-link:focus-visible { border-bottom-color: var(--teal); outline: none; }
  .registration-mark { position: absolute; right: 24px; bottom: 16px; font-size: 18px; color: var(--stamp-amber); opacity: 0.6; }

  @media (max-width: 767px) {
    .head-band { padding: 40px 16px 0; }
    .essay-body { padding: 0 16px; }
    .inline-body-4q { padding: 0 16px; }
    .canonical-links { padding: 0 16px; }
    .next-prev { padding: 0 16px; grid-template-columns: 1fr; gap: 8px; }
    .np-next { text-align: left; }
    .registration-mark { right: 16px; }
  }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add src/pages/architecture/\[slug\].astro
git commit -m "$(cat <<'EOF'
feat(phase-3c.2): /architecture/[slug] — 11-band deep-dive route

Composes the 11 bands from architecture-spec §2.2:
  1. Dateline + reading-time strip (top-right)
  2. ARCHITECTURE + status pills row
  3. Title (view-transition-name arch-title-<slug>) + tags
  4. Thesis pullquote (Newsreader 24px italic, shared component)
  5. Essay body (fetched long-form when essaySourceUrl set;
     otherwise the MDX body via <Content />)
  6. ER diagram (when mermaidSource set)
  7. Scoreboard (when scoreboard frontmatter set)
  8. HONEST NOTE callouts (zero to three; honestNotes[])
  9. Try It Yourself callout (when tryItYourselfUrl + Command set)
 10. Methods strip (imported from case-study, prop-shape unchanged)
 11. 4Q block (canonical via shared FourQBlock when explanationUrl
     fetched; inline-body fallback via <Content /> otherwise)
 12. Two "Read the canonical →" links
 13. Related block (with reverse-derived namedInEssays[] from
     .crosslinks.json)
 14. Next/Prev nav (desc shipped)
 15. Registration mark (◐)

OQ-C handling: when explanationUrl is null AND essaySourceUrl is
null, the page renders the MDX body inline (the OQ-C fallback path).
When the upstream sources ship, swap the placeholder MDX's nulls to
real URLs + the next build's fetch_canonical_sources writes both
files; the renderer transitions to canonical mode automatically.

Per architecture-spec §2.2 + §9 + §10 + §14 + §15 +
BLUEPRINT §3.2 (shared component imports).
EOF
)"
```

### Task 6.3: `/essays/` index + RSS endpoint

**Files:**
- Create: `src/pages/essays/index.astro`
- Create: `src/pages/essays/rss.xml.ts`

Per essays-spec §2.1 + §12.

- [ ] **Step 1: Write the index page**

Create `src/pages/essays/index.astro`:

```astro
---
/**
 * /essays/ — index page.
 *
 * Sorted desc by published ISO. Bands: dateline → title block →
 * essay rows → footer fold (RSS / Substack / fleet).
 *
 * Per essays-spec §2.1 + §4.1.
 */
import BaseLayout from "~/layouts/BaseLayout.astro";
import { getCollection } from "astro:content";
import IndexHeader from "~/components/essays/IndexHeader.astro";
import EssayRow from "~/components/essays/EssayRow.astro";

const essays = (await getCollection("essays"))
  .sort((a, b) => b.data.published.localeCompare(a.data.published));

const count = essays.length;
const lastPublished = essays[0]?.data.published ?? new Date().toISOString().slice(0, 10);

// Find a Substack URL across all essays for the footer link.
let substackUrl: string | null = null;
for (const e of essays) {
  const cp = (e.data.crossPostedTo ?? []).find((c) => c.name === "Substack");
  if (cp) { substackUrl = cp.url; break; }
}
---
<BaseLayout
  title="Essays — Sean Winslow"
  description="Thesis-shaped writing where the artifacts back the claim."
>
  <IndexHeader count={count} lastPublishedISO={lastPublished} />

  <section class="essays-list" aria-label="Essay rows">
    {count === 0 && (
      <p class="empty-state">No essays yet. Check back ~quarterly.</p>
    )}
    {essays.map((e) => (
      <EssayRow
        slug={e.slug}
        published={e.data.published}
        status={e.data.status}
        title={e.data.title}
        excerpt={e.data.excerpt}
      />
    ))}
  </section>

  <footer class="footer-fold">
    <p>
      <a href="/essays/rss.xml">&rarr; subscribe via RSS</a>
      {substackUrl && (
        <>
          <span aria-hidden="true" class="sep">&nbsp;&nbsp;&nbsp;</span>
          <a href={substackUrl} rel="external">&rarr; read on Substack</a>
        </>
      )}
      <span aria-hidden="true" class="sep">&nbsp;&nbsp;&nbsp;</span>
      <a href="/">&rarr; view the fleet <span aria-hidden="true">&uarr;</span></a>
    </p>
  </footer>
</BaseLayout>
<style>
  .essays-list { max-width: 1120px; margin: 0 auto; padding: 0 24px; }
  .empty-state { font-family: var(--font-mono); font-size: 13px; color: var(--ink-secondary); padding: 24px 0; text-align: center; }
  .footer-fold { max-width: 1120px; margin: 80px auto 60px; padding: 0 24px; }
  .footer-fold p { font-family: var(--font-mono); font-size: 13px; font-weight: 500; letter-spacing: 1.0px; color: var(--teal); margin: 0; display: flex; flex-wrap: wrap; align-items: baseline; }
  .footer-fold a { color: var(--teal); text-decoration: none; border-bottom: 1px solid transparent; transition: border-color 160ms var(--ease-out); }
  .footer-fold a:hover, .footer-fold a:focus-visible { border-bottom-color: var(--teal); outline: none; }
  @media (max-width: 767px) { .essays-list { padding: 0 16px; } .footer-fold { padding: 0 16px; margin: 56px auto 40px; } }
</style>
```

- [ ] **Step 2: Write the RSS endpoint**

Create `src/pages/essays/rss.xml.ts`:

```ts
/**
 * /essays/rss.xml — RSS 2.0 feed for essays.
 *
 * content:encoded carries the full essay body (fetched sourceUrl when
 * present; otherwise the MDX body). Sort matches the index.
 *
 * Per essays-spec §12.
 */
import rss from "@astrojs/rss";
import { getCollection } from "astro:content";
import fs from "node:fs/promises";
import path from "node:path";

const ROOT = process.cwd();

async function readBody(slug: string): Promise<string | null> {
  const p = path.join(ROOT, "src/content/essays/essay-bodies", `${slug}.md`);
  try { return await fs.readFile(p, "utf8"); } catch { return null; }
}

export async function GET(context: { site?: URL }) {
  const essays = (await getCollection("essays"))
    .sort((a, b) => b.data.published.localeCompare(a.data.published));

  const items = await Promise.all(
    essays.map(async (e) => {
      let content = "";
      if (e.data.sourceUrl) {
        const body = await readBody(e.slug);
        if (body) content = body;
      }
      if (!content) content = e.body ?? "";
      return {
        title: e.data.title,
        link: `/essays/${e.slug}/`,
        pubDate: new Date(`${e.data.published}T12:00:00Z`),
        description: e.data.excerpt,
        content,
        customData: `<status>${e.data.status}</status>`,
      };
    })
  );

  return rss({
    title: "Sean Winslow — Essays",
    description: "Thesis-shaped writing where the artifacts back the claim.",
    site: context.site ?? new URL("https://seanwinslow.com"),
    items,
    customData: `<language>en-us</language>`,
  });
}
```

- [ ] **Step 3: Smoke**

Run in parallel:
- `curl -sI http://localhost:4321/essays/ | head -1`
- `curl -sI http://localhost:4321/essays/rss.xml | head -1`

Expected: both return `HTTP/1.1 200 OK`. Index shows the empty state (`No essays yet.`).

- [ ] **Step 4: Commit**

```bash
git add src/pages/essays/index.astro src/pages/essays/rss.xml.ts
git commit -m "$(cat <<'EOF'
feat(phase-3c.2): /essays/ index + RSS endpoint

Index: sorted desc by published ISO. Renders empty state when zero
essays (the manifesto placeholder lands in Task 7.2). Footer fold
carries 3 links: RSS + (when a Substack URL exists in any essay's
crossPostedTo[]) Read on Substack + view the fleet.

RSS endpoint mirrors /transactions/rss.xml + /architecture/rss.xml
shapes. content:encoded uses fetched body from src/content/essays/
essay-bodies/<slug>.md when present, otherwise the MDX body.

Per essays-spec §2.1 + §4.1 + §12 + §16.
EOF
)"
```

### Task 6.4: `/essays/[slug].astro` — 12-band deep-dive

**Files:**
- Create: `src/pages/essays/[slug].astro`

Per essays-spec §2.2 + §7 + §8 + §9 + §10 + §14 + §15. Composes the 12 bands. Imports `<ThesisPullQuote />` from `architecture/` (shared per BLUEPRINT §3.2) and `<FourQBlock />` from `case-study/`. Does NOT render `<MethodsStrip />` (essays-spec §3.1 explicitly excludes methods).

- [ ] **Step 1: Write the deep-dive route**

Create `src/pages/essays/[slug].astro`:

```astro
---
/**
 * /essays/<slug>/ — deep-dive page.
 *
 * Composes the 12-band layout per essays-spec §2.2:
 *   1. Dateline + reading-time strip
 *   2. ESSAYS + status pills row
 *   3. Title + subtitle + tags
 *   4. Published/Revised strip (conditional)
 *   5. Thesis pullquote
 *   6. Essay body (5 sections per §7 voice contract; rendered from
 *      fetched sourceUrl when present, otherwise MDX <Content />).
 *      Embedded: quadrantChart + legend + role-map table inline.
 *   7. Plotted artifacts (auto-rendered)
 *   8. 4Q block (canonical or inline-body fallback)
 *   9. Three foot-of-page links (Substack + canonical source +
 *      canonical EXPLANATION)
 *  10. Related block
 *  11. Next/Prev nav + registration mark
 *
 * Per essays-spec §2.2.
 */
import BaseLayout from "~/layouts/BaseLayout.astro";
import { getCollection, type CollectionEntry } from "astro:content";
import fs from "node:fs/promises";
import path from "node:path";

import ThesisPullQuote from "~/components/architecture/ThesisPullQuote.astro";
import QuadrantChart from "~/components/essays/QuadrantChart.astro";
import QuadrantLegend from "~/components/essays/QuadrantLegend.astro";
import RoleMap from "~/components/essays/RoleMap.astro";
import PublishedRevisedStrip from "~/components/essays/PublishedRevisedStrip.astro";
import PlottedArtifacts from "~/components/essays/PlottedArtifacts.astro";
import FootLinks from "~/components/essays/FootLinks.astro";
import RelatedBlock from "~/components/essays/RelatedBlock.astro";
import FourQBlock from "~/components/case-study/FourQBlock.astro";

type Essay = CollectionEntry<"essays">;

export async function getStaticPaths() {
  const all = await getCollection("essays");
  return all.map((entry) => ({ params: { slug: entry.slug }, props: { entry } }));
}

interface Props { entry: Essay }
const { entry } = Astro.props;
const { Content } = await entry.render();
const data = entry.data;
const slug = entry.slug;

// Adjacent essays (desc published)
const all = (await getCollection("essays")).sort((a, b) => b.data.published.localeCompare(a.data.published));
const idx = all.findIndex((t) => t.slug === entry.slug);
const next = idx >= 0 && idx + 1 < all.length ? all[idx + 1] : null;
const prev = idx > 0 ? all[idx - 1] : null;

// Cross-link graph + title lookups
const ROOT = process.cwd();
let crosslinks: any = {};
try {
  const raw = await fs.readFile(path.join(ROOT, "src/content/essays/.crosslinks.json"), "utf8");
  crosslinks = JSON.parse(raw);
} catch { /* fresh checkout */ }

const allEssays = await getCollection("essays");
const titleByEssaySlug = Object.fromEntries(allEssays.map((e) => [e.slug, e.data.title]));
const allTxn = await getCollection("transactions");
const titleByTxnSlug = Object.fromEntries(allTxn.map((t) => [t.slug, t.data.title]));
const allWork = await getCollection("work");
const titleByWorkSlug = Object.fromEntries(allWork.map((w) => [w.slug, w.data.title]));
const allArch = await getCollection("architecture");
const titleByArchSlug = Object.fromEntries(allArch.map((a) => [a.slug, a.data.title]));

const txnSlugs = new Set(allTxn.map((t) => t.slug));
const archSlugs = new Set(allArch.map((a) => a.slug));

// 4Q + body rendering
let canonicalMarkdown: string | undefined;
if (data.explanationUrl) {
  try { canonicalMarkdown = await fs.readFile(path.join(ROOT, "src/content/explanations", `${slug}.md`), "utf8"); } catch {}
}
const useFourQBlock = Boolean(canonicalMarkdown);

let essayBodyMarkdown: string | undefined;
if (data.sourceUrl) {
  try { essayBodyMarkdown = await fs.readFile(path.join(ROOT, "src/content/essays/essay-bodies", `${slug}.md`), "utf8"); } catch {}
}

const pageTitle = `${data.title} — Sean Winslow`;
const pageDescription = data.excerpt;
---
<BaseLayout title={pageTitle} description={pageDescription}>
  <article class="essay-deep-dive">
    <header class="head-band">
      <p class="dateline">
        <span class="dateline__city">{data.dateline.split(" — ")[0] ?? data.dateline}</span>
        <span class="reading-time">{data.readingTime} MIN READ</span>
      </p>
      <hr class="dateline-rule" />
      <p class="pills-row">
        <span class="pill surface-pill">ESSAYS</span>
        <span class={`pill status-pill status-${data.status.toLowerCase()}`}>{data.status}</span>
      </p>
      <h1 class="page-title" style={`view-transition-name: essay-title-${slug}`}>{data.title}</h1>
      {data.subtitle && <p class="subtitle">{data.subtitle}</p>}
      <p class="tags">
        {data.tags.map((t, i) => (
          <>
            <span>{t}</span>{i < data.tags.length - 1 && <span class="sep" aria-hidden="true"> &middot; </span>}
          </>
        ))}
      </p>
      <PublishedRevisedStrip published={data.published} lastRevised={data.lastRevised} />
    </header>

    <ThesisPullQuote text={data.excerpt} />

    <section class="essay-body">
      {essayBodyMarkdown ? (
        <article set:html={essayBodyMarkdown} />
      ) : (
        <Content />
      )}
    </section>

    {data.mermaidSource && (
      <QuadrantChart source={data.mermaidSource} caption={data.mermaidCaption} />
    )}

    {Array.isArray(data.quadrantLegend) && data.quadrantLegend.length > 0 && (
      <QuadrantLegend entries={data.quadrantLegend} txnSlugs={txnSlugs} archSlugs={archSlugs} />
    )}

    {data.roleMap && (
      <RoleMap lastValidated={data.roleMap.lastValidated} rows={data.roleMap.rows} />
    )}

    <PlottedArtifacts
      slugs={data.plottedArtifacts ?? []}
      txnSlugs={txnSlugs}
      archSlugs={archSlugs}
      titleByTxnSlug={titleByTxnSlug}
      titleByArchSlug={titleByArchSlug}
    />

    {useFourQBlock ? (
      <FourQBlock frame="ESSAY" slug={slug} canonicalMarkdown={canonicalMarkdown} explanationUrl={data.explanationUrl ?? undefined} />
    ) : (
      <section class="inline-body-4q" aria-labelledby={`4q-${slug}`}>
        <h2 id={`4q-${slug}`} class="section-heading">&mdash; EXPLANATION &mdash;</h2>
        <div class="prose">
          {!essayBodyMarkdown && <Content />}
        </div>
      </section>
    )}

    <FootLinks
      crossPostedTo={data.crossPostedTo}
      sourceUrl={data.sourceUrl}
      explanationUrl={data.explanationUrl}
    />

    <RelatedBlock
      slug={slug}
      relatedLedgerRow={data.relatedLedgerRow}
      relatedCaseStudy={data.relatedCaseStudy}
      relatedArchitecture={data.relatedArchitecture}
      relatedEssays={data.relatedEssays}
      crosslinks={crosslinks}
      titleByEssaySlug={titleByEssaySlug}
      titleByTxnSlug={titleByTxnSlug}
      titleByWorkSlug={titleByWorkSlug}
      titleByArchSlug={titleByArchSlug}
    />

    <nav class="next-prev" aria-label="Adjacent essays">
      {next ? (
        <a href={`/essays/${next.slug}/`} class="np-link np-prev">
          &larr; {next.data.published.replace(/-/g, ".")} {next.data.title}
        </a>
      ) : <span></span>}
      {prev ? (
        <a href={`/essays/${prev.slug}/`} class="np-link np-next">
          {prev.data.published.replace(/-/g, ".")} {prev.data.title} &rarr;
        </a>
      ) : <span></span>}
    </nav>

    <span class="registration-mark" aria-hidden="true">&#9680;</span>
  </article>
</BaseLayout>
<style>
  .essay-deep-dive { position: relative; padding-bottom: 80px; }
  .head-band { max-width: 1120px; margin: 0 auto; padding: 60px 24px 0; }
  .dateline { font-family: var(--font-mono); font-size: 12px; font-weight: 500; letter-spacing: 1.6px; color: var(--ink-secondary); margin: 0 0 12px; display: flex; justify-content: space-between; }
  .dateline__city { color: var(--stamp-amber); }
  .reading-time { color: var(--ink-secondary); }
  .dateline-rule { border: none; border-top: 0.5px solid rgba(10, 62, 66, 0.25); margin: 0 0 24px; }
  .pills-row { display: flex; gap: 12px; margin: 0 0 16px; }
  .pill { font-family: var(--font-mono); font-size: 11px; font-weight: 500; letter-spacing: 1.8px; text-transform: uppercase; padding: 3px 8px; border: 1px solid currentColor; }
  .surface-pill { color: var(--ink); }
  .status-pill.status-published { color: var(--stamp-amber); }
  .status-pill.status-draft { color: var(--ink-secondary); }
  .page-title { font-family: var(--font-serif); font-size: clamp(40px, 5vw, 72px); font-weight: 400; letter-spacing: -0.4px; line-height: 1.05; color: var(--teal); margin: 0 0 12px; max-width: 880px; }
  .subtitle { font-family: var(--font-serif); font-size: clamp(20px, 2.2vw, 28px); font-weight: 300; font-style: italic; letter-spacing: -0.2px; color: var(--ink-secondary); margin: 0 0 16px; max-width: 720px; }
  .tags { font-family: var(--font-mono); font-size: 12px; font-weight: 500; letter-spacing: 1.6px; color: var(--ink-secondary); margin: 0 0 12px; }
  .tags .sep { color: var(--ink-secondary); }

  .essay-body { max-width: 680px; margin: 32px auto; padding: 0 24px; }
  .essay-body :global(article), .essay-body :global(p) {
    font-family: var(--font-serif);
    font-size: 20px;
    font-weight: 300;
    letter-spacing: -0.1px;
    line-height: 1.6;
    color: var(--ink);
  }
  .essay-body :global(h2) {
    font-family: var(--font-serif);
    font-size: clamp(28px, 3.2vw, 40px);
    font-weight: 500;
    letter-spacing: -0.4px;
    color: var(--teal);
    margin: 48px 0 16px;
  }
  .essay-body :global(p) { margin: 0 0 16px; }

  .inline-body-4q { max-width: 1120px; margin: 80px auto 0; padding: 0 24px; }
  .section-heading {
    font-family: var(--font-mono);
    font-size: 14px;
    font-weight: 500;
    letter-spacing: 2.4px;
    color: var(--teal);
    opacity: 0.8;
    text-transform: uppercase;
    margin: 0 0 24px;
  }
  .prose { max-width: 680px; }
  .prose :global(h2) {
    font-family: var(--font-mono);
    font-size: 14px;
    font-weight: 500;
    letter-spacing: 2.4px;
    color: var(--teal);
    text-transform: uppercase;
    margin: 48px 0 12px;
  }
  .prose :global(p) {
    font-family: var(--font-serif);
    font-size: 18px;
    font-weight: 300;
    line-height: 1.55;
    color: var(--ink);
    margin: 0 0 16px;
  }

  .next-prev { max-width: 1120px; margin: 64px auto 0; padding: 0 24px; display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
  .np-link { font-family: var(--font-mono); font-size: 13px; font-weight: 500; letter-spacing: 1.0px; color: var(--teal); text-decoration: none; border-bottom: 1px solid transparent; transition: border-color 160ms var(--ease-out); }
  .np-next { text-align: right; }
  .np-link:hover, .np-link:focus-visible { border-bottom-color: var(--teal); outline: none; }
  .registration-mark { position: absolute; right: 24px; bottom: 16px; font-size: 18px; color: var(--stamp-amber); opacity: 0.6; }

  @media (max-width: 767px) {
    .head-band { padding: 40px 16px 0; }
    .essay-body { padding: 0 16px; }
    .inline-body-4q { padding: 0 16px; }
    .next-prev { padding: 0 16px; grid-template-columns: 1fr; gap: 8px; }
    .np-next { text-align: left; }
    .registration-mark { right: 16px; }
  }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add src/pages/essays/\[slug\].astro
git commit -m "$(cat <<'EOF'
feat(phase-3c.2): /essays/[slug] — 12-band deep-dive route

Composes the 12 bands from essays-spec §2.2:
  1. Dateline + reading-time strip
  2. ESSAYS + status pills row
  3. Title (view-transition-name essay-title-<slug>) + subtitle + tags
  4. Published/Revised strip (conditional render via component)
  5. Thesis pullquote (imported from architecture/ — shared primitive)
  6. Essay body (fetched sourceUrl OR <Content />); embedded
     quadrantChart + legend + role-map table inline
  7. Plotted artifacts auto-rendered from frontmatter slugs
  8. 4Q block (canonical via FourQBlock when fetched; inline-body
     fallback via <Content /> otherwise — same OQ-C pattern as
     architecture)
  9. FootLinks (Substack via crossPostedTo[] + canonical source +
     canonical EXPLANATION — each conditional on its URL being set)
 10. Related block (with reverse-derived siblingEssays from
     .crosslinks.json)
 11. Next/Prev nav (desc published)
 12. Registration mark

NO MethodsStrip — essays-spec §3.1 explicitly excludes methods from
the schema. Essays cite artifacts via plottedArtifacts, not methods.

Per essays-spec §2.2 + §7 (voice contract) + §8 + §9 + §10 + §14 + §15.
EOF
)"
```

---

## Section 7 — Hand-written placeholder content

Two placeholder MDXs exercise both surfaces end-to-end:

| Slug | Collection | Status | 4Q path | Cross-links exercised |
|---|---|---|---|---|
| `vault-knowledge-mcp` | architecture | SHIPPED | inline-body 4Q (OQ-C fallback) | `relatedLedgerRow: vault-knowledge-mcp` — closes the forward-declared link from Phase 3c.1's ledger row |
| `meaning-over-access` | essays | PUBLISHED | inline-body 4Q (OQ-C fallback) | `plottedArtifacts: [intent-engineering-mcp, vault-knowledge-mcp, phase-d-typed-edges]` — exercises the reverse "← named in" derivation onto all 3 existing ledger rows + the new architecture page |

**MDX gotcha reminder** (from Phase 3c.1 commit 07a8cf1): every `<ComponentName />` in placeholder *prose* MUST be wrapped in backticks. MDX parses bare `<UppercaseTag />` as a JSX component reference and fails the build. The Q2-Q4 body text below uses backticks throughout for safety. Apply the same rule to any `[PLACEHOLDER —` text Sean rewrites later.

### Task 7.1: `vault-knowledge-mcp.mdx` (architecture · SHIPPED · forward-declared link)

**Files:**
- Create: `src/content/architecture/vault-knowledge-mcp.mdx`
- Create: `src/content/architecture/diagrams/concept-edges-erd.mmd`

Slug is byte-identical to the existing `src/content/transactions/vault-knowledge-mcp.mdx` (Phase 3c.1) — the cross-link graph closes bidirectionally on first build. Architecture-spec §1 names Vault Scorecard as the first occupant; we ship under the slug `vault-knowledge-mcp` to match the Phase 3c.1 forward declaration and the architecture-spec Appendix B example (which uses Vault Scorecard frontmatter at the `vault-knowledge-mcp` slug throughout §14).

- [ ] **Step 1: Write the Mermaid source**

Create `src/content/architecture/diagrams/concept-edges-erd.mmd`:

```
erDiagram
    CONCEPT ||--o{ EDGE : "source of"
    CONCEPT ||--o{ EDGE : "target of"
    EDGE }o--|| RELATION_TYPE : "typed by"
    EDGE {
        int id PK
        int source_concept_id FK
        int target_concept_id FK
        string relation_type
        timestamp created_at
    }
    CONCEPT {
        int id PK
        string name
        string vault_path
        timestamp last_synthesized
    }
    RELATION_TYPE {
        string name PK
        string description
    }
```

- [ ] **Step 2: Write the architecture MDX**

Create `src/content/architecture/vault-knowledge-mcp.mdx`:

```mdx
---
title: "Vault as Agent Infrastructure"
dateline: "BOSTON, JUNE 3, 2026"
shipped: "2026-06-03"
readingTime: 8

status: SHIPPED
tags:
  - agent-infrastructure
  - sqlite
  - obsidian
  - linear

lead: "Three passes, two failures, and the failures are where vault-knowledge-mcp and the Judge Layer are going next."

methods:
  - task: persistent state
    tool: SQLite + git + JSONL
    cost: local / $0
    link: null
  - task: typed edges
    tool: Code Brain (concept_edges)
    cost: Sonnet 4.6 (HybridRouter)
    link: /work/code-brain
  - task: er diagram gen
    tool: scripts/generate_schema.py
    cost: stdlib / $0
    link: null
  - task: fixture author
    tool: Sean + LLM seed
    cost: ~$0.10
    link: null

# OQ-C inline-body fallback: both URLs null until Sean ships the
# upstream VAULT_AS_AGENT_INFRASTRUCTURE.md + _EXPLANATION.md
# sources to the code-brain repo. The validator (Task 3.2) accepts
# this row because the MDX body below carries the 4 canonical h2
# headings (inline-body 4Q detection).
essaySourceUrl: null
explanationUrl: null

mermaidSource: diagrams/concept-edges-erd.mmd
mermaidCaption: "concept_edges schema — 6 relation types, validated against 47 production edges."

scoreboard:
  columns: ["STATE", "VERBS", "OWN", "PERM", "AUDIT"]
  rows:
    - label: "Notion"
      scores: ["✗", "✗", "✓", "✓", "⚠"]
    - label: "Obsidian (default)"
      scores: ["⚠", "⚠", "✗", "✗", "✗"]
    - label: "Linear"
      scores: ["✓", "✓", "✓✓", "✓✓", "✓"]
    - label: "Sean's vault"
      scores: ["✓✓", "✓✓", "⚠", "⚠", "✓✓"]

honestNotes:
  - anchor: ownership
    body: "Linear is genuinely better here. That gap is exactly why vault-knowledge-mcp matters — it's the first step toward giving the vault Linear-grade ownership semantics on the axes that matter for agent control."
  - anchor: permissions
    body: "Filesystem permissions only. POSIX read/write + git remotes. Linear's RBAC is the upgrade path."

relatedLedgerRow: vault-knowledge-mcp
relatedCaseStudy: code-brain
relatedEssay: null
relatedArchitecture: []

sourceRepoUrl: https://github.com/seanwinslow28/code-brain
tryItYourselfUrl: https://github.com/seanwinslow28/code-brain/tree/main/examples/public_vault_fixture
tryItYourselfCommand: |
  git clone https://github.com/seanwinslow28/code-brain
  cp -r code-brain/examples/public_vault_fixture /tmp/test_vault
  AGENT_VAULT_PATH=/tmp/test_vault python3 ./agents-sdk/scripts/query.py \
    "what brewing methods compete with espresso?"
---

## What is this?

A 5-test scoreboard that scores knowledge systems on the axes that matter when an agent has to act on the vault, not just read it: Persistent State, Defined Verbs, Ownership, Permissions, and a Queryable Audit History. Notion, Obsidian (default), Linear, and Sean's vault each get a row. Three passes, two failures — and the two failures are the architectural argument for what ships next.

## Why this approach?

[PLACEHOLDER — Q2] Most "PM tools" discourse is about how nice the editing surface feels. That's a content question. The agent question is structural: does the system have *typed edges* the agent can query without scanning, does the agent have *defined verbs* it can call without a free-form LLM bridge, does it have an *audit history* a reviewer can replay? Vault Scorecard is the diagnostic that turns the structural question into a single visible result. Comparison is the discipline — equality of presentation across all four systems is the honesty signal. The reader reaches the conclusion from the symbols, not from a `Sean's-vault-highlighted-row` accent.

## What would break?

[PLACEHOLDER — Q3] Three named failure modes: (1) the synthetic public vault fixture drifts from the production schema as `concept_edges` evolves — mitigation is the `scripts/generate_schema.py` build step that re-emits the ER diagram from the live SQLite file on every synthesis run; (2) the Notion/Obsidian/Linear rows go stale as those products ship features (Linear's recent RBAC additions push its OWN/PERM scores higher; the next Notion AI release may flip its STATE score) — mitigation is a quarterly rescore commit; (3) recruiters who arrive expecting a `Notion-killer` pitch bounce because the scoreboard is honest about Linear-better axes — that's the intended cost. The page filters for the audience that values calibration over salesmanship.

## What did I learn?

[PLACEHOLDER — Q4] Equality of presentation IS the credibility signal. The temptation was to highlight Sean's-vault row, or wash the passing cells in success green, or stack the columns in Sean's order. Resisting that produced a scoreboard a Linear PM could read without rolling their eyes. The two `HonestNote` callouts (Ownership + Permissions, both Linear-better) were the load-bearing part of getting that read — the mono register switch inside Newsreader essay prose signals "I am the author stepping outside the argument to calibrate it," which is what Tier-1 recruiters detect for.
```

- [ ] **Step 3: Validator + crosslinks + smoke**

Run: `npm run validate && node scripts/derive_crosslinks.mjs && node scripts/fetch_canonical_sources.mjs 2>&1 | tail -25`

Expected:
- Validator: `✓ architecture/vault-knowledge-mcp` line; the scoreboard 5-column × 4-row consistency check passes (each row has 5 scores); 0 errors; 0 warnings (no fetched essay body yet for the honestNotes anchor check to even fire).
- derive_crosslinks: `✓ vault-knowledge-mcp → /work/code-brain/ (methods)`, `✓ vault-knowledge-mcp → /transactions/vault-knowledge-mcp/`, `✓ vault-knowledge-mcp → /work/code-brain/ (relatedCaseStudy → archByCaseStudy)`. The Phase 3c.1 forward-declared link from the ledger row now closes — `transactions/vault-knowledge-mcp: relatedArchitecture "vault-knowledge-mcp" → /architecture/vault-knowledge-mcp/` no longer prints the `(forward-declared)` suffix.
- fetch_canonical_sources: `[architecture]` + `[architecture-body]` headers each report 0 fetches (both URLs null per OQ-C).

- [ ] **Step 4: Restart dev server + render smoke**

Kill the dev server (Ctrl-C in its terminal) and re-run `npm run dev`. Wait ~10s for content collection sync.

Run: `curl -sI http://localhost:4321/architecture/vault-knowledge-mcp/ | head -1`

Expected: `HTTP/1.1 200 OK`. Visual (browser): the deep-dive renders the 11 bands; the ER diagram renders as inline SVG (Mermaid SSR); the Scoreboard renders with 5 columns + 4 rows of inline SVG symbols; both HONEST NOTEs render in mono with stamp-amber prefix; Try It Yourself callout renders with the bash code snippet; the Related block shows `→ ledger row: Vault Knowledge MCP — Design Lock` + `→ companion case study: Code Brain`.

- [ ] **Step 5: Verify the reverse link lights up on the existing ledger row**

Run: `curl -s http://localhost:4321/transactions/vault-knowledge-mcp/ | grep -c "architecture/vault-knowledge-mcp"`

Expected: `≥1`. The Phase 3c.1 `<RelatedBlock />` on the ledger deep-dive renders the `relatedArchitecture: vault-knowledge-mcp` slug as a link; that link is now live (200 OK) and the ledger row's Related block shows `→ architecture: vault-knowledge-mcp` (or similar label, depending on how Phase 3c.1's RelatedBlock formats the line).

- [ ] **Step 6: Commit**

```bash
git add src/content/architecture/vault-knowledge-mcp.mdx src/content/architecture/diagrams/concept-edges-erd.mmd
git commit -m "$(cat <<'EOF'
feat(phase-3c.2): vault-knowledge-mcp architecture placeholder + ER diagram

First architecture writeup. Exercises end-to-end:
  - status SHIPPED rendering (stamp-amber pill)
  - ER diagram via mermaidSource: diagrams/concept-edges-erd.mmd
    (3-entity erDiagram + relationship cardinalities; renders to
    inline SVG via astro-mermaid + the 5-var palette override)
  - 5×4 Scoreboard with inline SVG ✓ / ✓✓ / ⚠ / ✗ symbols
  - 2 HONEST NOTEs (ownership + permissions — both Linear-better,
    the load-bearing credibility signal per spec §8.4)
  - Try It Yourself callout with bash snippet (Shiki rendered)
  - inline-body 4Q (OQ-C fallback — both URLs null until upstream
    EXPLANATION.md ships)
  - relatedLedgerRow: vault-knowledge-mcp closes Phase 3c.1's
    forward-declared cross-link bidirectionally
  - relatedCaseStudy: code-brain
  - methods strip with link to /work/code-brain/

Slug is byte-identical to Phase 3c.1's transactions/vault-knowledge-mcp
— the 4-way graph closes the first build after this commit.

Q1 is real prose (the spec lead reworked to first-person + structural
voice); Q2-Q4 carry [PLACEHOLDER —] markers for Sean's rewrite. All
component-shaped names in prose are backticked per the MDX gotcha
lesson from 3c.1 commit 07a8cf1.

Per architecture-spec §2.2 + §7 + §8 + §9 + §10 + §14.
EOF
)"
```

### Task 7.2: `meaning-over-access.mdx` (essays · PUBLISHED · 5 plotted artifacts)

**Files:**
- Create: `src/content/essays/meaning-over-access.mdx`
- Create: `src/content/essays/diagrams/access-meaning-spectrum.mmd`

Per essays-spec Appendix B. First and only essay in v1; the URL in Sean's email signature. Plotted artifacts cite the 3 existing transactions (`intent-engineering-mcp`, `phase-d-typed-edges`, `vault-knowledge-mcp`) + 2 yet-unbuilt slugs (`judge-layer`, `control-architecture`) — the unbuilt slugs render as legend rows but get filtered out of the `<PlottedArtifacts />` list (the component silently skips unresolved slugs; the validator's `plottedArtifacts` resolution rejects them only when neither txn nor arch has the slug, so we constrain plottedArtifacts[] to only resolved slugs for v1).

- [ ] **Step 1: Write the Mermaid quadrantChart source**

Create `src/content/essays/diagrams/access-meaning-spectrum.mmd`:

```
quadrantChart
    title Access vs Meaning × Infrastructure vs Workflow
    x-axis "Access" --> "Meaning"
    y-axis "Workflow" --> "Infrastructure"
    quadrant-1 Meaning + Infra
    quadrant-2 Access + Infra
    quadrant-3 Access + Workflow
    quadrant-4 Meaning + Workflow
    "intent-engineering MCP": [0.85, 0.78]
    "vault-knowledge-mcp": [0.82, 0.85]
    "concept_edges": [0.78, 0.88]
    "browser-use agents": [0.18, 0.22]
    "MCP HTTP transports": [0.25, 0.78]
```

- [ ] **Step 2: Write the essay MDX**

Create `src/content/essays/meaning-over-access.mdx`:

```mdx
---
title: "Access vs Meaning"
subtitle: "A bet on the semantic layer."
dateline: "BOSTON, JUNE 19, 2026"
published: "2026-06-19"
lastRevised: null
readingTime: 6

status: PUBLISHED
tags:
  - meaning-layer
  - agents
  - pm-thesis

excerpt: "I don't think the durable enterprise value is agents clicking around UIs. I think it's the semantic layer: typed work objects, authorization, memory provenance, reviewable decisions."

# OQ-C inline-body fallback path: both URLs null until Sean ships
# MEANING_OVER_ACCESS.md + _EXPLANATION.md upstream.
sourceUrl: null
explanationUrl: null

mermaidSource: diagrams/access-meaning-spectrum.mmd
mermaidCaption: "fig 1 — three artifacts on the meaning side; two negative-space callouts on the access side."

quadrantLegend:
  - key: iemcp
    label: "intent-engineering MCP"
    artifact: intent-engineering-mcp
  - key: vkmcp
    label: "vault-knowledge-mcp"
    artifact: vault-knowledge-mcp
  - key: ce
    label: "concept_edges (memory primitive)"
    artifact: phase-d-typed-edges
  - key: browser
    label: "browser-use / computer-use agents (access + workflow)"
    artifact: null
  - key: http
    label: "MCP HTTP transports / SaaS connectors (access + infra)"
    artifact: null

roleMap:
  lastValidated: "2026-06-19"
  rows:
    - buyer: "Anthropic FDE Boston"
      position: "meaning + workflow"
      vocabularyTell: '"control architectures around production agent deployments"'
      jdUrl: "https://anthropic.com/jobs/fde-boston"
    - buyer: "Glean"
      position: "meaning + infrastructure"
      vocabularyTell: '"knowledge governance," "action primitives"'
      jdUrl: "https://glean.com/jobs/agent-governance"
    - buyer: "Sierra / Decagon"
      position: "meaning + workflow"
      vocabularyTell: '"agent PM loops," "structured action surfaces"'
      jdUrl: "https://sierra.ai/careers/agent-pm"
    - buyer: "Cursor / Cognition"
      position: "mixed"
      vocabularyTell: '"developer workflow primitives"'
      jdUrl: "https://cursor.com/careers"
    - buyer: "Manus / Adept / browser-use"
      position: "access + workflow"
      vocabularyTell: '"computer-use," "browser automation"'
      jdUrl: "https://browser-use.com/careers"

# Plotted artifacts: only slugs that resolve on at least one surface
# in v1. judge-layer + control-architecture are future ledger entries;
# adding them here would fail the validator's plottedArtifacts
# resolution check (Task 3.3 derive_crosslinks rejects dangling slugs).
plottedArtifacts:
  - intent-engineering-mcp
  - vault-knowledge-mcp
  - phase-d-typed-edges
relatedLedgerRow: null
relatedCaseStudy: null
relatedArchitecture:
  - vault-knowledge-mcp
relatedEssays: []

# crossPostedTo: Substack URL is forward-declared — Sean fills in
# after publishing to Substack post-plan. Leaving the array empty
# is valid; FootLinks renders nothing when crossPostedTo is undefined.
crossPostedTo: []
sourceRepoUrl: https://github.com/seanwinslow28/code-brain

ogImage: /og-cards/essays/meaning-over-access.png
---

## The bet

I keep watching demos where an agent clicks around a browser to book a flight. The agent is impressive. The flight booking is not the durable enterprise value. The durable value is upstream — in the layer where work objects are typed, where the agent has defined verbs to act on those objects, where every action leaves an audit trail a reviewer can replay. The browser-clicking demos win the conference talk; the semantic-layer work wins the production deployment.

That's the bet. Five artifacts on the meaning side of the chart below; two negative-space callouts on the access side. The negative space is the load-bearing part — I keep building on the meaning side because the access side is a treadmill.

## Artifact map

[PLACEHOLDER — middle section #1] The chart plots five artifacts on a meaning ↔ access × workflow ↔ infrastructure spectrum. `intent-engineering MCP` (top-right, infra+meaning) is the production-shipped artifact. `vault-knowledge-mcp` (top-right, deeper into meaning) and `concept_edges` (top-right, deeper into infra) are the next two ships. Together they form the *semantic infrastructure* triangle the bet rests on. The two negative-space callouts (`browser-use agents` lower-left, `MCP HTTP transports` upper-left) represent the access side — the work I'm explicitly *not* building. The legend below maps each plotted point to its ledger row or architecture writeup so the reader can click through to the proof.

## Role map

[PLACEHOLDER — middle section #2] The role map is the recruiter funnel close. Five buyers, each with a market position on the meaning↔access spectrum + the vocabulary tell that distinguishes their JD from the others. Anthropic FDE Boston ("control architectures") is the canonical meaning+workflow buyer; Glean ("knowledge governance") is meaning+infrastructure. Sierra and Decagon are meaning+workflow ("agent PM loops"). Cursor/Cognition is mixed — developer-workflow primitives sit on the meaning/workflow boundary. Manus/Adept/browser-use sit firmly on the access+workflow side — the lane I am explicitly not building for. Each row links to a real JD; the `last_validated` badge tells you when I last clicked through and confirmed the role.

## Why not browser-first

[PLACEHOLDER — middle section #3] Three named failure modes for the browser-first agent: (1) DOM drift — every UI change breaks the agent's mental model of the page; the agent costs scale with the UI's churn rate, not the user's task volume. (2) Authentication regression — every SaaS that adds 2FA or session timeouts breaks the agent's session; the agent's reliability is bounded by the worst SaaS in the stack. (3) Audit opacity — when a browser-clicking agent "buys the wrong flight," the audit trail is a click stream, not a typed action. The reviewer can't replay it without re-mounting the entire UI state. None of these are bugs I see going away in the next 24 months. The semantic layer sidesteps all three.

## The bet, restated

I bet on meaning, not access. If I'm right, the next four years of enterprise PM work is about typed work objects, authorization, memory provenance, and reviewable decisions. If I'm wrong, I will have spent that time learning the discipline of *building agent-shaped systems carefully* — which is a skill that travels. Either way I'm not building a Manus competitor. The artifacts plotted on the chart below are what I'm actually shipping; the negative space is what I'm not. That's the recruiter ask: hire me to build the meaning side.

## What is this?

A thesis-shaped manifesto plotting five artifacts on the meaning ↔ access × workflow ↔ infrastructure spectrum, with a role map of five buyer archetypes that read against the same axes. The visual centerpiece is a Mermaid quadrantChart; the calibration is the role map's `last_validated` badge; the recruiter funnel close is the plotted-artifacts list at the page foot.

## Why this approach?

[PLACEHOLDER — Q2] Three options were on the table: (1) write a long-form essay with no visual centerpiece; (2) write a list of "5 things I believe about agents"; (3) plot the artifacts on a chart, ground the chart in a role map, and let the geometry carry the argument. (3) won because the chart makes the negative space visible — you can SEE the access-side artifacts I'm not building. Geometry is honesty: if I had four artifacts in the bottom-left and called it "I bet on meaning," the chart would expose the contradiction. The role map is the buyer-side checkpoint — if all five buyer rows clustered in the same quadrant the chart would have nothing to argue against.

## What would break?

[PLACEHOLDER — Q3] Three failure modes: (1) the role map goes stale — JDs get filled, companies restructure, the buyer archetypes I named in June stop existing by December — mitigation is the `last_validated` badge + the validator's 30-day staleness warning; (2) a 6th buyer archetype emerges that doesn't fit any of the 5 existing rows — mitigation is `lastRevised` lets me update the essay editorially without burning the slug; (3) the access-side wins the platform layer faster than I expect (browser-use becomes 10× more reliable in 12 months) — mitigation is the essay is dated; the bet is timestamped; readers can judge whether the bet aged well.

## What did I learn?

[PLACEHOLDER — Q4] The chart was harder to author than the essay. The essay reads in 6 minutes; the chart took 4 hours to place five points where they're each defensible against critique. That asymmetry is real — visual arguments demand more precision than prose because there's nowhere to hide. The next essay will know that. The other thing I learned: the role map is the recruiter funnel close. The thesis pulls them in; the artifact map proves the bet; the role map says "here are the 5 places this hire makes sense." Without the role map the essay would be a manifesto without a buyer.
```

- [ ] **Step 3: Validator + crosslinks + smoke**

Run: `npm run validate && node scripts/derive_crosslinks.mjs 2>&1 | tail -30`

Expected:
- Validator: `✓ essays/meaning-over-access` line; the roleMap.lastValidated is today (2026-06-19 is future-dated in the spec but matches the current planning context — adjust to a current date if it triggers a future-date validator error; the staleness check only warns on >30 days OLD, not future-dated); the 5 jdUrl HEAD checks fire — each URL above may return 404 or 200 depending on whether the org is real, but the validator only warns (never errors) on 4xx/5xx; 0 errors; up to 5 warnings (HEAD-check warnings count as "warnings only, build proceeds").
- derive_crosslinks: 3 plotted artifact lines (`✓ meaning-over-access → /transactions/intent-engineering-mcp/`, `→ /transactions/vault-knowledge-mcp/`, `→ /transactions/phase-d-typed-edges/`); 1 architecture line (`→ /architecture/vault-knowledge-mcp/`); the reverse-derivation writes `namedInEssays: ["meaning-over-access"]` onto each cited ledger row + the architecture writeup.

If the 5 jdUrl HEAD checks slow the validate run noticeably (>10s), accept it — the spec calls for every-build HEAD checks (OPEN-2 default); revisit at 5+ essays. If a HEAD check hangs the script (>30s), set the offending URL to a known-200 placeholder (`https://example.com/`) for v1.

- [ ] **Step 4: Restart dev server + render smoke**

Kill the dev server and re-run `npm run dev`. Wait ~10s for content collection sync.

Run in parallel:
- `curl -sI http://localhost:4321/essays/ | head -1`
- `curl -sI http://localhost:4321/essays/meaning-over-access/ | head -1`

Expected: both return `HTTP/1.1 200 OK`. Visual (browser): the index shows 1 row (the manifesto); the deep-dive renders the 12 bands; the quadrantChart renders as inline SVG with 5 plotted points; the QuadrantLegend renders below it with linked artifact slugs; the RoleMap renders the 4-column table with the `last validated` mono badge; the PlottedArtifacts section auto-renders the 3 resolved artifact lines (intent-engineering-mcp on ledger, vault-knowledge-mcp on both surfaces, phase-d-typed-edges on ledger).

- [ ] **Step 5: Verify the 4-way reverse links light up**

Run in parallel (4 checks):
- `curl -s http://localhost:4321/transactions/intent-engineering-mcp/ | grep -c "named in.*meaning-over-access"`
- `curl -s http://localhost:4321/transactions/vault-knowledge-mcp/ | grep -c "named in.*meaning-over-access"`
- `curl -s http://localhost:4321/transactions/phase-d-typed-edges/ | grep -c "named in.*meaning-over-access"`
- `curl -s http://localhost:4321/architecture/vault-knowledge-mcp/ | grep -c "named in.*meaning-over-access"`

Expected: all four return `≥1`. The essays' `plottedArtifacts` reverse-derivation lights up the Related block on every cited surface — the 4-way close is operational. (NOTE: the Phase 3c.1 `<RelatedBlock />` on transactions deep-dives may not render the `namedInEssays[]` reverse entries by default — Phase 3c.1's RelatedBlock was written before this reverse type existed. If the grep returns 0 on the transactions URLs, either: (a) accept that the consumer-side render is a Phase 3c.2 v1.1 follow-up to Phase 3c.1's RelatedBlock, OR (b) extend `src/components/transactions/RelatedBlock.astro` here to also render the `crosslinks.transactions[slug].namedInEssays[]` array as `← named in: <essay>` lines. The cleaner path is (b); it's a ~10-line addition to the existing component. The architecture deep-dive's RelatedBlock from Task 4.5 ships with the rendering, so the architecture URL must return ≥1 even when transactions don't.)

If the transactions URLs return 0, run Step 6 below to extend the Phase 3c.1 RelatedBlock; otherwise skip directly to Step 7.

- [ ] **Step 6: (Conditional) Extend `src/components/transactions/RelatedBlock.astro` to render `namedInEssays[]`**

Only needed if Step 5's transactions URLs returned 0. Read the existing component:

Run: `grep -n "namedInEssays\|reverse" src/components/transactions/RelatedBlock.astro | head -5`

If `namedInEssays` doesn't appear, append a render block to the existing component. Locate the closing `</section>` tag of the Related block render and add (immediately before it, inside the `{hasAny && (...)}` conditional):

```astro
{(crosslinks.transactions?.[slug]?.namedInEssays ?? []).map((s) => (
  <li>
    <a href={`/essays/${s}/`}>&larr; named in: {titleByEssaySlug?.[s] ?? s} (essay)</a>
  </li>
))}
```

This requires also threading a `titleByEssaySlug` prop into the component. Add to the Props interface + the destructure block + the import site in `src/pages/transactions/[slug].astro`. The change is mechanical — when complete, re-run Step 5's transactions checks.

If this extension is needed, commit it separately:

```bash
git add src/components/transactions/RelatedBlock.astro src/pages/transactions/\[slug\].astro
git commit -m "feat(phase-3c.2): transactions RelatedBlock — render namedInEssays[] reverse links"
```

- [ ] **Step 7: Commit the essay placeholder**

```bash
git add src/content/essays/meaning-over-access.mdx src/content/essays/diagrams/access-meaning-spectrum.mmd
git commit -m "$(cat <<'EOF'
feat(phase-3c.2): meaning-over-access essay placeholder + quadrantChart

First essay. The manifesto. Exercises end-to-end:
  - status PUBLISHED + dateline + readingTime
  - thesis pullquote from excerpt
  - 5-section structure per essays-spec §7 voice contract: opening
    hook (personal voice) + 3 middle sections (sober/declarative) +
    closing kicker (personal voice). The 3 middle sections all carry
    [PLACEHOLDER —] markers for Sean's rewrite; the bookends are
    real prose so the cold-read test reads as production-ready.
  - Mermaid quadrantChart with 5 plotted points (3 resolved
    artifact slugs + 2 negative-space callouts)
  - QuadrantLegend with 5 entries — 3 with resolved artifact slugs
    auto-linking to ledger + architecture surfaces; 2 with
    artifact: null (negative-space callouts)
  - RoleMap with 5 buyer rows + lastValidated 2026-06-19; jdUrl HEAD
    checks fire as warnings (some URLs may be 404 in the synthetic
    examples — the validator warns, doesn't error)
  - PlottedArtifacts list at page foot resolving 3 artifacts:
    intent-engineering-mcp (ledger only), vault-knowledge-mcp
    (ledger + architecture — both links render), phase-d-typed-edges
    (ledger only)
  - relatedArchitecture: [vault-knowledge-mcp] companion link
  - inline-body 4Q (OQ-C fallback path — sourceUrl + explanationUrl
    both null)

The 4-way close: namedInEssays[] reverse-derives onto each cited
ledger row + the architecture writeup. Phase 3c.1's transactions
RelatedBlock may need the rendering extension to surface the reverse
lines (see Step 6 conditional).

All component-shaped names in body prose backticked per the MDX gotcha
lesson from Phase 3c.1 commit 07a8cf1. crossPostedTo: [] today —
Sean adds the Substack URL after publishing to Substack post-plan.

Per essays-spec §2.2 + §7 + §8 + §9 + §10 + §14 + Appendix B.
EOF
)"
```

---

## Section 8 — View Transition + sitemap verification

### Task 8.1: Verify View Transition seams `arch-title-<slug>` + `essay-title-<slug>`

**Files:**
- Read-only verification

Per architecture-spec §15 + essays-spec §15. Browser-side verification (no automated test — visual smoke per the 3c.1 + 3b template).

- [ ] **Step 1: Verify the view-transition-name appears on both architecture surfaces**

Run in parallel:
- `curl -s http://localhost:4321/architecture/ | grep -oE 'view-transition-name: arch-title-[a-z0-9-]+' | sort -u`
- `curl -s http://localhost:4321/architecture/vault-knowledge-mcp/ | grep -oE 'view-transition-name: arch-title-[a-z0-9-]+' | sort -u`

Expected:
- Index: 1 line (`arch-title-vault-knowledge-mcp`)
- Deep-dive: 1 line (same)

- [ ] **Step 2: Verify the view-transition-name appears on both essay surfaces**

Run in parallel:
- `curl -s http://localhost:4321/essays/ | grep -oE 'view-transition-name: essay-title-[a-z0-9-]+' | sort -u`
- `curl -s http://localhost:4321/essays/meaning-over-access/ | grep -oE 'view-transition-name: essay-title-[a-z0-9-]+' | sort -u`

Expected: 1 line each (`essay-title-meaning-over-access`).

- [ ] **Step 3: Visual smoke (Chrome) — optional but recommended**

Open `http://localhost:4321/architecture/` in Chrome. Click the writeup row title. Observe: the title morphs from its row position into the deep-dive page's `<h1>` position via Astro 5's View Transition. Repeat for `/essays/`.

In Safari: same behavior. In Firefox <128: instant page swap (graceful fallback).

If the morph doesn't animate in Chrome on either surface, the `<ClientRouter />` in BaseLayout isn't engaging — verify the layout still includes `<ClientRouter />` (Phase 2 baseline).

- [ ] **Step 4: No commit**

Verification only.

### Task 8.2: Run full `npm run build` + verify sitemap

**Files:**
- Generated: `dist/sitemap-0.xml`

- [ ] **Step 1: Production build from clean state**

Run: `rm -rf dist .cache/canonical-sources.lockfile && npm run build 2>&1 | tail -40`

Expected: prebuild gates re-run from cold cache (validate, fetch-canonical, derive-crosslinks); Astro build completes. Output enumerates all expected routes including:
- `dist/architecture/index.html`
- `dist/architecture/vault-knowledge-mcp/index.html`
- `dist/architecture/rss.xml`
- `dist/essays/index.html`
- `dist/essays/meaning-over-access/index.html`
- `dist/essays/rss.xml`

If build fails, the most likely culprits are: (a) a TS type error in a deep-dive route (verify all imports + Props interfaces match), (b) a missing `astro-mermaid` peer dep (re-run `npm install`), (c) an MDX parse error from an un-backticked `<Something />` in placeholder prose — search the MDX bodies for bare uppercase JSX-shaped tags.

- [ ] **Step 2: Verify sitemap entries for the new surfaces**

Run: `grep -oE "<loc>[^<]+</loc>" dist/sitemap-0.xml | sort -u | grep -E "architecture|essays"`

Expected: 4 entries — `/architecture/`, `/architecture/vault-knowledge-mcp/`, `/essays/`, `/essays/meaning-over-access/`. (The RSS endpoints don't appear in the sitemap; that's correct.)

- [ ] **Step 3: Verify RSS feeds validate as RSS 2.0**

Run in parallel:
- `head -5 dist/architecture/rss.xml`
- `head -5 dist/essays/rss.xml`
- `grep -c "<item>" dist/architecture/rss.xml`
- `grep -c "<item>" dist/essays/rss.xml`

Expected: both files start with `<?xml version="1.0"`; both have 1 `<item>` (one per placeholder).

- [ ] **Step 4: Verify no broken imports surface in dist**

Run: `grep -rE "TypeError|ReferenceError|Cannot find module" dist/ 2>&1 | head -5`

Expected: no matches. Any error string in the built HTML signals a runtime crash that escaped the build.

- [ ] **Step 5: No commit**

Verification only — if all checks pass, Phase 3c.2 is functionally complete.

---

## Section 9 — DoD verification

### Task 9.1: Walk architecture-spec §18 items 1-18

For each DoD item below, verify on `http://localhost:4321` (dev) AND in `dist/` (build) and mark `[PASS]` / `[FAIL]` / `[DEFERRED]`. If FAIL, return to the relevant earlier task to fix.

- [ ] **Item 1 — schema declared per §3 with all fields + constraints**

Run: `grep -E "^const architecture|architecture: " src/content/config.ts | head -3`

Expected: 2 matches — the `const architecture` declaration and the export. Visual scan confirms the 5-value enum on `status` (DRAFT, SHIPPED), `.min(2).max(8)` on scoreboard.rows, `.max(3)` on honestNotes.

Result: ☐

- [ ] **Item 2 — fetch_canonical_sources walks architecture + caches via ETag**

Verified Task 3.1 Step 4. Both URLs null in v1 means 0 fetches; the cache mechanism is exercised when Sean ships upstream sources.

Result: ☐ [PASS — INFRASTRUCTURE; activates when Sean ships upstream sources]

- [ ] **Item 3 — validate_content (formerly validate_architecture) walks + rejects dangling slugs + missing fields + invalid ISO + missing mermaidSource files**

Verified Task 3.2. Negative test:

Run (temporary): edit `src/content/architecture/vault-knowledge-mcp.mdx` to set `relatedCaseStudy: nonexistent-slug`, then run `node scripts/derive_crosslinks.mjs`.

Expected: exit 1 with `architecture/vault-knowledge-mcp: relatedCaseStudy "nonexistent-slug" → /work/nonexistent-slug/ — no such slug.` Revert.

Result: ☐

- [ ] **Item 4 — derive_crosslinks extended; reverse auto-render on architecture pages + paired ledger + case-study pages**

Verified Task 7.1 Step 5. The forward-declared link from Phase 3c.1's transactions row resolves; the Related block on `/architecture/vault-knowledge-mcp/` renders `→ ledger row: …` + `→ companion case study: …`.

Result: ☐

- [ ] **Item 5 — index renders page title block + all writeup rows in shipped desc order + footer fold**

Run: `curl -s http://localhost:4321/architecture/ | grep -cE "page-title|writeup-row|footer-fold"`

Expected: `≥3`.

Result: ☐

- [ ] **Item 6 — each deep-dive renders all 11 bands per §2.2**

For `/architecture/vault-knowledge-mcp/`:
```bash
curl -s http://localhost:4321/architecture/vault-knowledge-mcp/ > /tmp/dd-vkmcp.html
echo "dateline:    $(grep -c 'dateline__city' /tmp/dd-vkmcp.html)"
echo "title:       $(grep -c 'page-title' /tmp/dd-vkmcp.html)"
echo "pullquote:   $(grep -c 'thesis-pullquote' /tmp/dd-vkmcp.html)"
echo "essay-body:  $(grep -c 'essay-body' /tmp/dd-vkmcp.html)"
echo "er-diagram:  $(grep -c 'er-diagram' /tmp/dd-vkmcp.html)"
echo "scoreboard:  $(grep -c 'scoreboard' /tmp/dd-vkmcp.html)"
echo "honest:      $(grep -c 'honest-note' /tmp/dd-vkmcp.html)"
echo "try-it:      $(grep -c 'try-it' /tmp/dd-vkmcp.html)"
echo "methods:     $(grep -c 'methods-strip' /tmp/dd-vkmcp.html)"
echo "4q:          $(grep -cE 'four-q|inline-body-4q' /tmp/dd-vkmcp.html)"
echo "related:     $(grep -c 'related-block' /tmp/dd-vkmcp.html)"
echo "next-prev:   $(grep -c 'next-prev' /tmp/dd-vkmcp.html)"
echo "regmark:     $(grep -c 'registration-mark' /tmp/dd-vkmcp.html)"
```

Expected: each line ends with `≥1`.

Result: ☐

- [ ] **Item 7 — Scoreboard renders flat with SVG symbols + ARIA**

Run: `curl -s http://localhost:4321/architecture/vault-knowledge-mcp/ | grep -c 'score-symbol'`

Expected: `20` (4 rows × 5 cols).

Run: `curl -s http://localhost:4321/architecture/vault-knowledge-mcp/ | grep -oE 'aria-label="(passes|exceeds|partial|fails)"' | sort | uniq -c`

Expected: lists counts for `passes`, `exceeds`, `partial`, `fails` from the Vault Scorecard scoreboard data.

Result: ☐

- [ ] **Item 8 — Mermaid ER diagram renders as inline SVG via astro-mermaid + 5-var palette override; mobile overflow works**

Run: `curl -s http://localhost:4321/architecture/vault-knowledge-mcp/ | grep -c "<svg"`

Expected: `≥1` (the ER diagram + the 20 scoreboard SVG symbols sum to ~21+). Visual: open the page in Chrome at viewport 375px; the diagram + scoreboard scroll horizontally without page overflow.

Result: ☐

- [ ] **Item 9 — HONEST NOTE callouts render with mono prefix + 1px left border + anchored**

Run: `curl -s http://localhost:4321/architecture/vault-knowledge-mcp/ | grep -c 'honest-note__prefix'`

Expected: `2` (Vault Scorecard ships with 2 HONEST NOTEs: ownership + permissions).

Result: ☐

- [ ] **Item 10 — Try It Yourself callout renders only when tryItYourselfUrl set; Shiki code highlight**

Run: `curl -s http://localhost:4321/architecture/vault-knowledge-mcp/ | grep -cE 'try-it__prefix|shiki|astro-code'`

Expected: `≥2` (prefix + Shiki wrapper).

Result: ☐

- [ ] **Item 11 — 4Q block fetches from canonical when present; renders via shared FourQBlock**

OQ-C v1 fallback: explanationUrl is null. The inline-body 4Q render path fires. Verify the MDX body's 4 h2 headings render in the deep-dive output:

Run: `curl -s http://localhost:4321/architecture/vault-knowledge-mcp/ | grep -cE "What is this\?|Why this approach\?|What would break\?|What did I learn\?"`

Expected: `4`.

Result: ☐ [PASS — inline-body path; canonical path activates when explanationUrl is non-null]

- [ ] **Item 12 — View Transition: index row → deep-dive h1 via arch-title-<slug>**

Verified Task 8.1.

Result: ☐ [PASS — programmatic + visual smoke per Task 8.1]

- [ ] **Item 13 — RSS validates RSS 2.0 + full content in content:encoded + sort matches**

Verified Task 8.2 Step 3. Optional: paste `dist/architecture/rss.xml` into https://validator.w3.org/feed/.

Result: ☐

- [ ] **Item 14 — Permalinks on essay section headings work**

V1 ships the section headings rendered via MDX/Astro defaults — the `<h2>` elements have implicit IDs generated by remark-gfm slug behavior IF Astro's MDX integration enables it. If section permalinks are not auto-generated, this item is `[DEFERRED — v1.1]` per architecture-spec §17's restraint posture (no client-side TOC).

Run: `curl -s http://localhost:4321/architecture/vault-knowledge-mcp/ | grep -oE 'id="[^"]+"' | grep -E "what-is|why-this|what-would|what-did" | wc -l`

Expected: `≥4` IF Astro's default MDX renderer slugs h2 headings. If `0`, the result is `[DEFERRED]`.

Result: ☐ [PASS or DEFERRED depending on MDX default behavior]

- [ ] **Item 15 — Reduced motion: view transition + row hover + section fades disable**

Chrome DevTools → Rendering → Emulate `prefers-reduced-motion: reduce` → reload. Expected: row hover bg fade, glyph translate, title underline animations all disable; View Transition becomes instant. Astro 5 honors reduced-motion at the View Transition level natively.

Result: ☐

- [ ] **Item 16 — Lighthouse Performance ≥90 on index; ≥85 on deep-dive; A11y ≥95; Best Practices = 100**

Open `http://localhost:4321/architecture/` + `/architecture/vault-knowledge-mcp/` in Chrome → DevTools → Lighthouse → Mobile.

Expected:
- Index: Performance ≥ 90, A11y ≥ 95, Best Practices = 100.
- Deep-dive: Performance ≥ 85 (image + Mermaid + scoreboard heavier), A11y ≥ 95, Best Practices = 100.

Common A11y failure: missing `<caption>` on scoreboard — already shipped (visually-hidden caption per Task 4.1). Color contrast: stamp amber on paper = 5.8:1 (AA at 11px weight 500 — clears).

Result: ☐

- [ ] **Item 17 — First occupant fully populated**

Verified Task 7.1 end-to-end. Vault Scorecard has all 5 essay sections (ish — Q1 is real prose; Q2-Q4 carry placeholders for Sean's rewrite), ER diagram, 5×4 scoreboard, 2 HONEST NOTEs, Try It Yourself callout, inline-body 4Q, paired ledger row at `/transactions/vault-knowledge-mcp/` cross-linked bidirectionally.

Result: ☐

- [ ] **Item 18 — Recruiter cold-read test (90s)**

Sean's manual test post-build. The structural shell ships; cold-read carrying Sean's real prose is post-plan.

Result: ☐ [PARTIAL — structure ships v1; prose validation is Sean's post-plan]

### Task 9.2: Walk essays-spec §19 items 1-19

- [ ] **Item 1 — schema declared per §3**

Run: `grep -E "^const essays|essays })" src/content/config.ts | head -3`

Expected: 2 matches. Visual scan confirms 2-value status enum, `.min(2).max(6)` tags, `.max(280)` excerpt.

Result: ☐

- [ ] **Item 2 — fetch_canonical_sources walks essays a 4th time**

Verified Task 3.1.

Result: ☐ [PASS — INFRASTRUCTURE]

- [ ] **Item 3 — validate_content unified replaces per-collection validators + JD URL HEAD + lastValidated staleness**

Verified Task 3.2. Negative test for jdUrl HEAD: temporarily set a roleMap row's jdUrl to `https://this-domain-does-not-exist-12345.example`; re-run validate; expect warning (not error).

Result: ☐

- [ ] **Item 4 — derive_crosslinks: plottedArtifacts reverse-renders "← named in: <essay>" on every cited surface**

Verified Task 7.2 Step 5. The architecture writeup's Related block surfaces the reverse line; the transactions deep-dive may need the Phase 3c.2 v1.1 extension (Task 7.2 Step 6 conditional).

Result: ☐

- [ ] **Item 5 — /essays/ index renders all bands + 3 subscribe affordances in footer**

Run: `curl -s http://localhost:4321/essays/ | grep -cE "page-title|essay-row|footer-fold|rss\.xml|Substack|view the fleet"`

Expected: ≥4 (the footer's Substack link renders only when at least one essay's crossPostedTo[] has a Substack URL; the v1 placeholder ships with crossPostedTo: [] so Substack link doesn't render on the index — that's correct).

Result: ☐

- [ ] **Item 6 — each deep-dive renders all 12 bands per §2.2**

For `/essays/meaning-over-access/`:
```bash
curl -s http://localhost:4321/essays/meaning-over-access/ > /tmp/dd-moa.html
echo "dateline:     $(grep -c 'dateline__city' /tmp/dd-moa.html)"
echo "pills:        $(grep -c 'pills-row' /tmp/dd-moa.html)"
echo "title:        $(grep -c 'page-title' /tmp/dd-moa.html)"
echo "subtitle:     $(grep -c 'subtitle' /tmp/dd-moa.html)"
echo "pub-rev:      $(grep -c 'pub-rev-strip' /tmp/dd-moa.html)"
echo "pullquote:    $(grep -c 'thesis-pullquote' /tmp/dd-moa.html)"
echo "essay-body:   $(grep -c 'essay-body' /tmp/dd-moa.html)"
echo "quadrant:     $(grep -c 'quadrant-chart' /tmp/dd-moa.html)"
echo "legend:       $(grep -c 'quadrant-legend' /tmp/dd-moa.html)"
echo "role-map:     $(grep -c 'role-map' /tmp/dd-moa.html)"
echo "plotted:      $(grep -c 'plotted-artifacts' /tmp/dd-moa.html)"
echo "4q:           $(grep -cE 'four-q|inline-body-4q' /tmp/dd-moa.html)"
echo "foot-links:   $(grep -c 'foot-links' /tmp/dd-moa.html)"
echo "related:      $(grep -c 'related-block' /tmp/dd-moa.html)"
echo "next-prev:    $(grep -c 'next-prev' /tmp/dd-moa.html)"
echo "regmark:      $(grep -c 'registration-mark' /tmp/dd-moa.html)"
```

Expected: each line ends with `≥1` EXCEPT `pub-rev` which renders the "PUBLISHED only" variant (lastRevised null in v1) — still counts as ≥1.

Result: ☐

- [ ] **Item 7 — voice register contract honored (5-section structure)**

Manual: read the placeholder MDX body. Confirm 5 h2 sections — opening hook + 3 middle sections + closing kicker — per §7. The personal-voice bookends carry real prose; the middle 3 carry `[PLACEHOLDER —]` for Sean's rewrite.

Result: ☐ [PARTIAL — structure ships; full voice calibration is Sean's post-plan]

- [ ] **Item 8 — quadrantChart renders as inline SVG via astro-mermaid + shared 5-var palette**

Run: `curl -s http://localhost:4321/essays/meaning-over-access/ | grep -c "mermaid"`

Expected: `≥1`. The shared palette + `quadrant1Fill`-`quadrant4Fill` overrides apply because the same `astro.config.mjs` block configures them globally.

Result: ☐

- [ ] **Item 9 — QuadrantLegend renders structured legend below chart; linked slugs resolve**

Run: `curl -s http://localhost:4321/essays/meaning-over-access/ | grep -c 'quadrant-legend.*ledger\|quadrant-legend.*architecture'`

Expected: ≥3 (the 3 entries with non-null artifact slugs — iemcp, vkmcp, ce — each render at least one link).

Result: ☐

- [ ] **Item 10 — RoleMap renders structured table + last validated badge; jdUrl HEAD-validated at build**

Run: `curl -s http://localhost:4321/essays/meaning-over-access/ | grep -cE 'role-map|last validated'`

Expected: ≥2.

Result: ☐

- [ ] **Item 11 — plotted-artifacts section auto-renders from frontmatter; cross-link graph resolves**

Run: `curl -s http://localhost:4321/essays/meaning-over-access/ | grep -c 'transactions/intent-engineering-mcp\|architecture/vault-knowledge-mcp\|transactions/phase-d-typed-edges'`

Expected: ≥3. (The vault-knowledge-mcp slug resolves to BOTH /transactions/ + /architecture/, so the count may be ≥4.)

Result: ☐

- [ ] **Item 12 — 4Q block fetches from canonical when present; renders via shared FourQBlock**

Same OQ-C inline-body path as architecture. Verify:

Run: `curl -s http://localhost:4321/essays/meaning-over-access/ | grep -cE "What is this\?|Why this approach\?|What would break\?|What did I learn\?"`

Expected: `4`.

Result: ☐ [PASS — inline-body path; canonical path activates when explanationUrl set]

- [ ] **Item 13 — View Transition: index row → deep-dive h1 via essay-title-<slug>**

Verified Task 8.1.

Result: ☐

- [ ] **Item 14 — RSS feed validates RSS 2.0 + content:encoded + sort**

Verified Task 8.2 Step 3.

Result: ☐

- [ ] **Item 15 — Substack syndication: crossPostedTo[] renders "Read on Substack →" at page foot**

V1 ships `crossPostedTo: []` — the Substack link doesn't render. When Sean publishes to Substack post-plan + updates the frontmatter, the FootLinks component renders the line automatically.

Result: ☐ [PASS — INFRASTRUCTURE; activates on first Sean-edit post-plan]

- [ ] **Item 16 — Reduced motion: animations disable**

Same as architecture item 15.

Result: ☐

- [ ] **Item 17 — Lighthouse Performance ≥90 index, ≥85 deep-dive; A11y ≥95; Best Practices = 100**

Same as architecture item 16.

Result: ☐

- [ ] **Item 18 — First occupant fully populated**

Verified Task 7.2.

Result: ☐

- [ ] **Item 19 — Recruiter cold-read test (90s)**

Sean's manual test post-build.

Result: ☐ [PARTIAL — structure ships v1; prose validation is Sean's post-plan]

### Task 9.3: Note HEAD + commit count

- [ ] **Step 1: Note HEAD + commit count**

Run: `git log --oneline 07a8cf1..HEAD | wc -l`

Expected: ~28-32 commits.

Run: `git rev-parse HEAD`

Note the SHA. This is the Phase 3c.2 completion mark; Phase 3d's plan references it as its baseline.

Result: ☐

---

## Section 10 — What's NOT in this plan (deferrals)

For clarity to the next planning sessions:

| Defer | Spec / Reason | Owner plan |
|---|---|---|
| Real prose authoring beyond the 2 placeholder bodies | Sean's authoring pass post-plan. The 5-section essay structure ships v1; the bookend prose is real (opening hook + closing kicker for the essay; Q1 for the architecture writeup); the middle prose carries `[PLACEHOLDER —` for grep-ability — `grep "PLACEHOLDER" src/content/architecture/*.mdx src/content/essays/*.mdx` surfaces every remaining edit. | **Sean post-plan** |
| Upstream `VAULT_AS_AGENT_INFRASTRUCTURE.md` + `VAULT_AS_AGENT_INFRASTRUCTURE_EXPLANATION.md` authoring + `essaySourceUrl:` / `explanationUrl:` activation on the architecture placeholder | OQ-C v1 deviation from architecture-spec §3.1. Placeholder ships with both URLs null + inline-body 4Q. When Sean ships the upstream files, swap nullable URLs → real URLs + (optionally) drop the inline-body sections; next build's `fetch_canonical_sources.mjs` activates the canonical path. | **Sean post-plan + per-slug** |
| Upstream `MEANING_OVER_ACCESS.md` + `MEANING_OVER_ACCESS_EXPLANATION.md` + `sourceUrl:` / `explanationUrl:` activation on the essay placeholder | Same OQ-C pattern. | **Sean post-plan + per-slug** |
| Substack publication of the manifesto + `crossPostedTo[]` activation | Sean publishes manually to Substack; the portfolio renders the link once `crossPostedTo[]` is filled in. Currently `crossPostedTo: []` → FootLinks renders the canonical-source + canonical-EXPLANATION lines only when their URLs are non-null (none are in v1). | **Sean post-plan** |
| Phase 3c.1 transactions `<RelatedBlock />` extension to render `namedInEssays[]` reverse entries | Forward-declared in Task 7.2 Step 6 as a conditional follow-up. If the architecture writeup's RelatedBlock surfaces the reverse line but the transactions deep-dives don't, the 4-way close is asymmetric — the architecture surface gets the close; the ledger surface needs the ~10-line component extension to also surface it. Land inline within Phase 3c.2 if Step 5 grep returns 0 on the transactions URLs; otherwise defer to v1.1. | **Inline in 3c.2 OR v1.1 of 3c.1** |
| Reading-time auto-computation from word count | Architecture-spec OPEN-4 default: manual for v1. Adds build complexity for low payoff at 3-5 writeup scale. | v2 |
| Aggregated root `/rss.xml` (transactions + architecture + essays) | Architecture-spec §12.2 + essays-spec §12.2 + BLUEPRINT §4.7 — gated until `/essays/` >1 entry. V1 has 1 essay; aggregated feed is v2. | v2 |
| Per-surface RSS feeds (`/architecture/draft/rss.xml`, etc.) | Specs §12 — single feed per surface is the v1 discipline. | v2 |
| Year filter, status filter, tag filter on either surface | Architecture-spec §17 + essays-spec §18 — small collections, no filter axis needed. | v2 |
| Sticky TOC for long essays | Architecture-spec §17 + essays-spec §18 — matches /transactions/ + /work/ restraint. | v2 |
| Interactive Mermaid diagrams | Architecture-spec §17 + essays-spec §18 — static SVG only via astro-mermaid SSR. | v2 |
| OG image auto-generation via satori | Specs §17 + §18 — v1 uses static PNG per slug (placeholder `ogImage: /og-cards/essays/meaning-over-access.png` declared in the essay frontmatter; Sean authors the PNG post-plan and drops it in `public/og-cards/essays/`). | v2 |
| Cross-surface View Transition morphs (architecture ↔ ledger, essay ↔ ledger ↔ architecture) | Architecture-spec §15 + essays-spec §15 — v2 deferred (risk of too many shared transition targets). | v2 |
| Thesis-clustering on `/essays/` index when 5+ entries | Essays-spec §17 OPEN-4 default — defer until 5+ essays. | v2 |
| Inline Substack embed widget OR inline newsletter signup form | Essays-spec §16.3 + §16.4 — intentional omission per OPEN-5 default. | Killed v1; v2 if Sean later wants the form |
| Mermaid mmdc PNG fallback (build-time PNG export instead of SSR SVG) | OQ-B default: ship SSR. The PNG fallback is documented as the v1.1 escape hatch only if the rendered SVG looks broken on either placeholder. | v1.1 escape hatch only |
| Daily Driver `architecture_total` / `essays_total` virtual patterns | Architecture-spec §13 + essays-spec §13: build-time render, not Daily Driver patterns. The home-hero dateline already fires `ledger_row` when an architecture or essay ships because each has a paired ledger row. Phase 4 wires the producer; v1 ships the build-time render. | Phase 4 |
| `/about/` page body | Phase 3d. | **Phase 3d** |
| Plausible analytics; Vercel deploy; custom-domain DNS; hard cutover from V3 bridge | BLUEPRINT §6 Phase 4. | **Phase 4** |
| Re-attempting subagent-driven-development dispatch | Phase 2 + 2b + 3a + 3b + 3c.1 + 3c.2 (this plan) all default to direct controller execution. Re-attempt only if the harness has demonstrably improved between this phase and Phase 3d. | Each subsequent planning session re-evaluates |

---

## Self-review summary

- **Spec coverage:** architecture-spec §18 items 1-18 + essays-spec §19 items 1-19 are each covered by a verification step in Section 9 (Task 9.1 + Task 9.2). The schemas (§3) land in Task 2.1; both collections honor their Appendix B field names exactly (camelCase). The 11 deep-dive bands from architecture §2.2 are composed in Task 6.2's `[slug].astro` from 6 architecture components (`<ThesisPullQuote />`, `<ErDiagram />`, `<Scoreboard />`, `<HonestNote />`, `<TryItYourself />`, `<RelatedBlock />`) + 2 Phase-3b shared imports (`<MethodsStrip />` + `<FourQBlock />`). The 12 essays bands from essays §2.2 are composed in Task 6.4's `[slug].astro` from 7 essays components + the shared `<ThesisPullQuote />` (imported from architecture/) + `<FourQBlock />`. The cross-link graph 4-way close (§14 on both specs) is built in Task 3.3 with the architecture + essays passes; essays' `plottedArtifacts[]` reverse-renders onto every cited ledger + architecture surface. The 4Q dual-mode contract (OQ-C inline-body fallback) is implemented in both deep-dives' `useFourQBlock` branch. Mermaid SSR (architecture §9.2 + essays §8.2) lands in Task 1.1 via `astro-mermaid` + the 5-var palette override. Validator's JD URL HEAD checks + roleMap.lastValidated staleness (warnings only per essays §9.2) land in Task 3.2. View Transition seams `arch-title-<slug>` + `essay-title-<slug>` (§15 on both) are wired between `<WriteupRow />` + architecture `[slug].astro` (Task 4.5 + Task 6.2) and between `<EssayRow />` + essays `[slug].astro` (Task 5.4 + Task 6.4).

- **Phase 3c.1 forward declaration closes automatically.** The transactions row `src/content/transactions/vault-knowledge-mcp.mdx` ships in Phase 3c.1 with `relatedArchitecture: vault-knowledge-mcp` — a forward declaration that Phase 3c.1's derive_crosslinks accepted because `archSlugs` was empty. Task 7.1 of this plan creates `src/content/architecture/vault-knowledge-mcp.mdx`; the next prebuild run populates `archSlugs` + the cross-link graph closes bidirectionally with zero code changes downstream. The architecture writeup's Related block auto-renders `→ ledger row: …` via the `archByLedger` reverse-derivation in derive_crosslinks (Task 3.3). The ledger row's Related block (Phase 3c.1) renders `→ architecture: …` via the forward declaration that's now resolvable.

- **Phase 3b + 3c.1 component reuse:** `<MethodsStrip />` + `<FourQBlock />` from `case-study/` are imported by architecture `[slug].astro` (Task 6.2) directly — no fork. `<MethodsStrip />` is NOT imported by essays `[slug].astro` (Task 6.4) because the essays schema has no `methods[]` field per essays-spec §3.1. `<FourQBlock />` IS imported by essays `[slug].astro`. The `<ThesisPullQuote />` primitive lands in `architecture/` (Task 4.4) and is imported by essays `[slug].astro` per BLUEPRINT §3.2.

- **Open questions resolved:** All 4 OQs from the kickoff prompt locked in the header table. OQ-A (Mermaid 5-var palette) → ship v1 with the 5-var override; observe; expand only if visually inconsistent. OQ-B (Mermaid SSR vs. mmdc PNG fallback) → ship SSR; mmdc is the v1.1 escape hatch. OQ-C (inline-body 4Q fallback on architecture + essays placeholders) → same as Phase 3c.1's OQ3 (a) posture; document the deviation in the validator + each placeholder. OQ-D (split 3c.2 from 3c.3?) → ship together; ~26 tasks; comfortably below the 30-35 ceiling.

- **Placeholders:** Every text Sean will eventually replace is marked `[PLACEHOLDER —` for grep-ability. The architecture Q1 + essay opening hook + essay closing kicker carry real prose (production-ready cold-read material); the architecture Q2-Q4 + essay middle 3 sections carry placeholders. All component-shaped names in body prose are backticked per the Phase 3c.1 MDX-gotcha lesson (commit 07a8cf1).

- **Type consistency:** `methods[]` shape (`{ task, tool, cost?, link? }`) is consistent between `src/content/config.ts` (Task 2.1 — `cost` is now optional to accept the architecture schema's omission), the validator (Task 3.2), the derive_crosslinks methods-extraction (Task 3.3 — unchanged from Phase 3c.1), `MethodsStrip.astro` Props interface (Phase 3b carry-over), and both placeholder MDXs (Tasks 7.1 + 7.2). The architecture `scoreboard` shape (`{ columns: string[], rows: { label, scores[] } }`) is consistent between schema (Task 2.1), validator's row/column count check (Task 3.2), `Scoreboard.astro` Props (Task 4.1), and the Vault Scorecard MDX (Task 7.1). The essays `quadrantLegend[]` shape (`{ key, label, artifact? }`) is consistent between schema, derive_crosslinks `extractQuadrantLegendArtifacts` (Task 3.3), `QuadrantLegend.astro` Props (Task 5.1), `PlottedArtifacts.astro` Props (Task 5.3), and the manifesto MDX (Task 7.2). The essays `roleMap` shape (`{ lastValidated, rows: { buyer, position, vocabularyTell, jdUrl } }`) is consistent between schema, validator's HEAD check + staleness check (Task 3.2), `RoleMap.astro` Props (Task 5.2), and the manifesto MDX (Task 7.2). The view-transition-name templates `arch-title-${slug}` (Task 4.5 `<WriteupRow />` + Task 6.2 `<h1>`) and `essay-title-${slug}` (Task 5.4 `<EssayRow />` + Task 6.4 `<h1>`) are byte-identical between row + page on each surface. The cross-link field names (`relatedLedgerRow`, `relatedCaseStudy`, `relatedEssay`, `relatedArchitecture`, `relatedEssays`, `plottedArtifacts`) are consistent between schemas (Task 2.1), derive_crosslinks extraction (Task 3.3), RelatedBlock Props on both surfaces (Tasks 4.5 + 5.4), and both placeholder MDXs.

- **Routing-collision safety:** Neither `/architecture/` nor `/essays/` declares a `[category]/index.astro` route. Each surface ships a single `[slug].astro` dynamic route + a static `index.astro` + a static `rss.xml.ts`. No `forbiddenSlugs` entries on either schema. If a future iteration adds a second dynamic route under either surface, re-spec the surface IA before landing it.

- **Forward declarations:** `relatedEssay` slots in transactions (Phase 3c.1) + architecture (this plan) point at `/essays/<slug>/` resolvable on first build after Task 7.2 ships the manifesto. The architecture page's `<RelatedBlock />` (Task 4.5) renders the link with the resolved title via `titleByEssaySlug`. No code changes after Task 7.2 — the graph self-closes.

- **No new npm dependencies except `astro-mermaid`.** Astro 5 + Tailwind 4 + Node 20's built-in `fetch` (for HEAD checks + canonical fetches) + Astro's built-in `<Code />` Shiki integration (for Try It Yourself's bash highlight) cover everything else. `@astrojs/rss` was installed in Phase 3c.1.

- **OQ-C deviation is reversible.** When Sean ships upstream `VAULT_AS_AGENT_INFRASTRUCTURE.md` + `VAULT_AS_AGENT_INFRASTRUCTURE_EXPLANATION.md` + `MEANING_OVER_ACCESS.md` + `MEANING_OVER_ACCESS_EXPLANATION.md`, the placeholder MDX edits are: swap 4 `null` URLs to real URLs + (optionally) drop the 4 inline-body h2 sections from each MDX body. The validator's `requiredOneOf: [explanationUrl, __inline_4Q__]` rule remains in place — when explanationUrl is non-null, the rule satisfies without needing the inline-body 4Q. The architecture schema can later be tightened to strict canonical-only per architecture-spec §3.1 by removing `__inline_4Q__` from the `requiredOneOf` array; that's a 1-line edit in `scripts/validate_content.mjs`.

---

## Phase 3d hand-off prompt

> sw-ai-pm-portfolio — Phase 3d planning. Phases 3a + 3b + 3c.1 + 3c.2 shipped on branch `phase-2-foundations`. Site chrome + case-study body + `/transactions/` + `/architecture/` + `/essays/` are all in. The 4-collection content graph closes 4-way via `scripts/derive_crosslinks.mjs`; `astro-mermaid` SSR renders erDiagram + quadrantChart inline; `<MethodsStrip />` + `<FourQBlock />` + `<ThesisPullQuote />` are shared primitives imported across surfaces.
>
> Your sole job this session: write the Phase 3d implementation plan via `/writing-plans` for the `/about/` page body per [`about-spec-v1.md`](../../specs/about-spec-v1.md). The about page is the human-self surface — Sean's lead line ("Raised by Saturday morning cartoons and Vercel deployment logs."), the daily-dated pulse strip reading `/api/about-pulse.json`, the 8-10-row timeline (Newsreader-coded Sedaris voice), the 6 Saturday-morning cartoon cels (already authored at `reference-images/about-cartoons/`), and Sean's full-body character (already authored at `reference-images/about-full-body.png`). The about-spec §17 validator (`scripts/validate_about.mjs`) lands here — asserts MDX `lead:` matches PMP §4 byte-for-byte.
>
> Default to direct controller execution. Subagent dispatch has been unreliable through Phase 2 / 2b / 3a / 3b / 3c.1 / 3c.2. Mirror the Phase 3c.2 plan structure: header directive, File Structure tree, Section-by-section task list with verbatim file contents, atomic commits per task with HEREDOC commit messages, DoD walk, deferral table, self-review.
>
> Out of scope for 3d: any further work on /architecture/ or /essays/ (both shipped v1); Daily Driver agent extensions (Phase 4); Plausible / Vercel / DNS (Phase 4). Stop after the plan is committed.

---

*Drafted 2026-05-21 from the Phase 3c.2 kickoff prompt. Open Questions A-D resolved in the plan header. Phase 3d (about page body) follows in a fresh session before the Phase 4 deploy. The branch state at plan-write time is `phase-2-foundations` HEAD `07a8cf1`; the plan's tasks land ~28-32 commits later.*

