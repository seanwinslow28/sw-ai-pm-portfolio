# Phase 3c.1 Implementation Plan — `/transactions/` Ledger Surface

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (direct controller execution) to implement this plan task-by-task. **Do NOT dispatch via superpowers:subagent-driven-development** — Phase 2 / 2b / 3a / 3b all attempted subagent dispatch and all hit hallucinated context constraints in the implementer subagent. Direct controller execution worked. Re-attempt subagents only if the harness has demonstrably improved since Phase 3b. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship `/transactions/` — the wire-service ledger of every shipped agent-fleet artifact — per [`transactions-spec-v1.md`](../../specs/transactions-spec-v1.md). Three route shapes (index / per-surface SSG / per-slug deep-dive) + RSS feed + extended prebuild gates + one-shot V3-bridge migration script (shipped un-run). Reuses Phase 3b's `<FourQBlock />` + `<MethodsStrip />` shared components verbatim by import.

**Architecture:** Astro 5 content collection (`transactions`) + 3 dynamic routes + RSS endpoint. Phase 3b's three prebuild scripts (`fetch_canonical_sources.mjs`, `validate_content.mjs`, `derive_crosslinks.mjs`) get extended in place at their declared extension points — no new scripts, no forks. The deep-dive composes the dateline → title block → value-prop pullquote → optional Loom → `<MethodsStrip />` → `<FourQBlock />` → Limitations → Related → Next/Prev (13 bands per spec §9.1). The index lists rows sorted desc by `shipped` date; the per-surface SSG pages narrow the list via filter pills (real `<a>` elements, no client-side filter). View Transition seam `ledger-title-<slug>` morphs the row title into the deep-dive `<h1>` per spec §15. Cross-link graph closes bidirectionally via the extended `derive_crosslinks.mjs`: a transaction with `relatedCaseStudy: code-brain` auto-renders "← named in transactions" on `/work/code-brain/` (forward declared in 3c.1; the case-study page consumes it once Sean authors that surface section post-plan).

**Tech Stack:** Astro 5 + Tailwind 4 (already installed Phase 2), `@astrojs/rss` (NEW), Node 20 for prebuild scripts, vanilla JS where dynamic behavior is unavoidable. No React. No client-side filter library.

**Scope:** Matches [`transactions-spec-v1.md`](../../specs/transactions-spec-v1.md) §§2-19 plus the `relatedArchitecture` additive flagged in [`architecture-spec-v1.md`](../../specs/architecture-spec-v1.md) §14.1 (per [`BLUEPRINT-COMPLETE.md`](../../specs/BLUEPRINT-COMPLETE.md) §5 punch-list #1).
- **IN:** transactions content collection schema (with `relatedArchitecture` additive folded in); three prebuild-script extensions (validator, fetcher, crosslink derivator); one-shot `scripts/migrate_v3_transactions.mjs` (ships un-run); 9 transactions-specific components (`IndexHeader`, `FilterPills`, `LedgerRow`, `ValuePropPullQuote`, `LoomPoster`, `LimitationsBlock`, `RelatedBlock`, `FooterFold`, `DeepDiveTitleBlock`); 4 routes (`index.astro`, `[surface]/index.astro`, `[slug].astro`, `rss.xml.ts`); 3 hand-written placeholder MDX entries (`phase-d-typed-edges` + `intent-engineering-mcp` + `vault-knowledge-mcp`) exercising the layout + cross-link graph end-to-end; DoD walk-through against spec §18 items 1-16.
- **OUT (deferred to Phase 3c.2):** `/architecture/` route + `/essays/` route + their components (`<Scoreboard />`, `<HonestNote />`, `<TryItYourself />`, `<QuadrantChart />`, `<QuadrantLegend />`, `<RoleMap />`, `<PlottedArtifacts />`), `astro-mermaid` integration, the unified-validator merge per essays spec §11.2. Hand-off prompt for 3c.2 lives at the foot of this plan.
- **OUT (Sean authors post-plan):** Running the V3 migration script (script ships in §3 un-run; Sean executes when ready); real ledger entries authored from canonical EXPLANATION sources (placeholders ship with sentinel prose marked `[PLACEHOLDER —` for grep-ability); the `relatedLedgerRow` field on `/work/<slug>/` pages' Related blocks (forward-declared in this plan; consumed by case-study chrome once Sean authors that integration — Phase 3b didn't ship a Related block on case-study pages).
- **OUT (Phase 3d):** `/about/` page body.
- **OUT (Phase 4):** Daily Driver `ledger_row` hero dateline pattern wiring (the *consumer* in `dateline.json` already exists from Phase 2; the *producer* logic — reading `src/content/transactions/*.md` filesystem at 08:45 to compute count + new-arrival signal — is Phase 4 agent work); Plausible analytics; Vercel deploy; custom-domain DNS.
- **OUT entirely (v2):** Cross-surface View Transition morphs (Methods strip cell → ledger row); aggregated root `/rss.xml`; per-surface RSS feeds; JSON Feed; year/status/tag filters; full-text search; recency badges.

**Open Question resolutions (locked at planning time):**

| OQ | Decision | Why |
|---|---|---|
| **OQ1 — split or single plan?** | **SPLIT.** Phase 3c.1 = transactions only (~25 tasks, this plan). Phase 3c.2 = architecture + essays (separate fresh session post-3c.1 execution). | Transactions alone has ~25 tasks plus the V3-bridge migration + the `relatedArchitecture` additive — comparable to Phase 3a's footprint. Adding architecture + essays (with the Mermaid integration + the unified-validator merge + 4 net-new components per surface) pushes the combined plan to 50+ tasks. Phase 3b at 37 tasks already stretched the executing-plans pattern; 50 would risk implementer drift. Sequencing 3c.1 → 3c.2 also lets the second plan import any patterns 3c.1 establishes (RSS endpoint shape, per-collection index template, content-collection-with-view-transition seam) without modification. Cost of the split: ~10% spec re-read overhead at the start of 3c.2. Benefit: each plan stays within the controller-execution comfort zone, and the V3 migration risk stays scoped to 3c.1. |
| **OQ2 — hero-media asset gaps?** | **No gaps.** Verified [`public/assets/projects/`](../../../public/assets/projects/) contains all 5 files (`animation-pipeline.webp`, `code-brain.webp`, `intent-engineering-mcp.png`, `the-block.webp`, `16bitfit.webp`). Transactions deep-dive does NOT reference new hero-media paths — it renders the value-prop pullquote, optional Loom poster (default null in v1), Methods strip, 4Q block, and chrome bands. No Task 0 asset audit required beyond Phase 3b's preflight. | The transactions spec §2.2 deep-dive anatomy has no `hero_media` analog — the surface is dense-information / less-narrative than a case study. The optional `loomPosterUrl:` field renders to nothing when null, which is the v1 default for all 3 placeholders. |
| **OQ3 — V3-bridge migration approach?** | **Approach (a) — write the script, leave it un-run.** Plan ships `scripts/migrate_v3_transactions.mjs` per transactions spec §11.1 + §12.2 (one-shot interactive); Sean runs it manually when he wants to backfill canonical V3 data. Phase 3c.1 ships 3 hand-written placeholder transactions to exercise the layout: `phase-d-typed-edges` (fleet · SHIPPED with inline-body 4Q to test the V3-fallback path) + `intent-engineering-mcp` (product · SHIPPED with `valueProp` mirroring the existing /work/ slug to test the bidirectional cross-link) + `vault-knowledge-mcp` (product · ACTIVE with `relatedArchitecture: vault-knowledge-mcp` to exercise the Phase 3c.2 cross-link slot ahead of architecture surface arrival). | Continuous-execution posture is non-negotiable. Interactive prompts break it. Approach (b) — non-interactive defaults — risks silent miscategorization (the spec §12.3 explicitly excludes mass-mapping of `cushion-economics` → surface). Approach (c) — skip entirely — loses the "ledger ships with real data day one" recruiter signal AND the migration script itself, which is reusable infrastructure. (a) preserves both: the script lives in the repo, runnable one command at a time when Sean decides; the 3 placeholders cover all 5 surfaces' filter pages (3 different surfaces × 5 enum values, validated by §5.2's getStaticPaths) and exercise the cross-link graph forward-and-backward. Slugs `phase-d-typed-edges` + `intent-engineering-mcp` are byte-identical to V3 — when Sean runs the migration later, conflicts surface immediately and Sean adjudicates. |

**Branch state:** `phase-2-foundations` at HEAD `62c01c3` (12 commits past Phase 3a baseline, +32 commits Phase 3b). 2 untracked screenshots (`phase-2b-full.png`, `teaser-closeup.png`) are pre-existing; ignore. **Do NOT switch branches.** Sean's standing rule is "Sean works on `main`" but Phase 2 / 2b / 3a / 3b accumulated on this branch — the spec'd cutover to main is Phase 4. Each task in this plan commits atomically; ~30-35 incremental commits total.

**Operating posture (direct controller execution):** Run each task in order. After each commit, verify the next task's preconditions are still true (e.g., dev server still serving 200 on existing routes, `npm run validate` still passes). Surface BLOCKED status only if genuinely stuck (real error in build log, route returning non-2xx that should be 200). Don't pause to check in between routine tasks — the user signed off on continuous execution.

---

## File Structure

Files this plan creates or modifies, organized by responsibility:

```
sw-ai-pm-portfolio/
├── package.json                                          ← Task 6.1: add @astrojs/rss dependency
│
├── scripts/
│   ├── fetch_canonical_sources.mjs                       ← Task 2.2: extend WALK_COLLECTIONS array
│   ├── validate_content.mjs                              ← Task 2.1: extend COLLECTIONS array
│   ├── derive_crosslinks.mjs                             ← Task 2.3: rewrite — bidirectional graph
│   └── migrate_v3_transactions.mjs                       ← Task 3.1: NEW — one-shot, ships un-run
│
├── src/
│   ├── content/
│   │   ├── config.ts                                     ← Task 1.1: add transactions schema + relatedArchitecture additive
│   │   └── transactions/                                 ← NEW folder
│   │       ├── phase-d-typed-edges.mdx                   ← Task 7.1: fleet · SHIPPED · inline-body 4Q
│   │       ├── intent-engineering-mcp.mdx                ← Task 7.2: product · SHIPPED · 4Q via explanationUrl path
│   │       ├── vault-knowledge-mcp.mdx                   ← Task 7.3: product · ACTIVE · forward-declared relatedArchitecture
│   │       └── .crosslinks.json                          ← Task 2.3: derived at build (gitignored)
│   │
│   ├── components/
│   │   └── transactions/                                 ← NEW folder
│   │       ├── IndexHeader.astro                         ← Task 4.1
│   │       ├── FilterPills.astro                         ← Task 4.2
│   │       ├── LedgerRow.astro                           ← Task 4.3
│   │       ├── ValuePropPullQuote.astro                  ← Task 4.4
│   │       ├── LoomPoster.astro                          ← Task 4.5
│   │       ├── LimitationsBlock.astro                    ← Task 4.6
│   │       ├── RelatedBlock.astro                        ← Task 4.6
│   │       ├── FooterFold.astro                          ← Task 4.6
│   │       └── DeepDiveTitleBlock.astro                  ← Task 4.6
│   │
│   ├── pages/
│   │   └── transactions/
│   │       ├── index.astro                               ← Task 5.1: ALL filter index
│   │       ├── [surface]/
│   │       │   └── index.astro                           ← Task 5.2: SSG per surface (5 outputs)
│   │       ├── [slug].astro                              ← Task 5.3: deep-dive
│   │       └── rss.xml.ts                                ← Task 6.2: RSS endpoint
│   │
│   └── styles/
│       └── transactions.css                              ← Task 5.1: page-scoped styles
│
├── .gitignore                                            ← Task 2.3: add src/content/transactions/.crosslinks.json
│
└── docs/superpowers/plans/
    └── 2026-05-21-phase-3c.1-transactions.md             ← this file
```

The `transactions/` folders are new under both `src/components/` and `src/pages/`. Mirrors the Phase 3b `case-study/` + `artifacts/` + `annotations/` split.

**Astro routing collision note (load-bearing for §5):** `src/pages/transactions/[surface]/index.astro` and `src/pages/transactions/[slug].astro` both resolve to `/transactions/<value>/`. Astro 5 evaluates this as a collision unless the two `getStaticPaths()` return DISJOINT sets. Phase 3c.1 guarantees disjointness via the validator: Task 2.1 asserts no transaction slug matches the 5 surface enum values (`fleet` / `pipeline` / `product` / `writing` / `infra`). The 3 hand-written placeholder slugs in §7 are all multi-word kebab-case, so no collision in practice — but the validator catches the typo case.

---

## Section 0 — Pre-flight

### Task 0.1: Verify Phase 3b baseline + clean working tree

**Files:**
- Read-only verification

- [ ] **Step 1: Verify branch + HEAD + working tree state**

Run: `git branch --show-current && git rev-parse HEAD && git status --porcelain | grep -v "phase-2b-full.png\|teaser-closeup.png"`

Expected: branch `phase-2-foundations`, HEAD `62c01c3` (or later if any v1.1 follow-ups landed since planning), `git status --porcelain` returns no output once the two untracked screenshots are filtered out.

- [ ] **Step 2: Verify Phase 3b artifacts are in place**

Run in parallel (3 reads):
- `ls scripts/fetch_canonical_sources.mjs scripts/validate_content.mjs scripts/derive_crosslinks.mjs`
- `ls src/components/case-study/FourQBlock.astro src/components/case-study/MethodsStrip.astro`
- `ls src/content/work/*.mdx`

Expected: all paths exist. The work collection has 5 MDX entries.

- [ ] **Step 3: Verify prebuild gates pass on existing state**

Run: `npm run validate && node scripts/derive_crosslinks.mjs && node scripts/fetch_canonical_sources.mjs`

Expected: all three exit 0. The validator prints `done.`, derive_crosslinks prints `done. validated N methods cross-links.`, fetch_canonical_sources prints `done.`. If any fails, fix the regression before continuing — Phase 3c.1 builds on a green Phase 3b baseline.

- [ ] **Step 4: Start dev server in background**

Run: `npm run dev` in a background terminal (or confirm one is already running via `curl -sI http://localhost:4321/ | head -1` → `HTTP/1.1 200 OK`).

- [ ] **Step 5: No commit**

Verification only.

---

## Section 1 — Content collection schema

The transactions collection lands in `src/content/config.ts` alongside the existing `work` and `teaserDeck` collections. The `relatedArchitecture` field is included from day one per the architecture-spec §14.1 additive (BLUEPRINT-COMPLETE §5 punch-list #1) — folded in directly, no separate task needed since Phase 3c.1 is the first opportunity.

### Task 1.1: Extend `src/content/config.ts` with the `transactions` schema

**Files:**
- Modify: `src/content/config.ts`

Per transactions spec §3.2 + architecture spec §14.1. The schema mirrors the spec's camelCase YAML field names exactly. Note the deliberate divergence from Phase 3b's `work` schema casing (the work schema uses `snake_case` per case-study spec Appendix B; transactions uses `camelCase` per transactions spec Appendix B) — each collection follows its own spec verbatim. The validator (Task 2.1) dispatches per collection with the right field names.

- [ ] **Step 1: Read existing config**

Run: `wc -l src/content/config.ts`

Expected: ~73 lines. Confirm before editing — the edit appends a new collection definition without touching the existing `work` or `teaserDeck` blocks.

- [ ] **Step 2: Replace `src/content/config.ts` with the extended version**

Replace the entire file with:

```ts
import { defineCollection, z } from "astro:content";

const methodRow = z.object({
  task: z.string(),
  tool: z.string(),
  cost: z.string(),
  link: z.string().nullable().optional(),
});

const fourQ = z.object({
  what: z.string(),
  why: z.string(),
  break: z.string(),
  learn: z.string(),
});

const work = defineCollection({
  type: "content",
  schema: z.object({
    // `slug` is reserved by Astro — comes from frontmatter override or filename
    frame: z.enum(["A-1", "A-2", "A-3", "A-4", "A-5"]),
    title: z.string(),
    tagline: z.string().optional(),
    status: z.enum(["ACTIVE", "COMING", "PAUSED", "ARCHIVED", "SHIPPED"]),
    tags: z.array(z.string()),
    hero_media: z.string(),
    hero_media_type: z.enum(["video", "image"]),
    hero_media_alt: z.string(),
    order: z.number().int().min(1).max(5),
    date_started: z.coerce.string().optional(),
    date_active_through: z.coerce.string().optional(),
    case_study_dateline_pattern: z
      .enum(["fleet_pulse", "ship_log", "reading_log", "now_line", "ledger_row"])
      .optional(),

    // --- Phase 3b additions (case-study-spec Appendix B) ---
    role: z.string(),
    anchor_metric: z.string(),
    investigation_order: z.enum(["reverse", "forward"]).default("reverse"),

    methods: z.array(methodRow).min(1),

    // One of (four_q, explanation_url) is required — enforced by
    // scripts/validate_content.mjs since Zod can't express "one-of" cleanly.
    four_q: fourQ.optional(),
    explanation_url: z.string().url().optional(),

    // --- Status-specific frontmatter (validator enforces presence per status) ---
    shipped_at: z.coerce.string().optional(),
    shipped_stats_endpoint: z.string().optional(),
    return_condition: z.string().optional(),
    archived_reference_url: z.string().url().optional(),
  }),
});

// ============================================================
// Phase 3c.1 — transactions collection
// Source: transactions-spec-v1.md §3.2
//        + architecture-spec-v1.md §14.1 (relatedArchitecture additive)
//        + BLUEPRINT-COMPLETE.md §5 punch-list #1
// ============================================================

const SURFACE_ENUM = ["fleet", "pipeline", "product", "writing", "infra"] as const;

const transactions = defineCollection({
  type: "content",
  schema: z.object({
    // --- Identity ---
    title: z.string(),
    dateline: z.string(),                                    // "BOSTON, MAY 13, 2026"
    shipped: z.string().regex(/^\d{4}-\d{2}-\d{2}$/),        // ISO YYYY-MM-DD

    // --- IA + status ---
    surface: z.enum(SURFACE_ENUM),
    status: z.enum(["ACTIVE", "SHIPPED", "PAUSED", "ARCHIVED"]),

    // --- Comprehension layer ---
    valueProp: z.string().max(280),
    methods: z.array(methodRow).min(1),
    limitations: z.array(z.string()).min(1),

    // --- 4Q source (one of two required — enforced by validate_content.mjs) ---
    explanationUrl: z.string().url().optional(),
    // OR: inline-body 4Q headings (V3-bridge fallback) — validator detects the
    // presence of all 4 canonical `## What is this?` / `## Why this approach?`
    // / `## What would break?` / `## What did I learn?` h2s in the MDX body.

    // --- Cross-links (bidirectional graph; derive_crosslinks.mjs enforces resolution) ---
    relatedCaseStudy: z.string().optional(),                 // /work/<slug>
    relatedTransactions: z.array(z.string()).optional(),
    relatedEssay: z.string().optional(),                     // /essays/<slug>  (consumed in 3c.2)
    previousVersion: z.string().optional(),
    relatedArchitecture: z.union([z.string(), z.array(z.string())]).optional(),
    // ↑ ADDITIVE per architecture-spec §14.1 / BLUEPRINT-COMPLETE §5 punch-list #1.
    //   Pure additive — legacy rows that omit the field still validate.
    //   Slug or array of slugs into /architecture/. Bidirectional via
    //   derive_crosslinks.mjs (Task 2.3). The /architecture/ surface itself
    //   ships in Phase 3c.2.

    // --- Optional editorial ---
    repoUrl: z.string().url().optional(),
    loomUrl: z.string().url().optional(),
    loomPosterUrl: z.string().url().optional(),
  }),
});

const teaserDeck = defineCollection({
  type: "data",
  schema: z.object({
    cards: z.array(
      z.object({
        cardIndex: z.number().int().min(0).max(9),
        src: z.string(),
        alt: z.string(),
        style: z.string(),
      })
    ).length(10),
  }),
});

export const collections = { work, teaserDeck, transactions };
```

- [ ] **Step 3: Verify Astro content sync**

Run: `npx astro sync 2>&1 | tail -20`

Expected: sync completes cleanly. No errors on `work` (existing entries are valid). No errors on `transactions` either — the collection directory `src/content/transactions/` doesn't exist yet, so Astro reports zero entries for it (Section 7 creates the 3 placeholder MDX files; Astro re-syncs automatically on first build that touches them).

If Astro errors with `Collection "transactions" not found at src/content/transactions`, that's acceptable for this task — the directory is created in Task 7.1. If Astro errors with a Zod parse issue on the `transactions` schema itself, fix the issue inline (most likely a typo in the field names or enum values).

- [ ] **Step 4: Commit**

```bash
git add src/content/config.ts
git commit -m "$(cat <<'EOF'
feat(phase-3c.1): extend content schema — transactions collection + relatedArchitecture additive

Adds the transactions collection per transactions-spec §3.2:
  - title, dateline, shipped (ISO regex)
  - surface (5-enum: fleet/pipeline/product/writing/infra), status (4-enum)
  - valueProp (max 280), methods[] (≥1), limitations[] (≥1)
  - explanationUrl OR inline-body 4Q (one-of, enforced by validator)
  - relatedCaseStudy, relatedTransactions[], relatedEssay,
    previousVersion, relatedArchitecture (string | string[])
  - repoUrl, loomUrl, loomPosterUrl (all optional editorial)

The relatedArchitecture additive comes from architecture-spec §14.1 +
BLUEPRINT-COMPLETE §5 punch-list #1. Pure additive — rows omitting the
field still validate. The /architecture/ consumer surface ships in
Phase 3c.2; this field is the cross-link slot ledger rows declare today.

Mirrors transactions-spec Appendix B field casing exactly (camelCase)
— deliberately different from the work schema (snake_case per
case-study-spec Appendix B). validate_content.mjs (Task 2.1)
dispatches per collection with the right field names.

Per transactions-spec §3.2 + architecture-spec §14.1.
EOF
)"
```

---

## Section 2 — Prebuild script extensions

Phase 3b shipped three prebuild scripts with `work`-collection-only scope, each with a Phase 3c TODO comment naming the extension point verbatim. Section 2 follows those extension points — no new scripts, no forks. The Phase 3c.2 plan extends the same scripts a second time to add `architecture` + `essays`.

### Task 2.1: Extend `validate_content.mjs` with the transactions collection

**Files:**
- Modify: `scripts/validate_content.mjs`

Per transactions spec §11.1 + Phase 3b's named extension point (the `// Phase 3c will add transactions / architecture / essays here.` comment in the COLLECTIONS array). Adds a new collection entry with the camelCase field names from the transactions schema. Also adds a transactions-specific rule: `slug must not match a surface enum value` (the Astro routing-collision safety net flagged in the File Structure note above).

- [ ] **Step 1: Read current validator**

Run: `wc -l scripts/validate_content.mjs`

Expected: ~202 lines. The COLLECTIONS array sits at lines 25-42; the new collection entry appends inside it.

- [ ] **Step 2: Edit the COLLECTIONS array + add the surface-collision check**

Replace the existing `COLLECTIONS` const declaration in `scripts/validate_content.mjs`:

```js
const COLLECTIONS = [
  {
    name: "work",
    contentDir: path.join(ROOT, "src/content/work"),
    requiredFields: [
      "frame", "title", "status", "hero_media", "hero_media_type",
      "hero_media_alt", "order", "role", "anchor_metric",
    ],
    requiredOneOf: [["four_q", "explanation_url"]],
    statusFields: {
      SHIPPED: ["shipped_at", "shipped_stats_endpoint"],
      PAUSED: ["return_condition"],
      ARCHIVED: ["archived_reference_url"],
    },
    requiredArrays: ["tags", "methods"],
  },
  {
    name: "transactions",
    contentDir: path.join(ROOT, "src/content/transactions"),
    requiredFields: [
      "title", "dateline", "shipped", "surface", "status", "valueProp",
    ],
    // explanationUrl OR inline-body 4Q (validator detects the latter
    // by scanning the MDX body for all 4 canonical h2 headings).
    requiredOneOf: [["explanationUrl", "__inline_4Q__"]],
    statusFields: {
      // SHIPPED has no extra required fields on the ledger (no shipped_at;
      // `shipped` IS the date). Status-specific fields are intentionally
      // narrower than work — transactions spec §3.2 doesn't require
      // shipped_stats_endpoint / return_condition / archived_reference_url
      // at the ledger row level. Those live on case-study pages only.
    },
    requiredArrays: ["methods", "limitations"],
    // Transactions-specific: slug must not collide with a surface enum value
    // (Astro routing safety — `src/pages/transactions/[surface]/index.astro`
    // and `src/pages/transactions/[slug].astro` both resolve under
    // /transactions/<value>/; disjoint slug sets prevent collision).
    forbiddenSlugs: ["fleet", "pipeline", "product", "writing", "infra"],
    // Cross-link fields whose value must resolve to a real slug in another
    // collection (derive_crosslinks.mjs Task 2.3 enforces; the validator
    // just notes them here for completeness).
    crossLinkFields: [
      "relatedCaseStudy", "relatedTransactions", "relatedEssay",
      "previousVersion", "relatedArchitecture",
    ],
  },
];
```

- [ ] **Step 3: Add the inline-4Q detection logic + the forbidden-slugs check to `validateCollection`**

Replace the body of `validateCollection` (currently lines ~143-180) with:

```js
async function validateCollection(coll) {
  const entries = await fs.readdir(coll.contentDir).catch(() => []);
  const errors = [];
  for (const fname of entries) {
    if (!fname.endsWith(".mdx") && !fname.endsWith(".md")) continue;
    const slug = fname.replace(/\.mdx?$/, "");
    const src = await fs.readFile(path.join(coll.contentDir, fname), "utf8");
    const fm = parseFrontmatter(src);
    if (!fm) {
      errors.push(`${coll.name}/${slug}: missing or malformed frontmatter`);
      continue;
    }
    for (const f of coll.requiredFields) {
      if (fm[f] === undefined || fm[f] === "") {
        errors.push(`${coll.name}/${slug}: missing required field "${f}"`);
      }
    }
    for (const arrField of coll.requiredArrays ?? []) {
      if (!Array.isArray(fm[arrField]) || fm[arrField].length === 0) {
        errors.push(`${coll.name}/${slug}: required array "${arrField}" is missing or empty`);
      }
    }
    // Forbidden slugs (transactions: slug must not match a surface enum value)
    for (const forbidden of coll.forbiddenSlugs ?? []) {
      if (slug === forbidden) {
        errors.push(`${coll.name}/${slug}: slug "${slug}" collides with the surface enum — pick a different slug`);
      }
    }
    // Inline-4Q detection — used by transactions' requiredOneOf
    const hasInline4Q = detectInline4Q(src);
    for (const oneOf of coll.requiredOneOf ?? []) {
      const present = oneOf.filter((f) => {
        if (f === "__inline_4Q__") return hasInline4Q;
        return fm[f] !== undefined && fm[f] !== "";
      });
      if (present.length === 0) {
        const label = oneOf.map((f) => (f === "__inline_4Q__" ? "inline-body 4Q headings" : f)).join(", ");
        errors.push(`${coll.name}/${slug}: must declare one of [${label}]`);
      }
    }
    const statusReq = coll.statusFields?.[fm.status] ?? [];
    for (const f of statusReq) {
      if (fm[f] === undefined || fm[f] === "") {
        errors.push(`${coll.name}/${slug}: status=${fm.status} requires field "${f}"`);
      }
    }
    process.stdout.write(`  ✓ ${coll.name}/${slug}\n`);
  }
  return errors;
}

/**
 * Scans an MDX file body (everything after the closing `---`) for the
 * 4 canonical EXPLANATION.md h2 headings. Returns true when all 4 are
 * present. Used by the transactions validator's requiredOneOf:
 * [explanationUrl, __inline_4Q__] — the V3-bridge fallback path.
 */
function detectInline4Q(src) {
  const bodyMatch = src.match(/^---\n[\s\S]+?\n---\n([\s\S]*)$/);
  if (!bodyMatch) return false;
  const body = bodyMatch[1];
  const required = [
    /^##\s+What is this\?/m,
    /^##\s+Why this approach\?/m,
    /^##\s+What would break\?/m,
    /^##\s+What did I learn\?/m,
  ];
  return required.every((re) => re.test(body));
}
```

- [ ] **Step 4: Update the header comment block**

Replace the existing JSDoc-style header at the top of `scripts/validate_content.mjs` with:

```js
#!/usr/bin/env node
/**
 * scripts/validate_content.mjs — schema validator.
 *
 * Phase 3b scope (still active): work collection.
 * Phase 3c.1 scope (added): transactions collection — required fields,
 *   array minimums, explanationUrl OR inline-body 4Q (one-of), forbidden
 *   slug collision check (slug must not match surface enum).
 * Phase 3c.2 will add: architecture + essays collections (the unified
 *   validator merge per essays-spec §11.2 also folds JD URL HEAD checks
 *   + `roleMap.lastValidated` staleness check at that point).
 *
 * Astro's content collection schema (src/content/config.ts) catches
 * most of this at build time, but THIS validator catches semantic
 * rules Astro can't express:
 *   - exactly one of {four_q, explanation_url} must be present (work)
 *   - exactly one of {explanationUrl, inline-body 4Q headings} (transactions)
 *   - status-specific frontmatter (work)
 *   - methods[] / limitations[] are non-empty
 *   - slug must not collide with a routing-level enum value
 *
 * Source: case-study-spec-v1.md §15 + transactions-spec-v1.md §11.1 +
 *         BLUEPRINT-COMPLETE.md §3.3.
 */
```

- [ ] **Step 5: Smoke-test (transactions dir doesn't exist yet — should not crash; should pass)**

Run: `npm run validate 2>&1 | tail -10`

Expected: validator walks `work` (5 entries, all pass), then walks `transactions` (directory doesn't exist; the `.catch(() => [])` in the readdir returns empty; loop emits no rows; no errors). Exits 0. Output ends with `done.`.

If validator crashes with `ENOENT` on the transactions directory, the readdir error swallow isn't working — verify the `.catch(() => [])` is in place.

- [ ] **Step 6: Commit**

```bash
git add scripts/validate_content.mjs
git commit -m "$(cat <<'EOF'
feat(phase-3c.1): extend validate_content.mjs — transactions collection

Adds transactions as a second entry in the COLLECTIONS array. New rules:
  - Required fields per transactions-spec §3.2: title, dateline, shipped,
    surface, status, valueProp
  - Required arrays: methods (≥1), limitations (≥1)
  - One-of: explanationUrl OR inline-body 4Q headings (the V3-bridge
    fallback path — validator scans the MDX body for the 4 canonical
    h2s when explanationUrl is absent)
  - Forbidden slug collision: slug must not be one of the 5 surface
    enum values (Astro routing safety net for the [surface]/ + [slug]
    dual-route shape — see plan §File Structure note)

The graceful no-dir behavior (transactions/ doesn't exist until §7
ships placeholder MDX) is preserved via readdir .catch.

Extends the named Phase 3c TODO in the previous COLLECTIONS array.
Phase 3c.2 extends a second time to add architecture + essays + the
JD URL HEAD checks per essays-spec §11.2 unified-validator merge.

Per transactions-spec §11.1 + BLUEPRINT-COMPLETE §3.3.
EOF
)"
```

### Task 2.2: Extend `fetch_canonical_sources.mjs` to walk transactions

**Files:**
- Modify: `scripts/fetch_canonical_sources.mjs`

Per transactions spec §11.1 — extend the WALK_COLLECTIONS array (Phase 3b left the extension point named verbatim in the comment) to add the transactions collection. The transactions collection uses `explanationUrl` (camelCase) where work uses `explanation_url` (snake_case); the per-collection `urlField` already handles the case divergence.

- [ ] **Step 1: Read current fetcher**

Run: `wc -l scripts/fetch_canonical_sources.mjs`

Expected: ~138 lines. The WALK_COLLECTIONS array sits at lines 24-34; the new collection entry slots in there.

- [ ] **Step 2: Update WALK_COLLECTIONS in `scripts/fetch_canonical_sources.mjs`**

Replace the existing `WALK_COLLECTIONS` const with:

```js
const WALK_COLLECTIONS = [
  {
    name: "work",
    contentDir: path.join(ROOT, "src/content/work"),
    urlField: "explanation_url",
    outDir: OUT_DIR,
  },
  {
    name: "transactions",
    contentDir: path.join(ROOT, "src/content/transactions"),
    urlField: "explanationUrl",
    outDir: OUT_DIR,
  },
  // Phase 3c.2 will add:
  //   { name: "architecture", contentDir: .../architecture, urlField: "explanationUrl", outDir: ... }
  //   + a SECOND walk for architecture's `essaySourceUrl` long-form fetch
  //     (different outDir: src/content/architecture/essays/)
  //   + { name: "essays", contentDir: .../essays, urlField: "explanationUrl", outDir: ... }
  //   + a SECOND walk for essays' `sourceUrl` body fetch
  //     (different outDir: src/content/essays/essay-bodies/)
];
```

- [ ] **Step 3: Update the header comment block**

Replace lines 1-15 (the JSDoc-style header) with:

```js
#!/usr/bin/env node
/**
 * scripts/fetch_canonical_sources.mjs — canonical 4Q fetcher.
 *
 * Phase 3b scope (still active): work collection (`explanation_url` field).
 * Phase 3c.1 scope (added): transactions collection (`explanationUrl` field).
 * Phase 3c.2 will add: architecture (`explanationUrl` for 4Q +
 *   `essaySourceUrl` for the long-form essay body, different outDir)
 *   and essays (`explanationUrl` for 4Q + `sourceUrl` for the body).
 *
 * Walks each collection's MDX files looking for entries that declare
 * the named URL field, fetches the markdown raw via HTTPS, validates
 * against the 4 canonical EXPLANATION headings, writes to the per-
 * collection outDir. ETag-cached via .cache/canonical-sources.lockfile
 * so subsequent builds skip unchanged sources.
 *
 * Graceful no-dir: missing content directories return empty.
 *
 * Source: case-study-spec-v1.md §9 + §15; transactions-spec-v1.md §11.1;
 *         BLUEPRINT-COMPLETE.md §3.3.
 */
```

- [ ] **Step 4: Update the no-dir handling in `walkCollection`**

The Phase 3b version uses `fs.readdir(coll.contentDir)` directly, which throws ENOENT when the directory doesn't exist. The transactions collection directory doesn't exist until Section 7 creates it. Replace lines ~92-93:

```js
async function walkCollection(coll, cache) {
  const entries = await fs.readdir(coll.contentDir).catch(() => []);
  for (const fname of entries) {
```

- [ ] **Step 5: Update the stdout banner**

Replace the `process.stdout.write("\nfetch_canonical_sources.mjs — Phase 3b scope (work)\n");` line at the top of `main()` with:

```js
  process.stdout.write("\nfetch_canonical_sources.mjs — Phase 3c.1 scope (work + transactions)\n");
```

- [ ] **Step 6: Smoke-test (no MDX declares explanationUrl yet — should be a no-op)**

Run: `node scripts/fetch_canonical_sources.mjs`

Expected output:
```
fetch_canonical_sources.mjs — Phase 3c.1 scope (work + transactions)

[work]

[transactions]

done.
```

Zero fetches because no MDX entry declares `explanationUrl:` or `explanation_url:` yet. The transactions placeholders in Section 7 ship with inline-body 4Q (V3-bridge style), not `explanationUrl` — so the fetcher remains idle through Phase 3c.1.

- [ ] **Step 7: Commit**

```bash
git add scripts/fetch_canonical_sources.mjs
git commit -m "$(cat <<'EOF'
feat(phase-3c.1): extend fetch_canonical_sources.mjs — transactions collection

Adds a second entry to WALK_COLLECTIONS for the transactions content
collection, walking src/content/transactions/*.mdx for the camelCase
`explanationUrl` frontmatter field. Same canonical-4Q-headings
validation + ETag cache logic as the work walk.

The graceful no-dir behavior (transactions/ doesn't exist yet)
is handled via readdir .catch — the script logs the [transactions]
header and moves on with zero fetches.

Ships idle in Phase 3c.1 — the 3 placeholder MDXs use inline-body 4Q
(V3-bridge fallback), not explanationUrl. The fetcher activates when
Sean adds explanationUrl to any future transaction entry, or when the
Phase 3c.2 architecture + essays surfaces (which require canonical
explanationUrl per spec) bring upstream EXPLANATION.md files online.

Extends the named Phase 3c TODO in the previous WALK_COLLECTIONS array.

Per transactions-spec §11.1 + BLUEPRINT-COMPLETE §3.3.
EOF
)"
```

### Task 2.3: Rewrite `derive_crosslinks.mjs` — bidirectional graph derivation

**Files:**
- Modify: `scripts/derive_crosslinks.mjs`
- Modify: `.gitignore`

Per transactions spec §11.1 + §14.1 + §14.2. Phase 3b's version validated only `methods[].link` references; Phase 3c.1 expands to derive the bidirectional cross-link graph: every `relatedCaseStudy` / `relatedTransactions[]` / `relatedEssay` / `previousVersion` / `relatedArchitecture` declaration in a transactions MDX must resolve to a real slug in the target collection. The script also writes `src/content/transactions/.crosslinks.json` so the deep-dive's `<RelatedBlock />` (Task 4.6) can read the reverse-derived links at build time.

The forward direction (transactions → other surfaces) is enforced today. The reverse direction (other surfaces → transactions) is forward-declared today and consumed by Phase 3c.2 / Phase 3d when those surfaces' Related blocks ship.

- [ ] **Step 1: Replace the entire script**

Replace `scripts/derive_crosslinks.mjs` with:

```js
#!/usr/bin/env node
/**
 * scripts/derive_crosslinks.mjs — cross-link graph derivator.
 *
 * Phase 3b scope (still active): work collection's methods[].link
 *   cross-references to other /work/<slug>/ pages.
 *
 * Phase 3c.1 scope (added): transactions collection bidirectional graph.
 *   Walks src/content/transactions/*.mdx, reads:
 *     - methods[].link (same as work — internal /work/<slug> links)
 *     - relatedCaseStudy (string → /work/<slug>)
 *     - relatedTransactions (string[] → /transactions/<slug>)
 *     - relatedEssay (string → /essays/<slug>; forward-declared, consumer
 *       arrives in Phase 3c.2 — validation is permissive in 3c.1)
 *     - previousVersion (string → /transactions/<slug>)
 *     - relatedArchitecture (string | string[] → /architecture/<slug>;
 *       forward-declared, consumer arrives in Phase 3c.2)
 *
 *   Writes src/content/transactions/.crosslinks.json with reverse-lookup
 *   tables:
 *     {
 *       transactions: {
 *         "<slug>": {
 *           supersededBy?: "<slug>",   // reverse of previousVersion
 *           siblingShips?: ["<slug>"], // reverse of relatedTransactions
 *         }
 *       },
 *       byCaseStudy: { "<work-slug>": ["<txn-slug>", ...] },
 *         // forward-declared; consumer is case-study page Related block (post-plan)
 *       byArchitecture: { "<arch-slug>": ["<txn-slug>", ...] },
 *         // forward-declared; consumer is Phase 3c.2 architecture Related block
 *       byEssay: { "<essay-slug>": ["<txn-slug>", ...] },
 *         // forward-declared; consumer is Phase 3c.2 essay Related block
 *     }
 *
 *   The deep-dive page's <RelatedBlock /> (Task 4.6) reads this file to
 *   render the forward direction (transactions → other surfaces). The
 *   reverse direction lights up automatically when each consumer surface
 *   imports the same JSON.
 *
 * Phase 3c.2 will add: architecture + essays collections (the four-way
 *   bidirectional close — essays' plottedArtifacts auto-reverse-renders
 *   "← named in: <essay title>" on every cited ledger row + architecture
 *   writeup).
 *
 * Validator behavior on dangling slugs: build fails with a clear message
 * naming the offending file, field, slug value, and (when Levenshtein
 * distance ≤2) the closest matching real slug. Per transactions-spec §11.1
 * error message contract.
 *
 * Source: case-study-spec-v1.md §8.2 + transactions-spec-v1.md §11.1 +
 *         §14 + BLUEPRINT-COMPLETE.md §3.3.
 */
import fs from "node:fs/promises";
import path from "node:path";
import process from "node:process";

const ROOT = process.cwd();
const WORK_DIR = path.join(ROOT, "src/content/work");
const TXN_DIR = path.join(ROOT, "src/content/transactions");
const CROSSLINKS_OUT = path.join(TXN_DIR, ".crosslinks.json");

function parseFrontmatterBlock(src) {
  const m = src.match(/^---\n([\s\S]+?)\n---/);
  return m ? m[1] : null;
}

function extractSlug(fname) {
  return fname.replace(/\.mdx?$/, "");
}

function extractMethodsLinks(fm) {
  const lines = fm.split("\n");
  const links = [];
  let inMethods = false;
  for (const line of lines) {
    if (/^methods:/.test(line)) { inMethods = true; continue; }
    if (inMethods && /^\w+:/.test(line)) { inMethods = false; }
    if (!inMethods) continue;
    const linkMatch = line.match(/^\s+link:\s*(.+)$/);
    if (linkMatch) {
      const val = linkMatch[1].trim().replace(/^["']|["']$/g, "");
      if (val !== "null" && val !== "") links.push(val);
    }
  }
  return links;
}

/**
 * Extracts a scalar field's value from a frontmatter block. Returns
 * null when absent. Strips surrounding quotes. Ignores comments.
 */
function extractScalar(fm, field) {
  const re = new RegExp(`^${field}:\\s*(.+?)\\s*$`, "m");
  const match = fm.match(re);
  if (!match) return null;
  const val = match[1].replace(/^["']|["']$/g, "");
  if (val === "null" || val === "") return null;
  return val;
}

/**
 * Extracts a list-of-strings field from a frontmatter block. Returns
 * empty array when absent. Supports both `field:` followed by indented
 * `- entry` lines and the inline `field: [a, b]` form.
 */
function extractStringList(fm, field) {
  const lines = fm.split("\n");
  const list = [];
  let inList = false;
  for (let i = 0; i < lines.length; i++) {
    const line = lines[i];
    if (new RegExp(`^${field}:\\s*\\[`).test(line)) {
      // Inline form: field: [a, b, c]
      const inlineMatch = line.match(new RegExp(`^${field}:\\s*\\[(.+)\\]`));
      if (inlineMatch) {
        return inlineMatch[1]
          .split(",")
          .map((s) => s.trim().replace(/^["']|["']$/g, ""))
          .filter((s) => s && s !== "null");
      }
    }
    if (new RegExp(`^${field}:\\s*$`).test(line)) { inList = true; continue; }
    if (inList && /^\w+:/.test(line)) { inList = false; }
    if (!inList) continue;
    const itemMatch = line.match(/^\s+-\s+(.+)$/);
    if (itemMatch) {
      const val = itemMatch[1].trim().replace(/^["']|["']$/g, "");
      if (val && val !== "null") list.push(val);
    }
  }
  return list;
}

/**
 * Extracts a string | string[] union field (used for relatedArchitecture).
 * Returns array of slugs (single-string input wrapped to length-1 array).
 */
function extractStringOrList(fm, field) {
  const scalar = extractScalar(fm, field);
  if (scalar !== null) return [scalar];
  return extractStringList(fm, field);
}

function levenshtein(a, b) {
  const m = a.length, n = b.length;
  if (m === 0) return n; if (n === 0) return m;
  const d = Array.from({ length: m + 1 }, () => new Array(n + 1).fill(0));
  for (let i = 0; i <= m; i++) d[i][0] = i;
  for (let j = 0; j <= n; j++) d[0][j] = j;
  for (let i = 1; i <= m; i++) {
    for (let j = 1; j <= n; j++) {
      const cost = a[i - 1] === b[j - 1] ? 0 : 1;
      d[i][j] = Math.min(d[i - 1][j] + 1, d[i][j - 1] + 1, d[i - 1][j - 1] + cost);
    }
  }
  return d[m][n];
}

function suggestion(needle, haystack) {
  let best = null;
  let bestDist = Infinity;
  for (const candidate of haystack) {
    const d = levenshtein(needle, candidate);
    if (d < bestDist) { bestDist = d; best = candidate; }
  }
  if (best && bestDist <= 2) return ` Did you mean "${best}"?`;
  return "";
}

async function readSlugSet(dir) {
  const entries = await fs.readdir(dir).catch(() => []);
  return new Set(
    entries
      .filter((f) => f.endsWith(".mdx") || f.endsWith(".md"))
      .map(extractSlug)
  );
}

async function main() {
  process.stdout.write("\nderive_crosslinks.mjs — Phase 3c.1 scope (work + transactions bidirectional)\n");

  const workSlugs = await readSlugSet(WORK_DIR);
  const txnSlugs = await readSlugSet(TXN_DIR);
  // Forward-declared; not yet validated against real dirs in Phase 3c.1.
  // Phase 3c.2 swaps these to readSlugSet() calls on real dirs.
  const archSlugs = new Set();   // /architecture/ — arrives in Phase 3c.2
  const essaySlugs = new Set();  // /essays/ — arrives in Phase 3c.2

  const errors = [];
  const txnEntries = await fs.readdir(TXN_DIR).catch(() => []);

  const reverseTxns = new Map();   // txnSlug → { supersededBy, siblingShips: [] }
  const byCaseStudy = new Map();   // workSlug → [txnSlug]
  const byArchitecture = new Map();// archSlug → [txnSlug] (forward-declared)
  const byEssay = new Map();       // essaySlug → [txnSlug] (forward-declared)

  // --- Pass 1: work collection (Phase 3b methods cross-links — unchanged) ---
  process.stdout.write("\n[work] methods cross-links\n");
  const workEntries = await fs.readdir(WORK_DIR).catch(() => []);
  let totalWork = 0;
  for (const fname of workEntries) {
    if (!fname.endsWith(".mdx") && !fname.endsWith(".md")) continue;
    const slug = extractSlug(fname);
    const src = await fs.readFile(path.join(WORK_DIR, fname), "utf8");
    const fm = parseFrontmatterBlock(src);
    if (!fm) continue;
    const links = extractMethodsLinks(fm);
    for (const link of links) {
      totalWork++;
      const t = link.match(/^\/work\/([\w-]+)\/?$/);
      if (!t) continue; // external links allowed
      const targetSlug = t[1];
      if (!workSlugs.has(targetSlug)) {
        errors.push(`work/${slug}: methods cross-link to /work/${targetSlug}/ — no such slug.${suggestion(targetSlug, [...workSlugs])}`);
      } else {
        process.stdout.write(`  ✓ ${slug} → /work/${targetSlug}/\n`);
      }
    }
  }

  // --- Pass 2: transactions collection cross-links + reverse graph ---
  process.stdout.write("\n[transactions] cross-links + bidirectional graph\n");
  let totalTxn = 0;
  for (const fname of txnEntries) {
    if (!fname.endsWith(".mdx") && !fname.endsWith(".md")) continue;
    const slug = extractSlug(fname);
    const src = await fs.readFile(path.join(TXN_DIR, fname), "utf8");
    const fm = parseFrontmatterBlock(src);
    if (!fm) continue;

    // methods[].link → /work/<slug>
    for (const link of extractMethodsLinks(fm)) {
      totalTxn++;
      const t = link.match(/^\/work\/([\w-]+)\/?$/);
      if (!t) continue;
      const target = t[1];
      if (!workSlugs.has(target)) {
        errors.push(`transactions/${slug}: methods cross-link to /work/${target}/ — no such slug.${suggestion(target, [...workSlugs])}`);
      } else {
        process.stdout.write(`  ✓ ${slug} → /work/${target}/ (methods)\n`);
      }
    }

    // relatedCaseStudy → /work/<slug>
    const relCs = extractScalar(fm, "relatedCaseStudy");
    if (relCs) {
      totalTxn++;
      if (!workSlugs.has(relCs)) {
        errors.push(`transactions/${slug}: relatedCaseStudy "${relCs}" → /work/${relCs}/ — no such slug.${suggestion(relCs, [...workSlugs])}`);
      } else {
        process.stdout.write(`  ✓ ${slug} → /work/${relCs}/ (relatedCaseStudy)\n`);
        if (!byCaseStudy.has(relCs)) byCaseStudy.set(relCs, []);
        byCaseStudy.get(relCs).push(slug);
      }
    }

    // relatedTransactions[] → /transactions/<slug> (sibling ships, bidirectional)
    for (const sib of extractStringList(fm, "relatedTransactions")) {
      totalTxn++;
      if (!txnSlugs.has(sib)) {
        errors.push(`transactions/${slug}: relatedTransactions "${sib}" → /transactions/${sib}/ — no such slug.${suggestion(sib, [...txnSlugs])}`);
      } else {
        process.stdout.write(`  ✓ ${slug} ↔ /transactions/${sib}/ (sibling)\n`);
        if (!reverseTxns.has(sib)) reverseTxns.set(sib, { siblingShips: [] });
        const rt = reverseTxns.get(sib);
        rt.siblingShips = rt.siblingShips ?? [];
        if (!rt.siblingShips.includes(slug)) rt.siblingShips.push(slug);
        // The forward direction is also tracked so the source row's
        // Related block can render its declared siblings.
        if (!reverseTxns.has(slug)) reverseTxns.set(slug, { siblingShips: [] });
        const fwd = reverseTxns.get(slug);
        fwd.siblingShips = fwd.siblingShips ?? [];
        if (!fwd.siblingShips.includes(sib)) fwd.siblingShips.push(sib);
      }
    }

    // previousVersion → /transactions/<slug> (supersedes / superseded-by)
    const prev = extractScalar(fm, "previousVersion");
    if (prev) {
      totalTxn++;
      if (!txnSlugs.has(prev)) {
        errors.push(`transactions/${slug}: previousVersion "${prev}" → /transactions/${prev}/ — no such slug.${suggestion(prev, [...txnSlugs])}`);
      } else {
        process.stdout.write(`  ✓ ${slug} → /transactions/${prev}/ (supersedes)\n`);
        // Forward direction: this row knows it supersedes prev.
        // Reverse direction: prev gains supersededBy = slug.
        if (!reverseTxns.has(prev)) reverseTxns.set(prev, {});
        reverseTxns.get(prev).supersededBy = slug;
      }
    }

    // relatedArchitecture (string | string[]) → /architecture/<slug>
    // Forward-declared in Phase 3c.1; archSlugs is empty so validation
    // permits any value (warning rather than error).
    for (const archSlug of extractStringOrList(fm, "relatedArchitecture")) {
      totalTxn++;
      if (archSlugs.size > 0 && !archSlugs.has(archSlug)) {
        errors.push(`transactions/${slug}: relatedArchitecture "${archSlug}" → /architecture/${archSlug}/ — no such slug.${suggestion(archSlug, [...archSlugs])}`);
      } else {
        process.stdout.write(`  ✓ ${slug} → /architecture/${archSlug}/ (forward-declared; consumer in Phase 3c.2)\n`);
        if (!byArchitecture.has(archSlug)) byArchitecture.set(archSlug, []);
        byArchitecture.get(archSlug).push(slug);
      }
    }

    // relatedEssay → /essays/<slug> (forward-declared)
    const relEssay = extractScalar(fm, "relatedEssay");
    if (relEssay) {
      totalTxn++;
      if (essaySlugs.size > 0 && !essaySlugs.has(relEssay)) {
        errors.push(`transactions/${slug}: relatedEssay "${relEssay}" → /essays/${relEssay}/ — no such slug.${suggestion(relEssay, [...essaySlugs])}`);
      } else {
        process.stdout.write(`  ✓ ${slug} → /essays/${relEssay}/ (forward-declared; consumer in Phase 3c.2)\n`);
        if (!byEssay.has(relEssay)) byEssay.set(relEssay, []);
        byEssay.get(relEssay).push(slug);
      }
    }
  }

  if (errors.length > 0) {
    process.stderr.write(`\n✗ ${errors.length} cross-link errors:\n`);
    for (const e of errors) process.stderr.write(`  - ${e}\n`);
    process.exit(1);
  }

  // --- Write the reverse-lookup JSON ---
  const out = {
    schemaVersion: "phase-3c.1",
    generatedAt: new Date().toISOString(),
    transactions: Object.fromEntries(reverseTxns),
    byCaseStudy: Object.fromEntries(byCaseStudy),
    byArchitecture: Object.fromEntries(byArchitecture),
    byEssay: Object.fromEntries(byEssay),
  };
  await fs.mkdir(TXN_DIR, { recursive: true });
  await fs.writeFile(CROSSLINKS_OUT, JSON.stringify(out, null, 2), "utf8");
  process.stdout.write(`\n  wrote ${path.relative(ROOT, CROSSLINKS_OUT)}\n`);

  process.stdout.write(`\ndone. validated ${totalWork + totalTxn} cross-links (${totalWork} work + ${totalTxn} transactions).\n`);
}

main().catch((e) => {
  process.stderr.write(`derive_crosslinks.mjs: ${e.stack || e.message}\n`);
  process.exit(1);
});
```

- [ ] **Step 2: Gitignore the derived JSON**

Append to `.gitignore`:

```bash
cat >> .gitignore <<'EOF'

# Phase 3c.1 cross-link graph (derived at prebuild)
src/content/transactions/.crosslinks.json
EOF
```

Verify: `tail -3 .gitignore` shows the new entries.

- [ ] **Step 3: Smoke-test (no transactions MDX yet — should be a near no-op + write empty crosslinks.json)**

Run: `node scripts/derive_crosslinks.mjs`

Expected output (shape):
```
derive_crosslinks.mjs — Phase 3c.1 scope (work + transactions bidirectional)

[work] methods cross-links
  ✓ <slug> → /work/<slug>/   (one line per existing methods link in work)

[transactions] cross-links + bidirectional graph

  wrote src/content/transactions/.crosslinks.json

done. validated N cross-links (N work + 0 transactions).
```

Note: the transactions dir may not exist yet, in which case `fs.mkdir(TXN_DIR, { recursive: true })` creates it before the JSON write. That's fine; Section 7 fills it with MDX. The validator (Task 2.1) treats `src/content/transactions/` as empty when no MDX is present.

If `mkdir` errors, verify `fs.mkdir(TXN_DIR, { recursive: true })` precedes the `writeFile`. If the script reports any unexpected validation errors on the existing work MDXs, the methods-link extraction regex has regressed — revert and inspect.

- [ ] **Step 4: Verify the JSON output shape**

Run: `cat src/content/transactions/.crosslinks.json`

Expected: a JSON object with `schemaVersion: "phase-3c.1"`, an ISO `generatedAt`, and empty objects for `transactions` / `byCaseStudy` / `byArchitecture` / `byEssay` (because no transactions MDX exists yet). If the file content is `{}` or any of the top-level keys are missing, fix the `out` object literal in `main()`.

- [ ] **Step 5: Commit**

```bash
git add scripts/derive_crosslinks.mjs .gitignore
git commit -m "$(cat <<'EOF'
feat(phase-3c.1): rewrite derive_crosslinks.mjs — bidirectional graph

Phase 3b's version only validated work's methods[].link references.
Phase 3c.1 expands to the full transactions bidirectional graph:

Forward direction (transactions → other surfaces), validated today:
  - methods[].link → /work/<slug>
  - relatedCaseStudy → /work/<slug>
  - relatedTransactions[] → /transactions/<slug> (siblings, both directions)
  - previousVersion → /transactions/<slug> (supersedes; reverse = supersededBy)
  - relatedArchitecture (string | string[]) → /architecture/<slug>
    forward-declared; consumer in Phase 3c.2
  - relatedEssay → /essays/<slug>
    forward-declared; consumer in Phase 3c.2

Writes src/content/transactions/.crosslinks.json with reverse-lookup
tables. The deep-dive's <RelatedBlock /> (Task 4.6) reads this file
to render the forward direction. Reverse direction lights up when
each consumer surface arrives (case-study Related block post-plan;
architecture + essays in Phase 3c.2).

Error messages name the offending file, field, slug value, and
(Levenshtein ≤2) the closest matching real slug per transactions-spec
§11.1 error-message contract. Example:
  transactions/foo.mdx: relatedCaseStudy "code-bain" → no such slug.
    Did you mean "code-brain"?

Gitignores the derived JSON (rebuilt on every prebuild).

Per transactions-spec §11.1 + §14 + BLUEPRINT-COMPLETE §3.3.
EOF
)"
```

---

## Section 3 — V3-bridge migration script

Phase 3c.1 ships the script per transactions spec §11.1 + §12.2. **It does not run the script in this plan.** Sean runs it manually when he wants to backfill the 2 (or more) V3-bridge entries. Continuous-execution posture is preserved.

### Task 3.1: Write `scripts/migrate_v3_transactions.mjs`

**Files:**
- Create: `scripts/migrate_v3_transactions.mjs`

Per transactions spec §11.1 (the migration row) + §12.2 (script behavior) + §12.3 (beat → surface mapping table). One-shot interactive script: reads `~/Code-Brain/sw-portfolio/src/content/transactions/*.md`, prompts Sean for ISO date + methods split + surface mapping per row, writes new MDX into this repo's `src/content/transactions/<slug>.md` with slugs preserved + MDX bodies verbatim.

- [ ] **Step 1: Write the script**

Create `scripts/migrate_v3_transactions.mjs`:

```js
#!/usr/bin/env node
/**
 * scripts/migrate_v3_transactions.mjs — one-shot V3 → redesign migration.
 *
 * Per transactions-spec-v1.md §11.1 + §12.2 + §12.3.
 *
 * Reads ~/Code-Brain/sw-portfolio/src/content/transactions/*.md (the V3
 * bridge repo, sibling clone of this repo). For each file:
 *   1. Preserves the slug (filename unchanged)
 *   2. Preserves the MDX body verbatim — the inline-body 4Q headings
 *      carry forward via the validator's V3-fallback contract
 *   3. Prompts Sean interactively for the ISO `shipped` date (V3
 *      strings like "May 2026" are too coarse to auto-convert)
 *   4. Maps V3 `beat` to the redesign `surface` enum per §12.3 table
 *      (defaults pre-suggested; Sean confirms or overrides per row)
 *   5. Splits V3 `producedBy` (free-text "Tool A + Tool B") into a
 *      structured methods[] array (Sean reviews each row)
 *   6. Sets status: SHIPPED for all migrated rows (the 2 committed V3
 *      entries are both shipped — Sean can override per row)
 *   7. Drops V3-only fields: loomFrame (the redesign's loomPosterUrl
 *      carries the editorial-photo moment instead)
 *
 * Writes new MDX at src/content/transactions/<slug>.md in THIS repo.
 *
 * Operating posture: this script is interactive and breaks the
 * continuous-execution posture of the build pipeline. It runs OUTSIDE
 * the prebuild gate — Sean invokes it manually:
 *   node scripts/migrate_v3_transactions.mjs
 *
 * After Sean runs it and commits the output, **delete this file** —
 * one-shot, not recurring infrastructure.
 *
 * Source: transactions-spec-v1.md §11.1 + §12.2 + §12.3.
 */
import fs from "node:fs/promises";
import path from "node:path";
import process from "node:process";
import readline from "node:readline/promises";

const V3_BRIDGE = path.resolve(process.env.HOME ?? "", "Code-Brain/sw-portfolio/src/content/transactions");
const OUT_DIR = path.resolve(process.cwd(), "src/content/transactions");

// V3 beat → redesign surface mapping (transactions-spec §12.3).
// `cushion-economics` is flagged for manual confirmation per the spec.
const BEAT_TO_SURFACE = {
  "agentic-engineering": "fleet",
  "creative-pipeline": "pipeline",
  "cushion-economics": "MANUAL",          // surface must be picked per-row: product OR infra
};

const VALID_SURFACES = ["fleet", "pipeline", "product", "writing", "infra"];

function parseFrontmatter(src) {
  const m = src.match(/^---\n([\s\S]+?)\n---\n([\s\S]*)$/);
  if (!m) return { frontmatter: {}, body: src };
  const fm = {};
  for (const line of m[1].split("\n")) {
    const kv = line.match(/^([\w_]+):\s*(.+?)\s*$/);
    if (!kv) continue;
    let value = kv[2];
    if (value.startsWith('"') && value.endsWith('"')) value = value.slice(1, -1);
    fm[kv[1]] = value;
  }
  return { frontmatter: fm, body: m[2] };
}

async function prompt(rl, q, def = "") {
  const suffix = def ? ` [${def}]` : "";
  const ans = (await rl.question(`${q}${suffix}: `)).trim();
  return ans || def;
}

async function migrateRow(rl, fname) {
  const filePath = path.join(V3_BRIDGE, fname);
  const src = await fs.readFile(filePath, "utf8");
  const { frontmatter: v3, body } = parseFrontmatter(src);
  const slug = fname.replace(/\.md$/, "");

  process.stdout.write(`\n━━━ ${slug} ━━━\n`);
  process.stdout.write(`V3 frontmatter:\n`);
  for (const [k, v] of Object.entries(v3)) {
    process.stdout.write(`  ${k}: ${v}\n`);
  }

  // 1. dateline (preserve verbatim or override)
  const dateline = await prompt(rl, "dateline (e.g., BOSTON, MAY 13, 2026)", v3.dateline ?? "");

  // 2. shipped — ISO conversion (V3 is too coarse)
  const shipped = await prompt(rl, `shipped ISO (YYYY-MM-DD) [V3 was "${v3.shipped ?? "?"}"]`);
  if (!/^\d{4}-\d{2}-\d{2}$/.test(shipped)) {
    throw new Error(`invalid ISO date: "${shipped}"`);
  }

  // 3. surface — beat → surface mapping with confirmation
  const suggestedSurface = BEAT_TO_SURFACE[v3.beat] ?? "MANUAL";
  let surface = suggestedSurface;
  if (suggestedSurface === "MANUAL") {
    process.stdout.write(`  ⚠ beat "${v3.beat}" requires manual surface assignment.\n`);
    surface = await prompt(rl, `surface (one of: ${VALID_SURFACES.join(", ")})`);
  } else {
    surface = await prompt(rl, `surface`, suggestedSurface);
  }
  if (!VALID_SURFACES.includes(surface)) {
    throw new Error(`invalid surface: "${surface}"`);
  }

  // 4. status (defaults to SHIPPED; the 2 committed V3 entries are both shipped)
  const status = await prompt(rl, `status (ACTIVE / SHIPPED / PAUSED / ARCHIVED)`, "SHIPPED");

  // 5. valueProp (carry forward V3's valueProp verbatim)
  const valueProp = v3.valueProp ?? "";

  // 6. methods — split V3 producedBy on "+" into structured rows
  const producedBy = v3.producedBy ?? "";
  process.stdout.write(`  V3 producedBy: "${producedBy}"\n`);
  process.stdout.write(`  Split into methods[]; press Enter to accept Sean-typed entries one per row.\n`);
  const methods = [];
  const seedItems = producedBy.split("+").map((s) => s.trim()).filter(Boolean);
  for (let i = 0; i < seedItems.length; i++) {
    process.stdout.write(`  -- method row ${i + 1} (seeded from "${seedItems[i]}") --\n`);
    const task = await prompt(rl, `  task`);
    const tool = await prompt(rl, `  tool`, seedItems[i]);
    const cost = await prompt(rl, `  cost`, "local / $0");
    const link = await prompt(rl, `  link (/work/<slug> or external; "null" to omit)`, "null");
    if (!task) continue;
    methods.push({ task, tool, cost, link: link === "null" ? null : link });
  }
  // Add additional rows interactively (empty task ends the loop)
  while (true) {
    process.stdout.write(`  -- add another method row? (empty task to skip) --\n`);
    const task = await prompt(rl, `  task`);
    if (!task) break;
    const tool = await prompt(rl, `  tool`);
    const cost = await prompt(rl, `  cost`, "local / $0");
    const link = await prompt(rl, `  link`, "null");
    methods.push({ task, tool, cost, link: link === "null" ? null : link });
  }
  if (methods.length === 0) {
    throw new Error(`${slug}: methods[] must have at least 1 entry`);
  }

  // 7. limitations — V3 carries them as a list; preserve verbatim
  const limitations = [];
  const v3LimRaw = src.match(/^limitations:\s*\n((?:\s+-\s+.+\n)+)/m);
  if (v3LimRaw) {
    for (const line of v3LimRaw[1].split("\n")) {
      const m = line.match(/^\s+-\s+(.+)$/);
      if (m) limitations.push(m[1].trim().replace(/^["']|["']$/g, ""));
    }
  }
  if (limitations.length === 0) {
    process.stdout.write(`  no limitations[] in V3 — add at least one (transactions-spec §3.2 min 1).\n`);
    const lim = await prompt(rl, `  limitation 1`);
    if (lim) limitations.push(lim);
  }

  // Optional editorial
  const repoUrl = await prompt(rl, `repoUrl (https://github.com/... or empty)`, "");
  const loomUrl = await prompt(rl, `loomUrl (or empty)`, "");

  // Build new MDX
  const fm = [
    "---",
    `title: "${(v3.title ?? "").replace(/"/g, '\\"')}"`,
    `dateline: "${dateline}"`,
    `shipped: ${shipped}`,
    ``,
    `surface: ${surface}`,
    `status: ${status}`,
    ``,
    `valueProp: ${JSON.stringify(valueProp)}`,
    `methods:`,
    ...methods.flatMap((m) => [
      `  - task: ${JSON.stringify(m.task)}`,
      `    tool: ${JSON.stringify(m.tool)}`,
      `    cost: ${JSON.stringify(m.cost)}`,
      `    link: ${m.link === null ? "null" : JSON.stringify(m.link)}`,
    ]),
    `limitations:`,
    ...limitations.map((l) => `  - ${JSON.stringify(l)}`),
    ``,
    `# 4Q source: inline-body fallback (V3-bridge legacy contract).`,
    `# explanationUrl: <upstream EXPLANATION.md URL> when one exists.`,
    ``,
    `# Cross-links (all optional; derive_crosslinks.mjs enforces resolution):`,
    `# relatedCaseStudy: <work-slug>`,
    `# relatedTransactions: [<txn-slug>, ...]`,
    `# previousVersion: <txn-slug>`,
    `# relatedArchitecture: <arch-slug> | [<arch-slug>, ...]`,
    ``,
    repoUrl ? `repoUrl: ${repoUrl}` : `# repoUrl: <url>`,
    loomUrl ? `loomUrl: ${loomUrl}` : `# loomUrl: <url>`,
    `---`,
    ``,
    body.trim(),
    ``,
  ].join("\n");

  await fs.mkdir(OUT_DIR, { recursive: true });
  await fs.writeFile(path.join(OUT_DIR, `${slug}.mdx`), fm, "utf8");
  process.stdout.write(`  → wrote src/content/transactions/${slug}.mdx\n`);
}

async function main() {
  const rl = readline.createInterface({ input: process.stdin, output: process.stdout });
  process.stdout.write(`\nmigrate_v3_transactions.mjs — one-shot V3 → redesign migration\n`);
  process.stdout.write(`Reading V3 bridge: ${V3_BRIDGE}\n`);

  const files = await fs.readdir(V3_BRIDGE).catch((e) => {
    process.stderr.write(`✗ V3 bridge not found at ${V3_BRIDGE}: ${e.message}\n`);
    process.exit(1);
  });
  const mdFiles = files.filter((f) => f.endsWith(".md"));
  process.stdout.write(`Found ${mdFiles.length} V3 transaction(s).\n`);

  for (const fname of mdFiles) {
    try {
      await migrateRow(rl, fname);
    } catch (e) {
      process.stderr.write(`✗ ${fname}: ${e.message}\n`);
      const cont = await rl.question(`continue with next? (y/n): `);
      if (cont.trim().toLowerCase() !== "y") break;
    }
  }

  rl.close();
  process.stdout.write(`\ndone. Review the new MDX, run \`npm run validate\`, then commit.\n`);
  process.stdout.write(`After committing the migrated entries, **delete this script** — it's one-shot.\n`);
}

main().catch((e) => {
  process.stderr.write(`migrate_v3_transactions.mjs: ${e.stack || e.message}\n`);
  process.exit(1);
});
```

- [ ] **Step 2: Make it executable but do NOT run it**

Run: `chmod +x scripts/migrate_v3_transactions.mjs`

**Do NOT execute the script in this plan.** It is interactive and breaks the continuous-execution posture. Sean runs it manually after the plan ships, at his own pace, in a fresh terminal. The plan's §X deferral table documents this.

- [ ] **Step 3: Verify the script parses (no execution — just node syntax-check)**

Run: `node --check scripts/migrate_v3_transactions.mjs`

Expected: exits 0 with no output. Confirms the script is syntactically valid Node 20.

- [ ] **Step 4: Commit**

```bash
git add scripts/migrate_v3_transactions.mjs
git commit -m "$(cat <<'EOF'
feat(phase-3c.1): migrate_v3_transactions.mjs — one-shot V3 → redesign migration

Ships per transactions-spec §11.1 + §12.2 + §12.3. **NOT RUN in this plan.**

Interactive node script that reads ~/Code-Brain/sw-portfolio/src/content/
transactions/*.md (V3 bridge sibling clone), prompts Sean per row for:
  - ISO shipped date (V3 strings like "May 2026" are too coarse)
  - surface enum (defaults pre-suggested from beat per §12.3 table;
    cushion-economics flagged for manual product | infra assignment)
  - status (defaults SHIPPED; the 2 committed V3 entries are both
    shipped)
  - methods[] (seeded by splitting V3 producedBy on "+", row-by-row
    confirmation with task/tool/cost/link)
  - limitations[] (carried forward from V3 verbatim; prompts if empty)
  - optional editorial: repoUrl, loomUrl

Writes new MDX at src/content/transactions/<slug>.mdx in this repo
with slugs preserved + body (inline-body 4Q) verbatim. The validator
(Task 2.1) accepts inline-body 4Q as a valid one-of choice with
explanationUrl, so V3 entries flow through without upstream
EXPLANATION.md authoring.

After Sean runs this script and commits the output, **delete this
file** — one-shot, not recurring infrastructure.

Why not run in the plan: interactive; breaks continuous-execution
posture. Phase 3c.1 ships the script + 3 hand-written placeholder
transactions (§7) to exercise the layout end-to-end. Sean runs the
migration when he wants the real V3 data backfilled.

Per transactions-spec §11.1 + §12.2 + §12.3.
EOF
)"
```


---

## Section 4 — Shared transactions components

Nine Astro components in `src/components/transactions/`. Each is a focused, single-responsibility file matching the transactions spec §17 (Appendix A) component map. Mirrors Phase 3b's `case-study/` folder organization. No React — pure Astro + scoped CSS.

### Task 4.1: `<IndexHeader />` — h1 + subhead + metadata strip + dateline

**Files:**
- Create: `src/components/transactions/IndexHeader.astro`

Per transactions spec §2.1 + §5 (type table). The header sits above the filter pill row on both `/transactions/` (ALL) and the per-surface `/transactions/<surface>/` pages. The metadata strip carries the artifact count + last-updated ISO + "RSS →" link.

- [ ] **Step 1: Write the component**

Create `src/components/transactions/IndexHeader.astro`:

```astro
---
/**
 * <IndexHeader /> — page title block above the filter pills.
 *
 * Renders dateline strip → h1 → Newsreader subhead → mono metadata strip.
 * Per transactions-spec §2.1 + §5 (type table) + §10.3 (RSS link).
 *
 * The dateline body is build-time-rendered for v1 (count + most-recent
 * surface) — Phase 4 swaps in the Daily Driver's `ledger_total` pattern
 * when the agent's filesystem-read step lands.
 */
interface Props {
  count: number;
  lastUpdatedISO: string;          // "2026-05-21"
  surface?: string;                // when set, this is a per-surface index page
  hookText?: string;               // optional subhead override (defaults below)
}

const {
  count,
  lastUpdatedISO,
  surface,
  hookText = "Every shipped artifact, dated, with the explanation attached.",
} = Astro.props;

// Dateline body for the index page (build-time rendered per blueprint §3.3
// "build-time render, not Daily Driver write" — same primitive as
// /architecture/ + /essays/ index pages).
const month = new Date(`${lastUpdatedISO}T12:00:00Z`).toLocaleDateString("en-US", { month: "long", day: "numeric", year: "numeric" }).toUpperCase();
const datelineBody = surface
  ? `BOSTON, ${month} — ${count} ${surface} artifact${count === 1 ? "" : "s"}.`
  : `BOSTON, ${month} — ${count} artifact${count === 1 ? "" : "s"} on the ledger.`;

const heading = surface ? `Transactions / ${surface}` : "Transactions";
---

<header class="index-header">
  <p class="dateline">{datelineBody}</p>
  <hr class="dateline-rule" />

  <h1 class="page-title">{heading}</h1>
  <p class="page-hook">{hookText}</p>

  <p class="metadata-strip">
    <span class="meta-count">{count} ARTIFACT{count === 1 ? "" : "S"}</span>
    <span class="meta-sep">·</span>
    <span class="meta-updated">UPDATED {lastUpdatedISO}</span>
    <span class="meta-sep">·</span>
    <a href="/transactions/rss.xml" class="meta-rss">RSS &rarr;</a>
  </p>
</header>

<style>
  .index-header {
    max-width: 1120px;
    margin: 60px auto 0;
    padding: 0 24px;
  }

  .dateline {
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: var(--ink-secondary);
    margin: 0;
  }
  .dateline-rule {
    border: none;
    border-bottom: 0.5px solid var(--border-paper);
    margin: 16px 0 40px;
  }

  .page-title {
    font-family: var(--font-serif);
    font-size: clamp(56px, 6vw, 96px);
    font-weight: 400;
    letter-spacing: -0.6px;
    color: var(--teal);
    margin: 0 0 16px;
    line-height: 1.05;
  }

  .page-hook {
    font-family: var(--font-serif);
    font-size: clamp(18px, 1.8vw, 20px);
    font-weight: 300;
    letter-spacing: -0.1px;
    color: var(--ink);
    margin: 0 0 32px;
    max-width: 680px;
  }

  .metadata-strip {
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: var(--ink-secondary);
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    align-items: baseline;
  }
  .meta-sep { color: var(--border-paper); }
  .meta-rss {
    color: var(--teal);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 160ms var(--ease-out);
  }
  .meta-rss:hover,
  .meta-rss:focus-visible {
    border-bottom-color: var(--teal);
    outline: none;
  }

  @media (max-width: 767px) {
    .index-header { padding: 0 16px; margin-top: 40px; }
    .page-title { font-size: clamp(40px, 9vw, 56px); }
  }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add src/components/transactions/IndexHeader.astro
git commit -m "$(cat <<'EOF'
feat(phase-3c.1): IndexHeader component — page title + dateline + metadata

Top-of-page block for /transactions/ + /transactions/<surface>/ pages.
Renders dateline strip → page h1 (Newsreader clamp(56,6vw,96) weight 400
teal) → Newsreader subhead (clamp(18,1.8vw,20) weight 300 ink) → mono
metadata strip (N ARTIFACTS · UPDATED YYYY-MM-DD · RSS →).

Dateline body is build-time-rendered per BLUEPRINT-COMPLETE §3.3 — count
+ recent activity. When surface prop is set, the h1 becomes
"Transactions / <surface>" and the dateline scopes to that surface.

Per transactions-spec §2.1 + §5 + §10.3.
EOF
)"
```

### Task 4.2: `<FilterPills />` — wire-service filter row

**Files:**
- Create: `src/components/transactions/FilterPills.astro`

Per transactions spec §7.1. Six pills (`ALL` + 5 surface values) as real `<a>` elements pointing at per-surface SSG routes. Active pill carries `aria-current="page"` + 1px stamp-amber bottom border + weight 500. No client-side filter logic.

- [ ] **Step 1: Write the component**

Create `src/components/transactions/FilterPills.astro`:

```astro
---
/**
 * <FilterPills /> — static surface filter row.
 *
 * Six pills as real <a> elements pointing at per-surface SSG routes.
 * Active pill detected by Astro.url.pathname matching.
 *
 * Per transactions-spec §7.1 + §7.2 + §16 (a11y).
 */
interface Props {
  activeSurface?: string;   // "fleet" | "pipeline" | "product" | "writing" | "infra" | undefined (ALL)
}

const { activeSurface } = Astro.props;

const PILLS: Array<{ label: string; href: string; surface?: string }> = [
  { label: "ALL",       href: "/transactions/" },
  { label: "fleet",     href: "/transactions/fleet/",     surface: "fleet" },
  { label: "pipeline",  href: "/transactions/pipeline/",  surface: "pipeline" },
  { label: "product",   href: "/transactions/product/",   surface: "product" },
  { label: "writing",   href: "/transactions/writing/",   surface: "writing" },
  { label: "infra",     href: "/transactions/infra/",     surface: "infra" },
];

function isActive(pillSurface: string | undefined): boolean {
  // ALL is active when no surface is set; per-surface pills are active when their key matches.
  if (pillSurface === undefined) return activeSurface === undefined;
  return activeSurface === pillSurface;
}
---

<nav class="filter-pills" aria-label="Filter by surface">
  <ul>
    {PILLS.map((pill) => (
      <li>
        <a
          href={pill.href}
          class={isActive(pill.surface) ? "pill active" : "pill"}
          aria-current={isActive(pill.surface) ? "page" : undefined}
        >
          {pill.label}
        </a>
      </li>
    ))}
  </ul>
</nav>

<style>
  .filter-pills {
    max-width: 1120px;
    margin: 48px auto 32px;
    padding: 0 24px;
  }
  .filter-pills ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 24px;
    row-gap: 12px;
    align-items: baseline;
  }
  .pill {
    font-family: var(--font-mono);
    font-size: 13px;
    font-weight: 400;
    letter-spacing: 1.4px;
    color: var(--ink-secondary);
    text-decoration: none;
    line-height: 1;
    padding: 4px 0;
    border-bottom: 1px solid transparent;
    transition: color 160ms var(--ease-out), border-color 160ms var(--ease-out);
  }
  /* "ALL" pill is uppercase per spec §2.1; surface pills stay lowercase. */
  .pill:first-of-type,
  .pill[href="/transactions/"] {
    text-transform: uppercase;
  }
  .pill:hover { color: var(--teal); }
  .pill.active {
    font-weight: 500;
    color: var(--teal);
    border-bottom-color: var(--stamp-amber);
  }
  .pill:focus-visible {
    outline: 2px solid var(--teal);
    outline-offset: 4px;
  }

  @media (max-width: 767px) {
    .filter-pills { padding: 0 16px; margin: 32px auto 24px; }
    .filter-pills ul { gap: 16px; }
  }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add src/components/transactions/FilterPills.astro
git commit -m "$(cat <<'EOF'
feat(phase-3c.1): FilterPills component — wire-service surface filter

Six static <a> pills (ALL + 5 surface enum values) pointing at per-surface
SSG routes. Active pill detected by activeSurface prop, carries
aria-current="page" + 1px stamp-amber bottom border + weight 500.
ALL pill is uppercase; surface pills are lowercase per spec §2.1.

No client-side JS — navigation is real <a> clicks. SSG per-surface
routes are built in Task 5.2.

Per transactions-spec §7.1 + §7.2 + §16.
EOF
)"
```

### Task 4.3: `<LedgerRow />` — single row with View Transition seam

**Files:**
- Create: `src/components/transactions/LedgerRow.astro`

Per transactions spec §8.1 (desktop) + §8.2 (mobile) + §8.3 (hover/click) + §15 (View Transition). The row is a single `<a>` wrapping the grid contents. View Transition name `ledger-title-<slug>` shared with the deep-dive `<h1>` (Task 5.3).

- [ ] **Step 1: Write the component**

Create `src/components/transactions/LedgerRow.astro`:

```astro
---
/**
 * <LedgerRow /> — single ledger row.
 *
 * Desktop: 60px tall, grid (120px dateline | 80px surface | 96px status | 1fr title+valueProp | 24px glyph).
 * Mobile: 80px tall, stacked (surface+status+dateline / title+glyph / valueProp).
 *
 * View Transition: shares `view-transition-name: ledger-title-<slug>`
 * with the deep-dive <h1>, so the row title morphs into the page title
 * on navigation.
 *
 * Per transactions-spec §8.1 + §8.2 + §8.3 + §15 + §16.
 */
interface Props {
  slug: string;
  shipped: string;               // ISO YYYY-MM-DD
  surface: string;               // "fleet" | "pipeline" | "product" | "writing" | "infra"
  status: string;                // "ACTIVE" | "SHIPPED" | "PAUSED" | "ARCHIVED"
  title: string;
  valueProp: string;
}

const { slug, shipped, surface, status, title, valueProp } = Astro.props;

// Dateline format: "MAY 13, 2026"
const datelineLabel = new Date(`${shipped}T12:00:00Z`)
  .toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" })
  .toUpperCase();

const ariaLabel = `View transaction: ${title}, shipped ${shipped}`;
const statusClass = `status-pill status-${status.toLowerCase()}`;
---

<a href={`/transactions/${slug}/`} class="ledger-row" aria-label={ariaLabel}>
  <span class="row-dateline" aria-hidden="true">{datelineLabel}</span>
  <span class="surface-pill" aria-hidden="true">{surface}</span>
  <span class={statusClass} aria-hidden="true">{status}</span>
  <span class="row-body">
    <span class="row-title" style={`view-transition-name: ledger-title-${slug}`}>
      {title}
    </span>
    <span class="row-valueprop">{valueProp}</span>
  </span>
  <span class="row-glyph" aria-hidden="true">&rarr;</span>
</a>

<style>
  .ledger-row {
    display: grid;
    grid-template-columns: 120px 80px 96px 1fr 24px;
    column-gap: 16px;
    align-items: center;
    min-height: 60px;
    padding: 12px 24px;
    color: var(--ink);
    text-decoration: none;
    border-bottom: 0.5px solid var(--border-paper);
    transition: background-color 200ms var(--ease-out);
  }
  .ledger-row:hover {
    background-color: rgba(10, 62, 66, 0.04);
  }
  .ledger-row:hover .row-title {
    box-shadow: inset 0 -1px 0 0 var(--teal);
  }
  .ledger-row:hover .row-glyph {
    transform: translateX(4px);
  }
  .ledger-row:focus-visible {
    outline: 2px solid var(--teal);
    outline-offset: -2px;
  }

  .row-dateline {
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 400;
    letter-spacing: 1.2px;
    color: var(--stamp-amber);
  }
  /* Year portion of the dateline reads less prominently */
  .row-dateline {
    /* No CSS-only year-recolor; the dateline as one block in v1. */
  }

  .surface-pill {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: var(--ink);
    border: 1px solid var(--ink);
    padding: 3px 8px;
    background-color: var(--paper);
    justify-self: start;
  }

  .status-pill {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    padding: 3px 8px;
    background-color: var(--paper);
    justify-self: start;
  }
  .status-active   { color: var(--stamp-amber);     border: 1px solid var(--stamp-amber); }
  .status-shipped  { color: var(--success-teal);    border: 1px solid var(--success-teal); }
  .status-paused   { color: var(--ink-secondary);   border: 1px solid var(--ink-secondary); }
  .status-archived { color: var(--ink-secondary);   border: 1px solid var(--ink-secondary); opacity: 0.7; }

  .row-body {
    display: flex;
    flex-direction: column;
    gap: 4px;
    min-width: 0;
  }
  .row-title {
    font-family: var(--font-serif);
    font-size: 20px;
    font-weight: 400;
    letter-spacing: -0.2px;
    color: var(--ink);
    line-height: 1.2;
    transition: box-shadow 200ms var(--ease-out);
  }
  .row-valueprop {
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 400;
    letter-spacing: 0.6px;
    color: var(--ink-secondary);
    line-height: 1.4;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .row-glyph {
    font-family: var(--font-mono);
    font-size: 14px;
    color: var(--ink-secondary);
    transition: transform 200ms var(--ease-out);
    justify-self: end;
  }

  @media (max-width: 767px) {
    .ledger-row {
      grid-template-columns: auto 1fr auto;
      grid-template-rows: auto auto auto;
      grid-template-areas:
        "surface  status   dateline"
        "title    title    glyph"
        "valueprop valueprop valueprop";
      row-gap: 8px;
      min-height: 80px;
      padding: 12px 16px;
    }
    .row-dateline { grid-area: dateline; justify-self: end; font-size: 11px; }
    .surface-pill { grid-area: surface; }
    .status-pill { grid-area: status; }
    .row-body { display: contents; }
    .row-title { grid-area: title; font-size: 18px; }
    .row-valueprop { grid-area: valueprop; white-space: normal; }
    .row-glyph { grid-area: glyph; }
  }

  @media (prefers-reduced-motion: reduce) {
    .ledger-row,
    .row-title,
    .row-glyph { transition: none; }
    .ledger-row:hover .row-title { box-shadow: none; }
    .ledger-row:hover .row-glyph { transform: none; }
  }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add src/components/transactions/LedgerRow.astro
git commit -m "$(cat <<'EOF'
feat(phase-3c.1): LedgerRow component — single ledger row

Desktop: 60px grid (120px dateline | 80px surface | 96px status | 1fr
title+valueProp | 24px glyph). Mobile: 80px stacked (surface+status+
dateline / title+glyph / valueProp).

View Transition seam: row title carries `view-transition-name:
ledger-title-<slug>` shared with the deep-dive <h1> (Task 5.3) — title
morphs into page title on navigation. Per transactions-spec §15.

Hover: row bg → rgba(10,62,66,0.04); title gains 1px teal underline;
glyph translates 4px right; all transitions 200ms ease-out. Reduced
motion disables all animations.

Status pill colors per spec §6:
  ACTIVE → stamp-amber #7C2D12
  SHIPPED → success-teal #0F6E56
  PAUSED → ink-secondary #546E71
  ARCHIVED → ink-secondary @ 70% opacity

Per transactions-spec §8.1 + §8.2 + §8.3 + §15 + §16.
EOF
)"
```

### Task 4.4: `<ValuePropPullQuote />` — deep-dive pull-quote

**Files:**
- Create: `src/components/transactions/ValuePropPullQuote.astro`

Per transactions spec §2.2 + §5 (type table). Newsreader 24px italic, max-width 680px, 0.5px teal rules top + bottom, paper background. Sits between the title block and the optional Loom poster.

- [ ] **Step 1: Write the component**

Create `src/components/transactions/ValuePropPullQuote.astro`:

```astro
---
/**
 * <ValuePropPullQuote /> — the 1-sentence editorial pullquote at top of deep-dive.
 *
 * Per transactions-spec §2.2 + §5 (type table) + §6 (color rules).
 */
interface Props {
  text: string;
}
const { text } = Astro.props;
---

<aside class="value-prop-pullquote">
  <hr class="rule" />
  <p class="quote">&ldquo;{text}&rdquo;</p>
  <hr class="rule" />
</aside>

<style>
  .value-prop-pullquote {
    max-width: 1120px;
    margin: 48px auto 0;
    padding: 0 24px;
  }
  .rule {
    border: none;
    border-bottom: 0.5px solid var(--teal);
    margin: 0 auto;
    max-width: 680px;
    opacity: 0.4;
  }
  .quote {
    font-family: var(--font-serif);
    font-size: clamp(20px, 2.4vw, 24px);
    font-weight: 300;
    font-style: italic;
    letter-spacing: -0.2px;
    line-height: 1.45;
    color: var(--teal);
    max-width: 680px;
    margin: 24px auto;
    text-align: left;
  }

  @media (max-width: 767px) {
    .value-prop-pullquote { padding: 0 16px; }
    .quote { font-size: 18px; margin: 20px auto; }
  }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add src/components/transactions/ValuePropPullQuote.astro
git commit -m "$(cat <<'EOF'
feat(phase-3c.1): ValuePropPullQuote component — deep-dive pullquote

Newsreader clamp(20,2.4vw,24) weight 300 italic teal, max-width 680px,
0.5px teal rules top + bottom @ 40% opacity. Sits between the title
block and the optional Loom poster.

Per transactions-spec §2.2 + §5 + §6.
EOF
)"
```

### Task 4.5: `<LoomPoster />` — optional embedded video

**Files:**
- Create: `src/components/transactions/LoomPoster.astro`

Per transactions spec §9.1 step 6 + §16 (a11y). Renders only when `loomUrl` is set in frontmatter. Click expands to native `<dialog>` with iframe. ESC closes; focus returns to the trigger button. Strict 1-Loom-max per row (spec §19 OPEN-5 RESOLVED — locked to strict).

- [ ] **Step 1: Write the component**

Create `src/components/transactions/LoomPoster.astro`:

```astro
---
/**
 * <LoomPoster /> — optional editorial Loom video embed.
 *
 * Renders only when loomUrl is present. Poster click opens a native
 * <dialog> with the Loom iframe. Strict 1-Loom max per row
 * (transactions-spec §19 OPEN-5 RESOLVED).
 *
 * Per transactions-spec §9.1 step 6 + §16 (a11y).
 */
interface Props {
  loomUrl: string;
  posterUrl?: string;             // optional editorial poster image
  title: string;                  // for the aria-label + dialog title
}
const { loomUrl, posterUrl, title } = Astro.props;

// Convert https://www.loom.com/share/<id> to https://www.loom.com/embed/<id>
function toEmbedUrl(url: string): string {
  return url.replace("/share/", "/embed/");
}
const embedUrl = toEmbedUrl(loomUrl);
const dialogId = `loom-dialog-${title.toLowerCase().replace(/[^\w]+/g, "-").slice(0, 32)}`;
---

<section class="loom-section">
  <button
    type="button"
    class="loom-trigger"
    aria-label={`Play Loom video: ${title}`}
    data-dialog-id={dialogId}
  >
    {posterUrl
      ? <img src={posterUrl} alt="" class="loom-poster" loading="lazy" />
      : <span class="loom-poster-fallback" aria-hidden="true">▶ Loom</span>
    }
    <span class="loom-play" aria-hidden="true">▶</span>
  </button>

  <dialog id={dialogId} class="loom-dialog" aria-label={`Loom video: ${title}`}>
    <form method="dialog" class="loom-close-form">
      <button type="submit" class="loom-close" aria-label="Close video">&times;</button>
    </form>
    <iframe
      src={embedUrl}
      allow="fullscreen"
      allowfullscreen
      class="loom-iframe"
      title={`Loom video: ${title}`}
    ></iframe>
  </dialog>
</section>

<script>
  // Wire up trigger → dialog.showModal() + restore focus on close.
  document.querySelectorAll<HTMLButtonElement>(".loom-trigger").forEach((trigger) => {
    const dialogId = trigger.dataset.dialogId;
    if (!dialogId) return;
    const dialog = document.getElementById(dialogId) as HTMLDialogElement | null;
    if (!dialog) return;
    trigger.addEventListener("click", () => {
      dialog.showModal();
    });
    dialog.addEventListener("close", () => {
      trigger.focus();
    });
  });
</script>

<style>
  .loom-section {
    max-width: 880px;
    margin: 64px auto 0;
    padding: 0 24px;
  }
  .loom-trigger {
    position: relative;
    display: block;
    width: 100%;
    aspect-ratio: 16 / 9;
    background-color: var(--ink);
    border: 0.5px solid var(--border-paper);
    cursor: pointer;
    padding: 0;
    overflow: hidden;
    transition: opacity 160ms var(--ease-out);
  }
  .loom-trigger:hover { opacity: 0.92; }
  .loom-trigger:focus-visible {
    outline: 2px solid var(--teal);
    outline-offset: 4px;
  }
  .loom-poster {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
  .loom-poster-fallback {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--paper);
    font-family: var(--font-mono);
    font-size: 18px;
    letter-spacing: 1.2px;
  }
  .loom-play {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--paper);
    font-size: 48px;
    pointer-events: none;
    text-shadow: 0 2px 8px rgba(0,0,0,0.4);
  }

  .loom-dialog {
    border: none;
    background-color: transparent;
    padding: 0;
    max-width: 90vw;
    width: 960px;
  }
  .loom-dialog::backdrop {
    background-color: rgba(0,0,0,0.7);
  }
  .loom-iframe {
    width: 100%;
    aspect-ratio: 16 / 9;
    border: none;
    display: block;
  }
  .loom-close-form { margin: 0; }
  .loom-close {
    position: absolute;
    top: -32px;
    right: -8px;
    background: transparent;
    border: none;
    color: var(--paper);
    font-size: 24px;
    cursor: pointer;
    padding: 4px 8px;
    line-height: 1;
  }
  .loom-close:focus-visible {
    outline: 2px solid var(--paper);
    outline-offset: 2px;
  }

  @media (max-width: 767px) {
    .loom-section { padding: 0 16px; margin-top: 48px; }
  }

  @media (prefers-reduced-motion: reduce) {
    .loom-trigger { transition: none; }
  }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add src/components/transactions/LoomPoster.astro
git commit -m "$(cat <<'EOF'
feat(phase-3c.1): LoomPoster component — optional video embed

Renders only when loomUrl is present. Poster click opens a native
<dialog> with the Loom iframe. ESC closes; focus returns to trigger
button on close. Strict 1-Loom max per row (transactions-spec §19
OPEN-5 RESOLVED — locked to strict; a row needing 2 videos splits
into 2 rows or earns a case study).

Converts /share/<id> URLs to /embed/<id> for the iframe. Fallback
poster (▶ Loom on ink bg) when posterUrl is not set.

Per transactions-spec §9.1 step 6 + §16.
EOF
)"
```

### Task 4.6: `<LimitationsBlock />` + `<RelatedBlock />` + `<FooterFold />` + `<DeepDiveTitleBlock />`

**Files:**
- Create: `src/components/transactions/LimitationsBlock.astro`
- Create: `src/components/transactions/RelatedBlock.astro`
- Create: `src/components/transactions/FooterFold.astro`
- Create: `src/components/transactions/DeepDiveTitleBlock.astro`

Four small chrome components in one task — each <80 lines, none warrant their own task. Per transactions spec §2.2 + §9.1 step 10-11.

- [ ] **Step 1: Write `LimitationsBlock.astro`**

Create `src/components/transactions/LimitationsBlock.astro`:

```astro
---
/**
 * <LimitationsBlock /> — "WHAT THIS DOESN'T YET DO" bullets.
 *
 * Per transactions-spec §2.2 + §9.1 step 10 + §5 (type table).
 */
interface Props {
  items: string[];
}
const { items } = Astro.props;
---

<section class="limitations" aria-labelledby="limitations-heading">
  <h2 id="limitations-heading" class="section-heading">─ WHAT THIS DOESN&apos;T YET DO ─</h2>
  <ul>
    {items.map((item) => <li>{item}</li>)}
  </ul>
</section>

<style>
  .limitations {
    max-width: 1120px;
    margin: 80px auto 0;
    padding: 0 24px;
  }
  .section-heading {
    font-family: var(--font-mono);
    font-size: 14px;
    font-weight: 500;
    letter-spacing: 2.4px;
    color: var(--teal);
    opacity: 0.8;
    text-transform: uppercase;
    margin: 0 0 24px;
  }
  ul {
    list-style: none;
    padding: 0;
    margin: 0;
    max-width: 680px;
  }
  li {
    font-family: var(--font-mono);
    font-size: 13px;
    font-weight: 400;
    letter-spacing: 0.6px;
    color: var(--ink-secondary);
    line-height: 1.55;
    padding-left: 16px;
    margin: 0 0 12px;
    position: relative;
  }
  li::before {
    content: "·";
    position: absolute;
    left: 0;
    color: var(--ink-secondary);
  }
  @media (max-width: 767px) {
    .limitations { padding: 0 16px; }
    li { font-size: 12px; }
  }
</style>
```

- [ ] **Step 2: Write `RelatedBlock.astro`**

Create `src/components/transactions/RelatedBlock.astro`:

```astro
---
/**
 * <RelatedBlock /> — auto-rendered cross-link block.
 *
 * Reads frontmatter cross-link fields + the derived crosslinks.json
 * (Phase 3c.1 derive_crosslinks.mjs writes
 * src/content/transactions/.crosslinks.json) and renders 3-5 max
 * link rows. No author maintenance — the graph self-heals.
 *
 * Per transactions-spec §9.1 step 11 + §14 + §16.
 */
interface CrosslinkData {
  transactions?: Record<string, { supersededBy?: string; siblingShips?: string[] }>;
}
interface Props {
  slug: string;
  relatedCaseStudy?: string;
  relatedTransactions?: string[];
  relatedEssay?: string;
  previousVersion?: string;
  relatedArchitecture?: string | string[];
  crosslinks: CrosslinkData;
  titleByTxnSlug: Record<string, string>;  // {phase-d-typed-edges: "Phase D — Typed Reasoning Edges"}
  titleByWorkSlug: Record<string, string>; // {code-brain: "Code Brain"}
}
const {
  slug,
  relatedCaseStudy,
  relatedTransactions = [],
  relatedEssay,
  previousVersion,
  relatedArchitecture,
  crosslinks,
  titleByTxnSlug,
  titleByWorkSlug,
} = Astro.props;

const reverse = crosslinks.transactions?.[slug] ?? {};

// Build the row list. Each row is { href, label } — rendered as
// "→ <category>: <linked title>".
const rows: Array<{ href: string; category: string; label: string }> = [];

if (relatedCaseStudy && titleByWorkSlug[relatedCaseStudy]) {
  rows.push({
    href: `/work/${relatedCaseStudy}/`,
    category: "part of",
    label: titleByWorkSlug[relatedCaseStudy],
  });
}

// Siblings: union of declared + reverse-derived
const siblings = new Set<string>([
  ...(relatedTransactions ?? []),
  ...(reverse.siblingShips ?? []),
]);
siblings.delete(slug); // never link to self
for (const sib of siblings) {
  if (titleByTxnSlug[sib]) {
    rows.push({
      href: `/transactions/${sib}/`,
      category: "sister ship",
      label: titleByTxnSlug[sib],
    });
  }
}

// previousVersion (forward) and supersededBy (reverse, derived)
if (previousVersion && titleByTxnSlug[previousVersion]) {
  rows.push({
    href: `/transactions/${previousVersion}/`,
    category: "supersedes",
    label: titleByTxnSlug[previousVersion],
  });
}
if (reverse.supersededBy && titleByTxnSlug[reverse.supersededBy]) {
  rows.push({
    href: `/transactions/${reverse.supersededBy}/`,
    category: "superseded by",
    label: titleByTxnSlug[reverse.supersededBy],
  });
}

// Architecture (forward-declared; no consumer page until Phase 3c.2,
// so the link target may 404. v1 still renders the row — when 3c.2
// ships, the link lights up automatically.)
const archSlugs = Array.isArray(relatedArchitecture)
  ? relatedArchitecture
  : relatedArchitecture
    ? [relatedArchitecture]
    : [];
for (const arch of archSlugs) {
  rows.push({
    href: `/architecture/${arch}/`,
    category: "architecture writeup",
    label: arch,  // titleByArchSlug not available in 3c.1 — slug is sufficient placeholder
  });
}

// Essay (forward-declared)
if (relatedEssay) {
  rows.push({
    href: `/essays/${relatedEssay}/`,
    category: "essay",
    label: relatedEssay,
  });
}

const hasRows = rows.length > 0;
---

{hasRows && (
  <section class="related" aria-labelledby="related-heading">
    <h2 id="related-heading" class="section-heading">─ RELATED ─</h2>
    <ul>
      {rows.map((row) => (
        <li>
          <span class="row-category">&rarr; {row.category}:</span>{" "}
          <a href={row.href} class="row-link">{row.label}</a>
        </li>
      ))}
    </ul>
  </section>
)}

<style>
  .related {
    max-width: 1120px;
    margin: 64px auto 0;
    padding: 0 24px;
  }
  .section-heading {
    font-family: var(--font-mono);
    font-size: 14px;
    font-weight: 500;
    letter-spacing: 2.4px;
    color: var(--teal);
    opacity: 0.8;
    text-transform: uppercase;
    margin: 0 0 24px;
  }
  ul {
    list-style: none;
    padding: 0;
    margin: 0;
    max-width: 680px;
  }
  li {
    font-family: var(--font-mono);
    font-size: 14px;
    font-weight: 500;
    letter-spacing: 0.8px;
    color: var(--teal);
    margin: 0 0 12px;
    line-height: 1.5;
  }
  .row-category { color: var(--ink-secondary); font-weight: 400; }
  .row-link {
    color: var(--teal);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 160ms var(--ease-out);
  }
  .row-link:hover,
  .row-link:focus-visible {
    border-bottom-color: var(--teal);
    outline: none;
  }

  @media (max-width: 767px) {
    .related { padding: 0 16px; }
    li { font-size: 13px; }
  }
</style>
```

- [ ] **Step 3: Write `FooterFold.astro`**

Create `src/components/transactions/FooterFold.astro`:

```astro
---
/**
 * <FooterFold /> — "subscribe via RSS" + "view the fleet ↗".
 *
 * Renders below the ledger rows on /transactions/ index + per-surface
 * pages. NOT on the deep-dive — the deep-dive has Next/Prev nav instead.
 *
 * Per transactions-spec §2.1 + §10.3.
 */
import { FLEET_DASHBOARD_URL } from "~/lib/site";
---

<aside class="footer-fold" aria-label="Subscribe and external links">
  <hr class="fold-rule" />
  <ul>
    <li>
      <a href="/transactions/rss.xml" class="fold-link">&rarr; subscribe via RSS</a>
    </li>
    <li>
      <a href={FLEET_DASHBOARD_URL} class="fold-link" rel="noopener external">&rarr; view the fleet &nearr;</a>
    </li>
  </ul>
</aside>

<style>
  .footer-fold {
    max-width: 1120px;
    margin: 80px auto 60px;
    padding: 0 24px;
  }
  .fold-rule {
    border: none;
    border-bottom: 0.5px solid var(--border-paper);
    margin: 0 0 32px;
  }
  ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 48px;
  }
  li { margin: 0; }
  .fold-link {
    font-family: var(--font-mono);
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 1.2px;
    color: var(--teal);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 160ms var(--ease-out);
  }
  .fold-link:hover,
  .fold-link:focus-visible {
    border-bottom-color: var(--teal);
    outline: none;
  }

  @media (max-width: 767px) {
    .footer-fold { padding: 0 16px; }
    ul { flex-direction: column; gap: 16px; }
  }
</style>
```

- [ ] **Step 4: Write `DeepDiveTitleBlock.astro`**

Create `src/components/transactions/DeepDiveTitleBlock.astro`:

```astro
---
/**
 * <DeepDiveTitleBlock /> — dateline + surface/status pills + h1.
 *
 * Top-of-page block on /transactions/<slug>/. Pairs with
 * <ValuePropPullQuote /> below (separate component since the
 * pullquote has its own typographic register).
 *
 * View Transition: the h1 carries `view-transition-name: ledger-title-<slug>`
 * — the morph target from the index ledger row (Task 4.3).
 *
 * Per transactions-spec §2.2 + §5 + §9.1 steps 1-3 + §15.
 */
interface Props {
  slug: string;
  dateline: string;          // "BOSTON, MAY 13, 2026 — phase d typed reasoning edges, shipped."
  surface: string;
  status: string;
  title: string;
}
const { slug, dateline, surface, status, title } = Astro.props;
const statusClass = `status-pill status-${status.toLowerCase()}`;
---

<header class="title-block">
  <p class="dateline">{dateline}</p>
  <hr class="dateline-rule" />

  <p class="pills">
    <span class="surface-pill">{surface.toUpperCase()}</span>
    <span class="pills-sep" aria-hidden="true">·</span>
    <span class={statusClass}>{status}</span>
  </p>

  <h1 class="page-title" style={`view-transition-name: ledger-title-${slug}`}>
    {title}
  </h1>
</header>

<style>
  .title-block {
    max-width: 1120px;
    margin: 60px auto 0;
    padding: 0 24px;
  }
  .dateline {
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: var(--ink-secondary);
    margin: 0;
  }
  .dateline-rule {
    border: none;
    border-bottom: 0.5px solid var(--border-paper);
    margin: 16px 0 24px;
  }

  .pills {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    margin: 0 0 12px;
    display: flex;
    gap: 12px;
    align-items: baseline;
  }
  .surface-pill {
    color: var(--ink);
    border: 1px solid var(--ink);
    padding: 3px 8px;
  }
  .pills-sep { color: var(--border-paper); }
  .status-pill {
    padding: 3px 8px;
  }
  .status-active   { color: var(--stamp-amber);     border: 1px solid var(--stamp-amber); }
  .status-shipped  { color: var(--success-teal);    border: 1px solid var(--success-teal); }
  .status-paused   { color: var(--ink-secondary);   border: 1px solid var(--ink-secondary); }
  .status-archived { color: var(--ink-secondary);   border: 1px solid var(--ink-secondary); opacity: 0.7; }

  .page-title {
    font-family: var(--font-serif);
    font-size: clamp(40px, 5vw, 72px);
    font-weight: 400;
    letter-spacing: -0.4px;
    color: var(--teal);
    margin: 0;
    line-height: 1.05;
    max-width: 880px;
  }

  @media (max-width: 767px) {
    .title-block { padding: 0 16px; margin-top: 40px; }
    .page-title { font-size: clamp(32px, 8vw, 48px); }
  }
</style>
```

- [ ] **Step 5: Smoke-test by running validate + sync**

Run: `npm run validate && npx astro sync 2>&1 | tail -10`

Expected: validator passes (still no transactions MDX exists, so collection is empty); astro sync completes without errors. The 4 new components compile when consumed by Section 5's pages, not standalone — so this step is a baseline check, not a full render verification.

- [ ] **Step 6: Commit all 4 components**

```bash
git add src/components/transactions/LimitationsBlock.astro src/components/transactions/RelatedBlock.astro src/components/transactions/FooterFold.astro src/components/transactions/DeepDiveTitleBlock.astro
git commit -m "$(cat <<'EOF'
feat(phase-3c.1): four deep-dive + index chrome components

LimitationsBlock — "WHAT THIS DOESN'T YET DO" mono heading + bulleted
  list rendered from frontmatter limitations[]. Per spec §9.1 step 10.

RelatedBlock — Reads frontmatter cross-link fields + the derived
  crosslinks.json (Task 2.3 writes it). Renders 3-5 link rows: part
  of (case study), sister ship (sibling transactions, both forward
  + reverse), supersedes (previousVersion forward), superseded by
  (derived reverse), architecture writeup (forward-declared; target
  arrives Phase 3c.2), essay (forward-declared). No author maintenance
  — graph self-heals. Per spec §9.1 step 11 + §14.

FooterFold — "subscribe via RSS" + "view the fleet ↗" below ledger
  rows on index pages. Reads FLEET_DASHBOARD_URL from src/lib/site.ts.
  Not rendered on deep-dive pages. Per spec §2.1 + §10.3.

DeepDiveTitleBlock — dateline + surface/status pills + h1. The h1
  carries view-transition-name: ledger-title-<slug> — the morph target
  from the index ledger row's row-title (Task 4.3). Per spec §2.2 +
  §9.1 steps 1-3 + §15.

All 4 components are pure Astro + scoped CSS; no client-side JS.
RelatedBlock is the largest at ~120 lines because the bidirectional
graph merging happens at render time per row's reverse-lookup entry.

Per transactions-spec §2.1 + §2.2 + §9 + §14.
EOF
)"
```

---

## Section 5 — Routes: index, per-surface, deep-dive

Four files under `src/pages/transactions/`. Each consumes the components from Section 4 + the Phase 3b shared components (`<MethodsStrip />` + `<FourQBlock />`) by **import**. The Astro routing collision between `[surface]/index.astro` and `[slug].astro` is resolved by Task 2.1's `forbiddenSlugs` validation rule (transaction slug values never equal surface enum values).

### Task 5.1: `/transactions/` index page

**Files:**
- Create: `src/pages/transactions/index.astro`
- Create: `src/styles/transactions.css`

Per transactions spec §2.1 + §4.1. Lists all transactions sorted desc by `shipped`. Bands: dateline → page title block → filter pills (ALL active) → ledger rows → footer fold.

- [ ] **Step 1: Write the index page**

Create `src/pages/transactions/index.astro`:

```astro
---
/**
 * /transactions/ — ALL filter index page.
 *
 * Lists every transaction sorted desc by shipped ISO. Bands:
 *   dateline → page title block → filter pills (ALL active) →
 *   ledger rows → footer fold.
 *
 * Per transactions-spec-v1.md §2.1 + §4.1 + §10.3.
 */
import BaseLayout from "~/layouts/BaseLayout.astro";
import { getCollection } from "astro:content";
import IndexHeader from "~/components/transactions/IndexHeader.astro";
import FilterPills from "~/components/transactions/FilterPills.astro";
import LedgerRow from "~/components/transactions/LedgerRow.astro";
import FooterFold from "~/components/transactions/FooterFold.astro";

const transactions = (await getCollection("transactions"))
  .sort((a, b) => b.data.shipped.localeCompare(a.data.shipped));

const count = transactions.length;
const lastUpdated = transactions[0]?.data.shipped ?? new Date().toISOString().slice(0, 10);
---

<BaseLayout title="Transactions — Sean Winslow" description="Every shipped artifact across the agent fleet, dated, with the explanation attached.">
  <IndexHeader count={count} lastUpdatedISO={lastUpdated} />
  <FilterPills />

  <section class="ledger-list" aria-label="Ledger rows">
    {count === 0 && (
      <p class="empty-state">
        No artifacts on the ledger yet. Check back ~weekly.
      </p>
    )}
    {transactions.map((t) => (
      <LedgerRow
        slug={t.slug}
        shipped={t.data.shipped}
        surface={t.data.surface}
        status={t.data.status}
        title={t.data.title}
        valueProp={t.data.valueProp}
      />
    ))}
  </section>

  <FooterFold />
</BaseLayout>

<style>
  .ledger-list {
    max-width: 1120px;
    margin: 0 auto;
    padding: 0 24px;
  }
  .empty-state {
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--ink-secondary);
    padding: 24px 0;
    text-align: center;
  }
  @media (max-width: 767px) {
    .ledger-list { padding: 0 16px; }
  }
</style>
```

- [ ] **Step 2: Create the page-scoped stylesheet shell**

Create `src/styles/transactions.css`:

```css
/* ----------------------------------------------------------------------
 * src/styles/transactions.css — page-scoped overrides for /transactions/*
 *
 * Most styling is in the component <style> blocks. This file is for
 * anything that needs to span multiple components or interact with
 * Astro's <main> wrapper. Kept intentionally small in v1.
 * ---------------------------------------------------------------------- */

/* Reserve future cross-cutting overrides for the transactions surface. */
.transactions-surface { /* placeholder for future overrides */ }
```

- [ ] **Step 3: Smoke-test the build**

Run: `npm run dev` (if not already running) and then `curl -sI http://localhost:4321/transactions/ | head -1`

Expected: `HTTP/1.1 200 OK`. The page renders an empty state ("No artifacts on the ledger yet") because Section 7 hasn't shipped MDX yet. The IndexHeader + FilterPills + FooterFold all render even with zero rows. If `curl` returns 404, verify the file lives at `src/pages/transactions/index.astro` and dev server has reloaded.

- [ ] **Step 4: Commit**

```bash
git add src/pages/transactions/index.astro src/styles/transactions.css
git commit -m "$(cat <<'EOF'
feat(phase-3c.1): /transactions/ index page — ALL filter

Composes IndexHeader → FilterPills → LedgerRow[] → FooterFold. Pulls
transactions collection via getCollection("transactions"), sorts desc
by ISO `shipped`, passes to <LedgerRow /> per row.

Empty state ("No artifacts on the ledger yet. Check back ~weekly.")
renders when the collection has zero rows — true today; Section 7
ships 3 placeholders that exercise the populated state.

Styles live in each component's <style> block; transactions.css is
a small placeholder for future cross-component overrides.

Per transactions-spec §2.1 + §4.1.
EOF
)"
```

### Task 5.2: `/transactions/[surface]/` per-surface SSG pages

**Files:**
- Create: `src/pages/transactions/[surface]/index.astro`

Per transactions spec §7.1 + §7.2 + §7.3. Five static outputs (`fleet`, `pipeline`, `product`, `writing`, `infra`) — one per surface enum value. `getStaticPaths()` returns exactly those 5 values, never any transaction slugs (the validator enforces no slug collision via Task 2.1's `forbiddenSlugs`).

- [ ] **Step 1: Write the per-surface page**

Create `src/pages/transactions/[surface]/index.astro`:

```astro
---
/**
 * /transactions/<surface>/ — per-surface SSG page.
 *
 * One static output per surface enum value (5 total). Filters the
 * collection to entries with matching `surface`. Empty state per spec §7.3.
 *
 * Per transactions-spec-v1.md §7 + §16.
 */
import BaseLayout from "~/layouts/BaseLayout.astro";
import { getCollection } from "astro:content";
import IndexHeader from "~/components/transactions/IndexHeader.astro";
import FilterPills from "~/components/transactions/FilterPills.astro";
import LedgerRow from "~/components/transactions/LedgerRow.astro";
import FooterFold from "~/components/transactions/FooterFold.astro";

const SURFACE_ENUM = ["fleet", "pipeline", "product", "writing", "infra"] as const;
type Surface = typeof SURFACE_ENUM[number];

export async function getStaticPaths() {
  return [
    { params: { surface: "fleet" } },
    { params: { surface: "pipeline" } },
    { params: { surface: "product" } },
    { params: { surface: "writing" } },
    { params: { surface: "infra" } },
  ];
}

const { surface } = Astro.params as { surface: Surface };

const all = await getCollection("transactions");
const filtered = all
  .filter((t) => t.data.surface === surface)
  .sort((a, b) => b.data.shipped.localeCompare(a.data.shipped));

const count = filtered.length;
const lastUpdated = filtered[0]?.data.shipped ?? new Date().toISOString().slice(0, 10);

const pageTitle = `Transactions / ${surface} — Sean Winslow`;
const pageDescription = `Every shipped ${surface} artifact, dated, with the explanation attached.`;

// Empty-state next-ship date — spec §7.3 says pulls from next-piece.json
// filtered to rows whose target surface matches. v1 simplification: a
// generic "Check back soon" message; per-surface next-ship lookup arrives
// post-Phase 4 when Daily Driver populates next-piece.json with surface tags.
const emptyMessage = `No artifacts in ${surface} yet. Check back soon.`;
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <IndexHeader count={count} lastUpdatedISO={lastUpdated} surface={surface} />
  <FilterPills activeSurface={surface} />

  <section class="ledger-list" aria-label={`${surface} ledger rows`}>
    {count === 0 && (
      <p class="empty-state">{emptyMessage}</p>
    )}
    {filtered.map((t) => (
      <LedgerRow
        slug={t.slug}
        shipped={t.data.shipped}
        surface={t.data.surface}
        status={t.data.status}
        title={t.data.title}
        valueProp={t.data.valueProp}
      />
    ))}
  </section>

  <FooterFold />
</BaseLayout>

<style>
  .ledger-list {
    max-width: 1120px;
    margin: 0 auto;
    padding: 0 24px;
  }
  .empty-state {
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--ink-secondary);
    padding: 48px 0;
    text-align: center;
  }
  @media (max-width: 767px) {
    .ledger-list { padding: 0 16px; }
  }
</style>
```

- [ ] **Step 2: Smoke-test 5 routes**

Run in parallel (5 curls):

```bash
for s in fleet pipeline product writing infra; do
  echo "=== /transactions/$s/ ==="
  curl -sI "http://localhost:4321/transactions/$s/" | head -1
done
```

Expected: all 5 return `HTTP/1.1 200 OK`. Each renders the empty state until Section 7 ships placeholders.

Visual smoke (optional, browser): open `http://localhost:4321/transactions/fleet/` — confirm the `fleet` pill is active (1px stamp-amber bottom border, weight 500) and the other pills are inactive.

- [ ] **Step 3: Commit**

```bash
git add src/pages/transactions/[surface]/index.astro
git commit -m "$(cat <<'EOF'
feat(phase-3c.1): /transactions/<surface>/ — 5 SSG filter pages

getStaticPaths returns exactly the 5 surface enum values (fleet /
pipeline / product / writing / infra). Filters the collection to
matching surface; sorts desc by ISO shipped. Empty state per spec
§7.3 ("No artifacts in <surface> yet. Check back soon.").

Per-surface next-ship date lookup from next-piece.json (per spec
§7.3) is deferred — v1 ships a generic empty message; surface-tagged
next-piece arrives post-Phase 4 when Daily Driver populates surface
metadata. Documented in §X deferral table.

The active filter pill is set via `activeSurface={surface}` prop on
<FilterPills /> — 1px stamp-amber border + aria-current="page" per
spec §7.1.

Per transactions-spec §7 + §16.
EOF
)"
```

### Task 5.3: `/transactions/[slug]/` deep-dive page

**Files:**
- Create: `src/pages/transactions/[slug].astro`

Per transactions spec §2.2 + §9.1 (13 bands) + §15 (View Transition). Composes the 13-band layout from imported components — including Phase 3b's `<MethodsStrip />` + `<FourQBlock />` by direct import (no fork).

- [ ] **Step 1: Write the deep-dive page**

Create `src/pages/transactions/[slug].astro`:

```astro
---
/**
 * /transactions/<slug>/ — deep-dive page.
 *
 * Composes the 13-band layout per transactions-spec §9.1:
 *   1. Dateline strip                 → <DeepDiveTitleBlock />
 *   2. Surface + status pills row     → <DeepDiveTitleBlock />
 *   3. Title                          → <DeepDiveTitleBlock /> (view-transition target)
 *   4. Tags (optional, deferred v1 — no tags field on schema per §3.3)
 *   5. Value-prop pull-quote          → <ValuePropPullQuote />
 *   6. Loom poster (optional)         → <LoomPoster />
 *   7. Methods strip                  → <MethodsStrip /> (Phase 3b shared component)
 *   8. EXPLANATION (4Q) block         → <FourQBlock /> (Phase 3b shared component)
 *   9. "Read the canonical EXPLANATION.md →" link
 *  10. Limitations block              → <LimitationsBlock />
 *  11. Related block (conditional)    → <RelatedBlock />
 *  12. Next/Prev nav                  → inline (small primitive)
 *  13. Registration mark              → inline span (single-emoji-style closeout)
 *
 * Forbidden slug collision: [surface]/ and [slug].astro both resolve under
 * /transactions/<value>/. Disjoint sets enforced by validator (Task 2.1
 * forbiddenSlugs); Astro 5 resolves to whichever route's getStaticPaths
 * declares the value.
 *
 * Per transactions-spec-v1.md §2.2 + §9 + §15 + §16.
 */
import BaseLayout from "~/layouts/BaseLayout.astro";
import { getCollection, type CollectionEntry } from "astro:content";
import fs from "node:fs/promises";
import path from "node:path";

import DeepDiveTitleBlock from "~/components/transactions/DeepDiveTitleBlock.astro";
import ValuePropPullQuote from "~/components/transactions/ValuePropPullQuote.astro";
import LoomPoster from "~/components/transactions/LoomPoster.astro";
import MethodsStrip from "~/components/case-study/MethodsStrip.astro";
import FourQBlock from "~/components/case-study/FourQBlock.astro";
import LimitationsBlock from "~/components/transactions/LimitationsBlock.astro";
import RelatedBlock from "~/components/transactions/RelatedBlock.astro";

type Txn = CollectionEntry<"transactions">;

export async function getStaticPaths() {
  const all = await getCollection("transactions");
  return all.map((entry) => ({
    params: { slug: entry.slug },
    props: { entry },
  }));
}

interface Props { entry: Txn }
const { entry } = Astro.props;
const { Content } = await entry.render();

// Adjacent transactions (next/prev in desc-shipped order).
const all = (await getCollection("transactions"))
  .sort((a, b) => b.data.shipped.localeCompare(a.data.shipped));
const idx = all.findIndex((t) => t.slug === entry.slug);
const next = idx >= 0 && idx + 1 < all.length ? all[idx + 1] : null;
const prev = idx > 0 ? all[idx - 1] : null;

// Load the cross-link graph + a title lookup map.
const ROOT = process.cwd();
const crosslinksPath = path.join(ROOT, "src/content/transactions/.crosslinks.json");
let crosslinks: { transactions?: Record<string, { supersededBy?: string; siblingShips?: string[] }> } = {};
try {
  const raw = await fs.readFile(crosslinksPath, "utf8");
  crosslinks = JSON.parse(raw);
} catch {
  // Crosslinks file may be missing on a fresh checkout before prebuild
  // runs. RelatedBlock degrades gracefully — no reverse-derived rows.
}

const titleByTxnSlug: Record<string, string> = Object.fromEntries(
  all.map((t) => [t.slug, t.data.title])
);

const workCollection = await getCollection("work");
const titleByWorkSlug: Record<string, string> = Object.fromEntries(
  workCollection.map((w) => [w.slug, w.data.title])
);

const data = entry.data;
const slug = entry.slug;

// 4Q rendering:
//   - When explanationUrl is set AND a fetched canonical file exists
//     at src/content/explanations/<slug>.md, render that.
//   - Otherwise, the MDX body's inline-body 4Q headings render via
//     <Content /> (Astro's MDX renderer). FourQBlock is skipped in
//     that case — the body itself provides the 4 sections.
let canonicalMarkdown: string | undefined;
if (data.explanationUrl) {
  const canonicalPath = path.join(ROOT, "src/content/explanations", `${slug}.md`);
  try {
    canonicalMarkdown = await fs.readFile(canonicalPath, "utf8");
  } catch {
    canonicalMarkdown = undefined; // fetcher hasn't run yet — render link only
  }
}

const useFourQBlock = Boolean(canonicalMarkdown);
const pageTitle = `${data.title} — Sean Winslow`;
const pageDescription = data.valueProp;

// EXPLANATION.md link target — either the explanationUrl value (preferred)
// or, for V3-fallback rows, the repoUrl (or null — link omitted).
const canonicalLink = data.explanationUrl ?? data.repoUrl ?? null;
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <article class="txn-deep-dive">
    <DeepDiveTitleBlock
      slug={slug}
      dateline={data.dateline}
      surface={data.surface}
      status={data.status}
      title={data.title}
    />

    <ValuePropPullQuote text={data.valueProp} />

    {data.loomUrl && (
      <LoomPoster
        loomUrl={data.loomUrl}
        posterUrl={data.loomPosterUrl}
        title={data.title}
      />
    )}

    <MethodsStrip methods={data.methods} />

    {useFourQBlock ? (
      <FourQBlock
        frame="LEDGER"
        slug={slug}
        canonicalMarkdown={canonicalMarkdown}
        explanationUrl={data.explanationUrl}
      />
    ) : (
      <section class="inline-body-4q" aria-labelledby={`4q-${slug}`}>
        <h2 id={`4q-${slug}`} class="section-heading">─ EXPLANATION ─</h2>
        <div class="prose">
          <Content />
        </div>
        {canonicalLink && (
          <p class="canonical-link">
            <a href={canonicalLink} rel="external">&rarr; read the canonical EXPLANATION.md on github</a>
            <span class="info-glyph" aria-hidden="true">&#9432;</span>
          </p>
        )}
      </section>
    )}

    <LimitationsBlock items={data.limitations} />

    <RelatedBlock
      slug={slug}
      relatedCaseStudy={data.relatedCaseStudy}
      relatedTransactions={data.relatedTransactions}
      relatedEssay={data.relatedEssay}
      previousVersion={data.previousVersion}
      relatedArchitecture={data.relatedArchitecture}
      crosslinks={crosslinks}
      titleByTxnSlug={titleByTxnSlug}
      titleByWorkSlug={titleByWorkSlug}
    />

    <nav class="next-prev" aria-label="Adjacent transactions">
      {next ? (
        <a href={`/transactions/${next.slug}/`} class="np-link np-prev">
          &larr; {next.data.shipped.replace(/-/g, ".")} {next.data.title}
        </a>
      ) : <span></span>}
      {prev ? (
        <a href={`/transactions/${prev.slug}/`} class="np-link np-next">
          {prev.data.shipped.replace(/-/g, ".")} {prev.data.title} &rarr;
        </a>
      ) : <span></span>}
    </nav>

    <span class="registration-mark" aria-hidden="true">&#9680;</span>
  </article>
</BaseLayout>

<style>
  .txn-deep-dive {
    position: relative;
    padding-bottom: 80px;
  }

  .inline-body-4q {
    max-width: 1120px;
    margin: 80px auto 0;
    padding: 0 24px;
  }
  .section-heading {
    font-family: var(--font-mono);
    font-size: 14px;
    font-weight: 500;
    letter-spacing: 2.4px;
    color: var(--teal);
    opacity: 0.8;
    text-transform: uppercase;
    margin: 0 0 24px;
  }
  .prose {
    max-width: 720px;
  }
  .prose :global(h2) {
    font-family: var(--font-mono);
    font-size: 14px;
    font-weight: 500;
    letter-spacing: 2.4px;
    color: var(--teal);
    text-transform: uppercase;
    margin: 48px 0 12px;
  }
  .prose :global(p) {
    font-family: var(--font-serif);
    font-size: 18px;
    font-weight: 300;
    line-height: 1.55;
    color: var(--ink);
    margin: 0 0 16px;
  }
  .canonical-link {
    margin: 32px 0 0;
    font-family: var(--font-mono);
    font-size: 13px;
    font-weight: 500;
    color: var(--teal);
  }
  .canonical-link a {
    color: var(--teal);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 160ms var(--ease-out);
  }
  .canonical-link a:hover,
  .canonical-link a:focus-visible {
    border-bottom-color: var(--teal);
    outline: none;
  }
  .canonical-link .info-glyph {
    margin-left: 6px;
    color: var(--ink-secondary);
    font-size: 12px;
  }

  .next-prev {
    max-width: 1120px;
    margin: 64px auto 0;
    padding: 0 24px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
  }
  .np-link {
    font-family: var(--font-mono);
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 1.0px;
    color: var(--teal);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 160ms var(--ease-out);
  }
  .np-next { text-align: right; }
  .np-link:hover,
  .np-link:focus-visible {
    border-bottom-color: var(--teal);
    outline: none;
  }

  .registration-mark {
    position: absolute;
    right: 24px;
    bottom: 16px;
    font-size: 18px;
    color: var(--stamp-amber);
    opacity: 0.6;
  }

  @media (max-width: 767px) {
    .inline-body-4q { padding: 0 16px; }
    .next-prev { padding: 0 16px; grid-template-columns: 1fr; gap: 8px; }
    .np-next { text-align: left; }
    .registration-mark { right: 16px; }
  }
</style>
```

- [ ] **Step 2: Smoke-test the route (no MDX yet — should 404 cleanly because getStaticPaths returns no entries)**

Run: `curl -sI "http://localhost:4321/transactions/phase-d-typed-edges/" | head -1`

Expected: `HTTP/1.1 404 Not Found`. Section 7 ships the placeholders that populate `getStaticPaths()`. The 404 here is correct — it confirms the route file compiles + getStaticPaths runs against an empty collection.

If `curl` returns 500, the page has a compile error — most likely a typo in a component import path or a TS error on `data.methods`. Fix inline.

- [ ] **Step 3: Commit**

```bash
git add src/pages/transactions/[slug].astro
git commit -m "$(cat <<'EOF'
feat(phase-3c.1): /transactions/<slug>/ deep-dive — 13-band composition

Composes the deep-dive layout per spec §9.1 (13 bands):
  1-3. DeepDiveTitleBlock (dateline + pills + h1 with view-transition-name)
  5. ValuePropPullQuote (Newsreader italic, teal rules)
  6. LoomPoster (optional, only when loomUrl is set)
  7. MethodsStrip (IMPORTED from Phase 3b — no fork)
  8. FourQBlock (IMPORTED from Phase 3b — no fork) OR inline-body 4Q
     via <Content /> when explanationUrl is absent
  9. Canonical EXPLANATION.md link (mono teal)
  10. LimitationsBlock
  11. RelatedBlock (reads .crosslinks.json + the work title map)
  12. Next/Prev nav (adjacent rows in desc-shipped order)
  13. Registration mark (◐ amber, bottom-right corner)

4Q rendering branches on explanationUrl presence:
  - explanationUrl set + canonical file exists → FourQBlock with
    canonicalMarkdown prop
  - explanationUrl set + canonical file missing → FourQBlock with no
    canonicalMarkdown (renders the "read on github" link only;
    degrades gracefully when fetch hasn't run yet)
  - explanationUrl absent → inline-body 4Q via <Content /> (Astro's
    MDX renderer); the MDX body's 4 canonical h2 headings render as
    paragraphs. V3-bridge fallback path.

Astro routing collision with [surface]/index.astro is resolved by
the validator's forbiddenSlugs rule (Task 2.1) — transaction slugs
never match surface enum values.

Per transactions-spec §2.2 + §9 + §15 + §16.
EOF
)"
```

### Task 5.4: Wire route discovery into BaseLayout's RSS auto-discovery

**Files:**
- Read-only verification (the RSS link tag was already added during Phase 2 / Phase 3a)

- [ ] **Step 1: Verify the existing RSS auto-discovery link tag**

Run: `grep -c "transactions/rss.xml" src/layouts/BaseLayout.astro`

Expected: `1`. The `<link rel="alternate" type="application/rss+xml" href="/transactions/rss.xml" title="Sean Winslow — Transactions" />` tag was added during Phase 2 / Phase 3a baseline (verified during reconnaissance). The RSS endpoint that satisfies this link arrives in Task 6.2.

- [ ] **Step 2: No commit**

Verification only.


---

## Section 6 — RSS feed

Per transactions spec §10. Single endpoint at `/transactions/rss.xml` carrying full 4Q content as `content:encoded`. Uses `@astrojs/rss` (new dependency).

### Task 6.1: Install `@astrojs/rss`

**Files:**
- Modify: `package.json` (auto-updated by npm install)
- Modify: `package-lock.json` (auto-updated)

- [ ] **Step 1: Install the dependency**

Run: `npm install @astrojs/rss`

Expected: `package.json` gains `"@astrojs/rss": "^4.x"` under `dependencies`; `package-lock.json` updates. No other deps change.

- [ ] **Step 2: Verify install**

Run: `npm ls @astrojs/rss 2>&1 | head -5`

Expected: prints `@astrojs/rss@4.x.x` (or whichever 4.x is current). If npm errors with peer-dep conflicts, run `npm install @astrojs/rss --legacy-peer-deps` and document in the commit message.

- [ ] **Step 3: Commit**

```bash
git add package.json package-lock.json
git commit -m "$(cat <<'EOF'
chore(phase-3c.1): add @astrojs/rss dependency

Required for the /transactions/rss.xml feed endpoint (Task 6.2).
Astro's official RSS adapter — same primitive Phase 3c.2 uses for
/architecture/rss.xml and /essays/rss.xml.

Per transactions-spec §10.1 + §17.
EOF
)"
```

### Task 6.2: Write the RSS endpoint

**Files:**
- Create: `src/pages/transactions/rss.xml.ts`

Per transactions spec §10.1 + §10.2 (full content, not summaries). The endpoint sorts by ISO `shipped` desc, renders the value-prop as `description`, and includes the full 4Q content as `content:encoded` (read from either the fetched canonical EXPLANATION.md or the inline-body MDX rendering).

- [ ] **Step 1: Write the endpoint**

Create `src/pages/transactions/rss.xml.ts`:

```ts
/**
 * /transactions/rss.xml — RSS 2.0 feed for the ledger.
 *
 * Sorts desc by ISO `shipped`. Items carry full 4Q content as
 * content:encoded (read from the fetched EXPLANATION.md when
 * explanationUrl is set + the canonical file exists; otherwise
 * the MDX body's rendered HTML).
 *
 * Per transactions-spec-v1.md §10.
 */
import rss from "@astrojs/rss";
import { getCollection } from "astro:content";
import fs from "node:fs/promises";
import path from "node:path";

const ROOT = process.cwd();

async function readCanonical(slug: string): Promise<string | null> {
  const canonicalPath = path.join(ROOT, "src/content/explanations", `${slug}.md`);
  try {
    return await fs.readFile(canonicalPath, "utf8");
  } catch {
    return null;
  }
}

export async function GET(context: { site?: URL }) {
  const transactions = (await getCollection("transactions"))
    .sort((a, b) => b.data.shipped.localeCompare(a.data.shipped));

  const items = await Promise.all(
    transactions.map(async (t) => {
      // Prefer canonical EXPLANATION.md when fetched; fall back to the MDX body.
      let content = "";
      if (t.data.explanationUrl) {
        const canonical = await readCanonical(t.slug);
        if (canonical) content = canonical;
      }
      if (!content) {
        // Render the MDX body as a string — Astro's renderer is async + returns
        // an Astro component. For RSS we want the raw body text since the
        // inline-body 4Q already carries the same prose as the deep-dive page.
        const { body } = t;
        content = body ?? "";
      }

      return {
        title: t.data.title,
        link: `/transactions/${t.slug}/`,
        pubDate: new Date(`${t.data.shipped}T12:00:00Z`),
        description: t.data.valueProp,
        // content:encoded — wrap in <![CDATA[ ... ]]> so HTML entities don't double-escape
        content: content,
        customData: `<surface>${t.data.surface}</surface><status>${t.data.status}</status>`,
      };
    })
  );

  return rss({
    title: "Sean Winslow — Transactions",
    description: "Every shipped artifact across the agent fleet, dated, with the explanation attached.",
    site: context.site ?? new URL("https://seanwinslow.com"),
    items,
    customData: `<language>en-us</language>`,
  });
}
```

- [ ] **Step 2: Smoke-test the endpoint**

Run: `curl -sI "http://localhost:4321/transactions/rss.xml" | head -3`

Expected:
```
HTTP/1.1 200 OK
Content-Type: application/xml
...
```

Run: `curl -s "http://localhost:4321/transactions/rss.xml" | head -20`

Expected: XML output starting with `<?xml version="1.0"`, then `<rss version="2.0" ...>`, then `<channel>` with the title `Sean Winslow — Transactions`. The `<item>` list is empty until Section 7 ships placeholders.

If `curl` returns 500, the endpoint has a compile or runtime error — most likely an import path issue or the `@astrojs/rss` import didn't resolve. Inspect the dev server log.

- [ ] **Step 3: Commit**

```bash
git add src/pages/transactions/rss.xml.ts
git commit -m "$(cat <<'EOF'
feat(phase-3c.1): /transactions/rss.xml — RSS 2.0 feed endpoint

Sorts desc by ISO shipped. Items carry:
  - title (transaction title)
  - link (/transactions/<slug>/)
  - pubDate (ISO shipped at noon UTC)
  - description (valueProp — the 1-line thesis)
  - content (full 4Q content as content:encoded — from canonical
    EXPLANATION.md when fetched, else the MDX body)
  - customData (surface + status as inline XML elements for
    reader-side filtering)

Channel-level customData declares <language>en-us</language>.

The RSS link tag in BaseLayout (added Phase 2 baseline) auto-discovers
this endpoint. Recruiter subscribes once via reader; future ledger
rows arrive in their feed without bookmarking.

Per transactions-spec §10 + §16.
EOF
)"
```

---

## Section 7 — Hand-written placeholder content

Three transactions MDXs that exercise the layout end-to-end without running the V3 migration. Each placeholder demonstrates a different rendering path:

| Slug | Surface | Status | 4Q path | Cross-links exercised |
|---|---|---|---|---|
| `phase-d-typed-edges` | fleet | SHIPPED | inline-body 4Q (V3 fallback) | `relatedCaseStudy: code-brain` |
| `intent-engineering-mcp` | product | SHIPPED | inline-body 4Q (V3 fallback) | `relatedCaseStudy: intent-engineering-mcp` (same slug both surfaces — the canonical IA shape) |
| `vault-knowledge-mcp` | product | ACTIVE | inline-body 4Q (V3 fallback) | `relatedArchitecture: vault-knowledge-mcp` (forward-declared; Phase 3c.2 fills in `/architecture/vault-knowledge-mcp/`) |

Slugs `phase-d-typed-edges` and `intent-engineering-mcp` are byte-identical to their V3-bridge equivalents — when Sean runs the migration script later, the slug collision surfaces immediately for adjudication. The placeholder bodies carry the inline-body 4Q headings so the validator passes (Task 2.1's `detectInline4Q` matches the 4 canonical h2s).

### Task 7.1: `phase-d-typed-edges.mdx` (fleet · SHIPPED · inline-body 4Q)

**Files:**
- Create: `src/content/transactions/phase-d-typed-edges.mdx`

- [ ] **Step 1: Write the MDX**

Create `src/content/transactions/phase-d-typed-edges.mdx`:

```mdx
---
title: "Phase D — Typed Reasoning Edges"
dateline: "BOSTON, MAY 13, 2026"
shipped: "2026-05-13"

surface: fleet
status: SHIPPED

valueProp: "A typed-edge SQLite layer the nightly synthesizer writes to as a side effect of connection articles. Six relation types turn weekly knowledge-lint contradiction detection into a SQL query instead of an LLM scan."

methods:
  - task: edge insertion
    tool: Code Brain (synthesizer)
    cost: Sonnet 4.6 (HybridRouter)
    link: /work/code-brain
  - task: sql query
    tool: sqlite3
    cost: local / $0
    link: null
  - task: schema enforcement
    tool: SQLite CHECK constraints
    cost: local / $0
    link: null

limitations:
  - "Contradiction detection is still rigid; it flags syntax differences as logical conflicts."
  - "Nightly synthesize takes 4 minutes because it queries the entire graph."

# 4Q source: inline-body fallback (V3-bridge legacy contract — validator detects the 4 canonical h2 headings below).
# explanationUrl: <set when upstream EXPLANATION.md ships>

relatedCaseStudy: code-brain
relatedTransactions:
  - knowledge-loop-phase-6
relatedArchitecture: null

repoUrl: https://github.com/seanwinslow28/code-brain
---

## What is this?

The fourth phase of the knowledge loop. Instead of just storing notes, the system now stores typed edges between concepts. If Concept A contradicts Concept B, the database knows it directly — six relation types: `supports`, `contradicts`, `evolved_into`, `supersedes`, `depends_on`, `related_to`.

## Why this approach?

[PLACEHOLDER — Q2] Weekly lints were getting too expensive. Passing 100 pages of text to Claude and asking "are there contradictions?" cost $4 every Sunday. The redesign uses the LLM to structure the edges once during nightly synthesis, then the weekly lint runs `SELECT * FROM edges WHERE type='contradicts'`. Don't optimize prematurely, but do optimize when the pattern is proven.

## What would break?

[PLACEHOLDER — Q3] If the LLM hallucinates an edge type that isn't in the enum, the SQLite insert throws an error and the nightly job fails. This happened twice before strict schema enforcement (`CHECK type IN ('supports', 'contradicts', ...)`). The second failure mode is graph drift: an edge inserted in 2026-04 referencing concepts that were later renamed leaves dangling references — the schema doesn't enforce referential integrity across concept renames.

## What did I learn?

[PLACEHOLDER — Q4] LLMs are great at identifying relationships, but terrible at scanning large graphs repeatedly. Use the LLM to structure the data once, then use traditional code (SQL) to query it. The cost curve flipped from O(reads × LLM calls) to O(writes × LLM calls + reads × $0).
```

- [ ] **Step 2: Validator check**

Run: `npm run validate 2>&1 | tail -10`

Expected: `✓ transactions/phase-d-typed-edges` appears in the output; exits 0. The `methods` array has 3 entries (≥1 ✓), `limitations` has 2 (≥1 ✓), inline-body 4Q heading detection finds all 4 (`What is this?` / `Why this approach?` / `What would break?` / `What did I learn?`), and the cross-link `relatedCaseStudy: code-brain` resolves to `src/content/work/code-brain.mdx` (exists).

The `relatedTransactions: [knowledge-loop-phase-6]` slug DOES NOT exist yet (it's a future Phase 6 ledger row). Per the validator's behavior (Task 2.1), this triggers an error from `derive_crosslinks.mjs`. Adjust by removing that sibling entry OR by accepting the error and adjusting in Step 3. We adjust in Step 3.

- [ ] **Step 3: Adjust `relatedTransactions` to a sibling that exists in this plan**

Replace the `relatedTransactions:` block in the MDX with:

```yaml
relatedTransactions: []   # Phase 6 row arrives in a future ledger ship; not v1.
```

- [ ] **Step 4: Re-run prebuild gates end-to-end**

Run: `npm run validate && node scripts/derive_crosslinks.mjs && node scripts/fetch_canonical_sources.mjs`

Expected: all three exit 0. derive_crosslinks reports validating 1 transactions cross-link (`relatedCaseStudy → /work/code-brain/`). fetch_canonical_sources finds zero `explanationUrl` entries; transactions walk is a no-op.

- [ ] **Step 5: Visual smoke**

Run: `curl -sI "http://localhost:4321/transactions/phase-d-typed-edges/" | head -1`

Expected: `HTTP/1.1 200 OK`. Visual (browser): the deep-dive renders the 13 bands per spec §9.1; the `<MethodsStrip />` shows 3 rows including the `Code Brain (synthesizer) → /work/code-brain/` link; the inline-body 4Q renders the 4 sections from the MDX body; the `<RelatedBlock />` shows `→ part of: Code Brain`.

- [ ] **Step 6: Commit**

```bash
git add src/content/transactions/phase-d-typed-edges.mdx
git commit -m "$(cat <<'EOF'
feat(phase-3c.1): phase-d-typed-edges placeholder ledger row

First placeholder transaction. Exercises:
  - surface: fleet, status: SHIPPED rendering
  - inline-body 4Q rendering path (V3-bridge fallback contract)
  - methods[] with internal cross-link → /work/code-brain/
  - relatedCaseStudy → bidirectional cross-link via derive_crosslinks
  - LedgerRow on index page + DeepDiveTitleBlock + MethodsStrip
    (imported from Phase 3b case-study/) + LimitationsBlock + RelatedBlock

The Q2-Q4 prose carries [PLACEHOLDER —] markers for Sean's future
rewrite. Q1 is real prose (lifted from the V3-bridge body verbatim)
so the cold-read test (spec §18 DoD #16) has at least one section
that reads as production-ready.

The slug is byte-identical to the V3-bridge entry. When Sean runs
scripts/migrate_v3_transactions.mjs later, this file blocks the
migration's overwrite — Sean adjudicates which version (this
placeholder vs the V3 body) wins. Both carry inline-body 4Q so the
validator accepts either.

relatedTransactions: [] (Phase 6 sibling row not yet shipped in v1).

Per transactions-spec §3.2 + §9.2 + §12.4.
EOF
)"
```

### Task 7.2: `intent-engineering-mcp.mdx` (product · SHIPPED · cross-surface paired with /work/)

**Files:**
- Create: `src/content/transactions/intent-engineering-mcp.mdx`

This is the canonical IA shape: the same artifact lives at `/work/intent-engineering-mcp/` (case study) AND `/transactions/intent-engineering-mcp/` (ledger row). Same slug; bidirectional cross-link via `relatedCaseStudy: intent-engineering-mcp`. Tests the "an artifact can appear on multiple surfaces simultaneously with the same slug" rule from BLUEPRINT-COMPLETE §3.1.

- [ ] **Step 1: Write the MDX**

Create `src/content/transactions/intent-engineering-mcp.mdx`:

```mdx
---
title: "Intent Engineering MCP"
dateline: "BOSTON, MAY 12, 2026"
shipped: "2026-05-12"

surface: product
status: SHIPPED

valueProp: "A Model Context Protocol server exposing intent-engineering tools to Claude Desktop. DNS-verified namespace; shipped to npm + MCP registry 13 days early because the design was right."

methods:
  - task: protocol implementation
    tool: MCP SDK
    cost: open-source / $0
    link: null
  - task: registry verification
    tool: DNS-verified MCP registry
    cost: registry / $0
    link: null
  - task: package publish
    tool: npm
    cost: free tier
    link: null
  - task: case-study writeup
    tool: case study
    cost: portfolio time
    link: /work/intent-engineering-mcp

limitations:
  - "Claude Desktop's MCP client revs faster than the server; semver discipline is the v1.x discipline."
  - "DNS verification expires annually — renewal is on Sean's calendar but not automated."

# 4Q via inline-body fallback. Upstream EXPLANATION.md lives in the
# intent-engineering-mcp repo and will be wired via explanationUrl
# when the case-study Option-A canonical path activates.

relatedCaseStudy: intent-engineering-mcp
relatedTransactions: []
relatedArchitecture: null

repoUrl: https://github.com/seanwinslow28/intent-engineering-mcp
---

## What is this?

A Model Context Protocol server published to npm + the MCP registry on May 12, 2026. Two MCP tools registered (`tools/list` + `tools/call`); DNS-verified namespace. Installed by Claude Desktop with one line of config.

## Why this approach?

[PLACEHOLDER — Q2] Three options were on the table: ship as a standalone CLI tool, bake into a larger MCP, or ship as a registered server. The registered-server path won because DNS-verified registry adoption was the right scope-cut signal — recruiter could see the install count + registry presence at a glance. The case study at /work/intent-engineering-mcp documents what got cut to ship early.

## What would break?

[PLACEHOLDER — Q3] Three named failure modes: (1) Claude Desktop's MCP client revs faster than this server; (2) DNS verification expiring; (3) npm registry vetting failing on first publish. Mitigations: semver discipline on the server, calendar reminder for renewal, manual vetting check before each publish.

## What did I learn?

[PLACEHOLDER — Q4] It shipped 13 days early because the design was right. What got cut was right too — the interactive MCP-tool-call embed page is a future spec, not v1. The cut list is on the case-study page; the install count is on this ledger row's <ShippedNow /> block (rendered via the case-study route, not duplicated here).
```

- [ ] **Step 2: Validator + crosslinks + smoke**

Run: `npm run validate && node scripts/derive_crosslinks.mjs && curl -sI http://localhost:4321/transactions/intent-engineering-mcp/ | head -1`

Expected:
- Validator: `✓ transactions/intent-engineering-mcp` line; exits 0
- derive_crosslinks: `✓ intent-engineering-mcp → /work/intent-engineering-mcp/ (methods)` AND `✓ intent-engineering-mcp → /work/intent-engineering-mcp/ (relatedCaseStudy)`; exits 0
- curl: `HTTP/1.1 200 OK`

- [ ] **Step 3: Commit**

```bash
git add src/content/transactions/intent-engineering-mcp.mdx
git commit -m "$(cat <<'EOF'
feat(phase-3c.1): intent-engineering-mcp placeholder — cross-surface IA test

Second placeholder transaction. Same slug as /work/intent-engineering-mcp
— exercises the "artifact appears on multiple surfaces simultaneously
with the same slug" rule from BLUEPRINT-COMPLETE §3.1.

Bidirectional cross-link via relatedCaseStudy: intent-engineering-mcp.
The methods strip also carries an internal link to the same case study
(/work/intent-engineering-mcp). Both directions exercised end-to-end.

Surface: product (per transactions-spec OPEN-1 RESOLVED — MCP server
canonically maps to product per §12.3 free-text remap table).

The case-study route already renders the live <ShippedNow /> block
(Phase 3b, hand-seeded shipped-stats-intent-engineering-mcp.json).
This ledger row carries the wire-service register: dateline + pills +
valueProp + Methods + 4Q + Limitations + Related → /work/.../ + Next/Prev.

Per transactions-spec §3.2 + §14 + BLUEPRINT-COMPLETE §3.1.
EOF
)"
```

### Task 7.3: `vault-knowledge-mcp.mdx` (product · ACTIVE · forward-declared `relatedArchitecture`)

**Files:**
- Create: `src/content/transactions/vault-knowledge-mcp.mdx`

Third placeholder exercises: (a) status ACTIVE (not yet shipped), (b) `relatedArchitecture` field forward-declared so Phase 3c.2 can resolve it. The architecture page for `vault-knowledge-mcp` doesn't exist yet; the `<RelatedBlock />` renders the link anyway (404 today, becomes live when Phase 3c.2 ships).

- [ ] **Step 1: Write the MDX**

Create `src/content/transactions/vault-knowledge-mcp.mdx`:

```mdx
---
title: "Vault Knowledge MCP — Design Lock"
dateline: "BOSTON, MAY 21, 2026"
shipped: "2026-05-21"

surface: product
status: ACTIVE

valueProp: "Design-locked MCP that wraps Sean's vault — the only public PM vault that passes all five Nate-tests on the agent-infrastructure scoreboard. Ship target ~6/4."

methods:
  - task: protocol design
    tool: MCP SDK
    cost: open-source / $0
    link: null
  - task: vault read-layer
    tool: Code Brain (concept_edges + JSONL)
    cost: Sonnet 4.6 (HybridRouter)
    link: /work/code-brain
  - task: design lock
    tool: spec authoring + Notion review
    cost: $0
    link: null

limitations:
  - "Design only at this point — the read-layer is wired to concept_edges but the MCP transport layer is stubbed."
  - "The five-Nate-test scoreboard pass depends on the Audit pillar, which itself depends on the queryable JSONL filesystem layer landing first."

# 4Q via inline-body fallback. explanationUrl arrives when the upstream
# repo ships VAULT_KNOWLEDGE_MCP_EXPLANATION.md.

relatedCaseStudy: code-brain
relatedTransactions: []
relatedArchitecture: vault-knowledge-mcp
# ↑ Forward-declared. Architecture page at /architecture/vault-knowledge-mcp/
#   ships in Phase 3c.2. derive_crosslinks.mjs accepts the forward-declared
#   slug (archSlugs is empty in 3c.1; validation permits any value).

repoUrl: https://github.com/seanwinslow28/code-brain
---

## What is this?

A Model Context Protocol that exposes Sean's vault as a queryable agent surface. Design-locked May 21, 2026; ship-target ~June 4. The architecture argument lives at `/architecture/vault-knowledge-mcp/` (Phase 3c.2). This row tracks the *shipped artifact* — the MCP transport + tool surface — not the thesis.

## Why this approach?

[PLACEHOLDER — Q2] The vault is the only PM vault that passes all five Nate-tests. The MCP wraps it. Recruiter from Glean lands on /architecture/vault-knowledge-mcp/ → reads the scoreboard → clicks through to the ledger row → sees the ship date. The two surfaces close the loop: architecture argues, ledger ships.

## What would break?

[PLACEHOLDER — Q3] If the MCP HTTP transport spec revs between design-lock and ship, the design re-opens. If concept_edges hits the 47-edge production limit (likely), the read-layer paginates or the vault sharding strategy lands first.

## What did I learn?

[PLACEHOLDER — Q4] Design-lock is the artifact, not the code. Locking the design on May 21 with a June 4 ship target gives 14 days of runway — enough for a real implementation cycle without scope drift. The pattern: lock at T-14, ship at T-0.
```

- [ ] **Step 2: Validator + crosslinks**

Run: `npm run validate && node scripts/derive_crosslinks.mjs`

Expected:
- Validator: `✓ transactions/vault-knowledge-mcp` line; exits 0
- derive_crosslinks: `✓ vault-knowledge-mcp → /work/code-brain/ (methods)` AND `✓ vault-knowledge-mcp → /work/code-brain/ (relatedCaseStudy)` AND `✓ vault-knowledge-mcp → /architecture/vault-knowledge-mcp/ (forward-declared; consumer in Phase 3c.2)`; exits 0

Note: the `relatedArchitecture: vault-knowledge-mcp` slug doesn't exist in `/architecture/` yet (Phase 3c.2). The validator accepts it because `archSlugs` is empty in 3c.1 (forward-declared path). The link will 404 on the rendered page until Phase 3c.2 ships — that's expected and documented.

- [ ] **Step 3: Smoke + index render**

Run in parallel:
- `curl -sI http://localhost:4321/transactions/ | head -1`
- `curl -sI http://localhost:4321/transactions/product/ | head -1`
- `curl -sI http://localhost:4321/transactions/vault-knowledge-mcp/ | head -1`

Expected: all three return `HTTP/1.1 200 OK`. The index now lists 3 rows (sorted desc by shipped: vault-knowledge-mcp 2026-05-21 → phase-d-typed-edges 2026-05-13 → intent-engineering-mcp 2026-05-12). The `/transactions/product/` page filters to 2 rows (vault-knowledge-mcp + intent-engineering-mcp). The deep-dive renders all 13 bands.

- [ ] **Step 4: Verify cross-surface filter accuracy**

Run: `curl -s http://localhost:4321/transactions/fleet/ | grep -c "phase-d-typed-edges"`

Expected: `1` (the only fleet entry).

Run: `curl -s http://localhost:4321/transactions/writing/ | grep -c "ledger-row"`

Expected: `0` (empty state — no writing-surface placeholders in v1). Run also `curl -s http://localhost:4321/transactions/writing/ | grep -c "No artifacts in writing yet"` — expected `1`.

- [ ] **Step 5: Commit**

```bash
git add src/content/transactions/vault-knowledge-mcp.mdx
git commit -m "$(cat <<'EOF'
feat(phase-3c.1): vault-knowledge-mcp placeholder — forward-declared relatedArchitecture

Third placeholder transaction. Exercises:
  - status: ACTIVE (not yet shipped) rendering — stamp-amber status pill
  - relatedArchitecture forward-declared (slug "vault-knowledge-mcp"
    points at /architecture/vault-knowledge-mcp/ which arrives in
    Phase 3c.2; derive_crosslinks accepts forward-declared archSlugs
    in 3c.1 — see Task 2.3)
  - filter-page presence on /transactions/product/ alongside the
    intent-engineering-mcp row (2 rows in product surface)
  - empty-state rendering on /transactions/writing/ (no writing rows
    in v1; FilterPills' "writing" pill highlights when active)

The architecture-surface link will 404 today and light up automatically
when Phase 3c.2 ships /architecture/vault-knowledge-mcp/. This is the
shape the cross-link graph is designed for — declare once on the
source row, consumer pages auto-derive the reverse link.

Per transactions-spec §3.2 + architecture-spec §14.1 + BLUEPRINT-COMPLETE
§3.1 + §5 punch-list #1.
EOF
)"
```

---

## Section 8 — View Transition + sitemap verification

### Task 8.1: Verify View Transition seam `ledger-title-<slug>`

**Files:**
- Read-only verification

Per transactions spec §15. Browser-side verification (no automated test — visual smoke per Phase 3b template).

- [ ] **Step 1: Verify the view-transition-name appears on both surfaces**

Run in parallel:
- `curl -s http://localhost:4321/transactions/ | grep -oE 'view-transition-name: ledger-title-[a-z0-9-]+' | sort -u`
- `curl -s http://localhost:4321/transactions/phase-d-typed-edges/ | grep -oE 'view-transition-name: ledger-title-[a-z0-9-]+' | sort -u`

Expected:
- Index: 3 lines (one per placeholder slug: `ledger-title-intent-engineering-mcp`, `ledger-title-phase-d-typed-edges`, `ledger-title-vault-knowledge-mcp`)
- Deep-dive: 1 line (`ledger-title-phase-d-typed-edges`)

If either route shows zero matches, verify the `style` attribute on the row title (`<LedgerRow />` Step 1) + the page h1 (`<DeepDiveTitleBlock />` Task 4.6 Step 4) was written correctly.

- [ ] **Step 2: Visual smoke (Chrome) — optional but recommended**

Open `http://localhost:4321/transactions/` in Chrome. Click any row title. Observe: the title morphs from its row position into the deep-dive page's `<h1>` position via Astro 5's View Transition.

In Safari: same behavior. In Firefox <128: instant page swap (graceful fallback). All three are acceptable.

If the morph doesn't animate in Chrome, the `<ClientRouter />` in BaseLayout isn't engaging — verify the layout still includes `<ClientRouter />` (it was Phase 2 baseline).

- [ ] **Step 3: No commit**

Verification only.

### Task 8.2: Run full `npm run build` + verify sitemap

**Files:**
- Generated: `dist/sitemap-0.xml`

- [ ] **Step 1: Production build**

Run: `npm run build 2>&1 | tail -40`

Expected: prebuild gates pass (validate, fetch-canonical, derive-crosslinks), Astro build completes. Output enumerates routes including:
- `dist/transactions/index.html`
- `dist/transactions/fleet/index.html` (× 5 surfaces)
- `dist/transactions/<slug>/index.html` (× 3 placeholders)
- `dist/transactions/rss.xml`
- `dist/sitemap-0.xml`

If build fails, the most likely culprits are: (a) a TS type error in `[slug].astro` (verify all imports + Props interface match), (b) a missing dependency (`@astrojs/rss` install issue), (c) a Zod schema mismatch on one of the placeholder MDX files.

- [ ] **Step 2: Verify sitemap entries**

Run: `grep -oE "<loc>[^<]+</loc>" dist/sitemap-0.xml | sort -u | grep transactions`

Expected: 9 entries — `/transactions/` + 5 per-surface + 3 deep-dives.

If the sitemap has fewer entries, the build's `getStaticPaths()` didn't return all routes. Most likely cause: a typo in the surface enum or a content-collection sync issue.

- [ ] **Step 3: Verify RSS feed valid XML + correct row count**

Run: `grep -c "<item>" dist/transactions/rss.xml`

Expected: `3` (one per placeholder).

Run: `head -10 dist/transactions/rss.xml`

Expected: starts with `<?xml version="1.0"`, then `<rss version="2.0" ...>`, then `<channel>` with the title `Sean Winslow — Transactions`.

- [ ] **Step 4: No commit**

Verification only — if all checks pass, Phase 3c.1 is functionally complete.

---

## Section 9 — DoD verification

### Task 9.1: Walk transactions-spec §18 items 1-16

For each DoD item below, verify on `http://localhost:4321` (dev) AND in `dist/` (build) and mark `[PASS]` / `[FAIL]` / `[DEFERRED]`. If FAIL, return to the relevant earlier task to fix.

- [ ] **Step 1: §18 item 1 — schema declared per §3.2 with min constraints**

Run: `grep -c "transactions: transactionsCollection\|transactions });" src/content/config.ts`

Wait — the schema uses `transactions = defineCollection(...)` + `export const collections = { work, teaserDeck, transactions };`. Verify with:

Run: `grep -E "^const transactions|collections =" src/content/config.ts`

Expected: 2 matches — the `const transactions` declaration AND the export. Visual scan confirms `.min(1)` on `methods` and `limitations`, `.max(280)` on `valueProp`, the 5-value `surface` enum, the 4-value `status` enum, and the `relatedArchitecture` additive.

Result: ☐

- [ ] **Step 2: §18 item 2 — migration script ran; V3 entries exist with slugs preserved**

**Phase 3c.1 scope caveat:** Per OQ3 (a), the migration script ships un-run. The V3-bridge entries (`phase-d-typed-edges` + `knowledge-loop-phase-6`) are NOT auto-migrated. v1 ships with the hand-written placeholder `phase-d-typed-edges` (slug byte-identical to V3, ready for adjudication when Sean runs migration). `knowledge-loop-phase-6` is not in v1 — Sean runs the migration to bring it in later.

Run: `ls scripts/migrate_v3_transactions.mjs && ls src/content/transactions/phase-d-typed-edges.mdx`

Expected: both files exist.

Result: ☐ [PASS — IDLE; script ships, Sean runs at his pace per OQ3 (a)]

- [ ] **Step 3: §18 item 3 — validate_transactions runs in prebuild + rejects dangling slugs**

Spec named this script `scripts/validate_transactions.mjs`. Phase 3b consolidated to `scripts/validate_content.mjs` (the unified path forward per essays-spec §11.2). Task 2.1 extended that unified script with transactions rules.

Run: `grep -E "prebuild" package.json`

Expected: 1 line showing `"prebuild": "node scripts/validate_content.mjs && node scripts/fetch_canonical_sources.mjs && node scripts/derive_crosslinks.mjs"`.

Run: `npm run validate 2>&1 | grep -c "✓ transactions"`

Expected: `3` (one per placeholder).

Run (negative test): temporarily edit `src/content/transactions/phase-d-typed-edges.mdx` to set `relatedCaseStudy: nonexistent-slug`, then run `node scripts/derive_crosslinks.mjs`.

Expected: exit code 1; error message names the file + field + slug + Levenshtein suggestion: `transactions/phase-d-typed-edges: relatedCaseStudy "nonexistent-slug" → /work/nonexistent-slug/ — no such slug.` Revert the edit afterward.

Result: ☐

- [ ] **Step 4: §18 item 4 — fetch_explanations runs in prebuild + V3 entries fall back to inline body**

Run: `node scripts/fetch_canonical_sources.mjs 2>&1 | tail -5`

Expected: prints `[transactions]` header followed by no fetches (none of the 3 placeholders declare `explanationUrl`), then `done.` exit 0.

The fallback path is tested by the deep-dive page (Task 5.3): when `explanationUrl` is absent, the `inline-body-4q` branch renders the MDX body's 4 h2 sections via `<Content />`.

Verify the inline rendering: `curl -s http://localhost:4321/transactions/phase-d-typed-edges/ | grep -cE "What is this\?|Why this approach\?|What would break\?|What did I learn\?"`

Expected: `4`.

Result: ☐

- [ ] **Step 5: §18 item 5 — derive_crosslinks runs in prebuild + writes crosslinks.json + Related blocks render reverse links**

Run: `cat src/content/transactions/.crosslinks.json | head -20`

Expected: JSON with `schemaVersion: "phase-3c.1"`, `generatedAt`, `transactions` (with reverse-derived entries for any siblings + previousVersion), `byCaseStudy`, `byArchitecture`, `byEssay`.

Verify the Related block renders: `curl -s http://localhost:4321/transactions/intent-engineering-mcp/ | grep -c "→ part of:"`

Expected: `1`.

Result: ☐

- [ ] **Step 6: §18 item 6 — index page renders all rows in desc shipped order with §8.1 anatomy**

Run: `curl -s http://localhost:4321/transactions/ | grep -oE "ledger-title-[a-z-]+" | uniq`

Expected (in order):
```
ledger-title-vault-knowledge-mcp     ← 2026-05-21 (most recent)
ledger-title-phase-d-typed-edges     ← 2026-05-13
ledger-title-intent-engineering-mcp  ← 2026-05-12
```

If the order is wrong, the `.sort((a, b) => b.data.shipped.localeCompare(a.data.shipped))` call in `src/pages/transactions/index.astro` regressed.

Result: ☐

- [ ] **Step 7: §18 item 7 — per-surface pages render with active pill + aria-current="page"**

Run in parallel (5 routes):
```bash
for s in fleet pipeline product writing infra; do
  echo "=== $s ==="
  curl -s "http://localhost:4321/transactions/$s/" | grep -oE "aria-current=\"page\"" | head -1
done
```

Expected: each surface route prints `aria-current="page"` exactly once (the active filter pill).

Result: ☐

- [ ] **Step 8: §18 item 8 — empty filter state renders per §7.3**

Run: `curl -s http://localhost:4321/transactions/writing/ | grep -c "No artifacts in writing yet"`

Expected: `1`.

Result: ☐

- [ ] **Step 9: §18 item 9 — each deep-dive renders all 13 sections**

For `/transactions/phase-d-typed-edges/`:
```bash
curl -s http://localhost:4321/transactions/phase-d-typed-edges/ > /tmp/dd-pd.html
echo "dateline:    $(grep -c 'BOSTON, MAY 13, 2026' /tmp/dd-pd.html)"
echo "title-block: $(grep -c 'page-title' /tmp/dd-pd.html)"
echo "pullquote:   $(grep -c 'value-prop-pullquote' /tmp/dd-pd.html)"
echo "methods:     $(grep -c 'methods-strip' /tmp/dd-pd.html)"
echo "4q-section:  $(grep -cE 'What is this\?|inline-body-4q' /tmp/dd-pd.html)"
echo "limitations: $(grep -c 'WHAT THIS DOESN' /tmp/dd-pd.html)"
echo "related:     $(grep -c 'section-heading.*RELATED' /tmp/dd-pd.html)"
echo "next-prev:   $(grep -c 'next-prev' /tmp/dd-pd.html)"
echo "regmark:     $(grep -c 'registration-mark' /tmp/dd-pd.html)"
```

Expected: each line ends with `≥1`. The Loom block is absent (loomUrl null in this placeholder) — that's correct, it's optional.

Result: ☐

- [ ] **Step 10: §18 item 10 — 4Q block both modes work**

Already verified in Step 4 (inline-body fallback). The `explanationUrl` mode is testable by editing a placeholder to set `explanationUrl: <github raw url>` — out of scope for the v1 placeholders (none declare it), but the code path exists in `[slug].astro` (Task 5.3 Step 1: the `useFourQBlock` branch).

Result: ☐ [PASS — inline-body verified; explanationUrl mode untested in v1 because no placeholder declares it, but the rendering branch ships]

- [ ] **Step 11: §18 item 11 — View Transition seam morphs row title → page h1**

Visual smoke test in Chrome (Task 8.1). Programmatic verification of the shared `view-transition-name` attribute already done.

Result: ☐ [PASS — programmatic + visual smoke per Task 8.1]

- [ ] **Step 12: §18 item 12 — RSS feed validates against RSS 2.0 + carries full content + sort matches**

Run: `cat dist/transactions/rss.xml | head -40`

Expected: well-formed RSS 2.0 XML; `<item>` order matches the index (vault-knowledge-mcp → phase-d-typed-edges → intent-engineering-mcp); each `<item>` has `<title>`, `<link>`, `<pubDate>`, `<description>` (the valueProp), `<content:encoded>` (the inline-body 4Q), and `<surface>` + `<status>` customData.

Optional: paste the RSS into https://validator.w3.org/feed/ — should validate without errors.

Result: ☐

- [ ] **Step 13: §18 item 13 — hero `ledger_row` dateline pattern fires when new row landed in last 24h**

**Phase 4 caveat:** The Daily Driver agent's filesystem-read step that computes the `ledger_row` pattern body is Phase 4 work (per BLUEPRINT-COMPLETE §3.6 + §6 Phase 4). The hero already consumes `dateline.json` from Phase 2; Phase 4 wires the producer side.

Manual verification today: set `vault-knowledge-mcp.shipped` to today's date (2026-05-21 already, which is today), run `node scripts/derive_crosslinks.mjs` (no Daily Driver dependency), confirm the row appears at the top of the index. The hero pattern selection itself isn't testable from this surface.

Result: ☐ [PASS — INFRASTRUCTURE; Daily Driver writes nightly in Phase 4]

- [ ] **Step 14: §18 item 14 — reduced motion disables row hover + View Transition + Loom poster zoom**

Chrome DevTools → Rendering → Emulate CSS media feature `prefers-reduced-motion: reduce` → reload `/transactions/`.

Expected: hovering rows does NOT animate the title underline, the glyph translation, or the bg fade. The View Transition fires as instant page swap (Astro 5 honors prefers-reduced-motion at the View Transition level natively).

The Loom poster zoom-in animation is N/A (no v1 placeholder declares loomUrl).

Result: ☐

- [ ] **Step 15: §18 item 15 — Lighthouse Performance ≥95 on index; A11y ≥95; Best Practices = 100**

Open `http://localhost:4321/transactions/` in Chrome → DevTools → Lighthouse → Mobile → Performance / Accessibility / Best Practices / SEO → Analyze.

Expected:
- Performance ≥ 95 (the index is 100% static; only fonts + 3 rows)
- Accessibility ≥ 95
- Best Practices = 100
- SEO ≥ 90

Common failure: missing alt text on the dateline strip — already passes (no images on this index). Color contrast on the surface pill (ink on paper) = 18:1 (AAA). Color contrast on the ARCHIVED status pill = 4.6:1 (AA at 11px+ weight 500 — clears).

Result: ☐

- [ ] **Step 16: §18 item 16 — recruiter cold-read test (60s)**

Sean's manual test post-build. The structural shell ships; cold-read carrying Sean's real prose is post-plan.

Result: ☐ [PARTIAL — structure ships v1; prose validation is Sean's post-plan]

- [ ] **Step 17: Note HEAD + commit count**

Run: `git log --oneline 62c01c3..HEAD | wc -l`

Expected: ~28-32 commits.

Run: `git rev-parse HEAD`

Note the SHA. This is the Phase 3c.1 completion mark; Phase 3c.2's plan references it as its baseline.

Result: ☐

### Task 9.2: Final smoke — full production build clean

- [ ] **Step 1: Production build from clean state**

Run: `rm -rf dist .cache/canonical-sources.lockfile && npm run build 2>&1 | tail -25`

Expected: prebuild gates re-run from cold cache, then Astro build completes. The output enumerates all expected routes:
- `dist/index.html` (Phase 2 home)
- `dist/contact/index.html` (Phase 3a)
- `dist/404.html` (Phase 3a)
- `dist/work/<slug>/index.html` × 5 (Phase 3b)
- `dist/transactions/index.html` (Phase 3c.1)
- `dist/transactions/<surface>/index.html` × 5 (Phase 3c.1)
- `dist/transactions/<slug>/index.html` × 3 (Phase 3c.1)
- `dist/transactions/rss.xml` (Phase 3c.1)
- `dist/sitemap-index.xml` + `dist/sitemap-0.xml`

Total expected route count: 1 + 1 + 1 + 5 + 1 + 5 + 3 + 1 = 18 HTML/XML pages.

- [ ] **Step 2: Verify sitemap final shape**

Run: `grep -oE "<loc>[^<]+</loc>" dist/sitemap-0.xml | wc -l`

Expected: 17 (excludes the rss.xml — sitemap only counts HTML pages).

- [ ] **Step 3: Verify no broken imports surface in dist**

Run: `grep -rE "TypeError|ReferenceError|Cannot find module" dist/ 2>&1 | head -5`

Expected: no matches. Any error string in the built HTML signals a runtime crash that escaped the build.

- [ ] **Step 4: No commit**

Verification only.

---

## Section 10 — What's NOT in this plan (deferrals)

For clarity to the next planning sessions:

| Defer | Spec / Reason | Owner plan |
|---|---|---|
| Real ledger-row prose (~3 placeholders × Q2-Q4) | Sean's authoring pass post-plan. The structure ships v1; the prose is iterative. All Q2-Q4 placeholders carry `[PLACEHOLDER —` so `grep "PLACEHOLDER" src/content/transactions/*.mdx` surfaces every remaining edit. Q1 is real prose lifted verbatim from V3 (phase-d) or composed from existing /work/ frontmatter (intent-engineering-mcp). | **Sean post-plan** |
| Running `scripts/migrate_v3_transactions.mjs` | The script ships per Task 3.1 but does NOT run in this plan. Sean invokes interactively at his pace (the script prompts per row). After successful run + commit of migrated entries, the script is deleted (per its own header comment + transactions-spec §11.1 one-shot contract). When Sean runs it, the placeholder `phase-d-typed-edges.mdx` (already in this repo with byte-identical slug) blocks the migration's overwrite — Sean adjudicates which body wins. | **Sean post-plan (manual)** |
| Upstream `EXPLANATION.md` authoring + `explanationUrl:` activation on ledger entries | The 3 v1 placeholders use inline-body 4Q fallback. When Sean adds `explanationUrl: <github raw url>` to any entry, the next build's `fetch_canonical_sources.mjs` activates the canonical path. No code change required. | **Sean post-plan + per-slug** |
| `relatedLedgerRow` reverse-rendering on `/work/<slug>/` Related blocks | Forward direction (transactions → work) ships in 3c.1: `<RelatedBlock />` on each deep-dive renders `→ part of: <case-study title>`. Reverse direction (work → transactions) was NOT in Phase 3b's case-study scope — case-study pages don't have a Related block today. When Sean adds one (Phase 3b v1.1 or as part of a future iteration), `byCaseStudy` from `.crosslinks.json` is the ready-to-consume data source — no code change to `derive_crosslinks.mjs` needed. | v1.1 case-study enhancement OR Phase 4 |
| Per-surface next-ship-date in empty state per spec §7.3 | v1 ships generic empty message (`"No artifacts in <surface> yet. Check back soon."`). Spec §7.3 calls for the message to pull from `next-piece.json` filtered to rows matching the empty view's surface. Requires Daily Driver to author next-piece entries with surface tags — Phase 4 work. | Phase 4 |
| Daily Driver hero `ledger_row` dateline pattern producer | The consumer (`dateline.json` + hero spec §8) exists since Phase 2. The producer (Daily Driver agent reading `src/content/transactions/*.md` filesystem at 08:45 to compute count + new-arrival overnight signal + pick the right pattern) is Phase 4 work per BLUEPRINT-COMPLETE §3.6. Phase 3c.1 ships the *content* the producer reads; Phase 4 wires the *write* step. | Phase 4 |
| Aggregated root `/rss.xml` (transactions + architecture + essays) | Deferred per spec §10.4 + §19 + BLUEPRINT-COMPLETE §4.7 — gated until `/essays/` has >1 entry. v1 has 1 essay (the manifesto); aggregated feed is v2. | v2 |
| Per-surface RSS feeds (`/transactions/fleet/rss.xml`) | Spec §10.4 + §19 — single ledger feed is the v1 discipline. | v2 |
| JSON Feed | Spec §19 — RSS 2.0 is universal. | v2 |
| Year filter | Spec §19 — defer until ledger >50 rows or recruiter feedback signals a need. | v2 |
| Status filter | Spec §19 — killed. Discipline is "see the body of work in order." | Killed |
| Tag filter | Spec §19 — killed. Surface + status carry the only metadata pills. | Killed |
| Full-text search | Spec §19 — defer; ledger is short enough to scan. | v2 |
| Combined filters | Spec §19 — defer; single-axis is the v1 discipline. | v2 |
| Methods strip cross-link View Transition morph | Spec §15 — v2 deferred. Risks too many shared name targets per page. | v2 |
| Recency badges, "NEW" pills, amber wash on recent rows | Spec §19 — killed. Dateline IS the recency signal. | Killed |
| Pencil annotations on the index page | Spec §19 — killed. Wire-service register = no scribbles. | Killed |
| Pencil annotations on deep-dive (≤2/page per §9.3) | The annotation primitives (`<Annotation />` + 4 SVG vocabularies + positioner) all exist from Phase 3b. The transactions deep-dive ships with one annotation slot (the registration mark, inlined in `[slug].astro` Task 5.3 Step 1 as `<span class="registration-mark">◐</span>`). The "rev N" scribble per §9.3 (when `previousVersion` is set) is forward-declared — no placeholder declares `previousVersion`, so the rev-N annotation never fires in v1. When Sean ships a row with `previousVersion`, add the inline `<Annotation>` call to `[slug].astro` after the title block. | v1.1 OR per-slug as needed |
| Featured/curated row order | Spec §19 — killed. The ledger is exhaustive-by-default. | Killed |
| Per-page Plausible events | Spec §19 — out. Plausible script (if added later) covers page-view only. | Phase 4 |
| `/architecture/` route + components | **Phase 3c.2.** | **Phase 3c.2** |
| `/essays/` route + components | **Phase 3c.2.** | **Phase 3c.2** |
| `astro-mermaid` integration + 5-var palette override | Architecture + essays both need this; landing it in 3c.2 once is cleaner than landing it in 3c.1 for no current consumer. | **Phase 3c.2** |
| Unified-validator merge (consolidating per-collection validators) | Per essays-spec §11.2 — the merge happens when the 4th collection (essays) lands. 3c.1 keeps the dispatch-per-collection pattern via the COLLECTIONS array in `validate_content.mjs`; 3c.2 adds essays-specific rules (JD URL HEAD checks + `roleMap.lastValidated` staleness) at that point. | **Phase 3c.2** |
| `/about/` page body | **Phase 3d.** | **Phase 3d** |
| Plausible analytics; Vercel deploy; custom-domain DNS; hard cutover from V3 bridge | Per BLUEPRINT-COMPLETE §6 Phase 4. | **Phase 4** |
| Re-attempt subagent-driven-development dispatch | Phase 2 + 2b + 3a + 3b + 3c.1 (this plan) all default to direct controller execution after the implementer subagent hallucinated context constraints. Re-attempt only if the harness has demonstrably improved between this phase and the next. | Each subsequent planning session re-evaluates |

---

## Self-review summary

- **Spec coverage:** transactions-spec §18 items 1-16 are each covered by a verification step in Section 9 (Task 9.1 Steps 1-16). The schema (§3.2) lands in Task 1.1 with the architecture-spec §14.1 `relatedArchitecture` additive folded in (BLUEPRINT-COMPLETE §5 punch-list #1). The 5 surface enum values map 1:1 to the 5 per-surface SSG pages from Task 5.2's `getStaticPaths()`. The 13 deep-dive bands from §9.1 are composed in Task 5.3's `[slug].astro` from 9 Section-4 components + 2 Phase-3b shared imports (`<MethodsStrip />` + `<FourQBlock />`). The cross-link graph (§14) is built in Task 2.3 with bidirectional sibling + supersedes derivation; forward-declared `relatedArchitecture` / `relatedEssay` slots ready for Phase 3c.2 consumers. The 4Q dual-mode contract (§9.2: explanationUrl-canonical OR inline-body fallback) is implemented in `[slug].astro`'s `useFourQBlock` branch (Task 5.3). The migration script (§11.1 + §12.2 + §12.3) ships in Task 3.1 un-run per OQ3 resolution. The RSS feed (§10) lands in Tasks 6.1 + 6.2 with full content as `content:encoded`. View Transition seam `ledger-title-<slug>` (§15) is wired between `<LedgerRow />` Task 4.3 + `<DeepDiveTitleBlock />` Task 4.6.
- **Phase 3b component reuse:** `<MethodsStrip />` + `<FourQBlock />` are imported by `[slug].astro` Task 5.3 directly from `src/components/case-study/` — no fork, no duplication. The annotation primitives are forward-declared as `v1.1 OR per-slug` in the deferral table; they're not consumed in v1 because the deep-dive ships with one annotation slot (the registration mark, inlined) and the "rev N" scribble fires only when `previousVersion` is set (no placeholder declares it).
- **Open questions resolved:** All 3 OQs from the kickoff prompt locked in the header table. OQ1 (split) → 3c.1 = transactions only, 3c.2 = architecture + essays. OQ2 (assets) → no gaps; transactions deep-dive reuses no new hero-media. OQ3 (V3 migration) → script ships un-run (a); 3 placeholders ship instead. Each decision's reasoning is in the OQ resolution table; the deferral table in §10 documents the consequences.
- **Placeholders:** Every text Sean will eventually replace is marked `[PLACEHOLDER —` for grep-ability. Q1 paragraphs across all 3 entries carry production-ready prose (lifted from V3 or composed from existing /work/ frontmatter) so the cold-read test (spec §18 DoD #16) has at least one section per row that reads as final.
- **Type consistency:** `methods[]` shape (`{ task, tool, cost, link? }`) is consistent between `src/content/config.ts` (Task 1.1), the validator (Task 2.1), the derive_crosslinks methods-extraction (Task 2.3), `MethodsStrip.astro` Props interface (Phase 3b carry-over), and all 3 placeholder MDXs (Tasks 7.1-7.3). The transactions `surface` + `status` enums are consistent between the schema (Task 1.1), the validator's `forbiddenSlugs` check (Task 2.1), the per-surface `getStaticPaths()` (Task 5.2), the `FilterPills` matchPrefix list (Task 4.2), and the `status-${status.toLowerCase()}` CSS class generation in `LedgerRow` (Task 4.3) + `DeepDiveTitleBlock` (Task 4.6). The `view-transition-name: ledger-title-${slug}` template literal is byte-identical between `LedgerRow.astro` Task 4.3 Step 1 (on `.row-title`) and `DeepDiveTitleBlock.astro` Task 4.6 Step 4 (on `.page-title`) — same `slug` value flows through both via the row's `slug` prop and the deep-dive's `entry.slug`. The cross-link field names (`relatedCaseStudy`, `relatedTransactions`, `relatedEssay`, `previousVersion`, `relatedArchitecture`) are consistent between schema (Task 1.1), derive_crosslinks extraction (Task 2.3), `RelatedBlock.astro` Props (Task 4.6), and all 3 placeholder MDXs.
- **Routing-collision safety:** The `[surface]/index.astro` + `[slug].astro` collision risk is fully mitigated by Task 2.1's `forbiddenSlugs: ["fleet", "pipeline", "product", "writing", "infra"]` rule. Build fails fast if any future slug collides.
- **Forward declarations:** `relatedArchitecture` + `relatedEssay` slots are forward-declared in the schema (Task 1.1), in `derive_crosslinks.mjs` (Task 2.3 — `archSlugs.size === 0` permissive path), in `RelatedBlock.astro` (Task 4.6 — renders the link even when title isn't available; uses slug as label), and in the `vault-knowledge-mcp` placeholder (Task 7.3 — declares `relatedArchitecture: vault-knowledge-mcp` that points at a Phase 3c.2 target). When Phase 3c.2 ships, the architecture surface lights up automatically — zero code changes to 3c.1 artifacts.
- **No new npm dependencies except `@astrojs/rss`.** Astro 5 + Tailwind 4 + Node 20's built-in `fetch` (for the fetcher) + `readline/promises` (for the migration script) cover everything. The RSS adapter is the only addition.

---

## Phase 3c.2 hand-off prompt

> sw-ai-pm-portfolio — Phase 3c.2 planning. Phases 3a + 3b + 3c.1 shipped on branch `phase-2-foundations`. Site chrome + case-study body + `/transactions/` (with extended prebuild scripts + V3-bridge migration script shipped un-run + 3 hand-written placeholder transactions exercising the layout) are all in. The `<MethodsStrip />` + `<FourQBlock />` shared components live at `src/components/case-study/` and are imported, not forked, on every consumer surface. The `derive_crosslinks.mjs` script handles bidirectional cross-link derivation; `byArchitecture` + `byEssay` reverse-lookup tables are forward-declared today and consumed by your plan. The transactions schema already carries the `relatedArchitecture` additive per architecture-spec §14.1.
>
> Your sole job this session: write the Phase 3c.2 implementation plan via `/writing-plans` for the `/architecture/` and `/essays/` collection routes — each with index + per-slug deep-dive + RSS feed. Estimated ~25-30 tasks across these sections: (1) `astro-mermaid` integration + 5-var palette override (architecture-spec §9.2 + essays-spec §8.2 share the integration); (2) extend `src/content/config.ts` with the architecture + essays collections (schemas in architecture-spec §3 + essays-spec §3 + Appendix Bs); (3) extend `scripts/fetch_canonical_sources.mjs` a third + fourth time to walk architecture (`explanationUrl` + `essaySourceUrl`, two URL fields per row with different out-dirs) and essays (`explanationUrl` + `sourceUrl`); (4) extend `scripts/validate_content.mjs` with essays-spec §11.2's unified-validator merge (JD URL HEAD checks + `roleMap.lastValidated` staleness — warnings only); (5) extend `scripts/derive_crosslinks.mjs` to compute essays' `plottedArtifacts` reverse-derivation onto every cited ledger row + architecture writeup; (6) architecture-specific components (`<Scoreboard />` with custom inline SVG symbols + `<HonestNote />` mono callout + `<TryItYourself />` teal-wash code-block callout + `<ErDiagram />` wrapper); (7) essays-specific components (`<QuadrantChart />` Mermaid wrapper + `<QuadrantLegend />` structured table + `<RoleMap />` structured table with HEAD-validated jdUrl + `<PlottedArtifacts />` auto-rendered list + `<PublishedRevisedStrip />` conditional render + `<FootLinks />` Substack + canonical source + canonical EXPLANATION); (8) `/architecture/` route shape (`index.astro` + `[slug].astro` + `rss.xml.ts`); (9) `/essays/` route shape (`index.astro` + `[slug].astro` + `rss.xml.ts`); (10) one hand-written architecture MDX placeholder (Vault Scorecard per architecture-spec Appendix B — first occupant) + one hand-written essays MDX placeholder (Access-vs-Meaning Manifesto per essays-spec Appendix B); (11) DoD walk-through against architecture-spec §18 + essays-spec §19.
>
> Read end-to-end before planning: [`architecture-spec-v1.md`](../../specs/architecture-spec-v1.md) (full); [`essays-spec-v1.md`](../../specs/essays-spec-v1.md) (full); [`case-study-spec-v1.md`](../../specs/case-study-spec-v1.md) §8 + §9 + §15 (shared component contracts you'll import); [`BLUEPRINT-COMPLETE.md`](../../specs/BLUEPRINT-COMPLETE.md) §3.1 + §3.3 + §3.4 + §3.5 + §3.6 + §5 punch-list #14-15 (upstream EXPLANATION.md status — `VAULT_AS_AGENT_INFRASTRUCTURE_EXPLANATION.md` + `MEANING_OVER_ACCESS_EXPLANATION.md` need to exist upstream OR the architecture+essays placeholders ship inline-body fallback like 3c.1 did); the Phase 3c.1 plan body (this file) for the structural template + the extension-point patterns the prebuild scripts already encode + the `byArchitecture` / `byEssay` reverse-lookup table shape; the existing prebuild scripts at `scripts/fetch_canonical_sources.mjs` + `scripts/validate_content.mjs` + `scripts/derive_crosslinks.mjs` (each with named Phase 3c.2 TODO comments after 3c.1 lands); the existing `<MethodsStrip />` + `<FourQBlock />` Props interfaces.
>
> Default to direct controller execution. Subagent dispatch has been unreliable through Phase 2 / 2b / 3a / 3b / 3c.1. Mirror the Phase 3c.1 plan structure: header directive ("Do NOT dispatch via subagent-driven-development"); File Structure tree; Section-by-section task list with verbatim file contents; atomic commits per task with HEREDOC commit messages; Section 9-equivalent DoD walk; final §X deferral table; self-review summary at the end. Forward-declared cross-links pointing at `/architecture/<slug>/` from existing transactions placeholders will light up automatically when your plan ships — no code change to 3c.1 artifacts.
>
> Open questions you'll likely need to lock during planning: (A) `astro-mermaid` palette override completeness (architecture-spec OPEN-3 — ship 5-var override; observe; expand if needed); (B) Mermaid SSR vs. mmdc PNG fallback if SVG rendering is inconsistent (architecture-spec OPEN-3 default); (C) whether to ship Vault Scorecard with inline-body 4Q fallback (no upstream `VAULT_AS_AGENT_INFRASTRUCTURE_EXPLANATION.md` yet) — same pattern as 3c.1's transaction placeholders, OR gate on upstream EXPLANATION.md authoring. The Phase 3c.1 OQ3 decision pattern (ship the infrastructure now, hand-author placeholders, defer upstream canonical wiring) is the recommended posture for 3c.2 unless Sean has authored the canonical sources between sessions.
>
> Out of scope for 3c.2: real prose authoring (Sean owns post-plan); `/about/` (Phase 3d); cross-surface View Transition morphs (v2). Stop after the plan is committed.

---

*Drafted 2026-05-21 from the Phase 3c kickoff prompt. Open Questions 1-3 resolved in the plan header. Phase 3c.2 (architecture + essays) follows in a fresh session; Phase 3d (about page body) closes Phase 3 before the Phase 4 deploy. The branch state at plan-write time is `phase-2-foundations` HEAD `62c01c3`; the plan's tasks land ~28-32 commits later.*
