---
title: Ship Plan — seanwinslow.com v1 launch (post-rush remediation)
status: DRAFT — updated 2026-05-30 with Sean's delivered work (tiles done, Gate A green, +2 locked-decision changes)
created: 2026-05-29
authored_by: Claude Opus 4.8 (drafting), Sean Winslow (ratifying)
launch_target: window ~2026-06-01 → 2026-06-04 — both human-paced gates (tiles + architecture essay) now cleared; remaining critical path is Claude code work + Sean's deploy clicks
posture: do it right, no rush — first impression fully correct before going live
supersedes: docs/specs/SHIP-PLAN-2026-05-23.md
references:
  - docs/writing-council/IMPLEMENTATION-PROMPT-2026-05-28.md   # ground-truth lock-doc apply sequence + hallucination audit
  - docs/writing-council/*.md                                  # the 11 lock docs
  - docs/specs/hero-spec-v1.md                                 # §6 motion timeline + §7.5 / §7.5.5 (Issue B) + hero tagline (D8)
  - docs/specs/SHIP-PLAN-2026-05-23.md                         # superseded prior plan (Phase 4 deploy sequence still valid)
  - docs/specs/PORTFOLIO-MASTER-PLAN.md                        # §1.3 template trap, §3 voice register
  - /Users/seanwinslow/Code-Brain/anima/PHILOSOPHY.md          # canonical "Anima" project name source (D7)
  - CLAUDE.md                                                  # three load-bearing things + template-trap warning + locked decisions
---

# Ship Plan — seanwinslow.com v1 launch (post-rush remediation)

## 0. TL;DR

The 2026-05-24 push landed 22 commits and a green MVP-CONTENT-GATE, but it shipped fast: a 5/25–28 hallucination audit produced 11 writing-council lock docs that **are not yet applied to disk**. Every flagged fabrication still lives in the MDX. The 5/24 "launch" never actually deployed — Phase 4 (Vercel + apex DNS + LinkedIn) was scheduled for Tue 5/26, when the audit took over instead.

This plan sequences the full remediation as a **"do it right, no deadline" v1**:

1. **Apply the 11 lock docs** (the spine — already sequenced by `IMPLEMENTATION-PROMPT-2026-05-28.md`), treated as **reconciliation of divergent on-disk collections**, not greenfield creation.
2. **Integrate the 5 delivered project tiles** (Sean delivered all 5 incl. a re-drawn A-5 — now a Claude optimize/place/wire task, no longer a creative blocker).
3. **Reverse the hero loading choreography** (Issue B — a spec change, not a bug): a centered icon cycle on a cream overlay that fades away to reveal the hero. **Bundle the hero-title change** (D8) into the same work.
4. **Rename A-1 → "Anima" and A-5 → "16BitFit"** (D7 + D10 — display titles only; slugs stay).
5. **Swap the email** to `sean.winslow28@gmail.com` (overrides a locked decision — lock record updated per D4).
6. **Fix the stale daily-dated layer + OG cards** (Issue D findings).
7. **Deploy** (the Phase 4 that never ran), with a private-preview review checkpoint before the apex flip.

**Both gating creative streams are now done** (tiles delivered; architecture upstream essay shipped to code-brain main → Gate A green). The remaining critical path is Claude code work plus Sean's dashboard clicks. **Launch window ~6/1–6/4**, gated only on the code work clearing the launch criteria + Sean's gut-check.

---

## 1. Context

### 1.1 Reality vs. the documents

CLAUDE.md still says "the build is not started." That has been stale since before 5/24. The actual state:

| Claim | Reality (verified 2026-05-29/30) |
|---|---|
| CLAUDE.md: "build not started" | Full Astro 5 build on disk — 17+ pages, content collections for work/about/transactions/architecture/essays/cartoons/teaserDeck, components, prebuild scripts. |
| IMPLEMENTATION-PROMPT: locks "create" transactions/architecture/essays collections | All three collections **already exist** with 5/24 content that **diverges** from the locks. The apply is reconciliation, not creation. |
| Memory: "executed v1 launch 5/24" | MVP-CONTENT-GATE green, but **not deployed**. No apex, no Vercel project flip confirmed. Phase 4 still pending. |
| 11 lock docs | **Zero applied.** Every fabrication from the audit survives on disk. |
| SHIP-PLAN-2026-05-23 "tiles + arch essay are the long poles" | **Both now delivered** (see §1.3 D1/D3). |

**Sean's unmerged branch:** `feat/ledger-data-readiness-matrix` (one commit, `1422b79`) adds a new transactions ledger row, *Enterprise Data Readiness Matrix* (`surface: infra`, `status: SHIPPED`, real repo + EXPLANATION). Folded into the transactions reconciliation (S1 Stage 2).

### 1.2 The audit (why this exists)

Per `IMPLEMENTATION-PROMPT-2026-05-28.md` §1: the writing council's early passes wrote prose that sounded right but wasn't grounded in source-of-truth. The 5/28 audit corrected fabrications across all five case studies (A-4 The Block worst-affected: a fabricated 2-year tenure, 3-tier subscription narrative, and 18→134 subscriber chart, vs. the real 6-month institutional-crypto-research role). The 11 lock docs are the corrected, Sean-verified canon. Voice ceilings (iPad ≤3, "Comprehension is the artifact" ≤3, HybridRouter in `methods[]` only, no tenure-foregrounding, no traces of desperation) are enforced across all 11.

### 1.3 Decisions captured (2026-05-29 → 30)

| # | Decision | Status / consequence |
|---|---|---|
| D1 | **Tiles must land before launch.** | ✅ **DELIVERED.** All 5 tiles in `assets/projects-tiles/` (A-5 re-drawn hand-drawn for consistency; A-1 already named Anima; prior set archived in `old/`). S3 flips from creative long-pole to a Claude integration task. |
| D2 | **Hero Issue B ships in v1.** Full reversal to centered-cream-overlay choreography. | S2 on the critical path; hero-spec amendment required. |
| D3 | **Sean authors the architecture upstream essay now.** | ✅ **DONE.** `VAULT_AS_AGENT_INFRASTRUCTURE.md` + `_EXPLANATION.md` + `vault/SCORECARD.md` shipped to code-brain main (both HTTP 200). **Gate A green.** No council pass (Sean's call). |
| D4 | **Email overrides the lock.** Swap to `sean.winslow28@gmail.com`. | ✅ **Confirmed.** Update `site-chrome-prose-locked-2026-05-28.md` + BLUEPRINT OPEN-5 + CLAUDE.md "Locked decisions" so the next apply doesn't revert it. |
| D5 | **Gate B (essays) proceeds staged** as `status: DRAFT`. | Manifesto's `publish_target: 2026-06-19` does NOT gate launch. Flip to PUBLISHED on 6/19. |
| D6 | **Hero loading screen = transparent bg-removed icons, centered on a cream `#FFF9F0` full-viewport overlay that fades away.** | Refines S2. Source at `reference-images/hero-icons/background-removed/` (8 PNGs). Cleaner + safer for reduced-motion than a hero-reveal approach. |
| D7 | **Rename A-1 "2D Animation Pipeline" → "Anima"** (canonical per `anima/PHILOSOPHY.md`). | **Display title only; slug stays `animation-pipeline`.** Locked prose is already Anima-aware ("the pipeline is called *anima*"; methods strip headed "A-1 anima"). Keeping the slug preserves all locked `/work/animation-pipeline/` cross-links + OG + methods QA. |
| D8 | **Hero title drops "Product Manager."** New: *"The agents handle the loops. I handle the taste."* + **enlarge the `/AI PRODUCT MANAGER` role tag** so the role reads immediately. | Removes the redundant role statement (role already sits in the tag above the title). **Overrides a locked decision** — touches CLAUDE.md locked decisions, hero-spec, CHANGELOG, the Hero component + role-tag CSS, the `validate_about.mjs`/prebuild byte-match (if any), and the IMPLEMENTATION-PROMPT §7 QA string. Bundled into S2. |
| D9 | **New ledger row *Enterprise Data Readiness Matrix* ships in v1**, `surface: infra`. | Fold the `feat/ledger-data-readiness-matrix` branch into transactions. "infra" may need a FilterPills + `[surface]` route addition (lock #5 taxonomy was fleet/product). |
| D10 | **Rename A-5 "16BitFit Battle Mode" → "16BitFit"** (display title only; slug stays `16bitfit`). | Same treatment as D7. Swap `title:` on `16bitfit.mdx` + the literal in `work/index.astro`. "Battle Mode" appears in **no** lock docs — zero apply churn. |
| D11 | **Transactions ledger = union (curated).** Keep the 3 WIP rows (vault-knowledge-mcp, vault-scorecard, enterprise-data-readiness-matrix) AND add lock #5's 3 fleet rows (knowledge-loop-phase-6, vault-synthesizer-eval-suite, substack-drafter), verifying each upstream first. | Up to 8 rows. Resolves OPEN-T1 (vault-knowledge-mcp stays). Lock #5 page chrome still applies. |

### 1.3.1 Execution status (apply started 2026-05-30)

A Cowork session already executed part of S1 Stage 2 on branch **`feat/v1-remediation-apply`** (renamed from `feat/ledger-data-readiness-matrix`). **Done & lock-aligned:** architecture lock #6 (vault-knowledge-mcp → vault-scorecard surface, fully authored), the 6th transactions row, the D9 row, the essays/transactions cross-link backfills, tile assets, `AGENTS.md`/`.agents/`. **Strategy:** finish the full apply on this branch in clean commits → **one PR to main**. The rest of S1 (foundations #1-4, cleanup #9-11, transactions page chrome + union rows, essays #7, site-chrome #8) + S2 hero + S3 tile integration + S5 stale-data + OG fixes remain.

### 1.4 Gate status (verified 2026-05-30)

- **Gate A (Architecture #6): GREEN.** All three upstream files exist; both essays return HTTP 200 on code-brain main. Lock #6 unblocked. *Minor:* the essay has no `voice_pass_applied` frontmatter tag the gate nominally reads — Sean's explicit sign-off satisfies the gate's intent; recommend adding the tag to the upstream for the record.
- **Gate B (Essays #7): GREEN.** Manifesto carries `voice_pass_applied: 2026-05-21`; both raw URLs HTTP 200; title already "Access Over Meaning." Apply staged per D5.

---

## 2. Full scope — six work streams

Estimates are focused-hours for Claude work.

### S1 — Lock-doc apply (Claude) — ~4–6h across 2–3 sessions

Follows `IMPLEMENTATION-PROMPT-2026-05-28.md` §4 ordering and §6 commit strategy, **with one override: every "creates collection" step is a reconciliation of existing divergent content.** Per §9 ("unexpected state"): match by field name + section, trust the lock string over priors, log surprises to `IMPLEMENTATION-NOTES-2026-05-30.md`.

| Stage | Locks | Touches | Reconciliation notes |
|---|---|---|---|
| 1 Foundations | #1 about · #2 openers · #3 taglines · #4 4Q | `about/index.mdx` + 5 `work/*.mdx` + `work/index.astro` | Existing files. Strip fabrications (08:45→08:30, date corrections, anchor-metric swaps, remove `<RecruiterCallout />`). **Swap display titles: A-1 → "Anima" (D7), A-5 → "16BitFit" (D10); keep both slugs.** |
| 3 Cleanup | #9 the-block · #10 boards A2/A3/A5 · #11 methods ×5 | 5 `work/*.mdx` | Overwrites fabricated investigation boards + MetricCharts + the composed 16BitFit SlackQuote with verified receipts. |
| 2 Transactions | #5 (+ D9) | `transactions/` + FilterPills/`[surface]` route | **Reconcile to final row set.** Keep `intent-engineering-mcp` + `phase-d-typed-edges`; ADD lock #5's `knowledge-loop`, `eval-suite`, `substack-drafter`; ADD D9 `enterprise-data-readiness-matrix` (infra). Decide fate of on-disk `vault-knowledge-mcp` row (**OPEN-T1**). Add "infra" to the surface taxonomy/route if not present. |
| 2 Essays | #7 (Gate B green) | `config.ts` schema + `essays/` | Schema amendment FIRST (roleMap `jdUrl` optional + `isNegativeSpace`). Apply staged `status: DRAFT`. |
| 2 Site chrome | #8 (+ email D4) | nav/footer/BaseLayout/global.css/site.ts/contact/404 | Dark-mode wipe (terminal) + F-2 footer staging. **Fold email** (`site.ts:9` + hardcoded leak `contact.astro:17`). Run §3.4 dark-mode greps. |
| 2 Architecture | #6 (**Gate A green**) | `architecture/` | Reconcile on-disk `vault-knowledge-mcp.mdx` ("Vault as Agent Infrastructure") → lock's `vault-scorecard` re-spec, now backed by Sean's shipped upstream essay + SCORECARD.md. Adds 6th transactions row (resolves OPEN-T1 + IMPLEMENTATION-PROMPT §8 item #1). |
| 4 OG cards | §5 | `scripts/phase-0/generate_og_cards.py` + `og-cards/` | Essays title "Access vs Meaning"→"Access Over Meaning"; rename `vault-knowledge-mcp.png`→`intent-engineering-mcp.png` + URL fix; delete orphan. Run after locks land. |

**Ceiling-grep checkpoint** after Stage 1 + after Stage 3: confirm iPad ≤3, "Comprehension is the artifact" ≤3, HybridRouter only in `methods[]`, no fixed Code-Brain counts, no tenure-foregrounding.

### S2 — Hero Issue B reversal + hero-title change (Claude) — ~3–5h + Playwright re-verify

A spec change, not a bug (hero-spec §6/§7.5 place the cycle in the character lane with concurrent reveals). Per CLAUDE.md, **log a CHANGELOG entry + amend hero-spec first.** Two coupled changes, same component:

**(a) Loading choreography (D6):**
1. **Asset prep:** Pillow script — normalize the 8 bg-removed PNGs onto a consistent transparent square canvas, export WebP → `public/assets/hero-icons/loading/`. Commit script for traceability.
2. **New full-viewport loading overlay:** fixed, `#FFF9F0` cream field, centered transparent icon, cycles 8 frames per §6 timing, then **the overlay fades out** to reveal the hero rendered underneath.
3. **Hero renders normally underneath from first paint** — overlay is additive, not gating. Reduced-motion safety win: no JS-gated reveal that can strand a blank hero.
4. **Once-per-session** via sessionStorage.
5. **Reduced-motion / no-JS:** overlay does NOT mount; hero shows immediately; no `<video>` in DOM (preserve ad64ce1). Re-verify Playwright (`querySelector(".character-video")` → null) + new assertion the hero is visible immediately when the overlay is suppressed.

**(b) Hero title (D8):**
- Hero copy → *"The agents handle the loops. I handle the taste."* (drop the "Product Manager." line).
- Enlarge the `/AI PRODUCT MANAGER` role tag so the role reads immediately.
- Ripple: CLAUDE.md "Locked decisions" hero tagline · hero-spec tagline section · CHANGELOG · `Hero.astro` + role-tag CSS · check/update any tagline byte-match in `validate_about.mjs`/prebuild · IMPLEMENTATION-PROMPT §7 QA string (`/ renders hero tagline ...`).

Sequence S2's `global.css`/`BaseLayout` touches in series with S1 lock #8 (both touch those files).

### S3 — Project tile integration (Claude) — ~1.5–2.5h ✅ assets delivered

All 5 tiles delivered by Sean in `assets/projects-tiles/` (`A-1_Anima.png` … `A-5_16BitFit.png`, ~3MB each). Integration task:
1. Optimize PNG → WebP, resize/crop to the 4:5 portrait `ProjectTile` spec.
2. Place at `public/assets/projects/<slug>.webp` (slugs: animation-pipeline, code-brain, intent-engineering-mcp, the-block, 16bitfit).
3. Update `hero_media` + `hero_media_alt` frontmatter on the 5 `work/*.mdx` (A-1 alt reflects the new Anima art, not the old mascot-walk description).
4. Confirm View-Transition `view-transition-name: hero-media-<slug>` morphs cleanly tile→deep-dive.
5. Preserve traceability: reference the superseded 5/24 tile commits in the replacement commit message.

### S4 — Architecture upstream essay — ✅ DONE (Gate A green)

Shipped to code-brain main (D3). No further action except (optional) adding `voice_pass_applied` frontmatter to the upstream essay for the gate record.

### S5 — Stale-data + daily-dated fixes (Claude) — ~1–1.5h

- Daily Driver JSON (`public/api/dateline.json`, `about-pulse.json`, `next-piece.json`, `shipped-stats-*.json`): refresh to current + fix `08:45`→`08:30`. Re-do day-of-launch (daily-dated).
- `DatelineLabel.astro:10`: hardcoded `today = "2026-05-21"`, not wired to `dateline.json` — wire it (or update the static fallback) so the projects dateline isn't frozen.
- Coordinates with S1 lock #2's 08:45→08:30 prose fix (don't double-edit).

### S6 — Phase 4 deploy (Claude guide + Sean clicks) — ~2.5h, terminal gate

Inherits the still-valid Phase 4 sequence from SHIP-PLAN-2026-05-23 §2 (4A production sanity → 4B Vercel → 4C apex DNS Cloudflare DNS-only → 4D LinkedIn + comms + CLAUDE.md banner). **Added checkpoint:** deploy to a private Vercel preview for Sean's review BEFORE the apex flip + LinkedIn announce.

---

## 3. Sequencing + parallelism

Both human-paced gates are cleared, so the critical path is now **all Claude code work** plus Sean's review/deploy clicks.

```
CLAUDE SESSION A (lock-apply spine):
  S1 Stage 1 foundations (#1–4, incl. Anima title swap)  →  S5 stale-data (shared MDX)  →
  S1 Stage 3 cleanup (#9/#10/#11)  →  ceiling-grep checkpoint  →
  S1 Stage 2: transactions #5 + D9 row, essays #7 (staged), site-chrome #8 (+ email),
              architecture #6 (Gate A green) + 6th tx row (resolves OPEN-T1)  →
  S1 Stage 4: OG card fixes

CLAUDE SESSION B (parallel-safe, different files):
  S2 hero (spec amendment → loading overlay D6 + hero title D8 → reduced-motion re-verify)
     ⚠ serialize the global.css/BaseLayout touches against lock #8
  S3 tile integration (optimize → place → wire frontmatter → View-Transition check)

LAUNCH GATE (all green) → S6 deploy → private preview review → apex + LinkedIn
```

**Dependency notes:** essays #7 schema amendment commits before its content. The OG essays-title fix runs after lock #7 sets the canonical title. Methods cross-link graph (IMPLEMENTATION-PROMPT §7) validates after all 5 case studies + transactions rows exist. D7 keeps the `animation-pipeline` slug, so no cross-link/route edits are needed for the rename.

---

## 4. v1 vs. v1.1

**v1:** all 11 locks (architecture now in — Gate A green) · 5 integrated tiles · hero Issue B + hero-title change · Anima rename · new Enterprise Data Readiness Matrix ledger row · email swap + lock-record update · stale-data + dateline wiring · OG card fixes · deploy.

**v1.1 (genuinely post-launch):**

| Item | Trigger |
|---|---|
| About §B-3 cartoon-cels content collection | post-launch polish pass |
| A-5 anchor metric `PIPELINE 47% · GAME ON SHELF` (count-fragile) | any future A-5 frontmatter pass |
| Essays cross-link backfills (4 frontmatter + 6 quadrantLegend nulls) | fire as each upstream artifact ships its ledger/arch row |
| vault-knowledge-mcp OG card | when that surface ships ~6/4 with its own title-lock |
| Daily Driver morning cron automation (replace manual JSON writes) | post-launch |
| Agent-feed footer module (sub-daily ledger surface) | when it earns its own spec |
| Essays manifesto `DRAFT → PUBLISHED` flip | 2026-06-19 publish day |

*(A-4 hero asset re-author dropped from v1.1 — delivered in S3.)*

---

## 5. Launch criteria — the gate

Flip to S6 deploy only when ALL are green:

1. All 5 tiles integrated (WebP, 4:5, correct alts, clean View Transition) and meet Sean's bar.
2. S2 hero: Issue B passes reduced-motion re-verification (overlay suppressed, hero immediate, no `<video>` in DOM) + once-per-session confirmed; new hero title + enlarged role tag render correctly; prebuild passes (tagline validator updated).
3. S1 ceiling greps clean (iPad ≤3 · "Comprehension is the artifact" ≤3 · HybridRouter methods-only · no fixed counts · no tenure-foregrounding · no desperation cadence).
4. Architecture #6 applied (Gate A green); transactions final row set renders incl. the infra row.
5. Dark-mode wipe greps return zero (`data-theme` / `ThemeToggle` / `sw-theme` / `LIGHT · DARK`).
6. Prebuild validators pass; `npm run build` clean; `dist/` sitemap + RSS carry `seanwinslow.com` (not localhost).
7. Recruiter test on the live preview: hero 5s · case-study clickthrough 30s · deep-read 2min.
8. Sean's no-template-feeling gut-check (PMP §1.3).

Essays staying `DRAFT` is explicitly allowed — it does not block the gate.

---

## 6. Risk forks + rollback

| Stream | Risk | Mitigation | Rollback |
|---|---|---|---|
| S1 | Divergent on-disk collection makes a lock's "create" step ambiguous | Match by field name + section (§9); log to IMPLEMENTATION-NOTES-2026-05-30 | Skip the ambiguous lock; apply the rest; resolve with Sean |
| S1 | Partial/out-of-order apply re-violates a voice ceiling | Apply foundations (#1/#2/#4) as a coherent batch; grep after each stage | Re-apply the locked string verbatim |
| S1 | "infra" surface unsupported by FilterPills/`[surface]` route | Add "infra" to the taxonomy + route during the transactions apply | Re-label D9 row to an existing surface (product) |
| S2 | Centered-overlay refactor re-breaks the reduced-motion contract | Overlay is additive (hero renders underneath); doesn't mount under reduced-motion; Playwright re-verify | `git revert` S2; current lane-cycle is spec-compliant and functional |
| S2 | Hero-title change breaks a prebuild byte-match validator | Find + update the validator string in the same commit; run `npm run prebuild` locally | Revert the title commit; restore the locked string |
| S2 | bg-removed icons jump size between frames | Normalize onto consistent canvas in the Pillow prep step | Pad/recrop the offending frame; re-export |
| S3 | Tile WebP conversion loses fidelity / wrong crop | Per-tile visual QA at 390/1440 before commit; keep PNG source | Re-export at higher quality / re-crop; old tiles archived in `old/` |
| S5 | Daily JSON goes stale again before launch | Re-write day-of-launch; v1.1 cron automation removes the manual step | Manual re-write |
| S6 | Cloudflare proxy orange-cloud breaks SSL | DNS-only BEFORE adding Vercel domain (Roadmap Step 3.7) | Fall back to `*.vercel.app` in LinkedIn; fix apex next day |
| S6 | Vercel build fails on a prebuild validator | Run `npm run prebuild && npm run build` locally in 4A first | Fix validation drift; re-push |

---

## 7. Open items needing Sean

- **OPEN-T1:** Fate of the on-disk `transactions/vault-knowledge-mcp.mdx` row (not in lock #5). Likely re-homed as the 6th `vault-scorecard` row when architecture #6 lands, or retired. Resolve at architecture-apply time.
- **Transactions final row set:** confirm the v1 ledger composition once the apply surfaces what content actually exists for lock #5's `knowledge-loop` / `eval-suite` / `substack-drafter` (do their upstreams exist, or do they stage like essays?). Claude will surface during S1 Stage 2.

*(Resolved this session: Anima slug = keep `animation-pipeline` [D7]; ledger surface = infra [D9]; email lock-record update [D4]; no S4 council pass [D3]; per-tile path moot [tiles delivered].)*

---

## 8. Owners

- **Claude:** S1 lock-apply + reconciliation · S2 hero (spec amendment + loading overlay + title change + Playwright) · **S3 tile integration** · S5 stale-data · OG card script · S6 deploy guidance + DNS + CLAUDE.md/CHANGELOG/lock-record updates · this plan + the next-session kickoff.
- **Sean:** ✅ tile authoring (done) · ✅ architecture upstream essay (done) · Vercel/Cloudflare/LinkedIn dashboard clicks · final voice authority on every locked string · the launch gut-check.

---

## 9. References

- `docs/writing-council/IMPLEMENTATION-PROMPT-2026-05-28.md` — ground-truth apply sequence, hallucination audit, commit strategy, QA checklist, §8 deferral table
- `docs/writing-council/*.md` — the 11 lock docs (apply notes + per-row grounding each)
- `docs/specs/hero-spec-v1.md` §6 / §7.5 / §7.5.5 + tagline section — Issue B + hero-title amendment targets
- `docs/specs/SHIP-PLAN-2026-05-23.md` — superseded; Phase 4 deploy sequence (§2) still authoritative for S6
- `docs/specs/PORTFOLIO-MASTER-PLAN.md` §1.3 (template trap) + §3 (voice register)
- `/Users/seanwinslow/Code-Brain/anima/PHILOSOPHY.md` — canonical "Anima" name (D7)
- `CLAUDE.md` — three load-bearing things + template-trap warning + locked decisions
- `reference-images/hero-icons/background-removed/` — 8 transparent loading-icon source PNGs (D6)
- `assets/projects-tiles/` — 5 delivered tiles (S3 source); `feat/ledger-data-readiness-matrix` branch (D9 source)
