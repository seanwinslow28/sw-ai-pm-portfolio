# CHANGELOG

The running log of changes to the spec docs in [`docs/`](docs/). Each spec used to carry its own `## 1.1 Changelog` section — going forward, new entries land here instead so the specs themselves stay buildable and uncluttered.

## How to add an entry

When you make a substantive change to a locked or drafted spec (revision, restructure, scope change, decision reversal, killed idea), append a bullet to the relevant spec's section below:

```
- **YYYY-MM-DD (short label):** One- or two-sentence description of what changed and why. Reference the spec sections touched (§N.N) so a future reader can diff against the prior version without spelunking.
```

Rules:
- **Reverse chronological within each spec** (newest first).
- **Use absolute dates**, never "today" or "last week."
- **Lead with what changed, then why.** Skip filler. Match the voice rules in [`docs/specs/PORTFOLIO-MASTER-PLAN.md`](docs/specs/PORTFOLIO-MASTER-PLAN.md) §3.
- **One entry per coherent change.** Multiple unrelated edits on the same day get multiple bullets.
- If a spec doesn't yet have a section here, add one (alphabetical by filename under the `## Spec Changelogs` heading).
- If a change cuts across specs (e.g. a decision lock that ripples through 3 of them), record the lockstep update under each affected spec — don't try to invent a cross-cutting section.
- The pre-CHANGELOG entries below were lifted verbatim from each spec's original `## 1.1 Changelog` section. Those in-spec sections are now historical; treat them as superseded by this file.

---

## Spec Changelogs

### [`docs/specs/hero-spec-v1.md`](docs/specs/hero-spec-v1.md)

- **2026-06-10 (iOS hero character rendered as a white box — premultiplied-alpha re-encode, §7):** On iPhone Safari the character animation showed inside an opaque white rectangle (background not removed, floor shadow hidden behind it); desktop Chrome was fine. Root cause: `hevc_videotoolbox` always tags HEVC `AlphaChannelMode = PremultipliedAlpha`, but the prior encode fed it *straight* RGBA (transparent pixels still carried the paper color at α=0). WebKit's premultiplied source-over then adds that leftover RGB onto the page → clamps to white. Diagnosed by decoding the shipped `.mp4` through AVFoundation (the same path iOS uses) — transparent corners came back `(250,242,233, α=0)` instead of `(0,0,0,0)`. Fix: premultiply RGB before the HEVC encode so transparent areas are truly `(0,0,0,0)`, matching the declaration; the VP9 WebM stays *straight* alpha (what Chrome expects). Also added a downscaled 720-wide mobile pair (`hero-loop-alpha-mobile.{mp4,webm}`, ~half the bytes) served at ≤768px per Sean's "resized for mobile optimization" — [`CharacterLane.astro`](src/components/hero/CharacterLane.astro) now selects WebKit-vs-other × mobile-vs-desktop, constants in [`site.ts`](src/lib/site.ts). All four assets are regenerated reproducibly by [`scripts/phase-2/encode_hero_loop.sh`](scripts/phase-2/encode_hero_loop.sh) from the canonical desktop WebM loop. Verified: AVFoundation decode of both committed `.mp4`s now yields `(0,0,0,0)` transparent corners; local mobile render (390px) shows warm paper around the character + the floor shadow beneath, no white box. Follow-on (same session, Sean-confirmed the transparency on-device): the `≤1024px` floor-shadow was off-center ~44px to the right — it had been calibrated at `left:26%` while the character was still an opaque white box, so the offset was invisible until the alpha fix revealed it. Recentered from `left:26%` toward `left:15%` (Playwright-centered at 768px delta 0px / 390px feet x≈206); Sean then tuned it on-device to `left:18%; bottom:-10px`, which reads best to the eye on a real iPhone. Because the lane is a fixed 420px anchored `right:-24px`, one `left` value tracks the character at every width in the `≤1024px` range — desktop (>1024px) uses the separate base + `≥1600px` rules and is unaffected. **iOS transparency CONFIRMED on Sean's iPhone (192.168.68.200 LAN preview, 2026-06-10) — the character composites onto warm paper with the floor shadow beneath, no white box.** (Headless verification wasn't possible — WebKit's accelerated video-layer compositing isn't reproducible via CoreImage or WKWebView snapshots — so the on-device check was the gate, and it passed.)
- **2026-06-10 (rubber-stamp city seal on the hero dateline, §8 — Sean-approved):** The dateline's BOSTON prefix now renders as a literal rubber stamp — a 1px stamp-amber frame around "BOSTON" alone, rotated −2°, with the paper-tile fiber as an ink mask so it reads pressed, not printed; the date stays level on the wire line ([`Dateline.astro`](src/components/hero/Dateline.astro)). Audit lever 2 / delight 1: gives the unfakeable fleet evidence one earned register of visual weight without moving the locked calm paper-on-chrome layout. Sean picked V2 of three CSS-only variants (whole-prefix tilt and full-box rejected). Home hero only — the component has a single import site (Hero.astro); sub-page DatelineStrips unchanged. Static treatment, reduced-motion-safe by construction; verified on fresh build + preview screenshot.
- **2026-06-10 (P3-14 drift log — tagline clamp renders larger than spec'd, §4):** The implemented tagline sizes at `clamp(28px, 3.4vw, 60px)` ([`Hero.astro`](src/components/hero/Hero.astro)) vs the spec'd / DESIGN.md headline `clamp(26px, 3.2vw, 56px)`. The drift predates the 2026-06-10 audit and reads well at both breakpoints (weight 300 intact, type-step ratios still ≥1.25), so the rendered values are accepted as the de-facto lock; no spec body edit, no code change. Logged so the next session doesn't "correct" it in either direction without noticing it was a decision.
- **2026-06-10 (audit fixes — §9 cursor de-Reacted, floor shadow WebP, LCP priority hint):** Behavior-identical implementation changes from the 2026-06-10 impeccable audit ([report](docs/audits/2026-06-10-fable-impeccable-audit.md)). **(a)** The §9 custom cursor was the site's only React island (`Cursor.tsx`, `client:load` in BaseLayout) and shipped react + react-dom (~186KB raw / ~58KB gzip) on every page for an 80-line effect; ported to a vanilla `<script>` in [`Cursor.astro`](src/components/Cursor.astro) — same 6px ink dot, lerp constants, 10× hero-name hover growth, reduced-motion/coarse-pointer guards; verified live (dot lerps, hover scale 10, zero React chunks in dist). **(b)** `hero-floor-shadow.png` (159KB) converted to WebP (77KB); the dead PNG removed from `public/` (master remains reproducible via `scripts/phase-2/generate_substrate.py`). **(c)** `paper-tile.png` preload gained `fetchpriority="high"` — it is the measured LCP resource on every route (home LCP 5.6s → 5.0s in throttled Lighthouse). **(d)** The loading-overlay icon + tear-divider `<img>`s gained explicit width/height (Lighthouse unsized-images).
- **2026-06-04 (daily-dated-layer producer documented — runbook + CLAUDE.md pointer):** No spec body change — recording that the Daily Driver → portfolio bridge is now fully documented from the producer side at [`code-brain/agents-sdk/docs/portfolio-refresh-runbook.md`](../code-brain/agents-sdk/docs/portfolio-refresh-runbook.md), and CLAUDE.md's "Related context" section now points to it. The runbook is the authority on the consumer side of the §8 build-date dateline + `isFresh` gate this spec already documents: it covers the four `public/api/*.json` files and their sources (`dateline.json`, `about-pulse.json`, editorial `next-piece.json`, live `shipped-stats-<slug>.json`), the honesty mechanism (`date_iso` advances daily with real numbers → always a content diff → always a deploy; stale JSON falls back to evergreen undated copy), the pre-push `npm run validate` gate, and the push-only dedicated-worktree (`.portfolio-dateline-worktree`, pinned to `main`) commit/push flow. Two operator knobs noted for future sessions: `[portfolio.next_piece]` is editorial (edit when "what ships next" changes) and `[portfolio.shipped_stats]` is intentionally empty until `intent-engineering-mcp` is published to npm. Future Claude sessions touching anything that produces or consumes `public/api/*.json` should read the runbook first.
- **2026-06-01 (fonts self-hosted; build-date dateline; launch-critique fixes):** §13 — fonts moved off the `fonts.gstatic.com` CDN (the `astro-google-fonts-optimizer` `<GoogleFontsOptimizer>` only inlined the CSS while the woff2 binaries still fetched from Google at runtime, risking a generic-serif FOUT on locked-down corporate networks). Now self-hosted via `@fontsource` (Newsreader 300/400/500 + real italics, JetBrains Mono 400/500/700, Caveat 500), imported in `BaseLayout.astro`; no external font requests remain. **Caveat is logged here as the documented third-face exception to the two-font lock** (pencil-margin annotations only). §8 — the hero dateline and the projects `UPDATED` label now derive their *date* from the build date via `src/lib/dateline.ts` instead of the frozen `dateline.json` `date_iso`, so every dateline across the site stays mutually coherent on each deploy even when the Daily Driver JSON lags; the fleet JSON still supplies the body/stats. The home `about-pulse` "TODAY" prefix degrades to "LATEST" when the pulse file is >48h stale (`isFresh`), so stale stats are never labelled as today's. Triggered by the launch-day `impeccable critique`.
- **2026-05-30 (Issue B loading choreography D6 + hero-title D8):** Two coupled S2 changes per [`SHIP-PLAN-2026-05-29.md`](docs/specs/SHIP-PLAN-2026-05-29.md) §S2, documented in the inline "AMENDMENT 2026-05-30" block after §2's anatomy diagram. **(D8 hero-title)** The tagline drops its redundant first line `Product Manager.` (the role already sits in the `/ AI PRODUCT MANAGER` tag directly above) → tagline is now `The agents handle the loops. / I handle the taste.`; the role tag is enlarged so the role reads immediately on the 5-second scan. Rippled to CLAUDE.md "Locked decisions" hero tagline + IMPLEMENTATION-PROMPT-2026-05-28 §7 QA string. (No hero-tagline byte-match validator exists in `scripts/` — only `validate_about.mjs`'s about lead-line check — so no validator edit was required.) **(D6 loading choreography)** The §7.5 intro-icon-cycle-in-the-character-lane is superseded by an *additive, full-viewport cream `#FFF9F0` loading overlay* that cycles 8 bg-removed icons centered, then fades out to reveal the hero (rendered underneath from first paint; overlay never gates). Once-per-session via sessionStorage. Under reduced-motion/no-JS the overlay does NOT mount, the hero shows immediately, and no `<video>` is in the DOM (preserves the `ad64ce1` reduced-motion contract). Supersedes parts of §4 (tagline), §6 (phase 0), §7.5 / §7.5.5.
- **2026-05-20 (intro icon cycle replaces static hero-loop-poster):** The original Phase 0 punch-list item "hero-loop-poster.webp — frame 1 still" is superseded by an 8-icon pencil-test intro cycle that plays in the character lane for ~4.8 seconds at page load, then crossfades into the existing WebM character animation. The 8 icons (loop arrow, terminal cursor, graph nodes, pencil, sticky note, 2×2 matrix, Claude mascot, coffee cup) map Sean's AI PM craft — agents + loops + fleet on one side, craft + judgment on the other, with domestic-warm bookends. Sources generated via the `gemini-pencil-animation-image-gen` skill using the canonical character anchor sheet ([`2D-Character-Sketch-Sean-v1.png`](reference-images/2D-Character-Sketch-Sean-v1.png)) as style reference; for icon 7 (Claude mascot), Sean's [`claude-pencil-icon.png`](reference-images/hero-icons/claude-pencil-icon.png) was passed as a subject reference. All 8 PNG sources committed at [`reference-images/hero-icons/`](reference-images/hero-icons/). Phase 2 build converts to WebP (≤40KB each, ≤320KB total). Spec sections updated: §6 motion timeline (added cycle row at t=0; superseded the t=1300 character-lane-reveal row; added the cycle-end crossfade row at t=4800; hero-settled moved to t=5100), §7 character animation (poster image row + HTML snippet + fallback line updated to reference the cycle), NEW §7.5 "Intro icon cycle" added documenting the 8 icons, cycle timing (8 × 600ms = 4800ms), JS-driven implementation with `video.readyState` coordination, once-per-session sessionStorage flag, reduced-motion fallback (static icon-7-claude in place of cycle + WebM), asset budget, and accessibility.
- **2026-05-20 (LOCKED):** All OPEN items resolved (none flagged at lock — hero was the first spec to land at LOCKED status). The `math` → `loops` word swap also lands today (see below). Hero proceeds to Phase 2 as the primary build target alongside projects-section. Hero-spec is now the canonical reference for all home-page color/type/cursor/motion tokens inherited by the home-about-teaser and the universal site-chrome.
- **2026-05-20 (`math` → `loops` word swap):** §2 — the home-hero tagline word "math" replaced with "loops" per Sean's preference. Reasons: (a) "math" risked reading as math-shame (Sean understands math; the line wasn't about that); (b) "loops" carries the Karpathy-coded agentic-loops register (Linear / Vercel / Anthropic technical authority); (c) names what agents actually do at scale that humans can't sustain — code loops, feedback loops, inference loops, iteration loops all at once. Locked tagline is now `"Product Manager. The agents handle the loops. I handle the taste."` PMP §4 rows 1 + 7 + about-spec §2 anatomy + §4 voice-register table + §7.2 rationale + DESIGN.md Tagline-Weight-300 Rule + CLAUDE.md quick reference all updated in lockstep.
- **2026-05-19 (critique fix §2 rows 3 + 6 — name + tagline motion):** §6 — (a) Name reveal: `translateY(40px)` → `translateY(24px)`, stagger 20ms → 18ms (40px on each of 12 letters was a lot of distance for a confident name reveal). (b) Tagline reveal: `translateY(60%)` → `translateY(28px)` — fixed-px offset prevents the line briefly overshooting the line-box at small sizes. Per `impeccable-and-emil-design-critique.md` §2 rows 3 + 6.
- **2026-05-19 (critique fix punch #5 — cursor hover duration):** §9 — the name-hover cursor expansion (default 6px dot → 60px circle) was missing an explicit transition duration; specified 160ms `cubic-bezier(0.23, 1, 0.32, 1)`. Emil's snap-feedback range; leaving it unspecified would invite an implementer-default ease-in. Per `impeccable-and-emil-design-critique.md` §2 row 2.
- **2026-05-19 (audit fix C-2.1):** Patched §14 "Out of scope" MCP-embed line. The interactive embed page now resolves at `/work/intent-engineering-mcp` (not `/transactions/...`) to match the projects-spec §3 routing contract + case-study spec §13.3 canonical route. A ledger row at `/transactions/intent-engineering-mcp/` continues to exist at the same slug per blueprint §3.1 ("one artifact can appear on multiple surfaces simultaneously with the same slug"). Per `2026-05-19-portfolio-spec-roadmap-alignment-audit.md` finding C-2.
- **2026-05-19 (audit fix M-3):** Hand-off prompt in Appendix B updated — the working-directory path lost its erroneous `BMAD/` segment (`Code-Brain/BMAD/sw-ai-pm-portfolio/` → `Code-Brain/sw-ai-pm-portfolio/`). Pure string fix; no behavioral change. Per audit finding M-3.
- **2026-05-16 (prototype validation):** Composition revised from portrait shoulder-cutout to landscape desk-scene after the actual asset (`sean-typing-at-desk-hero-transparent.webm`) was rendered. The rotating "get in touch" badge was removed — character lane now owns the right side undisputed, contact CTA lives in the footer per §10. Name and tagline switched to `clamp()` so they scale on viewports >1440px. Lane sizing locked at 1024×576 with -180px right-bleed to clip the source canvas's empty band.
- **2026-05-16 (animation reconcile):** Originally three frame sets (walk-in / idle / blink). Replaced with a single seamless WebM loop per master plan §8.1.
- **2026-05-13:** Original spec locked + recall-tested.

### [`docs/specs/home-about-teaser-spec-v1.md`](docs/specs/home-about-teaser-spec-v1.md)

- **2026-06-16 (critique W2 — swipe affordance rework, §6.4):** The `← SWIPE ME` indicator was redesigned from a desktop-only, touch-suppressed caption to a touch-visible affordance positioned right of the card stack. Changes: indicator now renders on touch devices (was hidden via `hover: none` detection); repositioned to the right edge of the deck to match the swipe direction; a card-stack peek cue was added (second card's right edge visible behind the front card) so the deck reads as swipeable before any label is read. Reduced-motion fallback (§6.5) and card order (§6.3) unchanged. §6.4 deviation note updated in-spec.
- **2026-05-20 (deck expanded to 10 cards + style sequence locked):** Sean authored the deck assets and the locked sequence diverged from the original 9-card draft in two ways: (a) deck count moved from 9 → 10 cards (1 photograph + 9 AI-style variations, up from 1 + 8); (b) the thesis tightened around the **cartoons-that-formed-my-taste** family (tied to about-spec §11 cartoon canon) rather than the originally-drafted wide-aesthetic-range sequence. Locked sequence: (1) photograph bg-mat onto warm paper, (2) pencil-test character (Gemini-generated, matches hero WebM style), (3) watercolor + ink illustration, (4) Bob's Burgers / Bouchard family, (5) shōnen anime cel, (6) classic Nicktoons / Hey Arnold!-adjacent, (7) Total Drama / CN noodle-character, (8) comic illustration / Mike Allred-coded, (9) modern CN / Steven Universe-coded, (10) pop-art comic illustration. Gained: thematic coherence with the about-page cartoons thesis. Lost: editorial illustration / Studio Ghibli / Simpsons / blueprint linework registers (noted in §7 as "acknowledged register absences" for possible v2 swaps). Spec sections updated: §1 (nine → ten ways), §2 (anatomy diagram "stack of 9" → "stack of 10" + element count), §6.1 (card count + cardOrder array), §7 (heading + intro + full table rewrite + LOCKED marker), §7.1 (asset budget — adds source PNG paths + WebP conversion at build), §7.2 (rewritten to document executed pipeline + identity-preservation lesson), §10.2 (photo constraints checked off + pencil-in-hand note dropped), §12 (aria-labels "nine → ten" + "card N of 9 → 10"), §13 (OPEN-1 + OPEN-6 + OPEN-7 all marked RESOLVED), §14 DoD (3 items updated for 10 cards + WebP conversion), Appendix A (file map updated with new card names + reference-images/teaser-deck/ source path), Appendix B (hand-off prompt updated). Source PNGs committed to [`reference-images/teaser-deck/`](reference-images/teaser-deck/).
- **2026-05-20 (LOCKED):** All 7 OPEN-N items resolved to recommended defaults. Two items remain "deferred to asset-brainstorm session" (assets to be authored before Phase 2 build): OPEN-1 (9-style sequence — the draft table in §7 is the starting point) and OPEN-6 (Card 1 photo source — new shoot recommended). Sean to drop the source headshot into `reference-images/headshot-source/` (folder created at lock). The 8 AI-style variations + prompt-engineering strategy worked out together in the asset-brainstorm session, using the Gemini / OpenAI / OpenRouter API keys available in `.env`.
- **2026-05-20 (initial draft):** First draft of the home-page About teaser section spec — a new section sitting between the projects grid and the universal chrome footer. Introduces the 9-card swipeable character-deck pattern (1 photograph + 8 AI-style variations of Sean) as the resolution to the "photo vs. character" conflict surfaced in the 2026-05-20 brainstorming pass. Locks the Vonnegut-coded editorial line (*"A man, a pencil, an agent fleet. Same person, different tools."*) per the voice-modes variation pass. Reuses the existing `/api/about-pulse.json` Daily Driver endpoint — no new Daily Driver work. Inherits color/type/cursor/motion tokens from hero-spec; inherits torn-paper seam vocabulary from site-chrome-spec. Implementation is vanilla JS via Astro `<script>` module — no Framer Motion, no React island, no addition to the locked stack (per CLAUDE.md "no Framer, no GSAP, no Lenis"). Includes the §10.1 amendment to about-spec §784 anti-photo rule. 7 OPEN-N items flagged inline (style sequence, eyebrow text, CTA text, vanilla-vs-island, swipe semantics, photo source, card count).

### [`docs/specs/projects-section-spec-v1.md`](docs/specs/projects-section-spec-v1.md)

- **2026-06-11 (A-6 "check back ~&lt;date&gt;" subtitle removed, §4 deviation — defuses the P3-17 freshness bomb):** The next-in-production tile no longer renders the dated `check back ~jun 10` line — the date passed with Vault Scorecard already shipped (ledger MAY 31 + architecture writeup), at which point the promise inverts the honesty layer into evidence of abandonment, exactly the failure audit P3-17 flagged. Sean's call: drop the line entirely rather than re-date it; the pulsing COMING pill carries the forward promise without naming a day. [`NextInProduction.astro`](src/components/projects/NextInProduction.astro) no longer consumes `next-piece.json` (its `date_target` now has zero active consumers; the Daily Driver keeps writing the file — producer-side contract update handled in the code-brain repo). DESIGN.md §5 NIP subtitle bullet updated in lockstep. Everything else on the tile unchanged: A-6 frame, COMING pulse, construction marks, dashed border.
- **2026-06-10 (P3-14 drift log — NIP hover border 0.85; meta-strip shadow enters the vocabulary, §4):** Two reconciliations, no code changes. **(a)** The A-6 dashed border's hover animates to `rgba(255, 249, 240, 0.85)` ([`NextInProduction.astro`](src/components/projects/NextInProduction.astro)) vs the spec'd fill-to-solid `1.0`; 0.85 sits better next to the tiles' 0.25 hover hairlines (full-solid read like a focus ring), accepted as the de-facto lock. **(b)** The 2026-05-31 caption-band "soft upward shadow" (`0 -6px 14px rgba(12, 45, 47, 0.35)`, [`ProjectTile.astro`](src/components/projects/ProjectTile.astro) + NIP) was logged here but never entered the shadow vocabulary; now added to [`.impeccable/design.json`](.impeccable/design.json) as `tile-meta-strip-lift` (teal-deep-tinted, tiles only) so the deterministic detector stops reading it as an unspec'd shadow.
- **2026-06-10 (audit fix — NIP "check back" line was illegible, §4; tile aria-label):** The A-6 next-in-production subtitle rendered stamp amber `#7C2D12` on the `--teal-deep` caption band — measured **1.56:1** contrast, near-invisible (Lighthouse fail + two independent reviewers). DESIGN.md §5's component spec said stamp amber, but its own Paper-vs-Chrome Inversion Rule names amber-mid `#FAC775` as the chrome-side warm (9.4:1 here), and the rule's claim that stamp amber "passes contrast on cream *and* on teal" is measurably false on teal. Resolved in favor of the Named Rule: subtitle now `--amber-mid` ([`NextInProduction.astro`](src/components/projects/NextInProduction.astro)); no spec body edit. Same pass: removed the `aria-label="View case study: …"` from [`ProjectTile.astro`](src/components/projects/ProjectTile.astro) — it failed WCAG 2.5.3 label-in-name (visible tile text wasn't in the accessible name); the tile's own text now names the link.
- **2026-05-31 (design-critique fix — `ARCHIVED` displays as "PRIOR ROLE", §3):** The A-4 The Block status pill now reads **PRIOR ROLE** instead of ARCHIVED — `ARCHIVED` was the coldest label in the set and undersold the strongest "real B2B PM job" credential to a 5-second recruiter scan. Implemented as a display-label map in [`StatusPill.astro`](src/components/projects/StatusPill.astro); the underlying `status: ARCHIVED` enum value is unchanged, so the validator, the `archived_reference_url` null-shape, and the page-shape desaturation hooks all keep working untouched. Lockstep with the case-study-spec entry below. (No spec body edit — the §3 status table still lists `ARCHIVED` as the data value.)
- **2026-05-31 (design-critique fix — tile caption legibility, resolves §6 vs §7.1 conflict):** Tile metadata strip background changed from the `rgba(0, 0, 0, 0.15)` multiply (§7.1 colour table line "multiplied over the media") to a **solid `--teal-deep` caption band** with a `0.5px` paper-faint top hairline + soft upward shadow. The 15% multiply left the cream metadata text (title/tagline/tags) illegible over the light sketchbook tiles (A-1) and low-contrast over the dark-scene tiles (A-2, A-3), worst on mobile where tiles are larger. The solid band lands on the **§2 ASCII anatomy intent** (lines 40–52 drew the metadata as a separated strip below the media with a divider rule, not a transparent overlay), so this resolves a spec-internal conflict in favour of the diagram. Reads as a film-cell slate / mounted caption — on the "pencil test mounted in a frame" philosophy. Tagline contrast nudged `0.75 → 0.85`. No layout/aspect change. File: [`src/components/projects/ProjectTile.astro`](src/components/projects/ProjectTile.astro).
- **2026-05-31 (design-critique fix — 6th "next in production" cell given tile anatomy, §4):** The 6th cell was a near-empty dashed box that read as a missing tile beside five rich illustrations. Rebuilt to mirror `ProjectTile` anatomy — a media area carrying faint pencil construction marks (peg-bar registration crosshair + corner crop ticks) over the same solid `--teal-deep` caption band, now showing an `A-6` frame number + a pulsing `COMING` status pill + the live `check back ~<date>` line from `next-piece.json`. Dashed outer border retained as the "still on the drawing board" signal (vs the tiles' solid hairline). Reads as a reserved-and-dated sixth frame, balancing the grid. File: [`src/components/projects/NextInProduction.astro`](src/components/projects/NextInProduction.astro).
- **2026-05-24 (Phase 3F · Pass B · B3 — 5 project tile images authored):** All 5 project tile images at `public/assets/projects/<slug>.webp` (800×1000 each, total payload 210 KB vs 900 KB budget) replacing 4 placeholder stubs and 1 misdirected OG-card asset. Per-tile direction per [`docs/superpowers/specs/2026-05-24-project-tile-media-design.md`](docs/superpowers/specs/2026-05-24-project-tile-media-design.md) "Option 3" (each tile carries its own visual register):
  - **A-5 16bitfit** — photographic illustration of hands holding a Nintendo Game Boy with a pixel-art runner on the screen. Crop from `assets/projects-tiles/16bitfit-photo-1.png`. 40 KB.
  - **A-3 intent-engineering-mcp** — editorial terminal pane on cream paper with `npm install @swins/intent-engineering-mcp` resolving + DNS-verified registry success, Sean as top-right corner marginalia. Generated via OpenAI Image 2; v1 one-shot, text-legibility check passed. 32 KB. Replaces misdirected 1200×630 OG-card .png stand-in (`git rm`'d). Frontmatter `hero_media` extension `.png` → `.webp`.
  - **A-2 code-brain** — pencil-and-ink concept graph, 7 nodes, Sean holding the amber thread through 3 of them. v2 after v1 had wrong labels. 67 KB. Partial-pass deviations: 2 node labels render as gibberish marginalia; edge labels missing; character legs extend into bottom safe zone (flagged for visual review).
  - **A-1 animation-pipeline** — 4-frame pencil-test walk cycle (contact · recoil · passing · high-point) of the peach AI companion character. v2 after v1 mislabeled poses. 29 KB. Frame dividers render as full borders rather than hairlines (minor deviation).
  - **A-4 the-block** — hand-drawn 3-tier customer matrix with Sean in right margin annotating Tier 2. v2 after v1 violated the strict ARCHIVED palette. 39 KB. Cool register only (ink + cream + ink-secondary); no amber/teal.
  All 5 MDX `hero_media_alt` strings updated. Source prompts saved to `assets/tile-generation/prompts/` for traceability. Raw v1/v2 generator outputs at `assets/tile-generation/raw/` (gitignored). Pipeline validated in 2 hours wall-clock via 4 parallel `superpowers:subagent-driven-development` subagents after A-5's sequential crop.
- **2026-05-24 (Phase 3F · Pass B · B1 — A-3 tile content overflow CSS hygiene):** §8 grid implementation — added `min-width: 0` + `overflow: hidden` to `.grid-cell`. CSS Grid children default to `min-width: auto`, which allows tile content (title + tagline) to overflow the column if it grows. Preventive fix; verified at 390/1440 breakpoints. The visible cream-on-cream A-3 rendering in the 5/23 audit was actually the cream OG-card placeholder being used as `hero_media`; that issue closes in B3 tile-media work above.
- **2026-05-22 (Phase 3e — §3.5 added: `/work/` index page):** New §3.5 documents the `/work/` catalog index page that lands at `src/pages/work/index.astro` in Phase 3e. The original spec only anticipated a single surface over the `work` collection — the home Projects grid — but the top nav (added Phase 3a) already linked `/work/` and recruiters arriving via nav from other sub-pages expected the index to exist (Bug 7). The home grid remains the marketing surface; `/work/` is the IA-completeness surface. Both surfaces read the same content collection; row format on `/work/`, tile format on `/`. Click contract for both: `/work/<slug>/`.

- **2026-05-19 (critique fix §2 row 8 — drop annotation idle bob):** §9 — removed the "subtle bob (rotate ±2°) at 4s/cycle" from the updated-weekly arrow annotation. The annotation is decorative (the dateline below it carries the actual information); persistent ambient rotation on a decorative SVG is Emil's "looks cool, will see it 1000+ times" anti-pattern. Settle on first reveal, then static. Per `impeccable-and-emil-design-critique.md` §2 row 8.
- **2026-05-19 (critique fix §2 row 7 — view-transition object-fit lock):** §10 — added an explicit bullet locking `object-fit: cover` at both endpoints of the shared `view-transition-name: hero-media-<slug>`. A `cover`/`contain` mismatch would snap the morph mid-transition; the case-study hero achieves full-bleed via container framing, not via `contain`. Per `impeccable-and-emil-design-critique.md` §2 row 7.
- **2026-05-19 (critique fix 1.2 — tile hover ≤300ms):** §8 tile-media hover timing was 700ms / scale(1.04) / cubic-bezier(0.4, 0, 0.2, 1) — sluggish at the exact moment recruiters scan tiles. Now 220ms / scale(1.03) / cubic-bezier(0.23, 1, 0.32, 1). Per Emil's UI-animation-under-300ms rule and `impeccable-and-emil-design-critique.md` blocker 1.2 + §2 row 1.
- **2026-05-19 (audit fix M-3):** Hand-off prompt in Appendix C updated — `Code-Brain/BMAD/sw-ai-pm-portfolio/` → `Code-Brain/sw-ai-pm-portfolio/`. Per `2026-05-19-portfolio-spec-roadmap-alignment-audit.md` finding M-3.

### [`docs/specs/case-study-spec-v1.md`](docs/specs/case-study-spec-v1.md)

- **2026-06-16 (explainer-graphic code path lands, §2 amendment):** W3 of the [critique execution design](docs/superpowers/specs/2026-06-16-portfolio-critique-execution-design.md) wired the explainer-graphic band the W1 amendment reserved. Three pieces shipped, all **code path only**: an optional `explainer:` field on the `work` schema (`src` + `alt` required, `caption` optional ≤160 — [`src/content/config.ts`](src/content/config.ts), commit b42bff3); a new [`ExplainerGraphic.astro`](src/components/case-study/ExplainerGraphic.astro) that renders a `<figure>` (lazy `<img>` + optional mono caption) **only when** an `explainer:` block is present; and its wiring into [`work/[slug].astro`](src/pages/work/[slug].astro) between the 4Q and Methods bands (`{data.explainer && <ExplainerGraphic .../>}`, commit 596b31e). The §2 amendment now documents the `explainer:` frontmatter shape plus the per-graphic brand constraints (warm paper, ink + teal, pencil-test, one splash accent, no photoreal/gradients, WebP < ~200 KB, legible at ~1100px + mobile, real alt text). **The 5 graphics themselves do not exist yet** — no `work/*.mdx` carries an `explainer:` block, so the band renders on **zero** pages today; the images are authored later in a separate, Sean-gated generation pass, and each project gains its `explainer:` block as its graphic is approved.
- **2026-06-16 (hiring-manager critique redesign — scannable project pages, §§2-6):** A PM-lead reviewed the live site as a hiring manager and found the project pages slow to scan. Re-shaped to lead with comprehension (W1 of the [critique execution design](docs/superpowers/specs/2026-06-16-portfolio-critique-execution-design.md)): the title block now renders only **title + tagline** ([`TitleBlock.astro`](src/components/case-study/TitleBlock.astro) — role pill, IN FLIGHT, `frame · status`, tags, anchor metric all removed); the **Opener** and **Investigation Board** are removed ([`Opener.astro`](src/components/case-study/Opener.astro) / [`InvestigationBoard.astro`](src/components/case-study/InvestigationBoard.astro) no longer rendered, the 5 MDX bodies are frontmatter-only, artifacts gone); the **4Q** is question-led ([`FourQBlock.astro`](src/components/case-study/FourQBlock.astro) — no `─ 4Q ─` heading, no `A-1.Q1` prefixes, no Q labels; the question is the heading); band order in [`work/[slug].astro`](src/pages/work/[slug].astro) is now `dateline → title → status callout → hero → 4Q → explainer-graphic slot → Methods → next/prev`, with an `ExplainerGraphic` band added between 4Q and Methods (wired in W3). Status callouts (`ShippedStamp` / `ReturnConditionCallout` / ARCHIVED preamble) kept. The §§2-6 anatomy/budgets/type/motion below are retained as historical reference under the amendment block at the top of the spec; where they conflict, the amendment wins. The 4Q copy itself is rewritten from real repo source via the voice chain in W1 Part B (run from code-brain).
- **2026-06-10 (character close-out beat lands on all five case studies — Sean-approved art):** The Character-on-Every-Key-Surface rule's "close-out on every case-study page" promise is now implemented — previously the pages ended Q4 → prev/next → registration glyph with no character (audit lever 3). New [`CharacterCloseout.astro`](src/components/case-study/CharacterCloseout.astro) renders a single shared pencil-test pose above the prev/next nav: the walk-off look-back (mid-stride exit, pencil raised, sheets under arm — "on to the next one"), generated via the `gemini-pencil-animation-image-gen` skill anchored to the canonical character sheet and picked by Sean from three candidates (sketchbook-salute and seated-review rejected; source PNGs at [`reference-images/character-closeout/`](reference-images/character-closeout/)). Asset is white-point-corrected so `mix-blend-mode: multiply` melts the cel's cream into the page paper (drawn-on-the-sheet, not pasted-over); 560×750 WebP, 22KB, lazy-loaded with explicit dimensions, static and therefore reduced-motion-safe. Verified on fresh build + preview screenshot.
- **2026-06-10 (audit fixes — build-tz-deterministic dates + frame numbers to stamp amber, §4 deviation):** **(a)** The SHIPPED stamp ([`ShippedStamp.astro`](src/components/case-study/ShippedStamp.astro)) and the IN FLIGHT range ([`TitleBlock.astro`](src/components/case-study/TitleBlock.astro)) formatted dates in the build machine's timezone, so the intent-engineering stamp read **"MAY 11, 2026"** on any ET build (vs the correct MAY 12 on Vercel's UTC build, the page's own prose, and the OG card), and 16BitFit's range read 11-2025 instead of 12-2025. Both now format in UTC — output is identical on ET and UTC builds (verified by double-build). **(b)** [`ShippedNow.astro`](src/components/case-study/ShippedNow.astro) timestamps now render in `America/New_York`: Vercel's UTC build was showing the fleet's 08:30 ET write as **"12:30"** on the live site, undercutting the morning-fleet narrative. **(c)** §4 deviation, no spec body edit: title-block + prev/next frame numbers move from the spec'd amber-mid `#FAC775` to stamp amber `#7C2D12` — amber-mid is chrome-only per DESIGN.md's Paper-vs-Chrome Inversion Rule and measured **1.48:1** on cream (Lighthouse contrast fail); the case-study page is a paper surface. Case-study a11y 96 → 100.
- **2026-06-01 (launch-critique fix — empty board cards + em-dash pass, §7.2):** Seven `<BoardArtifact>` cards (code-brain ×3, the-block ×3, 16bitfit ×1) shipped with a `boardLabel` but no `excerpt`/ticket, rendering the 180px polaroid frame as an empty box with a single amber title; their substance sat only in the right-hand `caption`. Authored a terse board-voice `excerpt` for each (the artifact body), distinct from the narrative caption. Same pass: ran the DESIGN.md em-dash ban across all five case studies, converting in-prose dashes and `title — subtitle` board/PRD/chart label separators to colons/commas/periods (voice preserved, no words changed), leaving the wire-service dateline stamps (`MAY 20 —`) and the locked `The Block — Campus + RevOps` project title. The pass was then extended across the full transactions + architecture deep-body prose (4Q answers, limitations/honest-note callouts, `title`/`tool`/`mermaidCaption` label separators) and the essay chart caption: every rendered in-prose em dash converted consciously, voice preserved. Only non-rendered code comments, the wire-service dateline/`KILLED` stamps, alt-text, and the sitewide `— Sean Winslow` page-title separator (incl. the locked `The Block — Campus + RevOps` project title) still carry em dashes by design.
- **2026-05-31 (design-critique fix — The Block reads "PRIOR ROLE" on the case study, §12.4):** Lockstep with the projects-section-spec entry. The case-study `TitleBlock` status pill and the §12.4 `FrameTheWorkPreamble` stamp now display **PRIOR ROLE** instead of ARCHIVED so a click-through from the grid stays consistent. Display-only: the `status: ARCHIVED` enum, the `.status-archived` / `[data-status="ARCHIVED"]` desaturation page-shape, and the "framing the work, not the exit" preamble behavior are all unchanged. Files: [`TitleBlock.astro`](src/components/case-study/TitleBlock.astro), [`FrameTheWorkPreamble.astro`](src/components/case-study/FrameTheWorkPreamble.astro).
- **2026-05-20 (LOCKED):** All 3 OPEN-N items resolved to recommended defaults per [`BLUEPRINT-COMPLETE.md`](docs/specs/BLUEPRINT-COMPLETE.md) §4.3: (1) Animation-pipeline `EXPLANATION.md` lives in `~/Code-Brain/code-brain/animation-pipeline/EXPLANATION.md` (co-located with the code-brain repo). (2) The Block ARCHIVED status locked; first-draft frames the work, Larry Cermak as primary reference. (3) 16BitFit PAUSED status locked; case study leans on the pipeline as the artifact, not a finished game. PMP §10 #4 + #5 lockstep-resolved per the same 2026-05-20 cross-cutting decisions entry.
- **2026-05-19 (critique fix §3 — 4Q body type step):** §4 — 4Q Q2-Q4 body size dropped from Newsreader 20/18 → 18/16 so the Q1 ("What is this?") → Q2-Q4 step lands at 1.33× desktop / 1.25× mobile (was 1.2× / 1.1×). Restores the type-step discipline the rest of the page already follows. Per `impeccable-and-emil-design-critique.md` §3 "Type hierarchy spot-check".
- **2026-05-19 (critique fix §2 row 7 — view-transition object-fit lock):** §6 — annotated the hero-media cross-fade row to lock `object-fit: cover` at both endpoints (matching the projects-spec §10 contract). Container is full-bleed; inner media is `cover`. Per `impeccable-and-emil-design-critique.md` §2 row 7.
- **2026-05-19 (audit fix M-3):** Hand-off prompt updated — `Code-Brain/BMAD/sw-ai-pm-portfolio/` → `Code-Brain/sw-ai-pm-portfolio/`. Per `2026-05-19-portfolio-spec-roadmap-alignment-audit.md` finding M-3.
- **2026-05-17:** Initial draft. Inherits projects-spec §10 (click contract) + §11 (high-level shape sketch). The 4Q block here is canonical — mirrors [`EXPLANATION-template.md`](../code-brain/vault/40_knowledge/templates/EXPLANATION-template.md) heading-for-heading. The `SHIPPED` status (added 2026-05-17 to the projects spec for Intent Engineering MCP) is treated as a full citizen here.

### [`docs/specs/about-spec-v1.md`](docs/specs/about-spec-v1.md)

- **2026-06-10 (Proof points become labeled rows, §13 deviation — Sean-approved):** The B-5 block's raw mono URLs (`linkedin.com/in/sean-winslow-204390a5` showing its hash slug right before the closing pull quote) are restyled into labeled rows with human display text: `→ LINKEDIN — Sean Winslow` · `→ GITHUB — seanwinslow28` · `→ TRANSACTIONS — the ledger, every artifact dated` (+ the dormant RESUME row, still gated on the PDF existing). Channel in teal mono 500, descriptor in ink-secondary 400; the em dash is wire-service register per the audit's D-2 precedent (mono-label em dashes inherit from newspaper typography). Deviates from §13's "no descriptions" lock per audit lever 4 — the page's biggest serif promise now ends at the same craft level it opens with. Hrefs unchanged; print stylesheet still exposes the real URLs ([`ProofPoints.astro`](src/components/about/ProofPoints.astro)).
- **2026-06-10 (audit fixes — lead line becomes the page h1; pull quote catches up to D8):** **(a)** `/about/` had zero `<h1>` — the locked lead line ("Raised by Saturday morning cartoons…") rendered as a `<p>` and the heading outline started at the B-1 `<h2>`. The lead is now an `<h1>` with identical styling ([`AboutHero.astro`](src/components/about/AboutHero.astro)); byte-identical copy, `validate_about` untouched. **(b)** The closing pull quote still read *"Product Manager. The agents handle the loops. I handle the taste."* — its own component contract says it echoes the home-hero tagline byte-identical, and D8 (2026-05-30) dropped the "Product Manager." sentence from the hero; the lockstep update was missed. Now matches the hero ([`PullQuote.astro`](src/components/about/PullQuote.astro)). Also: the home teaser's `TODAY ·` prefix moves to stamp amber — teaser-spec §4 names "stamp-amber" but carries amber-mid's hex (`#FAC775`), which is chrome-only and 1.48:1 on cream; resolved to the named token ([`AboutTeaser.astro`](src/components/home/AboutTeaser.astro)). The about-spec's own §4 lock of amber-mid for the `B-n ·` kickers was left untouched (documented intent; flagged in the audit report as a spec-vs-DESIGN.md conflict for Sean's call).
- **2026-06-01 (B-2 + B-4 prose rewrite — re-grounded in Sean's actual story):** Both body sections were reworked from a fresh brainstorming interview after Sean flagged that the prior drafts "didn't sound like me" — they were arguments Claude constructed (B-2: *"comprehension is the artifact / the spec is the product"*; B-4: *agent governance / intent engineering / verification loops*), then voice-mode-polished by the council, without anyone first asking Sean what was true. New process: interview → seed thesis → variance-profile [llm-council](docs/critiques/about-b2-b4-council-2026-06-01.md) generative pass (4 models write from the seed in Sean Mode, cross-rank, chairman synthesizes) → Sean's hand-edit pass. **B-2 heading** changed from *"Why a PM, not just a builder"* → **"The Long Way to the Obvious Answer"** (visible `<h2>` + `aria-label`; the §4/§1.2 hand-drawn `b2-why-pm.svg` is deferred, so this is a plain-text edit). **B-2 thesis** is now *a creative/director who got obsessed with AI and found a way to make it the day job; PM is the name for an instinct he already had; the only ceiling is your imagination* (2 paragraphs, the cerebral 3rd "Sunday-morning-fleet-digest" paragraph dropped). **B-4 thesis** is now the **"bridge person"** — fluent in spec and storyboard, building AI-native tools that let one creative ship at studio scale without studio permission; the 2D animation pipeline named as the bridge between his two worlds (1 paragraph; still employment-state-neutral per §12, no availability language). Guardrails enforced: no AI jargon, no invented daily-fleet set-pieces, lived experience referenced thematically (B-1 timeline not re-narrated). Pure MDX body + heading edit at [`src/content/about/index.mdx`](src/content/about/index.mdx); `BuildingToward` renders the single B-4 paragraph cleanly; `validate_about` + `validate_content` + full build all green. The §12 "2 paragraphs" footprint is now 2 (B-2) / 1 (B-4) — Sean's call; the surface contract (durable bet, no employment signal in prose) is unchanged.

- **2026-05-22 (Phase 3e — §11.3.1 v1 default: all 6 cels ship `break_grid: false`):** Samurai Jack's `break_grid: true` was orphaning Jake-the-Dog in a single-cel row 3 (Bug 8). Sean's call: scale Jack down to match the other 5 cels, packing row 2 cleanly (Jack · Iroh · Jake). The `.cartoon-cel--break-grid` CSS path in `CartoonCel.astro` stays as dead code for v1.1 emphasis if/when a future cel warrants the geometric break; the schema field default in `src/content/config.ts` remains `false`. v1.1 swap-back is a one-flag MDX edit. Documented inline at §11.3.1 with a Phase 3e v1-default banner.

- **2026-05-21 (§784 photo-exception sync to 10-card deck):** §784 — the home-teaser photo-exception line was authored against the original 9-card draft (1 photograph + 8 AI-style variations). The deck locked at 10 cards on 2026-05-20 (1 photograph + 9 AI-style variations) but this line wasn't updated in lockstep. Card count corrected. The amendment itself (allowing a photo of Sean as a single card in the home-teaser swiper) per [`home-about-teaser-spec-v1.md`](docs/specs/home-about-teaser-spec-v1.md) §10.1 remains unchanged.

- **2026-05-21 (cartoon canon LOCKED + §11.4 tooling correction):** Phase 0 asset authoring closed the last two open items on this spec. §11.2 — the placeholder 6-cartoon table (Coyote / Pinky / SpongeBob / Looney Tunes / Dexter / Hey Arnold) was replaced with the canonical 6: Tommy Pickles · Ash Ketchum · Rocko · Samurai Jack (`break_grid: true`) · Uncle Iroh · Jake the Dog. Bugs Bunny was generated, considered, and rejected during asset authoring in favor of Uncle Iroh — Bugs read as a gag-cartoon "composure under chaos" lesson; Iroh carries the deeper PM truth (the most experienced person in the room choosing not to use their authority). All 6 PNGs generated and committed to [`reference-images/about-cartoons/`](reference-images/about-cartoons/); MDX frontmatter draft at [`reference-images/about-cartoons/cartoons-content-collection-draft.md`](reference-images/about-cartoons/cartoons-content-collection-draft.md). §11.4 — the spec's previous "Seedream 2.0" reference was a tooling-name error (Seedance is Bytedance's video model, not the image generator on this project); corrected to **Google Gemini Nano Banana 2** (`gemini-3.1-flash-image-preview`) via the [`.claude/skills/gemini-pencil-animation-image-gen`](.claude/skills/gemini-pencil-animation-image-gen/) skill, Approach B (style anchor + per-character reference, both passed via `--reference`). §18 OPEN-3 + OPEN-4 marked RESOLVED with strikethrough. Hand-drawn replacements remain available as a v2 enhancement Sean owns if any v1 cel reads as off post-launch.

- **2026-05-20 (LOCKED with v1 decorative deferrals):** All 6 OPEN-N items resolved to recommended defaults per [`BLUEPRINT-COMPLETE.md`](docs/specs/BLUEPRINT-COMPLETE.md) §4.4: (1) Character composition: Sean alone, full-body, no AI companion. (2) Timeline content: 8-10 paired rows; prose authored at build time. (3) 6 Saturday-morning cartoons + lessons: canonical list authored at build time. (4) Pencil-test study authorship: Path C hybrid — 2-3 hand-drawn by Sean + 3-4 Seedream backfill. (5) V3 strikethrough artifact: not included (Sean's voice is comedic-confident, not self-flagellating). (6) Hand-drawn heading SVG authorship: **DEFERRED to v2** per new §1.2 — v1 uses plain Newsreader headings. **NEW §1.2 inserted** documenting v1 decorative deferrals: 5 B-N heading SVGs, handwritten signature, registration marks, all 12 pencil-margin annotations, kid-drawing scan margin artifact. The 6 cartoon cels + B-1 lane-tint rule + character PNG + pulse strip remain in v1 — story + animation cels carry the visual rhythm per Sean's call. DoD §17 updated in lockstep: #2 fixed to assert the locked lead-line string (the validator was still asserting the SUPERSEDED `"Trained by Looney Tunes..."` block — bug caught at lock); #6 + #9 drop annotation clauses; #10 + #11 marked DEFERRED with strikethrough; #12 substitutes plain Newsreader headings for hand-drawn SVGs.
- **2026-05-20 (§784 photo-rule addendum):** §784 anti-photo rule amended to allow the photograph as a single card in the home-page teaser swiper. The rule's spirit is preserved — photo never as primary headline, only as one register in nine in the swipeable character-deck. The 8 non-photo cards re-state the character thesis in 8 aesthetic languages, so the deck is the rule's resolution rather than its violation. See new [`docs/specs/home-about-teaser-spec-v1.md`](docs/specs/home-about-teaser-spec-v1.md) §3 + §7 + §10.
- **2026-05-20 (home-hero word swap lockstep):** The home-hero tagline word swap (`math` → `loops`, see hero-spec changelog) rippled through about-spec at three locations: §2 anatomy pull-quote text (the cross-page closeout reproduced verbatim at the page foot), §4 voice-register table pull-quote reference, and §7.2 rationale paragraph (which quotes the home-hero tagline when explaining why "Raised by..." earns the home hero's "taste" claim). Three string replacements; no behavioral change. The pull-quote at /about/ page foot now reads the new tagline.
- **2026-05-19 (critique fix §3 — daily-dated rename):** Swept "live" → "daily-dated" / "morning-fresh" wherever the data source is the Daily Driver. Section heading §8 "Live pulse strip" → "Daily-dated pulse strip"; all in-body references to the pulse strip follow. See the `## Non-spec changes` entry for the cross-spec rationale.
- **2026-05-19 (critique fix §2 row 4 + §3 cards — signature easing + cel grid):** Two edits. §6: signature stroke-dasharray easing was `cubic-bezier(0.6, 0, 0.4, 1)` (ease-in-out); switched to `cubic-bezier(0.16, 1, 0.3, 1)` (ease-out) — reads as a pen writing in, fast down-stroke then settle. §11.3 + new §11.3.1: cels now carry a frontmatter `angle: ±1.5°` (defaults randomized + cached) and one cel (the §11.7 "rewires-you" cel) sets `break_grid: true` to render 1.2× and span two columns — breaks the 3×2 bento-box feel; geometry now confirms what the annotation already flags. Per `impeccable-and-emil-design-critique.md` §2 row 4 + §3 "Identical card grids" finding.
- **2026-05-19 (critique fix 1.1 — SVG lane rule):** §9.2 + new §9.2.1 — the B-1 lane-tint rule is now explicitly an SVG `<line>` element with `stroke-dasharray` animation, not a CSS `border-left`. Sidesteps Impeccable's side-stripe-border ban by reframing the affordance as a timeline scrubber. Preserves the §6 reveal animation contract (the prior wording was self-inconsistent: §6 wanted `stroke-dasharray`, §9.2 implied a CSS border). Per `impeccable-and-emil-design-critique.md` blocker 1.1.
- **2026-05-19 (audit fix M-3):** Hand-off prompt updated — `Code-Brain/BMAD/sw-ai-pm-portfolio/` → `Code-Brain/sw-ai-pm-portfolio/`. Per `2026-05-19-portfolio-spec-roadmap-alignment-audit.md` finding M-3.
- **2026-05-18 (B-1 geometry swap + B-4 evergreen rewrite):** Two structural edits after Sean's review. **B-1** abandons the two-column animator-vs-PM parallel-lineage timeline (with torn-paper gutter + cross-gutter SVG arrows) in favor of a **single-column stacked-beats** layout: 6-8 moments running top-to-bottom, each a Newsreader sentence + JetBrains Mono subline, with a thin left-margin rule that runs **warm amber `#FAC775` for animator-coded beats**, **teal `#0A3E42` for PM-coded beats**, and **blends between** when a beat braids both. The parallel-lineage rule (PMP §3.1) survives — it's now expressed as a chromatic gradient on a single column, not as side-by-side columns. The "back-and-forth" reading rhythm Sean found exhausting collapses into a single down-the-page scan. Mobile no longer needs the alternating-row collapse rule. §2 anatomy diagram, §3 vertical budget (B-1 collapses from ~1200-1800px to ~960-1280px), §4 type + voice register, §6 motion timeline (cross-gutter arrow logic retired), §9 fully rewritten, §14 annotations (cross-gutter arrows removed from the curved-arrow vocabulary list, registration-mark corners retained as "beat-column registration spine"), §16 build stack (`<TimelineGutter />` and `<GutterArrow />` retired; `<BeatRow />` + `<LaneRule />` added), §17 DoD items 5 + 6 rewritten, Appendix A file map updated. **B-4** replaces the job-hunt-coded `AVAILABLE 2026-05 · AI PM > TECH PM > CREATIVE PM` pill + "I'm looking for a seat..." copy with **direction-of-craft prose** ("What I'm building toward" — 2 paragraphs naming Sean's bet on AI-native PM, agent governance, and the spec layer). A tiny `CURRENTLY @ FREE AGENT` or `CURRENTLY @ <COMPANY>` mono stamp top-right of the section is the only employment-state surface — driven by frontmatter `current_company`. The agent-fleet-config beat is preserved but moved into a single italic call-out that renders **only when `available: true`** ("if you're hiring around this, my agent fleet config goes out on second conversation"). Net effect: when Sean gets hired, he flips `available: false` and updates `current_company` — zero prose edits required, the page reads cleanly either way. §2 anatomy, §4 type + voice register, §12 fully rewritten, §17 DoD item 8 rewritten, Appendix B frontmatter shape updated.
- **2026-05-18 (lead-line + home-hero swap):** After drafting the About body, Sean preferred the single original line ("Raised by Saturday morning cartoons and Vercel deployment logs.") as the About header — the 3-sentence credentials block was retired. In lockstep, the home-hero tagline was repositioned to lead with the PM role explicitly ("Product Manager. The agents handle the math. I handle the taste.") — letting the home page declare the role and the About page do the biographical work. The cross-page rhyme architecture (home "Raised by..." → about "Trained by...") dissolves; the two pages now tell one story in two registers (declarative on home, biographical on About) without rhyming verbatim. PMP §4 updated in lockstep. The "Trained by Looney Tunes..." string is marked SUPERSEDED 2026-05-18 in PMP §4. The bottom-of-page pull quote is updated to echo the new home hero. §7.1 and §7.2 rewritten accordingly. The motion timeline collapses from per-sentence-stagger to a single-line reveal (one beat, not three).
- **2026-05-17 (revision):** Lead-line + B-3 section revised after Sean's review of v1. The dog framing was killed (it forced an artifact that wasn't load-bearing to Sean's actual personality). The PMP §4 About-page lead row gets updated in lockstep — the locked string moves from the dog deflation to a **compressed-parallel credentials block** that harmonizes with the home-hero tagline ("Raised by..." → "Trained by..."). B-3 transitions from a dog-Polaroid gallery to **Saturday morning canon** — six hand-drawn pencil-test studies of cartoon characters Sean takes as formative, each captioned with the product/craft principle the cartoon taught. Animation-cel frame treatment (with hand-drawn registration pegs) replaces Polaroid framing to signal "cultural-craft reference rendered through Sean's hand" rather than "personal artifact." Five dog-related OPEN questions in §18 are removed; three cartoon-canon OPENs added.
- **2026-05-17 (initial):** First draft. Honored hero §10's locked "About = paper, no splash" decision. Annotation density cap raised from ≤6 (case-study) to ≤12 (About).

### [`docs/specs/transactions-spec-v1.md`](docs/specs/transactions-spec-v1.md)

- **2026-06-01 (RSS feeds get a browser-facing styled landing, §10):** Opening any feed (`/transactions/rss.xml`, `/architecture/rss.xml`, `/essays/rss.xml`) in a browser showed the raw "this XML file has no style information" tree, which reads as broken to a recruiter who clicks the footer/header RSS link. Authored one XSL stylesheet at [`public/rss/styles.xsl`](public/rss/styles.xsl) and passed `stylesheet: "/rss/styles.xsl"` to all three `rss()` calls — `@astrojs/rss` injects an `<?xml-stylesheet?>` PI so a human sees a calm on-brand page (paper/ink/teal/stamp-amber, web-safe font stacks only — no CDN font, to keep the F3 self-hosting fix intact) listing the feed title, a one-line "paste this into your reader" blurb, and the items; feed readers still parse valid RSS 2.0 with `content:encoded` intact. **Open question resolved:** the spec was silent on a browser-styled feed; since the link is recruiter-facing, the styled-XSL landing is the right call over reader-only. Site-chrome-spec §2.4 lockstep entry below.
- **2026-06-01 (four launch-readiness prebuild guardrails, §11.1):** Turned the [2026-06-01 launch-critique post-mortem](docs/post-mortems/2026-06-01-impeccable-critique-launch-readiness.md) §5 lessons into hard checks in [`scripts/validate_content.mjs`](scripts/validate_content.mjs) (first in the `prebuild` chain), because every P0/P1 in that critique passed `astro build` — the failures were valid-but-incomplete data and authored-but-unwired assets. (1) **Empty-board-card** (F1): every `<BoardArtifact>` in `work/*.mdx` must carry an `excerpt` or both `ticketKey`+`ticketTitle`, else it renders an empty polaroid. (2) **Em-dash lint** (F6, DESIGN.md §213): hard-fail on em dashes / typographic `--` in rendered prose fields (`valueProp`, `lead`, `excerpt`, `subtitle`, `mermaidCaption`, `tagline`, `limitations[]`, `honestNotes[].body`) and MDX body prose; allowlists inline-code (so backticked CLI flags like `--skip-llm-judge` pass), wire-service `MON DD —` / `KILLED —` caption stamps, the locked `The Block — Campus + RevOps` title, and the non-scanned `title`/`description`/`alt`/`dateline` fields. (3) **Dateline coherence** (F2): `about-pulse.json.date_iso` may not be after the build date (the existing `dateline.json` future/staleness guard already covered the hero). (4) **Render-gated asset locks** (F3/F4): every `public/og-cards/**` file must be referenced by some page's `ogImage`, and no `src/**` file may load a font from `//fonts.gstatic.com`/`//fonts.googleapis.com`. All four pass clean on the current tree and were verified to fire on seeded violations (both directions).
- **2026-05-20 (LOCKED):** All 5 OPEN-N items resolved to recommended defaults per [`BLUEPRINT-COMPLETE.md`](docs/specs/BLUEPRINT-COMPLETE.md) §4.5: (1) Surface enum keeps all 5 values `[fleet, pipeline, product, writing, infra]`. (2) 4Q source dual-mode (canonical `explanationUrl` + V3 inline-body fallback) runs ~3-6 months until V3 entries earn upstream `EXPLANATION.md` files. (3) Manifesto earns a ledger row at `surface: writing` with `relatedEssay: access-vs-meaning`. (4) Daily Driver `ledger_row` body authorship locked to the 3 templates sketched in §13; agent picks based on overnight signal. (5) Loom embed budget strict at 1 per deep-dive page — a row needing 2 videos splits into 2 rows or earns a case study.

- **2026-05-19 (critique fix §4 — derive_crosslinks error messages):** §11.1 — declared the error-message contract: dangling-slug errors must name file + field + slug + nearest-real-slug suggestion. Eliminates the "one typo breaks the build with a generic error" surface flagged by `impeccable-and-emil-design-critique.md` §4.
- **2026-05-19 (audit fix I-1.1, additive — `relatedArchitecture`):** Added `relatedArchitecture: z.union([z.string(), z.array(z.string())]).optional()` to §3.2 schema per architecture-spec §14.1, with a matching bidirectional row in §14's cross-link contracts table and a `relatedArchitecture: null` example in Appendix B. Pure additive; legacy rows without the field still validate. Closes the 4-surface cross-link graph from the ledger side. Per `2026-05-19-portfolio-spec-roadmap-alignment-audit.md` finding I-1.
- **2026-05-19 (audit fix C-1, surface remap table):** Added "Free-text → canonical `surface` remap (forward-looking)" subsection inside §12.3 alongside the existing `beat → surface` table. Locks the canonical mapping for the free-text strings the unified roadmap + V3 bridge are already authoring (`MCP server` / `eval harness` / `SDK agent` / `control-plane interceptor` / `manifesto / thesis` / `architecture writeup`) into the 5-value enum. Migration script (`scripts/migrate_v3_transactions.mjs`, §11.1) is the consumer of record. Keeps the enum strict; no schema widening. Per audit finding C-1.
- **2026-05-19 (audit fix M-3):** Hand-off prompt in Appendix C updated — `Code-Brain/BMAD/sw-ai-pm-portfolio/` → `Code-Brain/sw-ai-pm-portfolio/`. Per audit finding M-3.
- **2026-05-17:** Initial draft. Re-architects the V3-bridge schema (which doesn't carry forward verbatim — see §3.1). Inherits case-study spec §9.2's canonical-EXPLANATION contract. Reuses `scripts/fetch_explanations.mjs`. Slugs from the 2 (or 5, depending on cadence at crossover) V3 entries are preserved across migration. Recruiter mental model: "Sean's resume is the ledger."

### [`docs/specs/architecture-spec-v1.md`](docs/specs/architecture-spec-v1.md)

- **2026-06-10 (audit fixes — Shiki re-themed to the palette, §10; index dateline via dateline.ts):** **(a)** The §10 Try-It-Yourself code block used Shiki's `github-light`, which injected pure `#fff` plus five foreign hues (`#005CC5`, `#032F62`, `#6F42C1`, `#D73A49`, `#24292e`) as inline styles that defeated the component's CSS override — the one No-Pure-White violation on a live page. Replaced with an inline custom theme on the system palette (paper bg, ink fg, teal/success-teal/stamp-amber tokens) in [`TryItYourself.astro`](src/components/architecture/TryItYourself.astro); zero foreign hex values remain in the rendered page. **(b)** [`IndexHeader.astro`](src/components/architecture/IndexHeader.astro) (and its essays sibling) built its "BOSTON, …" dateline from a bare `new Date()` instead of `src/lib/dateline.ts`, so any post-8pm-ET deploy (UTC server) would stamp tomorrow's date — the exact failure dateline.ts §8 exists to prevent; both now derive from `BUILD_DATE_PRETTY`. Also: `MetricChart`'s SVG kept the default `overflow: hidden` and clipped its last x-axis label ("L3-full" rendered "L3-ful" on the intent-engineering case study); now `overflow: visible`.
- **2026-05-20 (LOCKED):** All 5 OPEN-N items resolved to recommended defaults per [`BLUEPRINT-COMPLETE.md`](docs/specs/BLUEPRINT-COMPLETE.md) §4.6: (1) Control Architecture (Task 14, ~6/10) earns a writeup at `/architecture/control-architecture/` in addition to its ledger row — same artifact shape as Vault Scorecard. (2) Index page hook line: *"Most people see Obsidian as content. I treat my vault as agent infrastructure."* — kept as page hook until `/architecture/` has 3+ writeups. (3) Mermaid palette ships v1 with the 5-var override; fallback to `mmdc` PNG export if rendered output looks inconsistent. (4) Reading-time computation manual for v1 (`readingTime:` in frontmatter); auto-compute deferred to v2. (5) Honest-note anchor matching is warning-on-fail, not error — build keeps shipping.

- **2026-05-19 (critique fix §4 — fetch_canonical_sources fallback):** §11.1 — declared the never-requires-network contract: committed fetched files are the source of truth; the script refreshes but exits 0 on remote failure. Eliminates the build-failure-on-rename surface flagged by `impeccable-and-emil-design-critique.md` §4 "Build infrastructure fragility".
- **2026-05-19 (audit fix M-3):** Hand-off prompt updated — `Code-Brain/BMAD/sw-ai-pm-portfolio/` → `Code-Brain/sw-ai-pm-portfolio/`. Per `2026-05-19-portfolio-spec-roadmap-alignment-audit.md` finding M-3.
- **2026-05-17:** Initial draft. Establishes /architecture/ as the third top-level IA surface (alongside /transactions/ + /work/), specifically for **architectural arguments** (thesis-shaped, comparison-based) — distinct from /transactions/ which is for **shipped systems**. An artifact can appear on both surfaces with the same slug, cross-linked. Requires an **additive** update to `transactions-spec-v1.md` §3.2 — adding `relatedArchitecture: string \| string[]` as an optional field on the ledger schema (see §15.4). No removals or renames to the locked transactions spec.

### [`docs/specs/essays-spec-v1.md`](docs/specs/essays-spec-v1.md)

- **2026-06-10 (audit fixes — essay body rendered twice; role-map dead link, §2.2 + §9):** **(a) P0:** `/essays/meaning-over-access/` rendered the full ~2,000-word essay **twice** — once as the §2.2 band-6 body, then verbatim again under "— EXPLANATION —". The band-8 inline-4Q fallback rendered `<Content />` whenever no canonical EXPLANATION file existed, but for an essay whose MDX *is* the body (`sourceUrl: null`), that duplicates the whole essay; on the one page arguing "I review what agents produce," doubled boilerplate is the exact failure the thesis claims to prevent. The fallback now renders only when the body came from `sourceUrl` (then the MDX legitimately serves as the explanation); page height 14,333px → 9,478px, body renders once ([`essays/[slug].astro`](src/pages/essays/[slug].astro)). **(b)** The §9 role map's negative-space row (schema amendment 2026-05-30 made `jdUrl` optional) still rendered an href-less dead `<a>link ↑</a>`; [`RoleMap.astro`](src/components/essays/RoleMap.astro) now renders a muted `·` for rows with no JD, and its Props type matches the schema (was one of four `astro check` ts2322 errors, now zero). **(c)** The quadrant chart's mermaid SVG clipped its title at both ends ("…cess vs Meaning × …Workfl"); SVG overflow now visible. **(d)** Essays IndexHeader dateline now derives from `dateline.ts` (see architecture-spec entry).
- **2026-06-01 (essays RSS feed registered on early publish, §12):** Following the manifesto's early publish (below), the essays feed was un-gated in `RSS_FEEDS` so `/essays/rss.xml` is discoverable (auto-discovery `<link>` + footer link) and now carries the "Access Over Meaning" item. Per the §13.5 subscribe-affordances contract, this is the intended state once an essay is live; the feed also picked up the new browser-facing XSL landing (transactions-spec / site-chrome-spec entries). Lockstep with site-chrome-spec §2.4.
- **2026-06-01 (manifesto pulled forward to launch; OG wired):** "Access Over Meaning" was staged `DRAFT` with `published: 2026-06-19` (SHIP-PLAN D5) and was therefore excluded by `isLiveEssay`, leaving `/essays/` an empty room in the nav at launch. Per the launch-day critique it was flipped to `PUBLISHED`, dated `2026-06-01` (dateline + `roleMap.lastValidated` moved off the future 6/19), and its pre-publish editorial em-dash pass was completed (≈27 deliberate em-dash breath marks re-punctuated to commas/colons/parens/periods per DESIGN.md, voice and rhythm preserved, no words changed). Its dedicated OG card (`/og-cards/essays/meaning-over-access.png`) is now wired through `essays/[slug].astro` (the schema gained an optional `ogImage`; the same wiring lit the dedicated cards for the intent-engineering MCP case study and the vault-scorecard architecture writeup, which had been falling back to `og-default.png`).
- **2026-05-20 (LOCKED):** All 5 OPEN-N items resolved to recommended defaults per [`BLUEPRINT-COMPLETE.md`](docs/specs/BLUEPRINT-COMPLETE.md) §4.7: (1) Index page hook line: *"I bet on meaning, not access."* — Vault-Scorecard-style identity claim from the manifesto itself. (2) JD URL validation every build, warning-on-fail; revisit cadence at 5+ essays. (3) `plottedArtifacts` frozen at publish; `lastRevised` marks editorial updates only — growing the list earns a new essay. (4) Future thesis-clustering when `/essays/` grows past 5: deferred to v2; v1 ships flat list. (5) Newsletter form intentionally omitted; subscribe affordances are `/essays/rss.xml` + Substack link.

- **2026-05-19 (audit fix M-3):** Hand-off prompt updated — `Code-Brain/BMAD/sw-ai-pm-portfolio/` → `Code-Brain/sw-ai-pm-portfolio/`. Per `2026-05-19-portfolio-spec-roadmap-alignment-audit.md` finding M-3.
- **2026-05-17:** Initial draft. Establishes /essays/ as the **fourth top-level IA surface** (alongside /work/, /transactions/, /architecture/), specifically for **thesis-shaped writing that frames multiple artifacts into a single argument** — distinct from /architecture/, which argues *about a single system*. An artifact can appear on all four surfaces simultaneously with the same slug, bidirectionally cross-linked. Closes the four-surface cross-link graph: every artifact named in an essay's `plottedArtifacts` array auto-renders "← named in: [essay title]" on its ledger row + architecture writeup, build-derived by `derive_crosslinks.mjs`. No schema changes required on locked transactions or architecture specs (this is the third surface to receive reverse links via the same script).

### [`docs/specs/site-chrome-spec-v1.md`](docs/specs/site-chrome-spec-v1.md)

- **2026-06-16 (critique W2 — minimal corner nav, §2.1 + §3.2):** Three affordance changes from the hiring-manager critique pass, all touching the home-page chrome. **(a)** `HomeCornerNav.astro` added as a fixed top-right overlay on the home page: wordmark + `/work/` + `/about/` links, visible over the hero, keyboard-focusable. The full sticky `SiteNav` remains suppressed via `noChrome={true}`; the corner nav is additive. **(b)** The projects-section `UPDATED · <date>` dateline label was enlarged so it reads at arm's length. **(c)** The "updated weekly" pencil annotation was removed from the projects section — the live dateline already carries the freshness signal; the annotation was redundant noise. `rev-3` marker + registration mark untouched. Swipe-affordance changes logged under home-about-teaser-spec below.
- **2026-06-10 (404 earns its one cel, §10 — Sean-approved art):** The 404 page gains a single pencil-test drawing under the body copy: the character shrugging at a completely empty desk, abandoned mug and one crumpled page on the floor — the literal illustration of the locked "Filed, but it never ran." line (audit delight 5). The §10 tone discipline holds: fact stated calmly, no rescue energy, the cel is deadpan evidence rather than decoration. Generated via the `gemini-pencil-animation-image-gen` skill anchored to the canonical character sheet; source PNG at [`reference-images/character-closeout/404-empty-desk.png`](reference-images/character-closeout/404-empty-desk.png). White-point-corrected + `mix-blend-mode: multiply` (reads as drawn on the page), 1100×738 WebP 43KB, explicit dimensions, eager load (above the fold), static ([`404.astro`](src/pages/404.astro)). Verified on fresh build + preview screenshot.
- **2026-06-10 (audit fixes — about pulse in ET, Tailwind source scoping, wordmark label, explanations collection):** **(a)** [`NowPulse.astro`](src/components/about/NowPulse.astro) rendered the Daily Driver's `updated_at` in the build machine's timezone: the live (UTC-built) site showed **"updated 12:32 by daily driver"** for an 08:32 ET morning write — contradicting the morning-fleet framing the honesty layer depends on. Now renders in `America/New_York`. **(b)** Tailwind 4's auto source-detection was scanning `.claude/skills/` + `.agents/skills/` markdown and emitting dead utilities (`border-l-4`, `text-gray-400`, `bg-blue-500`) into the shipped CSS; `@import "tailwindcss" source("..")` scopes detection to `src/` ([`global.css`](src/styles/global.css)). **(c)** The nav wordmark's `aria-label` ("Sean Winslow — home") didn't contain the visible "SW" (WCAG 2.5.3); now "SW — Sean Winslow, home" ([`SiteNav.astro`](src/components/chrome/SiteNav.astro)). **(d)** `src/content/explanations/` is now a declared (empty, fs-read-only) collection in [`content/config.ts`](src/content/config.ts), silencing Astro's deprecated auto-generation warning. Home a11y 96 → 100 with the projects-section fixes.
- **2026-06-01 (essays RSS feed un-gated + feeds styled, §2.4):** The essays entry in `RSS_FEEDS` ([`src/lib/site.ts`](src/lib/site.ts)) was staged-out behind a "uncomment on 2026-06-19 publish day" comment, but the manifesto went live early (published 2026-06-01 — see essays-spec entry), so the feed link was un-commented. Its `<link rel="alternate" type="application/rss+xml">` auto-discovery (BaseLayout §2.4) and the footer SUBSCRIBE "rss" link now emit, and `/essays/rss.xml` carries the manifesto item. Lockstep with the transactions-spec RSS-stylesheet entry above (the same `public/rss/styles.xsl` styles all three footer/header feed links when opened in a browser).
- **2026-05-22 (Phase 3e — §8 theme toggle deviated: dark mode removed entirely):** The locked §8 theme toggle was removed end-to-end as part of Phase 3e Bug 1. The `LIGHT · DARK` toggle in the footer-strip (Task 1.3), the `ThemeToggle.astro` component (Task 1.2), the `:root[data-theme="dark"]` token override block in `global.css` (Task 1.1), the print-stylesheet `.theme-toggle` selector (Task 1.1), and the inline `sw-theme` cookie-reader in `BaseLayout.astro` (Task 1.4) all went. The V3 bridge `sw-theme` cookie may still persist in legacy browsers but is no longer read or honored — Phase 3e OQ-1 decided against writing a counter-cookie. §8.1–§8.6 retained as historical reference under a v1-deviation banner. Side effect: closes Bug 6 (section pages opening in dark mode because of stale V3 cookies). Re-introducing dark mode would require a fresh design pass — the warm-paper texture + hero animation aren't currently dark-mode-compatible.

- **2026-05-21 (OPEN-1 wordmark CONFIRMED + favicon + OG assets generated):** §16 OPEN-1 wordmark question formally CONFIRMED at the recommended default: **"SW" in JetBrains Mono uppercase (weight 700), teal `#0A3E42` on warm paper `#FFF9F0`**. The same wordmark now renders across the nav (§6.1), the universal footer (§7.1), all 4 OG cards (§12.2 lower-left), and the favicon set (Appendix A — `favicon.svg` + `favicon.ico` 16/32/48 multi-res + `apple-touch-icon.png` 180×180). All Phase 0 chrome assets generated and committed: favicons at [`reference-images/favicon/`](reference-images/favicon/), OG cards at [`reference-images/og-cards/`](reference-images/og-cards/) (og-default, vault-scorecard, vault-knowledge-mcp, essays/meaning-over-access — all 1200×630, all ≤55KB). Generation scripts preserved at [`scripts/phase-0/generate_og_cards.py`](scripts/phase-0/generate_og_cards.py) + [`scripts/phase-0/generate_favicons.py`](scripts/phase-0/generate_favicons.py) for any future regens or content updates. Assets move from `reference-images/` to `/public/` at Phase 2 scaffold.

- **2026-05-20 (LOCKED):** All 5 OPEN-N items resolved to recommended defaults per [`BLUEPRINT-COMPLETE.md`](docs/specs/BLUEPRINT-COMPLETE.md) §4.8: (1) Wordmark is `SW` in JetBrains Mono uppercase (not Newsreader — avoids competing with every page's Newsreader `<h1>`). (2) `position: sticky` nav on sub-pages — pragmatic for recruiters reading long architecture essays / case studies; home stays mynrd-pure-scrolls-away per hero-spec §13. (3) `/contact/` minimal route preserved for V3-bridge SEO; reassessed at 6 months. (4) "View the source" link included pointing at `github.com/seanwinslow/sw-portfolio` — comprehension-first thesis undermined if the portfolio's own source is hidden. (5) Email constant locks to `sean@seanwinslow.com` (the portfolio's own domain; one-character edit if Sean later prefers a portfolio-inbound alias). Lives in `src/lib/site.ts` `EMAIL` constant; imported by `<SiteFooter.astro>`, `src/pages/contact.astro`, and every `mailto:` link.

- **2026-05-19 (critique fix §4 — sticky-nav first-impression):** §6.1.1 — documented the two-navigation-grammars split (no-nav home, sticky sub-pages) and added three mitigation rules: (a) sticky nav uses the same chrome substrate, (b) first sub-page nav fades in 600ms after View Transition completes, (c) Playwright mockup verification gate. Per `impeccable-and-emil-design-critique.md` §4 "Sticky nav vs no-nav home discipline split".
- **2026-05-19 (audit fix M-3):** Hand-off prompt updated — `Code-Brain/BMAD/sw-ai-pm-portfolio/` → `Code-Brain/sw-ai-pm-portfolio/`. Per `2026-05-19-portfolio-spec-roadmap-alignment-audit.md` finding M-3.
- **2026-05-17:** Initial draft. Kills the V3-bridge `<form>`-based `/contact/` route (placeholder-alert pattern). Kills the V3 React islands `<NavIsland.jsx>` + `<SmoothScroll.jsx>`. Keeps the V3 theme-cookie pattern verbatim. Folds in Contact per About spec §18 deferral. Aggregates all three collection RSS feeds (`/transactions/`, `/architecture/`, `/essays/`) into the footer's Subscribe column. Reconciles the nav-budget tension between hero spec §13 (home has no nav) and the four collection specs (sub-pages have implicit ~56px desktop / ~48px mobile nav above their vertical budgets) via a `noChrome` BaseLayout prop.

### [`docs/specs/PORTFOLIO-MASTER-PLAN.md`](docs/specs/PORTFOLIO-MASTER-PLAN.md)

- **2026-05-20 (PMP §10 cross-cutting decisions LOCKED):** The 6 remaining open items in PMP §10 (after the 2026-05-18 resolution of #1, #2, #6) accepted at their recommended defaults per [`BLUEPRINT-COMPLETE.md`](docs/specs/BLUEPRINT-COMPLETE.md) §4.9. (#3) Light-mode-only home hero for v1; sub-pages render dark mode normally; time-of-day auto-switch deferred to v2. (#4) The Block ARCHIVED status, frame-the-work first-draft with Larry Cermak as primary reference. (#5) 16BitFit PAUSED status, case study leans on the pipeline as artifact. (#7) Global nav shape: all 5 routes as siblings (work · transactions · architecture · essays · about). (#8) A-3 tile media: 90-sec Loom poster, fallback to npm-registry screenshot or MCP protocol diagram. (#9) Crossover sequencing: hard cutover at the same apex, preserving the handmade first-impression. All 6 cross-cutting decisions formally locked across the spec graph.
- **2026-05-20 (`math` → `loops` word swap lockstep):** §4 row 1 (Home hero tagline) updated — word "math" replaced with "loops" per Sean's preference. Locked decision row rationale extended to capture the swap reasoning (avoids math-shame; gains Karpathy-coded agentic-loops register; preserves 2-word cadence match with the prior phrasing). §4 row 7 (Meta description / OG card / Substack tagline) lockstep-updated to reproduce the new tagline and to swap the "searchable nouns than 'math + taste'" parenthetical to "searchable nouns than 'loops + taste'". See hero-spec changelog for full reasoning.

### [`docs/specs/texture-and-artifacts-spec-v1.md`](docs/specs/texture-and-artifacts-spec-v1.md)

- **2026-06-10 (P3-14 stale-reference note — hero floor shadow is WebP now):** The spec body still references `hero-floor-shadow.png` / `Hero-floor-shadow-2800x560.png` in its HTML snippet, CSS class, asset list, and DoD (≈lines 300/319/499/583/606/693). The shipped asset has been `public/assets/textures/hero-floor-shadow.webp` since the 2026-06-10 audit (159KB PNG → 77KB WebP; PNG removed from `public/`, master reproducible via `scripts/phase-2/generate_substrate.py`). Per the no-spec-body-rewrite rule the references stay as authored; read `.png` as `.webp` when consulting this spec.
- **2026-05-19 (critique fix §4 — Phase 0 asset Gantt):** §7.0 — new subsection sequences ~19 hand-authored assets into 3 waves (substrate / hero+projects / about long-form), each unblocking a specific build phase. Eliminates the mid-build "blocked on art" failure mode flagged by `impeccable-and-emil-design-critique.md` §4 "Mid-build asset authoring".

---

## Non-spec changes

Use this section for changes to [`CLAUDE.md`](CLAUDE.md), [`docs/specs/PORTFOLIO-MASTER-PLAN.md`](docs/specs/PORTFOLIO-MASTER-PLAN.md), or other root-level files that aren't tied to a single spec.

- **2026-06-16 (critique W2 — CLAUDE.md "Home page shape" locked decision updated):** The "Home page shape" quick-reference line in [`CLAUDE.md`](CLAUDE.md) now records that `noChrome={true}` suppresses the full `SiteNav` but the home page carries a minimal corner nav (`HomeCornerNav.astro`, wordmark + work/about, top-right). The prior wording said "No top nav on home" which became misleading once the corner nav landed.
- **2026-06-10 (DESIGN.md license pass — audit P2-09b/P3-13/P3-15 + D-6 resolved per Sean's calls):** Four [`DESIGN.md`](DESIGN.md) amendments (mirrored in [`.impeccable/design.json`](.impeccable/design.json)), all decided by Sean against the 2026-06-10 audit's open items; zero rendered-page changes. **(1) Committed-Teal Rule scoped to the home page** (P2-09b option a): the 30–60% coverage target now names the home explicitly; sub-pages are the book's interior pages (single full-bleed `.page-sheet`, chrome at edges + footer seam, torn-edge reveals home-only per texture-spec §3.6/§4.6) and their ~0% visible teal is documented as intent, not defect. The §2 Deep-Teal bullet updated in lockstep. **(2) New Licensed-Infinite-Motion Rule** (P3-13): `live-pulse` (status semantics, omitted when stale) and `swipe-me-bob` (functional swipe affordance) join the COMING pulse as the only three licensed `infinite` animations, all reduced-motion-guarded. **(3) Ghost-watermark exception** (P3-15): amber-mid licensed on paper for sub-perceptible decorative annotation marks (~≤1.1:1 — the essay + 5 transaction deep-dive margin marks); never type, labels, links, or meaning. **(4) `B-n ·` kickers kept** (audit D-6, closes the 2026-05-30 P3-A open question): the about-spec §4 amber-mid kicker lock is licensed in DESIGN.md as the second paper exception — intentional internal-labeling brand, the spec showing its own seams. Same pass corrects the Inversion Rule's false claim that stamp amber "passes contrast on cream *and* on teal" — measured 1.26:1 on `#0A3E42` (audit P1-04/P1-05); the rule now states stamp amber is paper-only warm and amber-mid is the chrome warm.
- **2026-05-19 (critique implementation complete):** All 7 punch-list items + §2 motion review + §3 Impeccable findings + §4 cross-spec items from [`docs/impeccable-and-emil-design-critique.md`](docs/impeccable-and-emil-design-critique.md) are landed. Specs are ready for build per the critique's verdict: "Ready to build after three specific blockers are addressed." The three blockers were resolved in Tasks 1–3 of the implementation plan ([`docs/superpowers/plans/2026-05-19-impeccable-emil-critique-implementation.md`](docs/superpowers/plans/2026-05-19-impeccable-emil-critique-implementation.md)).
- **2026-05-19 (critique fix §3 — "daily-dated" honest framing):** [`CLAUDE.md`](CLAUDE.md) "three load-bearing things" block + [`docs/specs/PORTFOLIO-MASTER-PLAN.md`](docs/specs/PORTFOLIO-MASTER-PLAN.md) + cross-spec sweep. Renamed "the live layer" → "the daily-dated layer" wherever the data source is the Daily Driver (08:45 daily writes). "Live" is reserved for genuinely sub-daily surfaces (footer agent feed reading `agent-run-history.csv`). The honest framing reads stronger than the marketing framing — Sean's voice everywhere else is honest, this had been the one place it wasn't. Per `impeccable-and-emil-design-critique.md` §3 "The 'live' framing oversells slightly".
- **2026-05-19 (critique fix §3 — em-dash exception):** [`docs/specs/PORTFOLIO-MASTER-PLAN.md`](docs/specs/PORTFOLIO-MASTER-PLAN.md) §3.2 — documented the em-dash exception for wire-service register surfaces (datelines, ledger captions, methods-strip prefixes, Daily Driver-written body copy). The editorial register still follows Impeccable's no-em-dash rule. Locking this exception once prevents future sessions from "fixing" dateline em dashes. Per `impeccable-and-emil-design-critique.md` §3 "Em dashes in wire-service copy".
- **2026-05-19 (critique fix 1.3 — Committed-teal color strategy):** [`docs/specs/PORTFOLIO-MASTER-PLAN.md`](docs/specs/PORTFOLIO-MASTER-PLAN.md) §2.2 + §5 D2 row — declared the site's color strategy as **Committed-teal** (one saturated color carrying 30–60% of visible surface) rather than the prior Restrained-paper math ("95% paper, one accent ≤10%"). The texture-and-artifacts spec promoted teal `#0A3E42` from a per-section splash to full-bleed structural chrome at Z=0 — Committed is the honest framing now. The one-splash-per-section rule survives intact; chrome and splash blocks are at different z-layers serving different jobs. Per `impeccable-and-emil-design-critique.md` blocker 1.3.
- **2026-05-19 (audit fix M-2 — PMP roadmap links):** Repaired three broken relative links in [`docs/specs/PORTFOLIO-MASTER-PLAN.md`](docs/specs/PORTFOLIO-MASTER-PLAN.md) (§0.5, §3.4, §11). The link target moved from `2026-05-06-unified-roadmap%20copy.md` (which resolved to a non-existent file under `docs/specs/`) to `../prompts-and-references/2026-05-06-unified-roadmap%20copy.md` (the actual canonical location after the 2026-05-18 reorg). The " copy" suffix is retained — that is the on-disk filename. Per `docs/prompts-and-references/2026-05-19-portfolio-spec-roadmap-alignment-audit.md` finding M-2.
- **2026-05-19 (audit fix M-1 — roadmap Task 13 date drift):** Edited [`docs/prompts-and-references/2026-05-06-unified-roadmap copy.md`](docs/prompts-and-references/2026-05-06-unified-roadmap%20copy.md) line 901 heading: `draft-lock 2026-05-23` → `draft-lock 2026-05-22` to match the authoritative date used throughout the Task 13 body (Steps 4–7 + verification gate). Body-wins resolution per audit recommendation. Per audit finding M-1.
- **2026-05-19 (audit fix M-3 — BLUEPRINT + texture-spec hand-off prompts):** [`docs/specs/BLUEPRINT-COMPLETE.md`](docs/specs/BLUEPRINT-COMPLETE.md) §10 hand-off prompt and [`docs/specs/texture-and-artifacts-spec-v1.md`](docs/specs/texture-and-artifacts-spec-v1.md) Appendix C hand-off prompt: `Code-Brain/BMAD/sw-ai-pm-portfolio/` → `Code-Brain/sw-ai-pm-portfolio/`. Per audit finding M-3. (The eight other in-spec M-3 fixes are recorded per-spec above.)
- **2026-05-19:** Created this file. Extracted existing per-spec `## 1.1 Changelog` sections verbatim as a baseline. Added pointer in `CLAUDE.md` so future Claude sessions log spec changes here instead of inside the specs.
